# T-120 result — **the widening does not work, and I am not shipping it. A seed does.**

- **Status:** BUILT, GREEN, **MERGE PENDING** under `DEC-L-007` (gated).
- **`GATE_SCOPE=TASK` GREEN, 0 warnings.** `Architecture 532 -> 533` — one test, the floor.

---

# YOU RULED "WIDEN THE CLOSURE". I BUILT IT, MEASURED IT, AND IT FAILS.

**Attempt 1 — follow statically-called types by name, transitively.**

```
CompanyApiErrorMapper claimed 97 codes across Branch, Department, Employee, Position,
Role, Tenant, TenantStorage and TenantUser — the whole product.
```

**A name in source is not a call, and following names transitively means everything reaches everything
within three hops.**

**Attempt 2 — bounded to ONE hop, and to `Name.` member-access syntax rather than a bare mention.**

```
9 of 12 sites failed with large false sets.
```

**Shared scope resolvers are called by every module and name errors from all of them.** Bounding the shape
did not bound the reach.

**Reverted both.** The ruling was right about the problem and the mechanism does not exist at this level —
**a real fix needs call-graph analysis, not text.**

---

# WHAT I SHIPPED INSTEAD, AND IT IS ONE LINE

```csharp
typeof(SSAS.Payroll.Domain.Runs.PayrollCalculator),   // added to PayrollApiErrorMapper's seeds
```

**The register already has an explicit-seed mechanism. The blind spot was that nobody had put the statics
in it.**

**Proved by plant:** with T-118's mapper arm removed, the register reports
`actual unmapped: Payroll.OneOffPaymentElementNotPayable` — **that code and nothing else.** Restored, green.
**Exact, and no over-reach at all.**

---

# ⚠ THE SIZE, AND I FOUND MORE THAN YOU DID

**Your figure: nine static classes, 44 return sites, five assemblies. Mine: TWENTY-THREE static classes.**

**We measured different things and yours was the tighter criterion** — you counted `Result.Failure` in static
classes; mine requires **both** an error symbol and a `Result.Failure`, across every product assembly the
register can see. **I am not reconciling that quietly, as you asked: I found MORE, not fewer, and the number
that matters for the guard is mine because it is the set the guard must reach.**

**Both are upper bounds on REACH, not defect counts.** Only one of the twenty-three has a proven live
defect, and T-118 fixed it.

---

# THE FLOOR, HELD BY A TEST RATHER THAN BY A COMMENT

`Every_static_class_that_returns_an_error_is_seeded_by_some_site` — **new, and green.**

**The twenty-three are named in `KnownUnseeded` rather than silent.** A NEW static class returning an error
fires immediately; the existing twenty-three are a backlog somebody can see.

## Why they are not seeded now, and it is not laziness

**Each needs a judgement this task cannot make: which mapping site's surface does it answer behind?**
`EmployeeImportCsvParser` sits behind the import route, `PositionSearchCriteria` behind a read, the
`*ScopeErrors` helpers behind every route of their module.

**Seeding one to the wrong site makes that site responsible for codes it does not map — and the fix for a
false red is a wrong `KnownUnmapped` entry, which is how a register stops meaning anything.**

---

## ⚠ AND I HIT THE SAME BUG AS T-094, EXACTLY

```
source, $@"<BACKSPACE>{Regex.Escape(candidate.Name)}<BACKSPACE>"
```

**A literal backspace character where `\b` belonged** — the shell ate the escape. **T-094 had the identical
defect** (*"a literal backspace character where `\b` belonged, source index empty"*), and it produced the
identical symptom: **the walk matched nothing, the plant did not land, and the register passed while
reporting on an empty set.**

**I diagnosed it as "the widening does not reach" for three runs before reading the bytes.** `cat -A` is what
found it, both times.

---

## For the architect

- **Blocked on you:** the twenty-three. **Each is a small judgement and there are twenty-three of them** —
  worth a task, not worth guessing inside this one.
- **The ruling stands and the mechanism did not.** Widening was the right instinct; a text walk cannot
  express it.
- **`AttendanceSummaryStatus` for contradictory employment data** is still queued behind this, per your order.
