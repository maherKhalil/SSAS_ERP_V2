# T-023 result — DONE

- **Status:** DONE
- **Branch:** `agent/T-023-tenant-storage-adrs` — pushed, branched from `main` at `4238fab`
- **Merged:** no — non-gated (`DEC-L-002`). Awaiting `MERGE T-023`.
- **Commits:** `b6b81cb` — docs(T-023): three of the tenant-storage ADRs move to Accepted, two do not
- **Reported:** 2026-08-25

## Acceptance criteria

- [x] **Each of the five tested individually**, evidence named per ADR — code, tool, suites, packages.
- [x] **Only what clears moved.** Three moved, each with `status: Accepted`, an evidence block using
      the word **inference**, a Revision History row in that file's own column headings, and a version
      bump.
- [x] **Two left alone and reported**, with what each is waiting on.
- [x] **`ADR-021` reported explicitly.** It does not clear, and the reason is not the one anyone
      expected. `NEEDS-DECISION` below.
- [x] **`README.md` index updated**; `--stat` shows **four** files, all under `docs/14-Engineering/ADR/`.

**Three moved, two queried.** You said five moved would be suspicious and three-and-two normal. I did
not aim at that shape — `ADR-019` and `ADR-021` fail for entirely unrelated reasons, and the second
one inverts the task's premise.

## Correction to the task's premise, and it changes the outcome

> *"none of the five states its own acceptance precondition — I checked"*

**`ADR-021` does.** Line 37, in its Status section:

> *"Implementation scope: architecture-ready, implementation deferred. … **This ADR moves to Accepted,
> and its pending items are resolved, when a customer-hosted deployment is actually contracted.**"*

That is a stated, checkable acceptance precondition, and it puts `ADR-021` on `DEC-L-020`'s **strong**
branch — the `ADR-026` shape from T-011, with the opposite result. **`ADR-026` passed its own test;
`ADR-021` fails its own test**, and no argument from use can override a document's own terms.

The other four genuinely state none. I re-checked with a wider net than "should not be accepted"
after finding this one, since my first grep had missed it too.

## The five, individually

### `ADR-018` Tenant Schema Health and Migration Orchestration → **Accepted**, v1.7 → 1.8

`TenantDatabaseMigrationOrchestrator` ships in `SSAS.Platform.Infrastructure/TenantStorage/` and is
registered in `PlatformPersistenceServiceCollectionExtensions`. `tools/SSAS.Tenant.MigrationTool`
implements the two-stream deployment procedure. `TenantSchemaHealthSqlServerTests` and
`TenantDatabaseSchemaHealthTests` exercise the health model. The `nvarchar` rule is applied by every
module's EF configuration. **62 `.cs` files** cite it; four approved packages treat it as binding.

Its own v1.5 row already says *"Recorded that schema health and migration orchestration are
implemented"* — the fact was written down, the front-matter simply never followed. Closest of the
three to `ADR-027`'s "recorded but lagging" shape, though still short of it, so still inference.

### `ADR-019` Dynamic Tenant Placement Policy → **stays `Proposed`**

**Zero evidence of any kind.** Not weak evidence — none.

- **0** `.cs` files cite it.
- `PlacementEvaluator`, `PlacementPolicy`, `TenantPlacement`, `PlacementRecommendation` — **0 hits
  each**. I searched for the concepts, not only the identifier, in case it shipped under another name.
- **0** feature packages cite it.
- **0** documents outside `docs/14-Engineering/ADR/` cite it.

Nothing in this repository knows it exists. **Waiting on:** a first implementation, or a decision that
placement recommendation is not coming, in which case the question is supersession rather than status.

### `ADR-020` Shared-to-Dedicated Migration and Cutover → **Accepted**, v1.5 → 1.6

The strongest of the three. Ten production classes — `TenantCutoverOrchestrator`,
`TenantCutoverCopyPlan`, `TenantCutoverCopyService`, `TenantCutoverFreezeService`,
`TenantCutoverWriteFence`, `TenantCutoverRoutingFlipService`, `TenantCutoverOperationLock` and three
more — under **four architecture suites** and **four SQL Server integration suites**. Six approved
packages cite it.

**And five `Accepted` ADRs already declared `depends_on: ADR-020` while it stood `Proposed`** —
`ADR-023`, `ADR-024`, `ADR-025`, `ADR-026`, `ADR-030`. That was six pre-existing Accepted→Proposed
dependencies (`ADR-025` also names `ADR-018`), and this task repairs all six.

### `ADR-021` Customer-Managed Connectivity → **stays `Proposed`** — see `NEEDS-DECISION`

Fails its own precondition, and the code agrees. Its **27** `.cs` citations are not evidence of use;
they are **markers recording that it is deliberately unbuilt**:

- `TenantDatabaseResolver.cs:39` — *"no connectivity implementation exists (`ADR-021` is
  implementation-deferred)."*
- `TenantDatabaseHostingMode.cs:9` — *"no endpoint, credential or connectivity code consumes it until
  `ADR-021` is implemented."*
- `TenantDatabase.cs:14` — *"credential-reference fields belong to `ADR-021` and are not modelled in
  this slice."*
- `ITenantDatabaseMigrationOrchestrator.cs:50` — *"CustomerManaged hosting has no runtime connectivity
  path at all."*

**A citation count is not evidence of use, and this is the case that proves it.** Counting would have
scored `ADR-021` at 27 and moved it. Reading says the codebase has gone out of its way, in four
separate files, to record that this decision is not in force.

**Waiting on:** a contracted customer-hosted deployment. Its own words.

### `ADR-022` Tenant Database Backup and Recovery → **Accepted**, v1.2 → 1.3

Phases A, B and C **delivered** — the ADR says so itself, and the code matches:
`SqlServerTenantDatabaseBackupProvider`, `SqlServerBackupEvidence`, `SqlServerBackupVisibility` and the
restore-verification providers, under **four architecture suites** and **eight** SQL Server integration
suites. **120 `.cs` files.** Its own §14 session-loss gate is recorded **closed** at v1.1.

**One qualification, written into the ADR rather than left in this result:** acceptance covers the
**decisions**, not completion of the phasing. Phase D's implementation has not begun and Phase E is
future. That is not the `ADR-021` situation — `ADR-022` phases *implementation* of decisions it has
made, whereas `ADR-021` defers the *decision's activation* to a commercial event that has not happened.

## `DEC-L-021` within the family — checked, and it nets strongly positive

**No supersede / amend / resolve-an-open-question relationship exists between any two of the five.**
They are a dependency chain — `ADR-017` ← `018` ← `019` ← `020` ← `021` ← `022` — and layering is not
the `DEC-L-021` trigger. Nothing is split across statuses that should not be.

The `depends_on` edges are still worth the arithmetic:

| | Before | After |
|---|---|---|
| `Accepted` ADRs depending on a `Proposed` one | **6** (`023`, `024`, `025`, `026`, `030` → `020`; `025` → `018`) | **2** |
| Which two remain | — | `ADR-020` → `ADR-019`; `ADR-022` → `ADR-021` |

**Net six repaired, two created, and both new ones are stated at the site rather than left for a
reader to find.**

Both are also defensible on their own terms, which is why I moved the parents anyway:

- **`ADR-020` → `ADR-019`.** `ADR-020` itself rules recommendation **advisory** — *"`ADR-019`
  recommends; this workflow executes, and only after explicit approval"* — and dedicated-to-shared
  manual-only. The executed workflow never needed the policy engine to exist.
- **`ADR-022` → `ADR-021`.** A **boundary**, not a support: `ADR-021` defines what `ADR-022`
  **excludes** (no platform backup, no restore verification against a customer-managed database). The
  excluded case is exactly the one `ADR-021` is not accepted for, so the two are consistent.

Leaving `ADR-020` and `ADR-022` `Proposed` to avoid these two edges would have preserved six worse
ones. Stated so the trade is visible rather than implied.

## NEEDS-DECISION — `ADR-021`, and the FP-014 citation cannot be repaired from here

**A ratified package cites an ADR that fails its own acceptance test, and the ADR is right to fail it.**

- `REQ-SUB-0005` names `ADR-021:207` in its **Authority** column.
- `DEC-SUB-0004` rests on the same §10 passage.
- **`OD-SUB-0004`, your ruling, excludes a tenant-database projection because of it.**
- `decisions-open.md:487` states `ADR-021` *"constrains this package"* and is not reopened by any `OD`.

Moving `ADR-021` was the obvious fix and it is unavailable — it would mean overriding a document's own
stated terms with an argument from use, which is precisely the move `DEC-L-020` was written to stop.

**What I did not do, because it is yours:** the passage FP-014 actually relies on is §10's outage
behaviour — *the Platform database remains available when a customer database does not, so login, token
refresh, platform authority and **account, subscription and other platform-only pages** continue to
work.* That reasoning **does not depend on customer-managed hosting at all**; it follows from
`ADR-017`'s topology, which is `Accepted`. So the citation may be repointable rather than the ADR
movable. **I am not proposing that as the answer** — re-pointing an authority column in a ratified
package is a ratification act, and choosing between that and something larger is your call.

## For the architect

- **Blocked / needs a decision:** the `ADR-021` / FP-014 citation above. `ADR-019` needs no decision
  today — it is simply unbuilt, and nothing depends on it except `ADR-020`'s advisory reference.
- **Touched outside scope:** nothing. Four files, all under `docs/14-Engineering/ADR/`. No content of
  any of the five changed — every edit is front-matter, the Status block, or a Revision History row.
  `ADR-019` and `ADR-021` were not touched at all.
- **No contradiction with shipped code found** in any of the five, so no `NEEDS-DECISION` on that
  ground.

### Noticed, not swept — one observation about the other ADRs

The six pre-existing Accepted→Proposed dependencies were found by enumerating **every** ADR's
`depends_on`, which is a cheap check and caught something no per-file reading would. **It is now clean
for this family, and I did not check the rest of the register** — `ADR-001`…`ADR-016` and
`ADR-023`…`ADR-030` were out of scope and I did not audit them. Whether that enumeration is worth
running across the whole set is a question, not a finding.
