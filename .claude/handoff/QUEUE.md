# The work queue — authoritative

**This file is the RECORD of the queue. The MESSAGE is what starts work.** ⚠ **The older line here said
*messages are notifications about it, not the queue itself* — true as a statement about authority and
MISLEADING AS A STATEMENT ABOUT MECHANISM, which is how it was read.** The window it was written for
reported reading that sentence, agreeing with it, and continuing to treat the message as the queue —
**because agreeing changed nothing about when it acts.** **Both are needed and they do different jobs.**

⚠⚠ **READING THIS FILE IS THE LAST STEP OF COMPLETING AN ITEM. Not a thing to do before going idle —
THAT INSTRUCTION WAS INERT AND IS WITHDRAWN.**

**Measured from inside the loop on 2026-08-31 (item 186).** The old wording said *read this before going
idle.* ⚠ **GOING IDLE IS NOT AN ACTION ANYBODY TAKES: there is no moment where a window decides to stop —
the turn simply ends. AN INSTRUCTION ATTACHED TO A NON-EVENT HAS NOTHING TO FIRE ON.** In the whole of a
working session this file was opened **twice**, both times because a message said to.

**Compare the rule three lines below about grepping the results trail before building an instrument: that
one is attached to an action somebody actually performs, and it fires.** ⚠ **Every process rule in this
repository should be checked against that test — name the ACTION it hangs on, and if you cannot, it will
not run.**

⚠ **AND THE HONEST LIMIT: THIS FILE IS A MAILBOX WITH NO DOORBELL.** It is durable, authoritative and
immune to the transport, **and nothing reads it on its own.** Only a delivered message starts a turn, so a
ruling that reaches this file but not the wire leaves the file saying *work outstanding* while the other
window is stopped. **From the architect's side the queue is full; from the coder's, nothing happened.**
**Put the dispatch in a message AND in this file — the file is the record, the message is the trigger.**
Roughly half the architect's inbound messages are lost, and the coder's outbound reports are not always
received. **A silence is a lost message, not a decision.** This file is in the repository, so neither
window can lose it.

⚠ **IF `git fetch` FAILS, READ THIS FILE FROM DISK INSTEAD — IT DOES NOT NEED THE NETWORK:**
`C:/Users/User/Documents/SSAS_ERP_V2/SSAS_ERP_V2-board/.claude/handoff/QUEUE.md`

**That is the architect's worktree, on this machine, always current with its last commit.** This file was
put in the repository so a dropped message could not stall the coder — **and was then read through `origin`,
the one dependency that fails at the same time as everything else.** On 2026-08-30 a GitHub outage left the
coder believing the queue held one item when it held three. **A local path closes that.**

⚠ **GREP `.claude/handoff/results/` BEFORE BUILDING ANY INSTRUMENT.** On 2026-08-30 **nine instruments were
built over two days to re-derive a conclusion that was already written, already corrected, and already
reasoned through** — `T-201.md`, 142 lines, with the members, the method, the per-package breakdown and two
self-corrections. **It was never searched.** ⚠ **It had even stated the general rule one window later
"discovered": *"an instrument that reads only the machine-readable columns will miss the corrections people
wrote in the margin — and so will a reader who stops at the first word."*** **The results trail is cheaper
than every instrument in this thread combined.**

⚠ **EVERY ITEM PRODUCES A COMMITTED RESULT FILE IN `.claude/handoff/results/`, INCLUDING MEASUREMENT-ONLY
ITEMS.** One file per item, named for it. **Findings and their scope, not narrative** — the board carries the
reasoning, the results carry the measurements.

⚠ **AND THE FILE MUST LIST THE MEMBERS, NOT ONLY THE COUNT AND THE GROUPS.** On 2026-08-31 item 170 needed
165's 61 members and **the result file held only the number and the group names** — recovering them depended
on a probe log surviving in `%TEMP%`, **one cleanup away from unreproducible.** ⚠ **That is the `T-201`
failure in a file written FOUR ITEMS after its lesson was recorded**, by the window that recorded it.
**A remainder you can NAME is a finished measurement; one you can only COUNT is an unfinished one** — and
the members are the part the next item needs, every time.

**⚠ STATE THE BLIND SPOTS IN THE FILE, NOT ONLY IN THE MESSAGE.** Every report in this loop has named what
its population excluded, **and that caveat is the part most likely to be lost when a number is reused by
someone who was not here.** *"Zero real over 139"* was true and became misleading **precisely because its
scope did not travel with it.**

**Why this rule exists:** on 2026-08-30 items 150–157 ran for **78 minutes producing no commit at all** —
the phantom classification, the complete 199-route table, the built→documented mirror, the support-surface
verification — **all of it living only in messages and in board prose.** ⚠ **The 199-route table is the most
reusable artefact of the day and would have vanished with the session.** **That is the same failure as a
count written into a summary while its members stayed nowhere, which this loop had already diagnosed — and
was then doing to its own work.**

**Standing authority:** work down the list without waiting between items. If one is blocked, skip it, start
the next, and say which in the following message. Every item here is gated (`src/` + `tests/`) unless
marked otherwise, so **`DEC-L-007` applies: green gate, merge immediately — no `MERGE` word is required.**

⚠ **AN EMPTY QUEUE IS NOT A REASON TO STOP — IT IS A REASON TO PULL FROM `BACKLOG.md`.** Take the top item,
start it, and say which one in your next message. **No permission needed.** If the backlog is also empty,
pick the highest-value thing you can defend, state the reasoning in one line, and start.

**Never send a message whose only content is that you have no work.** If you are reporting an empty queue,
you should already be working and naming what.

**Refill:** the architect refills at TWO remaining. ⚠ **That rule has now failed ten times, and the reason
is structural: it fires only when the architect is looking.** A reactive refill always leaves a gap between
the coder finishing and the architect noticing — and the earlier wording covered *"drops to two with no
refill"*, which is the case that does not happen, while saying nothing about **zero**, which is the case
that does. **`BACKLOG.md` is the part that depends on nobody being awake.**

## ⚠ CORRECTION — ITEM 90 WAS FINISHED, NOT IN FLIGHT. THE WORKING TREE IS CLEAN.

**An earlier version of this section said 90 was part-done and uncommitted on `ClaudeBranch`, and listed
eight files to inspect. That was wrong.** 90 gated green and merged as **PR #347** in the same minutes; the
files seen modified were the finished change immediately before its commit. **Nothing is uncommitted and
there is nothing to recover.**

**Recorded rather than deleted, because it is the failure this file exists to prevent** — a status written
from a snapshot taken at the wrong instant, published as fact, three minutes after the architect finished
correcting the same class of staleness elsewhere. **A working-tree observation is a measurement with a
timestamp, and it decays in seconds.**

**True state: `ClaudeBranch` carries #345, #346 and #347. 91, 92 and 93 are unstarted and fully specified
below. Nothing is in flight.**
## Session ended 2026-08-30 — where to pick up

**The owner signed out. Both windows stopped cleanly.** ⚠ **This section records COMMITTED state only** — a
working-tree observation decays within one commit and has already misled this loop once today.

⚠ **ITEM 129 IS ON A BRANCH THAT IS DELIBERATELY RED AND MUST NOT BE MERGED:
`agent/T-268-precondition-code-RED`, commit `0d05986`, pushed.** The name carries the warning. **The gate
refused it and was right to.**

**Failure 1 — a real placement error, and its cause is worth reading before fixing it.**
`ProjectDependencyArchitectureTests.The_shared_api_project_names_no_business_concept` reddened on
`["ApiProblems.cs -> Company"]`: `CompanySelectionRequired` was put in the shared `ApiErrors` in
BuildingBlocks, **and the shared API project may not name a business concept.** ⚠ **That is why
`company.scope_denied` is declared separately in six module mappers rather than once centrally — apparent
duplication that is the rule being obeyed.** The correct placement is **a per-mapper constant in each of the
four mappers, matching the `CompanyScopeDenied` idiom exactly.** Small and mechanical.

**Failure 2 — not a mistake. It is the contract change 129 asks for.**
`D30_A_missing_or_malformed_company_header_is_a_validation_failure` asserts the CURRENT behaviour, that this
condition answers `request.invalid`. **It must be updated to expect `company.selection_required` and to say
why a precondition is distinct.** It was deliberately left untouched: **rewriting a wire-contract assertion
at sign-out is the exact edit that should be made carefully rather than quickly.**

**Sound and reusable on that branch:** the four mapper arms, the Platform alias, and
`PreconditionCodeArchitectureTests`, **whose plants both pass** — a module collapsing back to the generic
code reddens the ban, and a broken arm regex reddens both the floor and its own control.

**130 has no edits at all. Nothing of it is lost.**

**Both rulings are inline in the table below and neither needs re-deriving.**

**What remains after 129 and 130 is owner-gated, and that is an enumeration result rather than an empty
queue:** five ERP decisions blocking 41 capability rows, and three HIS placement decisions. `BACKLOG.md`
is empty of open B-items **deliberately** — the loop spent 2026-08-30 on instrument hardening because that
is where the defects were, and inventing another sweep to avoid saying *"this needs the owner"* would be
the failure this board has recorded under several other names.

**Also open, unchanged, and not urgent:** the 13 local-only `codex/*` branches on a PUBLIC repository, and
`agent/T-072-spec-and-authorization-model`, whose content was landed directly on 2026-08-30 and whose PR
was closed with an explanation — **the branch is left in place on purpose.**

## ⚠ CORRECTION — THE "QUEUE IS EMPTY" CLAIM ABOVE WAS FALSE AS WRITTEN

**An earlier version of this section listed nine closed sweeps as evidence that nothing remained.** ⚠ **Every
one of them was inside a single subject — guards, instruments, error codes — and the section implied the
product.** **The capability axes were never re-checked.**

**What that missed, found in one grep:** `NEXT-SESSION.md` records Axis 1 decomposing from 67 rows to
*"41 owner-gated, 15 already deferred, 9 our own doc errors, 2 real"* — ⚠ **and the two real ones are named
in no file in this repository.** A count survived and its members did not.

**The rule this loop wrote today and the architect then broke: a remainder you can NAME is a finished
measurement; one you can only COUNT is an unfinished one.** And: **a complete enumeration of the wrong set
reads exactly like a complete enumeration.**

⚠ **AND A SECOND BLIND SPOT, FOUND BY 160 ON 2026-08-30: EVERY SWEEP ASKED WHAT THE CODE CONTAINS, AND NONE ASKED WHETHER THE DOCUMENTS' CLAIMS ABOUT THE CODE STILL HOLD.** One question about one row turned up a **ratified** package whose README says *"No code and no schema. Nothing here is implemented"* while its first migration is dated **the next day**. **Eight enumerations ran with the tree in front of them and none could see it, because none was pointed at the documents.** Now Principle 20. **Item 161 is that measurement.**

**147 re-derives the whole decomposition — all four buckets, not just the two — because the count is a day
old and three items have shipped against Axis 1 since.**


## ⚠⚠ THE QUEUE IS EMPTY AS OF 2026-08-31, AND THIS IS THE STATE IT IS EMPTY IN

**Not a declaration that nothing remains. A statement of what closed and what is left.**

⚠ **`[GATE GREEN — PHASE scope: all eight suites, Debug and Release]`.** Integration **848/848 in both
configurations**; `test-baseline.txt` holds **sixteen rows**, its first recorded expectation for the
Integration suite and the Release configuration since 2026-08-27; **condition 4 compares every pair its
scope produces — seven on TASK, sixteen on PHASE — and the header now says so at the point of use.**

**The thread that opened at item 166 asking whether a domain-event dispatcher existed at all closed at 197.**
Along the way: the dispatcher was found to exist and an owner decision was withdrawn; a committed command
reported as failed was fixed; the untracked-aggregate hazard was closed across the complete mechanism set;
a test that had been red for four days was found, diagnosed as a wall-clock time bomb, and repaired; and
the suite nobody could see became one with a baseline.

⚠ **WHAT REMAINS IS THE OWNER'S: TWENTY-ONE DECISIONS IN `OWNER-DECISIONS.md`, none of which either window
can take.** **`BACKLOG.md` is empty of open B-items ON PURPOSE.** ⚠ **Do not invent a sweep to avoid saying
this needs the owner** — that failure is recorded on this board under several names, and the last time the
loop declared itself out of work it was wrong for a reason it could name.

**The doorbell timer on the architect's side is switched OFF, deliberately.** It exists to trigger queued
work; with none queued its only remaining behaviour was to report an empty queue every seventeen minutes.
**It is re-armed in one call the moment a row is added.**

⚠⚠ **AND BEFORE CLOSING A ROW: VERIFY ITS RESULT FILE IS COMMITTED, AND THAT THE CLOSED LIST NAMES THE ROW.** (The second half added 2026-08-31 after eight items closed against a list last updated at 197 — **the same shape as the missing result files, and silent in the same way.**) `CODER.md` makes the result file a
required step, and **twenty-seven of them across `T-001`…`T-201` were never committed** — 26 with no
history anywhere. **All three of the ones found in a single day — 177, 179 and `item-176` — would have been
caught by this one check at the moment the row was closed.**

⚠ **The document could not do it, and Principle 27 says why: the obligation sat on *producing a result*,
which is not a moment anyone passes through. CLOSING A ROW IS.** **The check belongs on the event that
exists, and it is the architect's, not a louder line in `CODER.md`.**

⚠⚠ **AND CLOSING A QUEUE IS TWO CLAIMS, NOT ONE. THEY WERE MERGED ONCE, ON THE DAY THIS SECTION WAS
WRITTEN.** *I am not pulling for something to do* is a statement about **WORK**. *Everything is committed*
is a statement about **the RECORD**. ⚠ **A session can satisfy the first and fail the second, and it did:
a result file carrying a correction sat uncommitted for the whole session while both windows called it the
other's.**

**So before declaring a queue empty: `git status` must be EMPTY, and nothing unpushed. CHECKED, not
inferred.**

⚠ **AND IF A FILE IN `git status` IS *SOMEBODY ELSE'S*, PROVE IT: `git log <file>` THEN READ THE DIFF.**
**Both windows commit as the same author in the same worktree, so ownership is unresolvable from git
metadata — the rule cannot be *check the author*, it has to be *read the content*, which names its own
item.** **Ten seconds, spent the first time an exclusion is typed.**

⚠ **`git show --stat` after a scoped commit confirms that everything you NAMED landed. It cannot see what
you never named.** **It is a positive-only control, and an exclusion is invisible to it by construction.**

**When a row appears: the architect writes it here AND sends it. The file is the record, the message is the
trigger — 186 established that neither does the other's job.**

⚠⚠ **AND WHEN A ROW FINISHES, THE SAME TWO ACTS ARE ONE ACT: RECORDING THE RESULT AND STRIKING THE ROW.**
**Added 2026-08-31 after FIFTEEN completed, committed items — 222 through 236 — were found still marked
`OPEN`, because the architect had been recording every outcome on the board and in the message thread and
never in this file.** ⚠ **The rot is invisible from the side that is doing the work: the coder's reports
were complete, the board was complete, and only THIS file was wrong.**

⚠ **SO THE FILE IS AUTHORITATIVE OVER THE MESSAGE THREAD, BOTH WAYS.** If a dispatch names a row that is
already committed, **say so and do not redo it** — a message can be stale, and the file plus `git log` is
what settles it.

⚠⚠ **AND A CHECK OVER THIS TABLE MUST READ THE `OPEN` MARKER, NOT THE ROW PATTERN.** A count of rows
matching `| **NNN** |` answers *does a row exist*, which is a different question from *is it open*, and the
two agree right up until they do not. **That is the same defect as a guard asserting a method TAKES a
parameter rather than APPLIES it — an instrument reading the cheaper adjacent field.**

---

| # | item | status | detail |
|---|---|---|---|
| **245** | ⚠⚠⚠ **NARROW THE TENANT-MODEL CONTRIBUTOR DISCOVERY TO PRODUCTION ASSEMBLIES — IT CURRENTLY SCANS ITS OWN TEST ASSEMBLY.** **`The_composed_tenant_model_has_exactly_the_expected_number_of_entities` enumerates every `SSAS.*.dll` in its own output directory and instantiates every non-abstract `ITenantModelContributor` it finds; `SSAS.Architecture.Tests.dll` MATCHES.** **So ANY test defining a contributor silently enters the model other tests take a census of — proven when two probe contributors took the count 36 → 38 and reddened the gate.** ⚠ **THE GUARD HAD BEEN PASSING ON AN ABSENCE, NOT ON ITS LOGIC: the population was wrong from the day it was written and no member of the wrong part existed.** | **WITHDRAWN 2026-09-01 — THE PRECONDITION EVAPORATED ON THE ENUMERATION THIS ROW REQUIRED.** ⚠⚠⚠ **A test contributor ALREADY EXISTED — `ProbeContributor` in `Platform.Tests` — AND HAS NEVER BROKEN THE CENSUS, because that assembly is not in `Architecture.Tests`' output directory.** **So the coupling is *a contributor defined in `Architecture.Tests` ITSELF*, not *any test assembly*, and the blocked file simply belonged in `Platform.Tests` beside the existing probe and the same `ComposedTenantModelSource` idiom.** **Moved: 0 warnings, both tests pass together and each alone, census green at 5 of 5, NOT EDITED.** ⚠⚠ **THE CENSUS DEFECT IS REAL AND DELIBERATELY UNFIXED — a contributor defined in `Architecture.Tests` still breaks it, and its comment still says the residual surface is the project references when it is also every type they define. CARRIED TO `B25`, NOT LOST.** ⚠ **AND THE RULING WAS DEFENSIBLE AND STILL WRONG: my counterfactual test asks whether a guard change is JUSTIFIED, never whether it is NECESSARY. BEFORE TOUCHING A SHARED GUARD, ENUMERATE THE ALTERNATIVES THAT DO NOT TOUCH IT.** | ⚠⚠ **DO THE ENUMERATION FIRST AND IT CAN INVERT THE RULING: who else consumes that discovery, and does any existing test depend on a contributor being found in a TEST assembly? If one does, this becomes a new assembly instead. Report the number before editing.** ⚠⚠⚠ **DO NOT RAISE `ExpectedEntityCount` — that makes the alarm accommodate the thing it exists to catch, and the file's own comment warns against that reconciliation.** ⚠ **THE EXCLUSION IS A NEGATIVE PREDICATE AND `B23` APPLIES TO IT: `*.Tests.dll` stops excluding the day an assembly is named `...Test.dll`. OVER-exclusion already reddens via the count; UNDER-exclusion is caught by nothing. Prefer a property-based filter (references xunit, or is not a production project reference) WHERE CHEAP; otherwise take the name-based one and NAME THE RESIDUAL IN THE COMMENT.** **AND UPDATE THE FILE'S OWN COMMENT: *discovery does not remove the list, it moves it* is true and incomplete — the population also included the test assembly itself, so the residual surface was never just which projects are referenced but every type any of them defines, INCLUDING TEST DOUBLES.** **OWN COMMIT, GATED ON ITS OWN, NOT FOLDED INTO 244.** ⚠ **RULED ON *would this be right if the blocked work did not exist*, and it passes: the census's population disagrees with its own name, and narrowing REMOVES ZERO PRODUCTION COVERAGE because no production entity lives in a test assembly.** |
| **250** | ⚠⚠ **AN APPLICATION LOCK IS A PROTOCOL, NOT A CONSTRAINT — SO ASSERT THAT EVERY PERIOD-STATE WRITER TAKES THE FENCE.** **249's fence binds only writers that take it: raw SQL bypasses it, a future handler that forgets it bypasses it, a migration bypasses it. That is true of every application lock in this codebase including `TenantCutoverWriteFence`, so it is the accepted idiom rather than a compromise — and it rots silently the first time someone adds a second writer.** **POPULATION VERIFIED BY THE ARCHITECT: `FiscalPeriod.Close()` and `Reopen()` are called from EXACTLY ONE PLACE TODAY, `CalendarCommandHandlers.cs:245`.** | **CLOSED 2026-09-01 — `3c9e779`, TASK green, 7 suites, 0 warnings.** **`FiscalPeriodStateWriterFenceTests`, 123 lines: the population is *types that transition fiscal period state*, the assertion is *takes the exclusive fence*, and the predicate does not name the assertion.** **PLANTED TWICE: a second writer that skips the fence → RED, and the derivation renamed so it matches nothing → RED via the floor.** ⚠⚠⚠ **AND THE FLOOR PLANT NEARLY PRODUCED A FALSE RECORD IN A NEW WAY: the renamed methods DID NOT COMPILE, so `dotnet test` never reached the test, AND THE ABSENT `Failed!` LINE READ AS THE PLANT NOT FIRING.** **Re-run with `--no-build` — this test reads SOURCE TEXT, not assemblies — and it fires.** **AN ABSENT FAILURE LINE IS NOT A PASSING TEST: grep for the POSITIVE result, and if a plant makes the code uncompilable, ask whether the test needs the build at all.** **Residual stated in the file: it constrains the application's writers and cannot constrain raw SQL, a DBA or a migration.** | ⚠⚠⚠ **THE POPULATION IS *TYPES THAT TRANSITION FISCAL PERIOD STATE*; THE ASSERTION IS *TAKES THE EXCLUSIVE FENCE*. Check the predicate does not name the assertion — it does not, and that is why this one is buildable.** ⚠ **FLOOR IT AT ≥1: a population of one is worth guarding precisely because it catches the SECOND, and a derivation that silently returns zero would pass while asserting nothing.** **Plant it: add a throwaway second writer that skips the fence and confirm RED, then restore.** ⚠⚠ **ADJACENT, NOT REQUIRED — 249 is correct today and this protects against a FUTURE regression, which is why it is its own row rather than folded in. Same line that put the Payroll mapping INSIDE 249 and `B26` outside it.** **AND STATE THE RESIDUAL HONESTLY IN THE TEST: it constrains the APPLICATION's writers and cannot constrain raw SQL, a DBA, or a migration. A cooperative protocol checked for completeness is still cooperative.** |
| **249** | ⚠⚠⚠ **CONFIRMED BY EXECUTION: A JOURNAL CAN BE COMMITTED INTO A CLOSED PERIOD. THE TRANSACTION GIVES ATOMICITY AND THE COMMENT CLAIMS ISOLATION.** **Probe result verbatim: *WINDOW IS OPEN: a journal was committed into a period whose status is 'Closed'.*** **Connection A opened the transaction FIRST — the order 248 step 1 asserts — read the year and got SUCCESS from `ResolveOpenPeriodFor`. Connection B then ran `SET LOCK_TIMEOUT 3000; UPDATE [tenant].[GlFiscalPeriods] SET [Status] = N'Closed'` AND IT SUCCEEDED IMMEDIATELY — no 1222, so the poster's read held no lock. A then inserted and committed. Final state: period `Closed`, journal present, count 1.** ⚠ **THE LOCK TIMEOUT WAS THE INSTRUMENT: blocking would have been OBSERVABLE as an error, and the absence of that error is what confirms READ COMMITTED behaviour rather than the absence of an `IsolationLevel` setting.** **`BR-GL-0003` satisfied at read time, false at write time.** | **CLOSED 2026-09-01 — FIX `6f6447c`, CONTROL `1fa4bf0`, both TASK green.** **`FiscalPeriodPostingFenceSqlServerTests`, 119 lines, three legs: the state change REFUSED while a poster holds the fence, GRANTED with no poster so the refusal is attributable, and TWO POSTERS BOTH GRANTED.** ⚠⚠⚠ **THE THIRD LEG IS THE ENTIRE DISCRIMINATOR — planting `"Shared"` → `"Exclusive"` on the poster side leaves A/B AND THE MIRROR PASSING and fails only `Two_posters_do_not_block_each_other`, so an implementation that closed the race correctly AND SILENTLY SERIALISED THE HOT PATH would have scored two of three.** **Run before landing — filtered, 10s green, 15s planted — rather than merged unrun.** **Reported in the agreed words: the red demonstrated a journal landing in a closed period; the green demonstrates the two APPLICATION paths now exclude each other, and not that no path can close a period mid-post.** | ⚠⚠⚠ **NAME THE GUARANTEE BEFORE WRITING ANY TEST. Four remedies and they differ IN KIND: (a) an application lock, as `DefineFiscalYearCommandHandler` already takes via `IFiscalYearDefinitionLock` (`CalendarCommandHandlers.cs:37`, `:89`); (b) a higher isolation level on the poster's transaction; (c) a re-read inside the transaction — ⚠ WHICH NARROWS THE WINDOW AND DOES NOT CLOSE IT under READ COMMITTED, and must not be adopted believing otherwise; (d) engage the period's EXISTING rowversion by touching the row, so the poster's save raises the concurrency conflict that already protects `SetFiscalPeriodStateCommandHandler`.** ⚠⚠ **PROPOSE ONE WITH ITS COST, DO NOT PICK SILENTLY: (a) and (d) serialise posts against period-state changes and put contention on a hot path; (b) risks deadlocks across every write in that transaction. The architect will rule on the proposal; the code is in front of you and not in front of me.** ⚠ **AND THE THREE POSTERS ARE THE POPULATION, NOT ONE — `PostJournalDraftCommandHandler`, `ReverseJournalCommandHandler`, `GlJournalPoster`. A fix on the first alone reproduces the asymmetry 248 was built to find.** **STEP 2 IS WRITABLE ONLY AFTER THE MECHANISM IS CHOSEN, because the test asserts whatever guarantee the fix provides.** **248 step 1 STAYS — it is true, its header already says it proves the code is SHAPED to close the window and not that the window IS closed, and that sentence is now load-bearing rather than cautious.** |
| **248** | ⚠⚠⚠ **THE JOURNAL POSTER'S READ-THEN-ACT WINDOW IS CLOSED BY AN ARGUMENT, AND ITS SIBLING HANDLER'S IDENTICAL RULE IS CLOSED BY A TEST.** **`PostJournalCommandHandlers.cs:67` opens the transaction before the `BR-GL-0003`/`BR-GL-0004` checks and cites `TS-GL-0011` for the closed-between case; `TS-GL-0011` HAS ZERO FILES IN `tests/`.** **Only the STATIC case exists — `CalendarDomainTests.A_closed_period_refuses_posting_and_says_so_distinctly_from_being_absent` closes a period and then resolves it; nothing closes one BETWEEN the read and the write.** ⚠⚠ **AND `FiscalYearDefinitionOrderTests.The_lock_is_acquired_before_both_checks_and_inside_the_transaction` ASSERTS EXACTLY THIS ORDERING FOR `DefineFiscalYearCommandHandler`, with a comment saying the poster *records* the same rule — and the only occurrence of `PostJournalCommandHandler` in `tests/` is that comment.** | **CLOSED 2026-09-01 — STEP 1 AT `be2b062`'s successor and STEP 2 MERGED INTO 249's CONTROL, whose A/B leg IS *a period open at read time, closed before the write*, expressed through the fence rather than through raw SQL.** ⚠ **AND THE ITEM'S REAL RESULT WAS THAT STEP 1 WAS TRUE AND GUARDED THE WRONG PROPERTY: the ordering assertion proves ATOMICITY and the comment claimed ISOLATION.** **Its header said it proves the code is SHAPED to close the window rather than that the window IS closed — written as caution, and the load-bearing sentence in the file by the time the probe came back red.** | ⚠⚠⚠ **STEP 1 IS THE CHEAP ONE AND IT HAS A TEMPLATE ONE FILE AWAY: THE STRUCTURAL ORDERING TEST FOR `PostJournalCommandHandlers`, MODELLED ON `FiscalYearDefinitionOrderTests` — `BeginTransaction → checks → Save`.** **It runs under TASK, it is the sibling's own idiom, and it converts *the poster records the same rule* from a comment into an assertion.** ⚠ **STEP 2, SEPARATE: the actual contention test — a period open at read time and closed before the write. That is Integration-scoped, so per `DEC-L-007` IT MERGES WITHOUT ITS SUITE RUNNING UNDER TASK. Say so when you land it, and do not report it as verified until a PHASE run has executed it.** ⚠⚠ **DO NOT LET STEP 1 STAND IN FOR STEP 2 IN THE REPORT: an ordering assertion proves the CODE IS SHAPED to close the window; only step 2 proves the window IS closed. Name which one each test does.** **AND IF STEP 1 FAILS, STOP IMMEDIATELY AND REPORT — that would be a live defect on a financial posting path, not a test to fix.** |
| **247** | ⚠⚠⚠ **THREE PRODUCTION FILES EXPLAIN WHY A PIECE OF LOGIC IS SAFE BY CITING A TEST THAT DOES NOT EXIST. FIND OUT WHETHER THE BEHAVIOUR IS ASSERTED UNDER ANOTHER NAME — THAT IS THE QUESTION, NOT WHETHER THE CITATION IS FALSE.** **Verified by the architect, per id: `tests/`=0, `src/`=1, `docs/`=2–3 for all three.** **(a) `PayrollCalculator.cs:431` — calendar-day proration, *"Both boundaries are inclusive… `TS-PAY-0010` asserts both ends, because off-by-one at a boundary is the defect this method exists to get right."* **(b) `PostJournalCommandHandlers.cs:67` — the transaction opens before the checks; *"`TS-GL-0011` asserts the closed-between case"* — a read-then-act window on `BR-GL-0003`/`BR-GL-0004`. **(c) `Position.cs:32` — `TS-POS-0044`, the cutover cycle, already understood.** | **CLOSED 2026-09-01 — THREE DISPOSITIONS, AND THE SPLIT IS THE RESULT.** ⚠ **PAYROLL: ASSERTED, BOTH ENDS.** `A_mid_period_joiner_is_prorated_by_calendar_days_at_both_boundaries:120` pays a last-day hire one day (3100/31 = 100) and `An_employee_terminated_within_the_period_is_paid_for_the_days_worked:138` pays a first-day termination one day — **and the test carries `PayrollCalculator.cs:431`'s own sentence verbatim.** **HR/POSITION: NOT ASSERTED, for the structural reason recorded at 244.** ⚠⚠⚠ **GL: NOT ASSERTED, AND THE STRUCTURAL PROXY IS MISSING TOO — carried to 248.** **A FALSE CITATION IS NOT AN UNTESTED BEHAVIOUR: two of three were false citations over covered code, and only one was a real gap.** ⚠ **AND THE ARCHITECT'S ESCALATION CRITERION IS WITHDRAWN — a missing test is WORK, not an owner decision; it becomes owner-facing only if the test reveals a defect or cannot be written.** | ⚠⚠⚠ **A FALSE CITATION IS NOT AN UNTESTED BEHAVIOUR AND THE TWO MUST NOT BE BLURRED. For (a) and (b), SEARCH FOR THE ASSERTION, NOT THE ID: does any test pay a hire on the LAST day of a period and a termination on the FIRST, each for one day? Does any test hold a period open at read time and close it before the write?** **Report per item: ASSERTED BY `<test>`, PARTIALLY ASSERTED (say which end), or NOT ASSERTED — and record the search behind every disposal so an *absent* can be falsified by re-execution.** ⚠⚠ **START WITH (a). It is money arithmetic at a boundary, the comment names off-by-one as the defect the method exists to prevent, and `untested-and-wrong-are-correlated` says the unconstructed path is where the defects were.** ⚠ **DO NOT WRITE THE MISSING TESTS IN THIS ITEM AND DO NOT EDIT THE COMMENTS. Bring the three dispositions back first — if any comes back NOT ASSERTED it is an owner-facing correctness item, and what the comment should say depends on the answer.** **Search-only, no gate needed.** ⚠ **THE DOCUMENT SIDE IS THE ARCHITECT'S AND IS NOT IN THIS ROW: every `docs/` and ADR correction is written by the architect.** |
| **246** | ⚠⚠ **THE FIVE PRODUCTION COMMENTS STILL SAY THE CYCLE IS *VERIFIED IN SOURCE*, AND SINCE `3f2efa5` THAT UNDERSTATES WHAT IS TRUE.** **`DepartmentManager.cs`, `DepartmentManagerConfiguration.cs`, `Position.cs` (`:30` is the explicit one), `JobGradeConfiguration.cs`, `PositionConfiguration.cs` each justify a design decision by an argument that is now a TEST.** ⚠ **A record that is stale in the direction that CAUSED the finding is the worst direction: the next reader repeats the source audit that item 244 existed to retire.** | **CLOSED 2026-09-01 — `50c2b12`, TASK green.** **The four clean comments now name `CutoverCopyOrderCycleTests`, carry the precision that the test calls the public `Build` while `Order` is private and holds the cycle check, and carry the bound in a paragraph of their own.** ⚠ **VERIFIED COMMENT-ONLY BEFORE THE GATE RAN, using `gate_condition_4`'s own filter — zero non-comment lines — SO CONDITION 4 REPORTED CORRECTLY RATHER THAN BY ACCIDENT.** **242(D) used as a forward instrument nine hours after it was written as a defect fix.** **`Position.cs` deliberately untouched: it cites `TS-POS-0044`, which does not exist, and replacing a false citation with a true one would have buried the fact that the false one was load-bearing.** | **Point each comment at `CutoverCopyOrderCycleTests` (`tests/Platform.Tests/TenantStorage/`) by NAME, so a reader can execute the claim rather than re-derive it.** ⚠⚠ **AND CARRY THE BOUND INTO EVERY ONE OF THEM, OR THIS ITEM MAKES THE RECORD WORSE: what is tested is that THE PLANNER REFUSES CYCLES; that this entity's shape PRODUCES one is still read from the model.** **Do not write *verified by test* unqualified.** ⚠ **Comment-only, five files, `src/`, TASK scope. If any comment turns out to rest on something step 1 does NOT prove, STOP AND REPORT IT — that is a finding, not an obstacle.** |
| **244** | ⚠⚠⚠ **ASSERT `CutoverCopyOrderUndecidable` — IT JUSTIFIES FIVE PRODUCTION DESIGN DECISIONS AND NO TEST HAS EVER EXECUTED IT.** **Zero non-comment matches across `tests/`; every occurrence in the tree is a comment.** **Cited in `DepartmentManager.cs`, `DepartmentManagerConfiguration.cs`, `Position.cs`, `JobGradeConfiguration.cs`, `PositionConfiguration.cs` — and `Position.cs:30` says *"verified in source for Department's naive manager"*, which admits the method.** ⚠ **`TenantCutoverCopyPlan.Build(IModel model)` TAKES A MODEL, so this is an ordinary unit test, not an integration one.** **The planner selects `ITenantOwnedEntity` implementors with a table name and that interface is ONE property (`Guid TenantId`).** | **CLOSED 2026-09-01 — STEP 1 IN AT `3f2efa5`; STEP 2 STOPPED ON PLACEMENT AND `AC-DEP-0034` RECORDED AS COVERED-BY-MECHANISM.** **TASK green, 7 suites, 0 warnings, `Platform.Tests` 1101 → 1103.** **Step 1 proves the planner returns `CutoverCopyOrderUndecidable` for a foreign-key cycle among tenant-owned entities, isolated by a matched acyclic control from the two unrelated conditions sharing that error value — which is the mechanism all five production comments rely on, so it discharges them.** ⚠⚠ **STEP 2 STOPPED FOR A REASON THE FALLBACK DID NOT ANTICIPATE — PLACEMENT, NOT COST: only `Architecture.Tests` references both `SSAS.HR.Domain` and `SSAS.Platform.Infrastructure`, `Platform.Tests` must not gain the reverse edge, and a contributor in `Architecture.Tests` would inject a FOREIGN-KEY CYCLE into a model five unrelated guards reason over while possibly moving no count at all.** ⚠ **`AC-DEP-0034` IS NOT CITED and carries the bound in the criterion itself: TESTED MECHANISM PLUS ARGUED SHAPE.** **Available again only if `B25` is done ON ITS OWN MERITS.** | ⚠⚠⚠ **STEP 1 AND IT IS THE WHOLE VALUE: TWO SMALL SYNTHETIC TENANT-OWNED ENTITIES IN A MUTUAL FOREIGN-KEY CYCLE. That is the mechanism the five comments rely on — *the planner refuses cycles* — and it discharges all five at once.** ⚠⚠ **THREE THROW SITES ARE THREE FACTS: `:99`, `:111`, `:153`. NAME THE SITE THE TEST FIRES AND REPORT THE OTHER TWO AS STILL UNEXERCISED.** **A message-only assertion cannot distinguish them if they share text — assert the site, by condition, or say you could not.** ⚠ **STEP 2, SEPARATE AND SMALLER: the real `Department` with a shadow manager foreign key, which is `AC-DEP-0034`'s literal wording. DO NOT CITE `0034` TO STEP 1 — a two-entity cycle proves the planner refuses cycles and proves nothing specific to Department, and that bound is the coder's own and is correct.** **If step 2 turns expensive, STOP and record `0034` as covered-by-mechanism with the bound named — do not stretch step 1 over it.** ⚠ **Gated, TASK scope. The `[Trait]` goes on step 2, not step 1.** |
| **251** | ⚠⚠⚠ **A RESTORE POINT THAT IS INHERITED RATHER THAN CREATED CAN SILENTLY DESTROY WORK, AND THE VERIFICATION WE HAVE USED ALL SESSION IS CIRCULAR.** **`git checkout-index -f --` after a plant restored a STALE index — staged before an analyser fix and the offender-collection change were made — and silently overwrote both.** ⚠⚠ **`git diff --quiet` COULD NOT SEE IT: after a restore FROM the index it compares AGAINST the index, which is the very version that did the overwriting. THE CHECK CONFIRMS THE RESTORE HAPPENED, NOT THAT THE RESTORED CONTENT IS WHAT YOU WANTED.** **Only the gate re-emitting a warning already fixed surfaced it.** **Every plant tonight used that pair.** | **CLOSED 2026-09-01 — PROTOCOL ADOPTED AND THE AUDIT FOUND ONE.** **POPULATION 7 items with a plant; 40 discrete claims taken from the commit messages and tested against the tree; 1 NOT SATISFIED.** ⚠⚠⚠ **THE ONE: 249's message says *`InspectPostingWindowAsync` deliberately takes no fence* — TRUE OF THE BEHAVIOUR AND ABSENT FROM THE FILE.** **The architect asked for it to be stated in the file BEFORE gating; the coder stated it IN THE MESSAGE AND IN THE COMMIT, and neither window checked the file.** **A COMMIT MESSAGE IS NOT WHERE A READER OF THE FILE LOOKS** — after 249 a reader finds two of the poster's three public methods fenced and the third not, with nothing there addressing it, which is the assumption of carelessness that was explicitly predicted, arriving through the one channel neither window checked.** **Now written at the method (verified by the architect at `GlJournalPoster.cs:265-280`), including that FENCING IT WOULD BE WORSE THAN USELESS: a shared lock held across a read that grants nothing would make the answer look like a promise it cannot keep.** **The `TS-GL-0011` correction was specifically re-checked after the stale-restore incident and is intact.** **39 of 40 satisfied, reported as a result rather than as silence.** | ⚠⚠⚠ **TWO PARTS. (1) PROTOCOL, FROM NOW ON: a restore point is CREATED AT THE MOMENT OF THE PLANT, never inherited — either stage immediately before planting, or restore from `HEAD` (`git checkout HEAD -- <path>`) and verify with `git diff HEAD --quiet -- <path>`. THE VERIFICATION MUST NOT COMPARE AGAINST THE RESTORE SOURCE.** ⚠⚠ **(2) BOUNDED AUDIT, AND IT IS TIME-SENSITIVE BECAUSE IT DEPENDS ON WHAT YOU REMEMBER INTENDING: tonight's commit messages are unusually specific — USE EACH AS THE SPECIFICATION AND CHECK THE TREE AGAINST IT.** **Same move as recounting the tree against the criteria, which is what found 18-planned-17-written.** **Cover the items that involved a plant: 242, 244, 246, 248, 249, 250, 243.** ⚠ **Report POPULATION, COUNT, DIFFERENCE — commits checked, claims checked, claims not satisfied — and if the answer is *nothing missing*, SAY SO AS A RESULT rather than reporting nothing.** **Search-only; no gate needed unless something is missing.** |
| **256** | ⚠⚠⚠ **A FIFTH SHAPE, FOUND BY ACCIDENT IN THE GUARD EVERY OTHER SCOPE GUARD IS MODELLED ON: AN ASSERTION LOOP THAT SKIPS EVERY MEMBER. `No_global_query_filter_scopes_company_or_branch` PUT EVERY ASSERTION INSIDE `if (filter is null) continue;` WITH NO FLOOR — a composed model contributing no query filter at all skips every iteration and REPORTS SUCCESS.** ⚠⚠ **AND THE NAME INVERTS: *no filter scopes company or branch* IS TRIVIALLY TRUE WHEN THERE ARE NO FILTERS, so the guard's own title describes the state vacuity produces.** **THIS IS NOT THE EMPTY-COLLECTION CASE: THE COLLECTION IS NON-EMPTY AND EVERY MEMBER IS SKIPPED, so a floor on the collection passes and only a floor on WHAT WAS EXAMINED catches it.** **Fixed and planted at `9680cf7`. POPULATION, derived by the architect 2026-09-01: `continue;` appears in 18 files under `tests/Architecture.Tests` — `RouteConstraintArchitectureTests` 6, `EmptyCatchArchitectureTests` 6, `RouteAndHandlerPermissionPairingTests` 4, then twos.** | **CLOSED 2026-09-01 BY MEASUREMENT, NOT BY WORK — ITS REAL POPULATION IS THE ONE INSTANCE ALREADY FIXED AT `9680cf7`. TEN OF TEN CANDIDATES CHECKED COME BACK ALREADY FLOORED.** ⚠⚠⚠ **AND THE `continue;` COUNT IN THIS ROW WAS THE ARCHITECT'S AND WAS A PROXY THAT DOES NOT HOLD: THE THREE FILES NAMED AT THE TOP ARE THE THREE BEST EXAMPLES OF THE DISCIPLINE IN THE REPOSITORY, NOT THE WORST.** **`EmptyCatchArchitectureTests` floors `files >= 40` AND `clauses >= 70` — the population AND what survived the skip; `RouteConstraintArchitectureTests` floors three separate counts; and `RouteAndHandlerPermissionPairingTests` asserts `pairings == ExpectedPairings`, AN EXACT COUNT OF WHAT SURVIVED BOTH `continue`s, which is STRONGER THAN A FLOOR — fewer means half a pairing vanished, more means a new one arrived, and either way a person decides rather than the number drifting.** **A FLOOR CATCHES ONLY SHRINKAGE; AN EXACT COUNT CATCHES BOTH DIRECTIONS.** ⚠⚠ **THE DISCRIMINATOR THAT HOLDS: A SKIP ON A DATA-DEPENDENT CONDITION CAN BE UNIVERSAL; A SKIP ON NAMED MEMBERS CANNOT.** **BOUNDS STATED BY THE CODER: ten of eighteen is A SAMPLE, NOT A CENSUS, and a floor being present is not proof it is the RIGHT floor — each was checked to count something the loop populates, not that its bound is well chosen.** | ⚠⚠⚠ **ONE DECIDABLE QUESTION PER SITE: DOES THE TEST COUNT WHAT IT EXAMINED, OR ONLY WHAT IT ITERATED? A floor on the source collection does not answer it.** **NOT EVERY `continue` IS A DEFECT — skipping members the rule legitimately does not reach is correct, and the defect is only that NOTHING PROVES ANY MEMBER SURVIVED THE SKIP.** ⚠ **PER-SITE AND STOPPABLE. Report the population and the SURVIVAL RATE per file, never a raw count — every list in this family has over-reported, and the last one measured 0 of 13 at the top.** **PLANT BY MAKING THE SKIP UNIVERSAL — force the condition that skips everything, and confirm the floor fails; a plant that only breaks one member proves the assertion, not the floor.** ⚠⚠ **AND KEEP IT SEPARATE FROM 252: that item is about an assertion's VOCABULARY, this one is about CONTROL FLOW, and folding them would give one item three unrelated remedies.** |
| **255** | ⚠⚠⚠ **SEVEN READ SERVICES IN `src/`, AND COMPOSED-PREDICATE SCOPE GUARDS FOR EXACTLY TWO OF THEM.** **Verified by the architect 2026-09-01: `AttendanceReadService`, `GlReadService`, `DepartmentReadService`, `EmployeeReadService`, `EmployeeRunHistoryReadService`, `PositionReadService`, `PayrollReadService` — and `tests/Architecture.Tests` holds `EmployeeReadScopeArchitectureTests` and, since `f49d998`, `DepartmentReadScopeArchitectureTests`. Nothing else.** ⚠ **`ReadSideEscapeArchitectureTests` IS NOT THE THIRD: it asserts a DIFFERENT property — that an event-raising aggregate never escapes through a read-side service — and names read services only to build that population.** ⚠⚠ **AND `PositionReadService:16` CARRIES THE IDENTICAL `TenantId = @tenant AND CompanyId IN (@companies)` COMMENT that `DepartmentReadService:16` does, which is the shape 253 had to strip before it could assert anything.** **`GlReadService` is the one that carried TWO LIVE PRODUCTION DEFECTS, found because no test had ever constructed it.** | **OPEN** | ⚠⚠⚠ **THIS IS A POPULATION QUESTION, NOT A DEFECT CLAIM, AND IT MUST BE REPORTED AS ONE. DO NOT ASSUME THE OTHER FIVE NEED THE SAME PREDICATE** — scoping obligations differ per module, and a guard demanding a company predicate of a service that is legitimately tenant-only would be a false alarm that gets the whole class switched off. **STEP 1, SEARCH-ONLY: for each of the seven, what scope does the module's own ADR or criteria REQUIRE, and does anything assert it? Report the seven with their required scope and their guard, and NAME THE ONES NOBODY HAS SPECIFIED.** ⚠ **STEP 2 ONLY AFTER STEP 1 IS REPORTED: build guards where step 1 established an obligation, one module at a time, each derived by MECHANISM (*reaches this entity set*), floored, comments stripped, and planted in both directions.** **Per-module and stoppable.** **AND CARRY THE BOUND EVERY TIME: an architecture guard asserts the SHAPE of a query and never that the shape was APPLIED at runtime.**  ⚠⚠⚠ **STEP 1 IS DONE (search-only, 2026-09-01) AND ITS ANSWER REFRAMES THE ITEM: NOT *FIVE UNGUARDED SERVICES* BUT **SIX MODULES WITH NO SPECIFIED OBLIGATION, AND ONE OF THE TWO GUARDS ENFORCING A RULE THAT EXISTS ONLY AS A TEST**. `AC-DEP-0044` is the ONLY criterion in the docs tree requiring this, and EMPLOYEE — the most elaborate scope guard in the repository — HAS NONE.** **And there is NO LIVE DEFECT: all seven compose explicit tenant and company predicates today, none calls `IgnoreQueryFilters`, and Attendance additionally composes branch.** ⚠⚠ **THE OBLIGATION IS NOW WRITTEN AND IS DERIVED RATHER THAN INVENTED — see *A scope predicate is owed by the entity's ownership classification* in `Architecture-Principles.md`: for each dimension an entity DECLARES (`ITenantOwnedEntity`, `ICompanyOwnedEntity`, `IBranchOwnedEntity`), every read path reaching it composes an explicit predicate on that dimension.** **So step 2's guard CITES a rule instead of CREATING one, and no module can be given an obligation it did not already owe — which is why Attendance owes branch and Gl does not.** ⚠⚠⚠ **AND THE GUARD MUST ANCHOR ON THE COLUMN (`TenantId`, `CompanyId`, `BranchId`), NEVER ON THE LOCAL VARIABLE: a matcher keyed to `scope.` reported `AttendanceReadService` as having NO tenant predicate when it has THREE, because it binds `resolved.Value.TenantId` and `readScope.TenantId`.** **That error was caught while MEASURING; inside the guard it would have fired falsely on day one, and A FALSE ALARM IN A REPORT COSTS A CORRECTION WHILE A FALSE ALARM IN A GUARD COSTS THE GUARD.** **Strip comments too — three read services open with the SQL predicate written out in prose.** **GL FIRST when step 2 runs: 14 tenant and 7 company predicates that nothing holds in place, in the service that carried two live production defects.** |
| **253** | ⚠⚠ **`AC-DEP-0044` NAMES AN ARCHITECTURE GUARD THAT DOES NOT EXIST, AND THE EMPLOYEE EQUIVALENT IS A WORKING MODEL ONE FILE AWAY.** **The criterion says an architecture guard asserts that Department reads are scoped by EXPLICIT COMPOSED PREDICATES rather than by a global filter.** **`EmployeeReadScopeArchitectureTests` asserts `scope.Companies.CompanyIds.Contains` IS present in the scoped query source and `IgnoreQueryFilters` IS NOT.** ⚠ **NO DEPARTMENT ARCHITECTURE FILE ASSERTS COMPOSED PREDICATES AT ALL — `CompanyId ==\|TenantId ==\|IgnoreQueryFilters` across `tests/Architecture.Tests/Department*.cs` returns nothing (coder's search, 2026-09-01).** **The *documented, unbuilt* shape, on a scope guard.** | **CLOSED 2026-09-01 — f49d998, TASK green, 7 suites, 3236 tests, 0 warnings, baseline Architecture Debug 632 to 634.** **`DepartmentReadScopeArchitectureTests`, two tests, both carrying the `AC-DEP-0044` criterion trait, both floored at `>= 1` with a message saying THE DERIVATION HAS STOPPED MATCHING rather than that nothing was found.** **Population derived BY MECHANISM — *reaches a Department entity set*, via `Set<Department>()` and `Set<DepartmentManager>()` — deliberately NOT *reads that carry a company predicate*, which would have let the defect leave the population.** ⚠⚠ **EACH PLANT REDDENED EXACTLY ONE OF THE TWO TESTS, TWICE, WHICH IS THE EVIDENCE THEY ARE INDEPENDENT rather than one broad test catching both while the other rides along — THE COUNT IS THE EVIDENCE, NOT THE COLOUR.** ⚠⚠⚠ **AND A TRAP WAS ALREADY WAITING: `DepartmentReadService:16` OPENS WITH A COMMENT CONTAINING THE SQL PREDICATE ITSELF — a raw-text guard would have been SATISFIED BY THE PROSE while the query composed nothing, and would have PASSED the plant that removes the predicate. Comments are stripped and the file says why.** **Strict AC-DEP coverage 39 to 40.** | ⚠⚠⚠ **DERIVE THE POPULATION, DO NOT NAME THE READS: the subject is *Department read paths*, the assertion is *scoped by explicit predicates and no `IgnoreQueryFilters`*. CHECK THE PREDICATE DOES NOT NAME THE ASSERTION** — *reads that carry a company predicate* would exclude exactly the defect. ⚠ **FLOOR IT: a derivation that finds no Department read path passes while asserting nothing.** **PLANT BOTH DIRECTIONS — remove a predicate from a read and confirm RED; introduce `IgnoreQueryFilters` and confirm RED — and per the new protocol the restore point is created AT THE MOMENT OF THE PLANT, `git add` as its first line, or restore from `HEAD` and verify `git diff HEAD --quiet`.** ⚠ **THIS IS A CONTROL, NOT A PROBE — IT LANDS.** **Cite `AC-DEP-0044` when it is green; strict count moves 39 → 40.** ⚠⚠ **AND SAY IN THE FILE WHAT IT DOES NOT COVER: an architecture guard asserts the SHAPE of a query, never that the shape was APPLIED at runtime — the same bound that let a total, cheap, passing scope guard sit above two broken read services.** |
| **254** | ⚠⚠ **`B26` — FOUR DISTINCT LEDGER REFUSALS REACH THE PAYROLL OPERATOR AS ONE MESSAGE, AND 249 MADE THIS SMALL.** **`PeriodNotFound`, `AccountUnavailable`, `Unbalanced` and `ReversalTargetUnavailable` all map to `PayrollErrors.LedgerRefusedPosting`. An operator whose run failed because an ACCOUNT WAS UNAVAILABLE is told the same thing as one whose journal was UNBALANCED — different problems, different fixes, one sentence.** ⚠⚠⚠ **AND `JournalPostingStatus.PeriodNotFound`'s OWN COMMENT IS THE ARGUMENT AGAINST IT, IN THE CONTRACT THE CONSUMER READS: *distinct from closed, because the operator's remedy is different: define the calendar rather than reopen a period.*** **After 249 the four are NAMED ARMS THAT SHARE A VALUE rather than a fall-through, so this is four one-line changes plus their messages — not the switch rewrite it was.** | **CLOSED 2026-09-01 — `0aad3bb`, gate green, API 940 to 941.** **`PayrollErrors.LedgerHasNoFiscalPeriod` → `payroll.fiscal_period_undefined` (409), wired at both the posting and reversal arms following `PeriodClosedForPosting`'s precedent of one error serving both.** ⚠⚠ **AND THE LIVE DECISION WAS MADE UNPROMPTED: THE EXISTING `PayrollErrors.FiscalPeriodNotFound` WAS NOT REUSED. It is the APPROVAL-time check, its message says *cannot be approved*, and `PayrollEndpointTests:213` already pins its generic 404 as a poor answer deliberately left alone — REUSING IT WOULD HAVE CARRIED BOTH DEFECTS INTO A NEW SITE UNDER COVER OF CONSISTENCY.** **409 because the same body succeeds unchanged once Finance defines the year, and `POST /fiscal-years` EXISTS — the remedy was checked to be one the product can perform rather than assumed.** ⚠ **Plant: reverting the arm reddened the new test AND ONLY the new test, 23 of 24 green including the `AccountUnavailable` sibling — and since BOTH ARE 409, status alone would not have distinguished them, so the plant proves the CODE is what carries the remedy.** | ⚠⚠ **EACH NEW `PayrollErrors` MEMBER IS A USER-FACING STRING AND A MAPPED STATUS CODE. READ WHAT THE API ANSWERS FOR EACH BEFORE CHOOSING, and do NOT invent a remedy the product cannot perform** — *define the calendar* is only useful advice if the operator can reach that surface. ⚠ **NAME EACH FOR THE SITUATION AND THE OPERATOR'S NEXT ACTION, NOT FOR THE MECHANISM — the rule that made `PostingFenceBusy` into `PeriodStateChanging`, and it is cheaper here because these are internal to Payroll rather than a cross-module contract.** **PER-MEMBER AND STOPPABLE: distinguishing ONE of the four is a complete improvement on its own, and `PeriodNotFound` is the one the contract already argues for.** ⚠ **The mapping guard from 249 (`JournalPostingStatusMappingTests`) must still pass — it asserts every member is NAMED in the consuming module's mapping source, and this item only changes what the named arms RETURN.** |
| **252** | ⚠⚠ **`B23` SWEEP — A NEGATIVE ASSERTION OVER A PREDICATE PASSES WHEN THE PREDICATE MATCHES NOTHING, AND IT IS PROVEN TO HAPPEN HERE.** **`Assert.DoesNotContain(xs, x => x.Name == "Foo")`, `Assert.Null(xs.FirstOrDefault(p))`, `Assert.Empty(xs.Where(p))`, `Assert.False(xs.Any(p))` cannot distinguish *the thing is absent* from *my predicate is broken*.** **MEASURED, NOT ARGUED: one literal in the cutover suite planted as `"Departmentt"` and the suite returned PASSED, 6 OF 6.** | **OPEN** | ⚠⚠ **SWEEP MECHANICALLY, NOT BY READING — and note WHY reading fails here: THE LOUD SITES SIT IN THE SAME FILE AS THE SILENT ONES, so a spot-check meets a correct site first and clears the file. Both windows made exactly that error on the same file.** **Grep `DoesNotContain`, `Assert.Null(`, `Assert.Empty(`, `Assert.False(` with a lambda; then ask of each: CAN THE PREDICATE MATCH ANYTHING AT ALL?** ⚠ **TWO FIXES, PREFER THE FIRST: make a wrong name a COMPILE ERROR (`nameof`, a typed reference, an enum); where the predicate must stay dynamic, ADD A POSITIVE COMPANION proving it matches when the subject IS present.** **PER-SITE AND STOPPABLE. Do NOT report a negative assertion as defective merely for being negative — the defect is a predicate NOTHING PROVES CAN MATCH.** ⚠ **Report the population and the difference; a bare count of sites proposes a frame the reader then reasons in.** |
| **243** | ⚠⚠⚠ **`B20`'s WORST INSTANCE, AND IT IS A SECURITY GUARD: FOUR AUTHORIZATION TESTS RUN OVER NINE HAND-WRITTEN ROUTES WHILE THE DERIVED POPULATION SITS ONE FILE AWAY.** **`PlatformSupportAuthorityAuthorizationTests.AuthorityRoutes()` is a `TheoryData` of literal method/path pairs (verified, `:44-50+`), feeding `Every_authority_route_rejects_an_anonymous_request`, `..._a_mixed_plane_token`, `..._a_platform_token_without_administer` and `..._a_tenant_plane_token_carrying_the_administer_name`.** ⚠⚠ **AND `PlatformRouteInventory.cs` — SAME DIRECTORY — READS `EndpointDataSource.Endpoints` AND FILTERS BY PREFIX, CONSUMED BY `PlatformSupportAuthorityRouteInventoryTests`.** **SO A TENTH AUTHORITY ROUTE IS CAUGHT BY THE INVENTORY TEST, WHICH IS ABOUT THE ROUTE LIST, AND IS SILENTLY EXEMPT FROM ALL FOUR AUTHORIZATION GUARDS, WHICH ARE ABOUT WHO MAY CALL IT.** **The cheap test sees the new route and the expensive one does not.** | **CLOSED 2026-09-01 — `7fbd0b6`, TASK green, 7 suites, 0 warnings, `API.Tests` 940.** **Both plants fired: the derivation matching nothing fails all four guards, AND A TENTH UNPROTECTED ROUTE MAPPED UNDER THE PREFIX FAILS ALL FOUR — the second is the item's only real proof, since before the substitution they iterated a hand-written nine and only the inventory test would have seen it.** **The offender-collection change is in: every guard now names EVERY unprotected route rather than the first.** ⚠ **`[MemberData]` IS EVALUATED AT DISCOVERY TIME, BEFORE `InitializeAsync` BUILDS THE HOST, so a host-dependent derived population cannot feed it — the loop-in-a-`Fact` is the CORRECT shape and the impossibility is recorded in `B20`.** **`PlatformRouteInventory.Under(IServiceProvider, prefix)` added with the existing overload delegating: ONE derivation, TWO hosts, and `B21` moved forward as a side effect.** **Header states the reason: *this changed nothing today; it changes what happens the day someone adds a route*.** ⚠ **Residual: four guards carry a mis-indented loop body from the edit — cosmetic, nothing gates on it, fold it in next time the file is touched.** | ⚠⚠ **STEP 1, AND IT NEEDS THE BUILD: DO THE NINE HAND-WRITTEN ROUTES EQUAL THE DERIVED SET TODAY? Nobody has verified that and it may well be exact — WHICH IS WHAT MAKES IT INVISIBLE. Report the comparison before changing anything.** **STEP 2: feed the four tests from `PlatformRouteInventory` instead of the literal list.** ⚠ **FLOOR IT AND NAME THE NUMBER, or a derived population that returns nothing turns four security guards into four vacuous passes — which is strictly worse than the hand-written list it replaces.** **Gated, TASK scope. Do not start while `bo8fis611` runs.** |
| **242** | ⚠⚠ **MAKE `scripts/gate.sh` IMMUNE TO BEING EDITED WHILE IT RUNS — SNAPSHOT AND EXEC THE COPY.** **Tonight it cost 51 minutes and sixteen green legs: bash reads a script BY BYTE OFFSET, my insertion shifted every offset after it, and the running instance resumed mid-statement and died at the very end with no verdict.** **`bash -n` was clean the whole time; the file was never wrong.** **PROPOSAL: at startup, copy the script to a temp path and `exec` that copy, so the executing text can never change underneath the run.** ⚠ **Guard it against re-entry — an env marker so the copy does not copy itself — and preserve `$0`-dependent behaviour, the exit codes, and the trap/abort paths exactly.** | **CLOSED 2026-09-01 — (A)(B)(C)(D) SHIPPED; THE ORIGINAL BELT WITHDRAWN, AND THE WITHDRAWAL IS THE RULING WORTH KEEPING.** ⚠⚠⚠ **(C) FIRED IN PRODUCTION ON `b1potbz2w` AND EVERY PART OF IT ANSWERED — the evidence line (*median of 5/5; spread 19 MB; mixed*), the *ALREADY COMPLETED THIS RUN: Debug*, and the process count with its discriminator (*3 … FEW means the box is genuinely occupied and the shutdown will free nothing*).** ⚠⚠ **BOTH DISCRIMINATORS GAVE THE RIGHT ANSWER, AND THE RETURN WAS A DECISION RATHER THAN A MESSAGE: 19 MB of spread says NOT the volatility case, so a restart is 40 minutes to the same wall — *try again and see* became *do not, and here is why*.** ⚠⚠ **AND THE OFFLINE DEMONSTRATION PREDICTED THE PRODUCTION OUTPUT EXACTLY — same lines, same order, different numbers. Those demos were justified as *the only instrument that reaches this code*, an argument from NECESSITY that cannot be wrong; it has now been checked against the real event once and held.** **(D) applied with a conclusive before/after on the identical harness — healthy path unchanged, both failure paths now `not compared: … exited 128` — and it is THE ONLY ONE OF THE SIX NOT OBSERVED FIRING: latent, found by reading, confirmed by injection.** ⚠⚠⚠ **THE SNAPSHOT-AND-EXEC BELT IS WITHDRAWN, NOT DEFERRED, ON THIS ROW'S OWN STOP CONDITION — *if it complicates the abort or exit paths at all, do not do it*. Tonight made that condition binding rather than theoretical: THE ABORT AND EXIT PATHS ARE NOW THE MOST VALUABLE CODE IN THE FILE AND THE ONLY PART WITH PRODUCTION EVIDENCE BEHIND IT, and an exec-the-copy wrapper wraps exactly them.** ⚠ **AND THE HAZARD IT INSURES AGAINST IS ALREADY CLOSED BY THE PROTOCOL: the architect does not edit `scripts/gate.sh` at all — ruling written here, applied there, between runs — and it has held for every change since. INSURANCE AGAINST A HAZARD THE PROCESS NO LONGER PRODUCES, BOUGHT BY EDITING THE ONE REGION THAT MUST NOT BREAK.** | ⚠⚠ **AND IF IT COMPLICATES THE ABORT OR EXIT PATHS AT ALL, DO NOT DO IT — SAY SO AND STOP. A gate that is harder to reason about is a worse trade than an edit-during-run I have already promised not to make.** **The protocol change is the real fix and it is already in force: I DO NOT EDIT `scripts/gate.sh` FROM HERE — I write the ruling, you apply it, between runs.** ⚠ **This row is the BELT, not the braces.** **Test it by editing the copy's SOURCE mid-run and confirming the run is unaffected — that is the plant, and it is the same shape as tonight's failure.** ⚠⚠⚠ **AND A THIRD CLAUSE, ADDED 2026-09-01 AND WORTH MORE THAN THE OTHER TWO: THE MEMORY FLOOR IS CHECKED BEFORE *RELEASE*, WHICH IS AFTER THE 25-MINUTE DEBUG INTEGRATION LEG HAS ALREADY PASSED.** **239 has now spent that 25 minutes THREE TIMES to fail at the same wall.** ⚠ **A PRE-FLIGHT MUST ASK *IS THERE HEADROOM FOR EVERY LEG THIS SCOPE WILL RUN*, AT MINUTE 0 — which is a different question from *is there enough right now*, and only the second exists.** **Cheapest honest form: at startup, when the scope includes a Release Integration leg, check the floor THEN and abort before anything expensive runs.** ⚠ **Do not make it a PREDICTION of future free memory — the box moved 800 MB in ten minutes tonight. Check the floor at minute 0 as a NECESSARY condition, and let the existing per-leg check remain the sufficient one.** ⚠⚠ **REFINED 2026-09-01 TWICE. FIRST, ON THE CODER'S COUNTEREXAMPLE: it predicted the Release check would land near 1893 MB from a measured 746 MB drawdown — EXACTLY THE PREDICTION THAT WOULD ABORT A RUN THAT MIGHT HAVE SUCCEEDED, so a projection is a reason to WARN and never to REFUSE.** ⚠⚠⚠ **SECOND, AND IT SUPERSEDES BOTH OUR CAUTIONS: THE FLOOR IS A SINGLE SAMPLE OF A QUANTITY THAT MOVES IN SECONDS. One attempt aborted at 471 MB free SECONDS AFTER 2639 WAS MEASURED, and read ~4300 two minutes later.** **A ONE-SHOT SAMPLE DOES NOT RELIABLY MEASURE EVEN THE PRESENT — so it will abort runs that would have succeeded AND pass runs that die at the next check, WITH NO OCCUPANCY CAUSE PRESENT AT ALL.** **CLAUSE: SAMPLE FIVE TIMES A SECOND APART AND COMPARE THE *MEDIAN* TO THE FLOOR. Report the SPREAD beside it — a wide spread says *this box is churning* where a low median says *this box is full*, and those want different responses.** ⚠ **AND REPORT WHETHER THE SAMPLES ARE MONOTONE: the architect measured 3526 → 3244 over five seconds, a 282 MB MONOTONE DECLINE, which is a DRAWDOWN; a lone 471 between 2639 and 4300 is an EXCURSION. Same five samples answer both, and the two mean different things.** **MEDIAN, NEVER MAX — a genuinely full box reads low in all five, and the per-leg check stays as the sufficient one.** ⚠⚠ **AND TWO MORE, ADDED 2026-09-01 AFTER READING THE SAMPLER ITSELF.** **(i) THE SHAPE IS A HINT, NOT A FINDING — the coder is right that FIVE OR SIX SAMPLES CANNOT DISTINGUISH THE CASES THEY APPEAR TO: a monotone fall is a drawdown OR one arm of a slow oscillation, and a lone dip is noise OR the trough of one. REPORT the shape, ACT on the median, and do not let the report claim to diagnose.** ⚠⚠⚠ **(ii) AN UNMEASURABLE BOX MUST BE ITS OWN OUTCOME. `gate.sh:1007` IS `FREE_MB=${FREE_MB:-0}` — VERIFIED — SO A FAILED OR EMPTY POWERSHELL CALL BECOMES `0`, BELOW EVERY FLOOR, AND ABORTS WITH `only 0 MB free`, WHICH IS INDISTINGUISHABLE FROM A GENUINELY FULL BOX.** **And `:1006` carries `2>/dev/null`, so the failure is SILENT as well as misread — two mechanisms compounding, and the second is what makes the first invisible.** ⚠ **THE FAILURE MODE CORRELATES WITH THE CONDITION THE CHECK IS FOR: a box under memory pressure is exactly where spawning a process is most likely to fail.** **RULE: abort on the median ONLY IF AT LEAST THREE OF FIVE SAMPLES RETURNED; with fewer, WARN AND PROCEED.** ⚠⚠ **AND THE WARNING MUST SAY *UNMEASURED* LOUDLY, BECAUSE THE RUN MAY THEN DIE OF THE VERY CONDITION NOBODY COULD MEASURE — and that death must not be read as a suite failure, which is the incident note 7a already exists to prevent.** **Do not silence stderr: capture it into the warning.** ⚠⚠⚠ **AND A THIRD MECHANISM, VERIFIED: THE PIPE MASKS THE EXIT STATUS. `gate.sh:2` IS `set -u` AND THE ONLY OCCURRENCE OF `pipefail` IN THE WHOLE FILE IS THE HEADER COMMENT AT `:68` WARNING ABOUT IT.** **So the pipeline's status is `tr`'s, and `tr` SUCCEEDS ON EMPTY INPUT: POWERSHELL CAN FAIL OUTRIGHT AND THE PIPELINE REPORTS SUCCESS.** **THREE MECHANISMS, ALL REQUIRED FOR THE SILENCE TO BE TOTAL: (1) `2>/dev/null` discards the error TEXT, (2) the unguarded pipe discards the exit STATUS, (3) `${FREE_MB:-0}` converts empty to a VALUE WORSE THAN ANY REAL READING, so it aborts unconditionally.** ⚠ **THE FIX IS ALL THREE OR IT IS NONE: capturing stderr alone leaves the pipe able to swallow a non-zero exit with partial output, and leaves the fallback able to turn any of it into an abort.** **Concretely: DROP THE PIPE — trim in the shell, not through `tr` — KEEP THE EXIT STATUS, and make an unmeasured sample ITS OWN OUTCOME rather than a zero.** ⚠⚠⚠ **AND THE CLASS IS TWO, NOT ONE — VERIFIED 2026-09-01 AFTER THE CODER BOUNDED IT TO ONE. `gate.sh:1061-1065` BUILDS THE TESTHOST PRECONDITION IDENTICALLY: `powershell.exe … 2>/dev/null | tr -d '[:space:]'`, THEN `HOSTS=${HOSTS:-0}`, THEN `if [ "$HOSTS" -ne 0 ]` — same three mechanisms, and `0` is in-domain because NO TESTHOSTS IS THE NORMAL CASE.** ⚠⚠ **AND THE DIRECTION IS THE OPPOSITE AND WORSE: `FREE_MB` FAILS LOUD-AND-WRONG (aborts a healthy box), `HOSTS` FAILS SILENT-AND-PERMISSIVE — a failed measurement makes the precondition PASS, so the gate proceeds believing no sibling suite is live and may drop catalogs in use, which is the exact incident that guard exists to prevent.** **FIX BOTH, THE SAME WAY, IN THE SAME PASS.** ⚠ **Milder third at `:976`: `[ "${ahead:-0}" = "0" ]` reports *HEAD matches ref* when `git rev-list` returns nothing — DISPLAY ONLY, nothing gates on it, and its own else-branch uses `:-?` correctly. Note it, do not change it.** ⚠⚠⚠ **AND THE SET IS FIVE, NOT TWO — CORRECTED AGAIN, AND MY *ONE SHORT* WAS ALSO SHORT: `:1007` FREE_MB (loud-and-wrong), `:1064` HOSTS, `:1083` PROTECTED, `:1217` BUILD_WARNINGS, `:1370`/`:1382` CHANGED (all silent-permissive).** ⚠⚠ **`PROTECTED` IS THE WORST AND GUARDS A DROP WITH NO OTHER FILTER: `:1095` IS `DROP DATABASE … FROM sys.databases WHERE name LIKE 'SSAS[_]%'`, and the ONLY thing separating a test catalog from a production-named one is that count — a transient `sqlcmd` failure yields empty, `${PROTECTED:-0}` yields 0, 0 MEANS NOTHING TO PROTECT, and the drop runs.** **VERIFIED LATENT BEFORE BEING CALLED SERIOUS: 15 `SSAS_` databases and ZERO match the protected shape.** ⚠⚠⚠ **AND THE FIX IS NOT *A SENTINEL OUTSIDE THE DOMAIN* — `:1103`'s `${LEFT:-1}` IS IN-DOMAIN AND CORRECT. THE FALLBACK MUST FAIL TOWARD THE SAFE OUTCOME FOR ITS OWN CONSUMER: `HOSTS`, `PROTECTED` AND `CHANGED` DEFAULT TO NON-ZERO; `FREE_MB` DEFAULTS *ABOVE* THE FLOOR, NOT BELOW IT.** **Same rule, opposite values, because the consumers differ.** ⚠⚠⚠ **DISPATCH ORDER, RULED 2026-09-01 — THREE CHANGES, NOT ONE, AND IN THIS ORDER, BECAUSE THIS FILE IS WHAT VERIFIES EVERYTHING ELSE AND A DEFECT HERE IS THE MOST EXPENSIVE KIND.** **(A) THE FIVE FALLBACKS FIRST — one line each, mechanical, and the DIRECTION RULE already tells you the value for each: `HOSTS`, `PROTECTED` and `CHANGED` to NON-ZERO, `BUILD_WARNINGS` to NON-ZERO, `FREE_MB` to ABOVE the floor.** **(B) THEN THE MEDIAN SAMPLER, as a separate change — it is a new loop with its own failure modes and must not ride in with five one-liners.** **(C) THEN THE PRE-FLIGHT AND THE ABORT TEXT.** ⚠⚠ **AND THE REQUIREMENT THAT MAKES ANY OF IT WORTH SHIPPING: *DEMONSTRATE THE UNMEASURED PATH FOR EACH FIX*. Force the measurement to fail — an injected empty value is enough — and show the new behaviour.** **Without that we would be adding five fallbacks NOBODY HAS EVER SEEN FIRE, which is exactly the class being fixed, and `${FREE_MB:-0}` sat there for months looking correct.** ⚠ **Stop after (A) if the demonstrations get expensive. Five verified one-liners beat three changes nobody has exercised.** ⚠⚠⚠ **AMENDED 2026-09-01 BY THE DEMONSTRATION ITSELF: (A) IS *FOUR* SITES, NOT FIVE — `CHANGED`, `UNTRACKED` AND `DEV_FILES` HAVE UNREACHABLE FALLBACKS, BECAUSE THEIR PIPELINES END IN `wc -l`, WHICH PRINTS A NUMBER EVEN WHEN EVERYTHING UPSTREAM FAILS.** **`${CHANGED:-0}` IS DEAD CODE — verified independently: `nosuchcmd | grep | wc -l | tr` yields `0`, not empty.** ⚠⚠ **SO THEY GET THEIR OWN PART (D): `wc -l` MANUFACTURES A LEGITIMATE-LOOKING ZERO FROM A FAILED UPSTREAM AND NO FALLBACK CAN CATCH A VALUE THAT IS NEVER EMPTY — THE FIX IS TO KEEP THE EXIT STATUS, WHICH IS A LARGER EDIT.** **Do NOT fold (D) into (A): changing a default that cannot execute would have shipped as a fix with no demonstration possible, which is the class this item exists to remove.** ⚠ **NOT CLAIMED AS TONIGHT'S CAUSE — 471 is not 0, so the second attempt was a real reading. A latent defect found by reading, and saying which is the point.** ⚠⚠ **AND A SECOND CLAUSE, RULED 2026-09-01 ON YOUR OWN OBSERVATION AND MINE TO OWN: RESTORE THE ORIGINAL REMEDY ALONGSIDE THE ONE I ADDED. *Quiet the box (editors, browsers)* IS EXACTLY RIGHT FOR THE 00:5x ABORT — three dotnet processes, `build-server shutdown` worth 26 MB — AND `dotnet build-server shutdown` IS EXACTLY RIGHT FOR THE 23:31 ONE, eighteen processes and 500 MB.** **I replaced a correct remedy with a different correct remedy and implied the first was wrong. A DIAGNOSTIC WITH TWO REAL CAUSES NEEDS BOTH REMEDIES, NOT THE MOST RECENT ONE.** ⚠ **And name the discriminator so the reader picks: COUNT THE `dotnet` PROCESSES FIRST — many means build servers, few means the box is genuinely full.** |
| **240** | **`B18` ON `DEP`, SEARCH-ONLY WHILE 239 RUNS, CITATIONS AFTER IT CLEARS.** **`DEP` is at 4 of 52 by the strict count, with NINE OF TEN MECHANISM GROUPS ALREADY NAMED AND UNSEARCHED — so this pass starts from mechanisms rather than from numbers, which is the whole reason the grouping was done first.** ⚠ **THE STRICT COUNT IS THE REPORTED NUMBER FROM NOW: the `[Trait]` count, never a text grep. Report both where they differ, because the GAP is information.** **Architecture suite first and TREE-WIDE — `AC-DEP-0032` needed a third file and another suite entirely, and a module-scoped search could not have reached either.** | **CLOSED 2026-09-01 — SEARCHED TO COMPLETION, `0a37893`. STRICT 39 OF 52, LOOSE 41, GAP 2 WITH BOTH MEMBERS ACCOUNTED FOR, READ FROM THE TREE RATHER THAN FROM MEMORY.** ⚠⚠ **AND THE DELIVERABLE IS THE CLASSIFICATION, NOT THE COUNT: THE 13 REMAINING ARE TEN GENUINELY UNASSERTED BEHAVIOURS, TWO PARTIALS WHOSE MISSING HALF IS NAMED, AND ONE THAT WAS BLOCKED ON A DOCUMENT AND IS NOW RESOLVED (`AC-DEP-0039` = OPTION A, `66de882`).** **A bare *39 of 52* would have read as thirteen things nobody looked at.** ⚠ **EVERY DISPOSAL CARRIES ITS SEARCH so it can be falsified by re-execution rather than by re-reading.** **Two search rules came out of the pass and are recorded in this row: START FROM THE ARCHITECTURE SUITE, and A CRITERION WHOSE CLAUSE NAMES A DATABASE OBJECT IS ANSWERED BY THE SUITE THAT TALKS TO THE DATABASE — both found by asking WHY a pass missed something rather than by adding effort.** **`AC-DEP-0044` confirmed absent with its model named, and its confusability with the unrelated, also-unbuilt `TS-DEP-0044` recorded at both dispositions.** | **Gated when the citations land, which is AFTER 239 clears — an edit mid-run corrupts both configurations.** ⚠ **RECORD THE SEARCH BEHIND EVERY DISPOSAL so an *unresolved* can be falsified by re-execution rather than by re-reading.** **Stoppable at any point; the grouping and the recorded searches both survive the stop.** ⚠⚠⚠ **AND A SEARCH RULE ADDED 2026-09-01 AFTER IT EXPLAINED TWO MISSES: A CRITERION WHOSE CLAUSE NAMES A DATABASE OBJECT IS ANSWERED BY THE SUITE THAT TALKS TO THE DATABASE.** **`AC-DEP-0012` names `CK_Departments_ParentIsNotSelf` and is answered by `DepartmentSchemaSqlServerTests.A_department_cannot_be_its_own_parent_even_in_raw_sql`, which bypasses every handler with a raw UPDATE and asserts the `SqlException` message contains THAT EXACT NAME — verbatim, including its instrument.** **`AC-DEP-0024` was missed the same way.** ⚠ **EARLIER PASSES SEARCHED THE DOMAIN AND APPLICATION SUITES; BOTH ANSWERS WERE IN THE SCHEMA SUITE.** **That is a rule about WHERE TO LOOK, not a coincidence — alongside the existing *start from the architecture suite*, which resolved three of seven in one search on `FP-006`.** ⚠⚠ **AND IF A GROUP DISSOLVES ON EXAMINATION — the criteria turn out not to share a mechanism after all — SAY SO AND SPLIT IT. The group orders the search; it never justifies a citation.** |

**Closed 2026-08-31 (late), verified against its commit:** **237** (*coverage run once over 2390 types across all eight suites — a binary per type, no gate change, no threshold, no ratio, instrument put away. 332 with zero executed lines, 202 legitimately so, SIX query-bearing. ⚠⚠ And the proxy check refuted the word FLOOR in both directions: three it called dead are live because EXECUTION THROUGH A CONTAINER IS NAMELESS, and two it called live are dead because a regex cannot tell a type reference from A FILENAME IN QUOTES*, `fcd9724`).

**Closed 2026-08-31 (late), verified against its commit:** **238** (⚠ *six query-bearing types measured dead by coverage, executed once each, and NOTHING WAS WRONG WITH ANY OF THEM — a clean result reported as a result. Re-measured after: 0 → non-zero on all six, so the loop closed with the instrument that opened it. ⚠⚠ And the re-measurement corrected how 237 must be read: async bodies compile into generated state-machine classes that the noise filter strips, so THE BINARY IS SOUND AND THE NUMBER IS NOT A MAGNITUDE*, `ca90d74`).

**Closed 2026-09-01 (morning), verified against its commit and its log:** **239** (*`[GATE GREEN — PHASE scope: all eight suites, Debug and Release]`, `e7f29dd`. 4122 tests per configuration, 8244 executions, `Failed: 0` on all sixteen legs, condition 4 `ok` at SIXTEEN suite totals, baseline refreshed to sixteen rows.* ⚠⚠ *And the result file leads with the part easiest to omit: IT SUCCEEDED ON HEADROOM AND NOT ON ANYTHING EITHER WINDOW FIXED — no code, script or configuration changed between the attempt that aborted and the one that passed. Four attempts, one verdict, and none of the three failures was about the code.*).

**Closed 2026-09-01, verified against its commit:** **241** (*three hand-named enumerations become checked partitions, derived from the NAMESPACE rather than the name — `ff5d152`, TASK green, Architecture 628.* ⚠⚠ *And the plant settled a claim BOTH windows had overstated from opposite sides: a SWAP is caught by the existing theories anyway, so set equality and the arithmetic form are EQUIVALENT IN COVERAGE. The only new coverage is OMISSION — plant 1 removed an `[InlineData]` row and the existing theory PASSED over its smaller list while only the new test failed.* *Kept on the two reasons that are not coverage: it does not depend on those theories continuing to assert what they assert, and its failure NAMES THE MEMBER.*).

**Closed 2026-08-31 (evening), each verified against its commit before the row was struck:** **236** (⚠⚠ *the defect class is EMPTY tree-wide — 109 ordering sites, 4 candidates, all correct code — and the ZERO IS TRUSTWORTHY because the scanner was run against `fa95522^` and flagged both real defects; the refined guard was REFUSED at 0-true-4-false, and the residue recounted from 41 to SEVEN once target-typed `new(...)` was accounted for*, `a86ed5d`), **235** (*every module registration binds its concrete read service; the NAMED module list is the load-bearing control, so a fifth module fails rather than sitting outside the guard*, `2e30218`), **234** (⚠ *the guard failed its first run on the coder's own two structural pointers, with the diagnosis sitting in the file it was modelled on; fixed as a RULE not a list, bound proved by a `decimal` spelled like a pointer*, `76eead3`), **233** (⚠⚠⚠ *TWO LIVE PRODUCTION DEFECTS — the trial balance and the GL fiscal-period read threw on EVERY call; the two modules whose read services were never constructed are the two that carried defects*, `893b874` + `fa95522`), **232** (*PHASE green, all sixteen legs; the named risk was refuted before the run and EIGHT STALE RELEASE BASELINES were found instead — right row, wrong reason*, `849835a`), **231** (*all 42 declaring types judged; four are KEY-IMMUTABLE and hold a stronger guarantee than the guard — two of them the ones I had ranked highest-consequence*, `e5bbf39`), **230** (*the set-driven test exists: the guard inspects a tracked ENTRY, not a valid aggregate*, `2b683bc`), **229** (*the refusal now names the grant class that is actually missing; fix the code, keep the comment*, `46786b9`), **228** (⚠ *the grid was never the instrument — 26 roots and 16 children, and a guard walking only roots passes every per-type test*, `0d62a95`), **227** (*three gaps, not one: Attendance had nothing, Payroll nothing, GL THE WRONG CASE*, `30daa57`), **226** (*the licence states the RULE, and the measurement it replaced is recorded rather than erased*, `c8fcb7d`), **225** (*`AC-EMP-0002` clause 3; the plant is a per-type exclusion, the failure the genericity invites*, `943ed98`), **224** (*the refusal exists twice — the gap was a missing FILE, invisible to every instrument that reads files*, `1e7efd2`), **223** (*`AC-EMP-0013` implemented by construction; the Employee half of a guard Company had carried alone*, `943ed98`), **222** (⚠ *7 of 11 — every promised value that is a compile-time constant kept its promise and every runtime one did not; `ApiError.Detail` landed and nothing came back for the tests whose names had been promising it*, `6d9c03f`).

**Closed 2026-08-31:** 221 (**`AC-EMP-0015` FULLY PINNED. The criterion's own wording settled it — *reserved WITHIN THE COMPANY* is a uniqueness SCOPE, not a persistence claim. ⚠ The clause named TWO identifiers in DIFFERENT states: the number was ALREADY PINNED with its intent in a comment and no citation; the national ID by nothing. ⚠⚠ And the existing national-id test uses TWO LIVE EMPLOYEES — the right rule in the wrong STATE**), 220 (**⚠⚠ ONE TEST, NOT THE FOUR RULED — reading first found THREE OF FIVE BANS ALREADY GUARDED, and writing them would have duplicated two live supersets. ⚠ The persistence guard proves a delete is REFUSED; the criterion bans the surface from EXISTING — a claim about the type system, not a request. ⚠⚠ THREE controls, because two reflection paths need a control EACH. And the third clause was named and LEFT**), 219 (**⚠⚠ THE CODER REFUSED IT AS A DOCS ITEM AND WAS RIGHT — the owner assigned documentation to the architect, and a peer cannot move an owner-set boundary. It did the JUDGEMENT half and left `docs/` untouched; the architect applied the six headings, mechanically verified that no body moved. ⚠ `AC-LOC-0001` stays NARROWER because its body carries two rules and a heading naming both is a sentence, not a title. 40/5/1 → 45/1/0. ⚠ And it MEASURED ITS OWN HYPOTHESIS about the cause and WITHDREW IT: FP-003 has twice the clause density and its headings agree**), 218 (**PARTIAL BY DESIGN — 10 of 46, 4 cited, FP-006 goes 1 to 5. ⚠⚠ The obvious candidate for `AC-EMP-0006` was WRONG: uniqueness versus VALUE EQUALITY, which a product can satisfy while persisting both. ⚠⚠ And the reframing outranks the count: ZERO uncovered in ten body readings — this is CITATION-BUILDING, not a gap hunt, and its value is per-citation so stopping wastes nothing. Moved to `BACKLOG.md` as standing entry B18**), 217 (**MISLEADING IS 1 IN 46 — heading scanning is SAFE, and four packages are STRUCTURALLY IMMUNE because bullets and table rows have no heading/body split. ⚠ The divergences CONCENTRATE: heading LENGTH predicts reliability, labels diverge and claims cannot**), 216 (**FP-006: 47 defined, 47 mentioned, zero cross-references, 0 Trait-claimed before this item, one cited with the body read. 255 criteria across four packages, 32 citations — and FP-006 being NON-PLATFORM makes the pattern the PRODUCT'S. The stated limitation is the load-bearing one: an ID-text search sees only whether a mapping is WRITTEN DOWN**), 215 (**ONE OF THE THREE WAS WRONG AND IT WAS THE CODER'S OWN: `AC-LOC-0019` is a HEADING MATCH — heading *Cache coherence*, body about timing bounds and isolation. Caught before citing; 214 corrected in place. The other two read as BODIES, and the trait keys were MATCHED not chosen**), 214 (**FP-004: 64 defined, 64 mentioned, ZERO cross-references, and ZERO CITED in a heavily tested package. The architect's prediction landed: the post-commit domain-event eviction test EXISTS and its criterion `AC-LOC-0019` is uncited. Three packages, 208 criteria, 31 citations — the convention is ABSENT FROM ENTIRE PACKAGES**), 213 (**THE DEFERRED BUCKET IS ONE CRITERION AND IT IS SATISFIED: `AC-TEN-0092` asserts the deferral HOLDS, so *not implemented* would have inverted it. All 93 headings READ, not matched. And item 202's guard pinned exactly that criterion while citing it NOWHERE — Principle 29's first live catch**), 212 (**FP-003 DEFINES 93 NOT 94 — the 94th is a prose cross-reference, so a per-file count over-reports any package citing its neighbours. 12 pinned, 1 PROSE-MENTION-ONLY which claims nothing, 80 unresolved, zero cited-but-undefined. ⚠ A FOURTH trait key exists. ⚠ And it REFUSED to report a deferred count from a keyword scan of headings**), 211 (**the `AC-SS-0004` citation was WRONG and the *positive half* hypothesis is disproved by the spec's own pairing — 0003 IS that half; a second citation was wrong too. FP-015 is 14 CITED OF 14. ⚠⚠ And a void plant reported `Passed! 7` FROM STALE BINARIES after a build that failed with `CS1002`**), 210 (**⚠⚠ THE PREMISE WAS WRONG — FP-015's tests ALREADY CITED, seven of fourteen, missed because THREE TRAIT KEYS carry criterion ids and a grep on one finds a third. Principle 29 survives; the grep must be KEY-AGNOSTIC. 13 of 14 cited, one SKIPPED AND LISTED, one wrong-looking citation REPORTED NOT CHANGED, FP-014's twenty refused as re-measuring under a backfill's name. ⚠ Control across all packages: 93 cited, 606 defined, ZERO undefined — and 607 was the architect's SUM where 606 is the UNION**), 209 (**FP-015 is 14 OF 14 — the composition asserted on the REAL running route, and the plant moved `/me/payslips` OUT of the gated group AT THE SAME PATH so only its gate was gone**), 208 (**FP-002: 51 CONFIRMED by two methods with an empty off-bullet set; 19 pinned by direct citation, with a control that no cited id is missing from the spec. ⚠⚠ The 32 are UNRESOLVED not uncovered, demonstrated with three spot-checks landing three different ways. ⚠ And milestone BOUNDARIES are not status claims — only the second decays**), 207 (**ALL FIVE IMPLEMENTED, NONE UNBUILT — nine becomes TWELVE pinned. ⚠⚠ And the candidate file list was wrong BY A HOMONYM: leave entitlement is not module entitlement, and a candidate list built by name matching is itself a name match. `AC-SS-0013`/`0014` called neither pinned nor unbuilt because the COMPOSITION is unasserted**), 206 (**FP-015: 14 criteria, NINE PINNED — two STRUCTURALLY, and `AC-SS-0007` carried by a shared rule generalised in T-089. ⚠ The other five are UNMEASURED, NOT UNBUILT — name matching cannot establish an absence — and neither is subject-undefined**), 205 (**⚠⚠ THE ZERO DID NOT ARRIVE: FP-015 claimed *specification only, no code* on 2026-08-27 and its routes shipped 2026-08-28. SECOND package falsified inside 24 hours. ⚠ FOUR criteria layouts, not two — bullets hold 171 criteria invisible to either pattern the architect named — so counting went FORMAT-BLIND: 607 identifiers across fourteen packages**), 204 (**⚠⚠ *item 157* IS NOT AMBIGUOUS — the two series are distinguished by their PREFIX, and all eight colliding numbers are DIFFERENT ITEMS. ⚠⚠ The real ambiguity is a THIRD series: `item N` below 152 was a different counter, cited for 25 numbers, and NOT ONE has a file — four mappings exist, inside `T-` headers. 22 unresolvable numbers now LISTED BY NAME in `00-HOW-TO-READ-A-CITATION.md`; `T-181.md` recovered with a provenance note; the two duplicates are VARIANTS of one item each**), 203 (**⚠⚠ 27 REQUIRED RESULT FILES NEVER COMMITTED, ALL 27 ISSUED — two with MERGED PRs, which a board-only check would have called *never issued*. ⚠ And it corrected 201's own figures first: a padded/unpadded key join yields a PLAUSIBLE NUMBER rather than an error**), 202 (**the tenant-transport deferral now FAILS, asserted over routes the running host maps; ⚠ a prefix check would have reddened the gate on shipped `tenant-users` routes, caught before writing; and breaking the matcher reddened the CONTROL while the ban passed vacuously**), 201 (**⚠ the architect's base was wrong — `origin/main` is 925 commits behind `ClaudeBranch`; content, not reachability. 145 of 151 `agent/*` branches hold nothing not live, and the sixth is the TENANT TRANSPORT HOLE**), 200 (**the `Char` rationale restored at both sites — ⚠ and the gate went RED on the comment, because a lowercase-dotted SQL column is indistinguishable from a filename**), 199 (**the two empty projects are the UNBUILT HALF OF A DECLARED TAXONOMY — owner decision 22**), 198 (**38 codex branches, not 13; only six carry anything, and content-wise all of it is live**), 197 (**`7` now reads as HEALTHY — the run printed the very line 196's reader would have tripped over, and seven is named as the healthy TASK value in the same paragraph as the instruction to check it. Data rows byte-identical, snapshot first. ⚠ And it bounded its own claim: this run does not prove the PHASE reading, which rests on 193 — the thread's own lesson applied to the report of the lesson**), 196 (**⚠ THE CARRY-FORWARD HOLDS — 16 rows before and after a TASK run, proven by an EMPTY SORTED DIFF of every data row, not by a matching count; and the baseline was SNAPSHOTTED FIRST because the check could have destroyed it. ⚠⚠ And the finding is against the architect's own fix: *it compares all sixteen from the next run on* is TRUE OF PHASE AND FALSE OF TASK, three lines above the instruction to read the number — so a healthy TASK run would have raised the alarm the instruction exists to raise. Corrected as scope-dependent**), 195 (**recorded, not converted — ZERO non-comment lines changed, verified by diffing against a comment-and-blank filter; and the revisit trigger came back as TWO checkable conditions with the source of the ~100× margin named**), 193 (**⚠⚠⚠ `[GATE GREEN — all eight suites, Debug and Release]`. INTEGRATION 848/848 IN BOTH — the inference CONFIRMED in the configuration that failed it, and the total moved 846→848 because CONTROLS WERE ADDED. `test-baseline.txt` 7 rows → 16, its first recorded expectation for Integration and Release since 2026-08-27. ⚠ Which exposed that condition 4 had been comparing SEVEN OF SIXTEEN pairs and saying so only as a count nobody read**), 194 (**⚠ NOT a zero but nearly, mechanism closed first: `new Process()` ZERO, bare `.Start()` ZERO — the one hit an OpenTelemetry span, which is why the unfiltered sweep was worth doing. ⚠⚠ `src/` and `tools/` START NO PROCESSES AT ALL, so the whole population is test code and none of it is a product defect. ONE additional site, split into an UNCONSTRUCTIBLE deadlock and a LIVE evidence loss that costs the reason rather than the correctness — and 189's fix would have DEADLOCKED its handshake, which qualifies Principle 28 the day it was written**), 192 (**⚠⚠ 175's HOLE WAS CLOSED ALL ALONG — and nobody could say so because the work landed in `0beb577`, a commit titled *record item 176*. The regression test is green in 13/13, in a suite TASK DOES run. ⚠ *Recorded and uninterpreted* a second time: the sweep was reported, its consequences recorded, and the NEXT consequence — that the thread would read as unfinished — never drawn. The ordering change is measured NOT load-bearing and kept against a named likely edit**), 191 (**⚠⚠ the sibling's *0 in 506,102* DOES NOT REPRODUCE — four configurations, none reaching zero, so the second competitor was NOT added. The observation window is now closed by CONSTRUCTION; the hit inside it is probability at 89.7%, first hit 24 ms against a 30,000 ms deadline. ⚠ And the architect's `FillAsync` premise was wrong — the scheduler CALLS the provider's — so *establish first* caught the ruling's own error**), 175 (**CLOSED on 192's evidence — the thread that began at 166 ends here**), **Closed 2026-08-31:** 190 (**⚠⚠ THE 5×5 DECLINED AND THE REFUSAL ACCEPTED: the answer was the SIBLING'S OWN COMMENT — *a one-shot competitor turns the test into a race it usually loses*, against *0 misses in 506,102 samples* where two overlapping processes close the window BY CONSTRUCTION. ⚠ Three commits hardened site 2 and site 1 got none of them — now Principle 28. Conclusion LABELLED INFERENCE, offer to run the counts left standing**), 189 (**`SqlcmdChildProcess` drains both streams from the instant the process starts; `DescribeAsync` reports how it left and what it said. ⚠ The architect's reference pattern did NOT fit — it is a handshake and `sqlcmd` emits no marker we control — and the coder said why rather than forcing it. ⚠ Pipe-buffer REMOVED not exposed; the plant is PERMANENT because these fixtures' evidence path would otherwise first run on the day it is needed**), 178 (**SUPERSEDED — 188 measured the Release half**), **Closed 2026-08-31:** 188 (**⚠ THE DELTA IS EMPTY AND STRUCTURALLY SO — six mechanisms at zero sites, one root `.editorconfig`, a per-configuration severity split IMPOSSIBLE BY CONSTRUCTION. INHERITED, not chosen. The standing `CA1826` anecdote DOES NOT REPRODUCE. Prediction recorded BEFORE the Release log existed. ⚠⚠ And the real Debug/Release difference is BEHAVIOURAL, not diagnostic — the question was the wrong shape**), 187 (**⚠⚠ INTEGRATION IS GREEN IN DEBUG, 846/846, FIRST SINCE 2026-08-27; Release 845/846; 175 passed. The baseline still has no rows, but the unknown is now ONE NAMED TEST rather than a whole suite. ⚠ And the re-anchoring TOOK HOLD: the coder started the run from the FILE, with no message**), 186 (**⚠⚠⚠ THE QUEUE IS A MAILBOX WITH NO DOORBELL — nothing reads this file; only a delivered message starts a turn. It had been opened TWICE all session. *Read this before going idle* could not execute, because GOING IDLE IS NOT AN ACTION — now Principle 27, and the header is re-anchored to completing an item. Partial by construction; the residual is owner decision 21**), 185 (**EF translation did NOT prevent the injection — `now` is captured before the query; the seam is pinned because no behavioural test could move, and gate condition 4 was treated as a finding**), 184 (**⚠ option (3) INFEASIBLE and said so — no public seam on `ITimeLimitedDataProtector`. A FOURTH option removes rather than defers: anchor the fixture clock to the RUN, so no date exists at which it breaks**), 183 (**⚠⚠ EXACTLY ONE ACTIVE MEMBER, AND IT IS THE ONE THAT FIRED — mechanism set M1-M7 stated before searching, with a fuse table per mechanism. ⚠ AND THE ARCHITECT'S *known positive* WAS INVALID: freezing a clock is not the hazard, a frozen instant REACHING A WALL-CLOCK VALIDATOR is — three fixtures freeze the same instant and reference CSRF, token issuance and HTTP clients ZERO times. Keyed on freezing the survey reports five suspects; keyed on the mechanism, one. ⚠ Two latent findings taken as 185; method limit stated: membership judged by reference, not call graph**), 182 (**⚠⚠ THE PRODUCT IS CORRECT AND THE TEST EXPIRED LITERALLY, at 2026-08-30 12:00:00 UTC, fifteen hours before the run that found it: the fixture freezes at 2026-07-31 12:00 and the CSRF value is dated frozen+30d, while the service validates against the REAL clock. `csrfExpiry=2026-08-30T12:00:00Z`, `realNow=2026-08-31T03:31:09Z`, `expired=True`. ⚠ A TIME BOMB, not a regression — diagnosed by INSTRUMENTING, since the code alone could not discriminate the transport gate from the CSRF check. Decision 20 amended so the red suite is not read as a broken product**), 181 (**the binding guard, 3 tests, architecture green at 616 — with a THIRD assertion nobody asked for and the invariant needed: those two factories remain the ONLY public construction points. ⚠⚠ AND ITS PHASE RUN FOUND THAT THE INTEGRATION SUITE HAS BEEN RED SINCE BEFORE THIS SESSION — bisected to `112cb31`, deterministic, both configurations, and `test-baseline.txt` had RECORDED that no PHASE run has completed since 2026-08-27 without anyone drawing the implication. Decision 20 rewritten around it; 403 dispatched as 182**), 180 (**⚠⚠ REFUSED BOTH ANSWERS THE DISPATCH OFFERED AND WAS RIGHT: the invariant is not pinned and NOTHING SHOULD BE — two public factories, one taking `Guid`/`long` non-nullable and one taking neither, so A MIXED BINDING IS NOT CONSTRUCTIBLE and the test would be one that cannot fail for the reason it claims. ⚠ The chain is COMPILER → COMMENT, with no test in it. ⚠ Residual stated: EF materialises through the private parameterless constructor, so the safety is a property of the WRITE PATH, not the table. `LeaveRequest`'s literal is inconsistency only — the ordinals were REFUSED BY THE DATABASE**), 179 (**NO FILTER IS WRONG — all 16 read from the model and checked against their columns and nullability. ⚠ THE FALLIBLE POPULATION IS 11, NOT 16, classified BY SHAPE because `GetFilterConfigurationSource()` returns nothing on a finalised model. ⚠ One is correct only because of an invariant THREE FILES AWAY and nothing connects them — item 180. ⚠ The correspondence guard is RECOMMENDED AGAINST and accepted: one false positive in 16 is the guard someone deletes**), 177 (**NO LIVE DEFECT: 62 unique indexes, 16 filtered, 46 unfiltered, ZERO over a nullable column without a filter — read from the EF MODEL via `IProperty.IsNullable`, not the CLR type. ⚠⚠ AND IT CORRECTED 176's WORST EXAMPLE BEFORE THE OWNER READ IT: deleting `HasFilter` changes nothing, because the provider supplies it BY CONVENTION; the reachable form is `HasFilter(null)`. Found by PLANTING THE DELETION AND SEEING THE GUARD STAY GREEN. ⚠ First enumeration was over a TWO-ENTITY STUB, caught by the guard's own control — the exact trap documented one file away in `TenantModelEntityCountArchitectureTests`**), 176 (**⚠⚠ 36% OF EVERYTHING THE REPOSITORY ASSERTS NEVER RUNS BEFORE A MERGE — 3,724 of 10,457, and NO TASK SUITE EVER MATERIALISES A SCHEMA: 144 `EnsureCreated`/`Migrate` in Integration against EIGHT elsewhere, pointed at `"Server=model-only;Database=none"`. ⚠ One deleted `HasFilter` line COMPILES, keeps every model assertion green, merges under `DEC-L-007`, and refuses the second employee with no national id. **Owner decision 20 opened; hole being closed by 177 rather than the rule amended****), 174 (**the mechanism set CLOSED BY EF'S OWN MODEL — nine mechanisms, each measured: `AsNoTracking` 203, `QueryTrackingBehavior` 0, `ChangeTracker.Clear` 0, `EntityState.Detached` 12, none touching an event-raising type; `DbSet.Update` never called in the product. ⚠ Known positive drawn from a PRIOR measurement rather than planted. NO MECHANISM REACHABLE IN PRODUCTION**), 173 (**PR #390 — all four parts fixed plus cancellation, which the ruling missed. ⚠⚠ THE FIRST PLANT REDDENED NOTHING: the ordering change is not load-bearing, the non-throwing dispatcher is — and a dispatcher that throws OUTRIGHT still reports a committed command as failed. Hole named, remedy built and REVERTED as a scope surprise, taken as 175**), **Closed 2026-08-31:** 172 (**PR #389 — ⚠⚠ A REAL DEFECT: the commit succeeds, the data is written, THE CALLER IS TOLD THE COMMAND FAILED, and the consumer's exception is DESTROYED — `RollbackAsync` on the committed transaction throws first, so `throw;` is never reached, and disposal then rolls back AGAIN. ⚠ The plant was the candidate fix, which is how it was found INCOMPLETE. Ruled and being fixed under 173**), 171 (**PR #388 — a module is a project under `src/Modules/`, read from the LAYOUT, so no second hand-written list was needed and the stop condition did not fire. ⚠ The directory name is NOT the assembly prefix — `Finance/` holds `SSAS.GL.*` — and a folder-name predicate would have PASSED WHILE MISSING A MODULE. ⚠ And naming is not redundant: it catches a module MOVING where a scan passes vacuously**), **Closed 2026-08-31:** 170 (**NONE of the 61 unconsumed members is reached without a compile-time reference, every answer MEASURED — by enumerating the MECHANISM (4 dynamic sites, 2 of them JSON lookups) rather than the names, where `TenantId` alone matches 2,236 literals. ⚠ The architect's requested DI control HAD NO SUBJECT: a container resolves TYPES, never interface MEMBERS. ⚠ And 165's result file recorded the COUNT but not the MEMBERS — the `T-201` failure four items after its own lesson**), 169 (**PR #387 — 14 hand-written assembly lists, 3 module-shaped; the sorting question is *would a new module be missing*, not *is it floored*. ⚠ THE CONTROL FAILED ON FIRST RUN, naming two assemblies 168's guard had never scanned. ⚠ Output directory not `GetReferencedAssemblies()`, because the compiler omits an unused reference and the check would have agreed with the stale list FOR THE WRONG REASON**), **Closed 2026-08-30:** 168 (**PR #386 — the read-side escape guard, three controls, three disjoint plants. ⚠⚠ AND IT DISPROVED 167'S CLOSING FACT: EIGHT command handlers DO take a read service — the search filtered on files named `*CommandHandler*.cs` and handlers here are named for their aggregate, PLURAL. The conclusion survives and the reason does not; `ADR-009` corrected. ⚠ Plant two emptied the population and THE GUARD PASSED — only the independent control and inventory failed**), 167 (**PR #385 — the detached-aggregate hazard is REAL and NO PRODUCTION PATH REACHES IT: 203 `AsNoTracking` sites, 30 touching event-raising aggregates, 8 entity-shaped, all in read services no command handler injects. ⚠ The first classification over-fired 26 → 8 — PROXIMITY IS NOT SEMANTICS, and a guard on that pass would have fired on 17 correct existence checks. ⚠ Guard shape ruled: NEITHER a ban nor a test alone — guard the ESCAPE. Pinned as current behaviour, not as correct**), 166 (**PR #384 — the withdrawal EXERCISED, not read: five tests on real infrastructure, consumer reached with metadata populated. ⚠ Events raised inside a transaction are WITHHELD AND RELEASED, not dropped — and rollback never dispatching is the POINT of the design. ⚠ The inverse bug — dispatching before commit — does not occur only because `TenantUnitOfWork` caches ONE inner `EfUnitOfWork`, which reading `EfUnitOfWork` alone cannot settle. ⚠⚠ And the cleanest statement of the 17 failure: THE INSTRUMENT WAS RIGHT AND THE QUESTION WAS WRONG**), 165 (**61 of 499 interface members have no production consumer; 5 test-only, 56 nothing. ⚠ The demanded known-positive validation KILLED the first instrument: a compile-error probe cannot enumerate consumers, because a failing project stops its dependents compiling and its own signal halts the search. ⚠ Attempt 2 — `[Obsolete]` at WARNING level, build succeeds, list complete — and the known positive proved the counts are consumption, not declaration. ⚠⚠ AND IT SURFACED THAT OWNER DECISION 17 WAS FALSE**), 164 (**PR #383 — `ADR-025` decision 4 ENFORCED rather than stated: `TenantForbiddenClaims`, and `ICurrentUser.CompanyId` removed. ⚠ The architect's "three references" was true of CONSUMERS and wrong about IMPLEMENTERS — there were ONE HUNDRED, and removing an interface member ORPHANS them into `CA1822` rather than breaking them. ⚠⚠ An INCREMENTAL build reported zero warnings while four orphans stood, because those projects were never recompiled. ⚠ Full out-of-set rejection COSTED AND DECLINED, reasoning and expiry condition recorded in the ADR**), 163 (**PR #382 — NO DIVERGENCE: the tenant token complies with `DEC-AUTH-0049` exactly, and the claim set is now pinned AS A SET rather than by a denylist. ⚠ The architect's dispatch was wrong that it was unpinned — it was PARTLY pinned, and the set was the missing part. ⚠⚠ The finding is `ADR-025` decision 4: a BINDING prohibition, guarded on the platform plane and UNGUARDED on the tenant plane it names — annotated in the ADR, being closed by 164**), 162 (**PR #381 — `EntitlementPermissionCouplingTests`, 7 tests: entitlement and the tenant permission set are UNCOUPLED, proven by exercising the path rather than searching it, with a STRUCTURAL pair that reddens on any future collaborator. ⚠ `AC-SUB-0026` cannot be exercised at all — there is no lapse event — and was ruled into a fifth state with `0008`; ⚠ a third proposed move, `AC-SUB-0045`, was REFUSED because its six permissions are a parenthetical and its subject is the whole 28-name set**), 161 (**⚠ FP-014: 20 PINNED · 11 IMPLEMENTED-UNPINNED · 19 NOT IMPLEMENTED · 4 SUBJECT UNDEFINED. The line falls between what a tenant MAY USE and what a tenant IS CHARGED — the entitlement half built and well tested, the billing half absent entirely. README rewritten and dated; `AC-SUB-0020`'s stale counts corrected and its TEST deliberately left alone**), 160 (**⚠ A FOURTH POSSIBILITY: login CANNOT refuse an expired subscription — eligibility is a total function of `TenantStatus` — but expiry DOES refuse, `403` at the module boundary. The row was filed against the WRONG SURFACE, not residue, and deleting it would have removed the product's only mention of a real refusal**), 77, 78, 159 (**every concrete recommendation in the results trail was acted on; four survive and all four were UNASSIGNED rather than unread**), 157 + 158 (**the anonymous door is well defended; PBKDF2-SHA512 at 100,000 iterations, CONFIGURED and FLOORED rather than inherited**), 156 (**DOCUMENTATION GAP, NOT A SECURITY GAP — the support surface is guarded better than most of the documented one**), 155 (**THE MIRROR: 26 of 155 mapped routes appear in no `api-contracts.md` — and the largest group is a privileged cross-tenant admin surface**), 154 (**exactly 2 true phantoms — and one of them sat OUTSIDE the population the architect scoped**), 153 (**ZERO of the 31 are documentation errors — 24 are the document annotating ITSELF, 7 were the matcher's fallback**), 152 (**AXIS 1 CLOSED on a stated, complete population: zero real over 200 distinct routes — and every one of the 62 previously invisible routes is BUILT**), 151 (**three route formats not four, nine packages MIXING them, and 62 routes invisible to every sweep in the thread**), 150 (**THREAD CLOSED — the row is in no documented route in the product, and may not exist at all**), 149 (**139 documented routes classified — the population was never 44 — and ZERO real gaps, so the missing row is prose-only**), 148 (**method killed by its own first result: the capability→endpoint link is NOT IN THE DOCUMENTS — one package of fifteen has both halves**), 147 (**one of the "2 real" rows was already SHIPPED; the second is unrecoverable, and the reason is the finding**), 146 (**all five explained — none a defect, none unfinished; ten of eleven never-raised codes accounted for**), 145 (**PR 379 + 380 — both published contracts now honoured**), 144 (**7 of 63 documented codes have no producer — and TWO are DIVERGENCES rather than absences**), 143 (**11 of 535 declared codes never raised — 2% — and the codebase had ALREADY RECORDED two of them, including 135's mechanism**), 141 (**PR 378 — the field sweep CLOSED: 63 rows, six modules, 77 reads, ZERO wire-contract errors, 42% rejection**), 142 (**PR 377 — `field` is a JSON path; and the population was TWO modules, not three, which did not matter because the population was never the load-bearing premise**), 140 (**collection-valued requests exist in FOUR modules, not GL alone — and the SPLIT matters more than the count**), B13 (**dissolved: all three "orphan" arms are alive; the instrument filtered on a FILENAME**), 137 (**PR 376 — Payroll and GL: ZERO wire-contract errors across all 38 rows, and GL's 80% rejection is a MECHANISM limit, not a candidate problem**), 138 (**INERT — `LimitAt` has no production caller at all, so the grant-or-deny question never arises**), 139 (**⚠ THE OPPOSITE of a recorded deferral: three ACCEPTED ADRs specify a dispatcher the product does not have — ADRs annotated, owner decision pending**), 136 (**17 Domain methods with no production caller, in FOUR classes — and most of the subscription cluster is owner decision 11, reached independently from the code**), 134 (**PR 375 — Attendance: 17 proposed, 11 accepted, 35% rejected, and ZERO wire-contract errors across 27 rows**), 135 (**MEASURED: Attendance-driven HOURLY overtime cannot be paid in production — locked twice, independently**), 133 (**the attributable population is ~71, not 16 — and the architect's "value object" premise was wrong: only 2 of 16 are value objects**), 132 (**PR 374 — the `field` extension, SIXTEEN codes not seventeen, guard running in THREE directions**), 131 (**17 of 19 clean, and the correspondence is NOT mechanically derivable — but a curated map with automatic verification is**), 129 (**PR 373 — the precondition code, placed per-mapper; RED branch deleted**), 130 (**both designs costed; the field identifier is STATIC PER CODE, which removes the plumbing cost**), 127 (**PR 372 — load-bearing controls renamed for what they hold up**), 128 (**28 of 129 are candidates, in five classes**), 126 (**the remaining matcher controls — and the count went 25 → 11 → 5, every step down from READING**), 125 (**PR 369 — route precedence pinned for the three literal/parameter pairs**), 124 (**PR 368 — the eight matcher controls; the FIRST one failed on its first run and found a hole where the canonical JWT type should have been**), 121 (**pagination split at both layers — and it surfaced a FOURTH condition: an export ceiling refused with the pagination code**), 123 (**PR 366 — `detail` shipped, and a second fail-closed class was found by an existing test**), 122 (**the domain message NEVER reaches a caller — no `detail` in any of 40 problem-document call sites**), 120 (**#363 — six structural guards floored, 34 tests, every guard planted twice; and probing the seventh caught the PROBE lying**), 115 (**all five FP-015 docs re-verified: 4, 3, 0, 0, 1 — every discrepancy an absence claim**), 118 + 119 (**#362 — the model-only tests moved, and the timing tell is now a guard set at the measured cliff**), 112 (**#360 — sampler path; the Process.Start sweep found nothing**), 113 (**FP-015 authorization-model: four of eight sections overtaken, all absence claims**), 114 (**8 → 1 on a current corpus — and the one is NEW, so the class regenerates**), 116 (**catch census closes at 11, all Group A**), 109 (**#358 — shared `TestSupport.CutoverModel`; the six C6 checks now run in every gate**), 110 (**#359 — TWO reasons, not one gap: empty scaffolds counting an accurate zero, and rows a mechanism postdating the only qualifying run by two hours could not have written**), 111 (**one real site, and it REFUSES TO START rather than relocating**), 106 (**#355 — two moved, six blocked on a shared type**), 107 (**#356 — the TRX escape REPRODUCED and fixed**), 108 (**#357 pending — guard built; the arm deliberately NOT added, and the architect overruled itself**), 105 (**no handler disagrees with the generic arms — 14 translations across 12 handlers, every reachable one resolving to 409; one LATENT gap found**), 103 + 104 (**#354 — and the timing tell found ZERO false greens in 806 tests; what it found instead was eight structural assertions misfiled into Integration**), 102 (**and TWO OF THE FOUR FLAGGED GUARDS NEEDED NOTHING — an exact-list assertion and a fixed type array are anti-vacuous by construction**), 97 + 99 (**#353 — EF DOES log it: one Error entry, exception attached, category `Microsoft.EntityFrameworkCore.Update` and NOT the predicted `Database.Command`; closes as a comment, no remedy**), 100 (**`correlationId` is populated in production — the empty value was the Attendance test host omitting `UseCorrelationId`; the architect's concern INVERTED, and cost nothing because it was reported unverified**), 101 (**#352 — the false green closed, two rules converted to reflection, five plants recorded IN THE FILE**), 95 (**⚠ demonstrated a LIVE FALSE GREEN: `PersistenceArchitectureTests` passes all nine when its file walk finds nothing; 18 text-matching guards, 12 assert an absence, and ALL TWELVE have no recorded plant**), 98 (**#351 — GL under the amended DEC-DEP-0027**), 94 (**212 clauses, 16 unreasoned, groups unchanged in SHAPE — and the architect's published 291 was worse than the original 207**), 93 (**#349 — no route registered with a non-literal pattern**), 96 (**#350 — 409 for Attendance and Payroll; GL STOPPED against a recorded decision**), 97 (**measured: no logger on either unit of work, and `Error(Code, Message)` cannot carry the `SqlException` that holds the index name**), 92 (**measured: a duplicate key returns 500 in three modules; and the architect's premise was FALSE — the unit of work does discriminate 2601/2627 from a deadlock, so Group B is harmless**), 91 (**#348 — 13 teardowns reasoned, tests-side unreasoned 13 to 0, and a REFUSABLE guard replacing the vacuous one the architect specified; it found a genuinely empty body whose reason sat on a `#pragma` line**), 90 (**#347 — the auth-path defect: both overloads log the cause, the caller's answer is unchanged, and the test asserts BOTH halves because either alone is satisfied by the wrong fix; the other nine group-C catches were sound and now say so**), 89 (**31 literal-matching instruments, only ONE exposed — the rest are structurally immune because C# forbids composing a type name in a type position**), 79 (docs — architect's, #341), 80 (#342/#343), 81 (**dissolved**), 82
(**cancelled — already measured**), 83 (#344), 84 (**overturned 80's premise**), 85 (**#345 — 71 constraints
removed, product-wide total 0, architecture test planted**), 86 (**#346 — 3 real gaps called, 2 allowlisted
with the segment check; the tightening caught a false green in its own implementation**), 87 (**the ruling
survived falsification and is pinned by a test**), 88 (**census: 207 catch clauses, 94 discard, 40
unreasoned; two instrument defects found and corrected before the number was trusted**).

**Group C of the 88 census is CLOSED (#347). Group A — the ~13 where the exception type IS the reason — is ruled LEAVE.** No comments
there; **the single collective statement is the architect's to write.**

**Division of labour, set by the owner 2026-08-30:** the coder does **coding and testing only**; planning
and documentation are the architect's. **A docs-shaped item is pushed back, not done.** When something
found while coding belongs in a document, **report the finding and the architect writes it.**

## ⚠ Session state 2026-09-01 (early) — FOUR ROWS OPEN, ALL BLOCKED ON ONE THING

**239, 240, 241 and 242 are open and NONE is abandoned. THE GATE'S MEMORY FLOOR HAS NOT CLEARED SINCE
~00:50: readings from both windows range 1296–1957 MB against a floor of 2048, and the two largest
reclaimable blocks are the owner's browser and its embedded webviews.** ⚠ **Our own eleven `claude`
processes hold ~1.2 GB and cannot be returned without ending the sessions doing the work.**

**WHAT IS DONE AND UNVERDICTED: a full PHASE run passed all sixteen legs at 00:40 and then died with no
verdict, because the architect edited `scripts/gate.sh` WHILE IT WAS RUNNING.** **The counts survive —
Debug and Release identical, `Integration 862` — and `test-baseline.txt` was never written.**

**WHAT IS READY THE MOMENT A GATE GOES GREEN: passes 16–18 name 26 `DEP` criteria a gated pass can cite
IMMEDIATELY, and 7 that cannot be cited as they stand.** ⚠ **NO CITATION WAS APPLIED, deliberately — a
`[Trait]` is a test-file edit and every test edit has gone through a green gate first.**

**THE `DEP` SEARCH IS COMPLETE — ALL TEN GROUPS, pass 19, `9cea139`. `DEP` stands at 4 CITED, 33 CITABLE, 10 OPEN** (the three Employee-shaped gaps `0002`/`0044`/`0050`, the product-wide `0008`, three partials, and four unsearchable without a gate). ⚠⚠⚠ **CORRECTED 2026-09-01: THAT SENTENCE WAS FALSE WHEN WRITTEN. THE MEMORY FLOOR APPLIES ONLY WHEN INTEGRATION IS IN SCOPE — `gate.sh:1037` tests `GATE_INTEGRATION_IN_SCOPE = 1` FIRST — so a `GATE_SCOPE=TASK` RUN NEVER REACHES IT.** **Only 239, the PHASE run, is blocked. The 33 citations, 241 and 242 are TASK-gated and were available throughout.** ⚠ **Caveat stated rather than discovered: a citation landing in `tests/Integration.Tests` merges without its suite running — `DEC-L-007`'s documented exposure, and how every Integration test merged tonight.**

---

## Session ended 2026-08-31 (late) — where to pick up

**Verified, not inferred: working tree clean, nothing unpushed, `HEAD` and `origin/ClaudeBranch` both at
`d08c0ff`, and this table has NO open rows.**

**THE ONE THING TO READ FIRST NEXT SESSION: the citation count is the `[Trait]` count, never a text grep.**
⚠ **Four packages read as covered and are at ZERO — `POS`, `IAM`, `SUB`, `LOC` — and the strict/loose gap
(147 against 169) is what names them.** **`B18` carries the rule.**

**PULLABLE, IN THE ORDER I WOULD TAKE THEM:**
- **`B18` on `DEP`** — 4 of 52, nine of ten mechanism groups NAMED AND UNSEARCHED, so the next pass starts
  from mechanisms rather than numbers. **`POS` (0 of 68) and `LOC` (0 of 64) are the larger unswept ones.**
- **`B19`** — a `PHASE` run at the end of a session or after any `src/` change to BuildingBlocks, Platform
  persistence or a shared context. ⚠ **The last one found eight stale Release baselines nobody had asked
  about.**
- **`AC-PAY-0013`'s positive side** — its applicability is asserted from the NEGATIVE by three tests, and
  *one line per applicable element per included employee* has no multi-employee count assertion anywhere.

⚠ **AND ONE THING IS UNVERIFIED IN THE FIELD: `scripts/gate.sh`'s new condition-4 note.** It fires only on
a **TASK** run where `src/` changed and NO suite total moved — a PHASE run cannot reach it. **When it first
appears, read the line and judge whether it says the right thing.**

---

## ⚠⚠ Session state 2026-09-01 (late) — SUPERSEDES THE BLOCK ABOVE. TWO ROWS OPEN, BOTH TASK-SCOPED AND UNBLOCKED; THE PHASE MATRIX IS WITH THE OWNER

**OPEN: 240** (`DEP` citations, 35 of 52 strict, 17 remaining) **and 243 step 2** (feed the four
`Every_authority_route_…` guards from `PlatformRouteInventory`, floored, and name the number). ⚠ **BOTH ARE
`GATE_SCOPE=TASK`, WHICH NEVER REACHES THE MEMORY FLOOR — `gate.sh` tests `GATE_INTEGRATION_IN_SCOPE = 1`
first. NEITHER IS BLOCKED BY ANYTHING ON THE BOX.** **239 and 242 are closed.**

**WHAT CHANGED TONIGHT, AND IT IS NOT A FIX:** `b1potbz2w` aborted before Release at **1599 MB free against
a floor of 2048**. **Its Debug leg was fully green including `Integration 862`**, in **37m56s against
26m09s**, with `min_free=271 MB`. ⚠ **FOURTH CONSECUTIVE PHASE ATTEMPT WITH NO VERDICT, AND NO FAILING TEST
BEHIND ANY OF THE FOUR — every leg that has ever completed has completed green.**

⚠⚠ **THE DIFFERENCE FROM THE EARLIER BLOCK IS THAT THE CAUSE IS NOW MEASURED RATHER THAN SUSPECTED.**
**A 19 MB spread across five samples says the box is GENUINELY FULL, not unluckily sampled; three
dotnet/testhost processes say `build-server shutdown` would reclaim nothing.** **Until tonight the three
reasons a run could abort were indistinguishable and each invited a retry.** **Escalated as
`OWNER-DECISIONS.md` #24 with every number.** ⚠ **DO NOT QUEUE ANYTHING NEEDING INTEGRATION IN RELEASE
UNTIL THAT IS ANSWERED — a restart is 40 minutes to the same wall, on evidence rather than on hope.**

⚠ **AND THE STALE-BLOCK HAZARD THIS SECTION IS ITSELF AN INSTANCE OF: the block above says *FOUR ROWS OPEN,
ALL BLOCKED ON ONE THING* and was true for about two hours.** **Left in place and superseded rather than
edited, because the correction — that TASK work was never blocked — is the finding, and deleting the false
claim would delete the evidence for it.** **A reader must take the LAST session-state block, and this line
is here to say so.**
