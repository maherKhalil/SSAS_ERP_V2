# T-077 result — the anonymous-route claim, planted and then closed twice

- **Status:** PENDING MERGE. **Gated** — `tests/` only in the commits. **Not self-merged.**
- **Branch:** `agent/T-077-anonymous-route`, from `ClaudeBranch` at `29ea745`
- **`GATE_SCOPE=TASK` GREEN.** Baseline `API|Debug|729 -> 730` — **exactly B1's one test; B2 adds none.**
- **This task planted in `src/`.** Three times, reverted three times, hash identical every time. **Nothing under `src/` is in any commit** — `condition 4: ok: no non-comment change under src/` on the final gate.

---

# Part A — the claim was right, and measured

# **CONFIRMED. A permission-less Attendance route in `src/` and the whole suite stayed GREEN.**

```
[GATE GREEN -- TASK scope]   2765 tests, 0 failures, seven suites
```

**Nothing failed. Not one test.** The claim in T-076 was three read facts and bucket arithmetic; it is
now a measurement.

## What my own guard did, reported because I predicted it

**Measured from the running Host under the plant, not derived:**

```
total=148   anonymous=10   policy=134   authOnly=4        10 + 134 + 4 = 148
```

**The identity balanced and `Every_endpoint_is_classified_and_no_policy_escapes_the_join` stayed
green**, exactly as predicted. The planted route sorted into the *anonymous* bucket, alphabetically
between `/` and the login routes:

```
/
/api/attendance/t077-probe      <-- the plant, indistinguishable from a health probe
/api/platform/auth/login
...
```

**`DEC-L-067` applied to my own guard rather than a neighbour's**, and it found the blind spot the rule
exists to find.

## One thing did notice, and it is not a test

```
--- condition 4: ATTENTION: totals unchanged in all 7 suite(s) while 1 non-comment line(s)
    under src/ changed -- condition 4 is yours to judge
```

**The gate's own condition 4 was the only thing in the entire toolchain that reacted to a
production-code change nothing tested.** It does not fail the gate, deliberately — it hands the
judgement to the person. **On this occasion the honest answer to "condition 4 is yours to judge" was
"yes, and that is the finding."**

---

# Part B — two fixes, measured separately

**Ordered rather than left to discipline, and the separation shows in the counts:**

```
B1   Every_attendance_route_requires_a_permission        API|Debug 729 -> 730   (+1 test)
B2   pin the anonymous nine by name                      API|Debug 730 -> 730   (+0, strengthens one)
```

**Each was planted, reddened and reverted on its own before the other was written.**

## B1 — parity with the three modules that had it

`tests/API.Tests/Attendance/AttendanceRoutePermissionTests.cs`.

HR, GL and Payroll each assert this (`HrRouteInventoryTests.cs:52`, `GlRouteInventoryTests.cs:60`,
`PayrollRouteInventoryTests.cs:60`). Attendance had none.

```
clean tree   GREEN
under plant  RED   "GET /api/attendance/t077-probe"
```

**It is not a route inventory, deliberately.** An inventory catches an **undocumented** route; this
catches an **unguarded** one. The second is the absence that was measured, so it is the one built. It
enumerates from the real Host rather than a per-module test fixture, because Attendance has none and
building one to hold this property would be larger than the property.

**Its non-vacuity assertion is the load-bearing line**: a prefix filter that stopped matching — the
mount point renamed, the module unregistered — would leave an empty set and a green loop.

## B2 — my own lesson, applied to the bucket I left as arithmetic

`EndpointPermissionCatalogJoinTests`, the accounting test.

**I argued in T-072 that a count passes when one endpoint is swapped for another, named the
authenticated-only four on that basis — and left the nine anonymous routes as a count.** The plant
walked straight through the gap I had reasoned about one bucket over.

The nine are now pinned by name, **measured from the running Host** rather than transcribed:

```
? /health · ? /health/live · ? /health/ready · GET /
POST /api/platform/auth/{login,refresh,select-tenant}
POST /api/platform/support/auth/{login,refresh}
```

*(`?` is not a wildcard — the health endpoints carry no `HttpMethodMetadata`, so they answer any verb.
Recorded in the comment so nobody reads it as a shrug.)*

```
clean tree   GREEN
under plant  RED   Collections differ ... ↓ (pos 4) ... "GET /api/attendance/t077-probe"
```

**The identity was not weakened to fit.** `Assert.Equal` on the named list is added *alongside* the
three-bucket identity, which is unchanged and still asserted. **B2 is the more general of the two: it
covers every module, not only Attendance.**

---

## The plant, and its reverts

**One plant, used three times** — Part A, B1, B2 — so all three measurements are of the same defect:

```
AttendanceEndpointRouteBuilderExtensions.cs:103
  group.MapGet("/t077-probe", GetRecordsAsync).WithName("AttendanceT077Probe");
```

**No `.RequirePermission(...)`, and it reuses an existing handler** so the plant is the missing
authorization and nothing else.

```
pre-plant   ab29659
revert A    ab29659
revert B1   ab29659
revert B2   ab29659
at HEAD     ab29659
```

**Nothing under `src/` is committed**, and the two temporary probe files used to read the bucket
counts were deleted before either commit.

## For the architect

- **Blocked / needs a decision:** nothing.
- **Part A is now measured and your board entry stands** — but note *what* stood: the claim was true and
  my reasoning for it was incomplete. **I predicted my guard would stay green and did not predict that
  condition 4 would flag it.** The toolchain was not silent; the *test suite* was.
- **The gap B2 closes was mine and it was one bucket away from an argument I had already made.** I
  named the four and left the nine, in the same test, in the same commit. **The reasoning was right and
  I stopped applying it one line early** — which is a different failure from not having the reasoning.
- **B1 and B2 overlap and I would keep both.** B2 catches a permission-less route in *any* module and is
  the general fix; B1 matches the convention the other three modules already follow, and a future reader
  looking for Attendance's equivalent of `Every_payroll_route_requires_a_permission` will find it where
  they expect. **Neither makes the other redundant** — B2 fails on the ninth-versus-tenth anonymous
  route, B1 fails on the Attendance route specifically, and the messages point at different fixes.
