# T-020 — Test `ADR-024` and `ADR-025` against their own terms

- **Branch:** `agent/T-020-adr-024-025` — branch from `main`
- **Module:** Docs / architecture governance
- **Issued:** 2026-08-25
- **Depends on:** T-011 (merged, PR #61)

## Goal

Both remain `Proposed` while their features have shipped — `ADR-025` is cited by
`CompanyOwnershipArchitectureTests` and by FP-014's authorization model.

You raised this and **explicitly declined to assert they should move**, citing your own two inverted
findings this week and naming the pattern: assuming a gap where there was a deliberate choice. That
hedge is why this is a task rather than an edit. `ADR-028` looked like a dangling reference and was a
reservation; the `gate.sh` abort looked broken and was correct. These two may be pending something
neither of us has read.

## Acceptance criteria

- [ ] **Each ADR is tested against its own stated precondition first** (`DEC-L-020`). Read the Status
      section and the whole document for a condition on acceptance — `ADR-026` carried one in plain
      words and it was the strongest evidence in T-011. Report what each states, or that it states none.
- [ ] **Where a precondition exists and is satisfied, move that ADR to `Accepted`** with an evidence
      line and a Revision History row in that file's own shape, exactly as T-011 did.
- [ ] **Where none exists, acceptance may rest on inference from use — and the row must say
      `inference` in those words**, as `ADR-017` now does. An inference presented as a record is the
      thing `DEC-L-020` exists to prevent.
- [ ] **Where the evidence does not clear, leave the file alone and report why.** Moving one and
      querying the other is a better outcome than moving both. This criterion is not a formality: I
      would rather this task returned "neither moved, here is what each is waiting on".
- [ ] `git diff main...agent/T-020-adr-024-025 --stat` shows **at most three files**, all under
      `docs/14-Engineering/ADR/`.

## Files in scope

- `docs/14-Engineering/ADR/ADR-024-Employee-Branch-Assignment-and-Transfer-Model.md`
- `docs/14-Engineering/ADR/ADR-025-Company-Execution-Context-and-Authorization.md`
- `docs/14-Engineering/ADR/README.md` — only for status rows

## Out of scope

- Every other ADR. `ADR-001` … `ADR-023` are not audited here. If one looks wrong, **report it**.
- The content of either decision. A genuine contradiction with shipped code is a `NEEDS-DECISION`.
- Any file under `src/` or `tests/`.

## Design constraints

- **Do not assume the answer is yes.** I am not ruling in advance that these two should move; that is
  the whole difference between this task and T-011, where `DEC-L-011` had already ruled and you were
  asked to test it. Here there is no ruling to test — only evidence to weigh.
- **Version and Revision History move together**, per your own T-011 correction: a row needs a version,
  and each row states "No decision changed" where none did.
- Match each file's own Revision History column headings rather than a house style.

## Tests required

**None** — `DEC-L-002`. Verification is the `--stat` assertion.

## References

- The method: `DEC-L-020`, and how `ADR-026` taught it in T-011.
- The shape: `ADR-017`, `ADR-026`, `ADR-027` as they now stand on `main` after PR #61.
- The evidence to weigh: `CompanyOwnershipArchitectureTests`, FP-014's `authorization-model.md`,
  FP-006's decision register for `ADR-024`.
