# T-029 — FP-014 citation repair: stable anchors, and `DEC-L-026` in the record

- **Branch:** `agent/T-029-citation-anchors` — branch from `main`
- **Module:** Platform (commercial plane)
- **Issued:** 2026-08-25
- **Depends on:** T-027 merge

## Goal

Two `DEC-L-027` instances your last task could not reach from its own scope, and you flagged both
rather than reaching outside it. This authorises them.

Your line-citation finding is now **`DEC-L-028`**: cite the stable anchor, not the line number. I
verified it — `BR-PLT-0008` sat at line **161** before T-022 and sits at **162** now, and FP-014's
README still cites `:161`. Nothing mechanical catches it, because the citation still resolves to a
real line. It is simply the wrong one.

## Acceptance criteria

- [ ] **FP-014's `README.md` citations of `Business-Rules.md` are corrected AND converted to stable
      anchors** — the identifier or heading, with a line number only as a convenience beside it if it
      helps a reader. `:161` and `:167`–`:169` are the known ones; **check the file for others**.
- [ ] **`decisions-ratified.md` applies `DEC-L-026`.** Both the `1.3` revision row and the amendment
      section say *"no ledger identifier was minted"* — true when you wrote it, falsified the moment I
      assigned the number. You called it "a `DEC-L-027` instance waiting to happen"; this is it happening.
- [ ] **Any other line-number citation inside FP-014 is converted** on the same basis, or reported if
      converting one would lose precision a reader needs. `ADR-017:164`, `:169` and `:376`–`:378` are
      load-bearing and were placed by you deliberately — **converting those is a judgement call, not an
      obligation.** Say what you decided and why.
- [ ] `trace-check` **full run byte-identical** to the T-027 baseline; FP-014 **GREEN `0 unruled`**.
- [ ] `git diff --stat` shows **at most three files**, all under `docs/17-features/FP-014-subscription/`.

## Files in scope

- `docs/17-features/FP-014-subscription/README.md`
- `docs/17-features/FP-014-subscription/decisions-ratified.md`
- One more inside the package if a citation is found there — **report it**.

## Out of scope

- **The eight `ADR-0##:<line>` citations elsewhere in the repository.** `DEC-L-028` applies going
  forward; a repository-wide conversion is a separate task and I have not decided it is worth doing.
  You established the exposure is otherwise nil for `Business-Rules.md`, which is why this stays small.
- `Business-Rules.md` itself, the roadmap, the master registers.
- Any file under `src/` or `tests/`.

## Design constraints

- **Do not convert a citation into a vaguer one.** `ADR-017:169` is precise and points into a long
  document; if no heading or identifier gives equivalent precision, **keep the line number and say so**.
  The rule is that a line number must not be the *only* handle, not that line numbers are forbidden.
- **`DEC-L-026`'s record should say what happened, not be rewritten** — the amendment was made before
  the number existed, and that ordering is itself the kind of fact `DEC-L-026` was ruled on.

## Tests required

**None** — `DEC-L-002`. The byte-identical `trace-check` run is the mechanical check.

## References

- `DEC-L-026`, `DEC-L-027`, `DEC-L-028` on the board.
- Your T-027 result, which names both instances.
