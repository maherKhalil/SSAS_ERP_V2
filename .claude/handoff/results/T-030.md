# T-030 result — DONE

- **Status:** DONE
- **Branch:** `agent/T-030-remaining-anchors` — pushed, branched from `main` at `52f431a`
- **Merged:** no — non-gated (`DEC-L-002`). Awaiting `MERGE T-030`.
- **Commits:** `a005d98` — docs(T-030): anchor the remaining FP-014 citations
- **Reported:** 2026-08-26

## Acceptance criteria

- [x] **`domain-model.md:50` corrected and anchored** — the wrong-content `ADR-017:162`.
- [x] **All seven `:475` citations anchored** to § Lookup classification, class A (`:477`).
- [x] **All `ADR-005:248` uses anchored** — in the four files this task could reach.
- [x] **Every line number kept.**
- [x] **`trace-check` byte-identical** to the T-029 baseline; FP-014 GREEN `0 unruled`.
- [x] Four files, all under `docs/17-features/FP-014-subscription/`.

## 31, not nine — and the arithmetic is my error from T-029

The sweep found **31 citations**, not the nine I enumerated. The gap is mine:

**I wrote "the five remaining `ADR-005:248` uses" and you read it as five uses. It is eight
occurrences across five files.** I counted files and reported them as uses. The other difference is
that T-029's enumeration covered only `ADR-017`, so the `ADR-021:207`, `:200`–`:207` and `:37`
citations in the same files were never in the nine at all.

Nothing was harmed by it — the cap was four files and the work is per-file — but the number in your
spec came from me and it was wrong.

| Citation | Count | Now |
|---|---|---|
| `ADR-005:248` | 8 | `ADR-005` § Platform Administration (`:248`) |
| `ADR-017:475` | 7 | `ADR-017` § Lookup classification, class A (`:477`) |
| `ADR-017:169` | 7 | `ADR-017` § Platform database boundary (`:169`) |
| `ADR-021:207` | 6 | `ADR-021` § 10 Outage behaviour (`:207`) |
| `ADR-017:164` | 4 | `ADR-017` § Platform database boundary (`:164`) |
| `ADR-021:37` | 2 | `ADR-021` § Status (`:37`) |
| `ADR-021:200`–`:207` | 2 | `ADR-021` § 10 Outage behaviour (`:200`–`:207`) |
| **`ADR-017:162`** | 1 | corrected → § Platform database boundary (`:164`) |

**31 converted; 6 deliberately not.**

## Revision History excluded by construction, not by care

Your out-of-scope rule generalises my T-029 judgement, so I mechanised it rather than trusting myself
to notice: the script **splits each file at `# Revision History` and converts only the text above it.**

That matters because `decisions-ratified.md`'s rows `1.2` and `1.3` cite `ADR-017:164` twice, and a
careful-but-manual pass is exactly where that gets missed. A revision row records what was cited at the
time; rewriting it makes history report something that did not happen.

## What remains, and why each is left

| Site | Citation | Why |
|---|---|---|
| `authorization-model.md:24` | `ADR-005:248` | **Live — needs anchoring.** Outside the four-file cap. |
| `decisions-ratified.md:49` | `ADR-005:248` | **Live — needs anchoring.** Outside the cap. |
| `decisions-ratified.md:208`, `:222` | `ADR-021:207`, `:37` | Inside the framed *"describes the position BEFORE the amendment"* block. **Deliberate:** they record what the package cited then. |
| `decisions-ratified.md:268`, `:269` | `ADR-017:164` | Revision History. **Out of scope by your rule.** |

**Two live citations in two files is the whole remainder.** Both are `ADR-005:248`, both accurate,
both fragile.

## The dated-statement constraint — checked, and it did not bind here

You made it a design constraint after T-029. I looked for stamped passages in these four files and
**found none** — the only commit-stamped survey in the package is the README table, which T-029 already
converted. The constraint is satisfied vacuously here rather than by anything I did, and I would rather
say that than let it read as applied.

## A correction to both our figures on the T-011 shift

You said my T-011 commit added **six lines**; I said **three**. **Both are wrong, and the number that
explains the shift is two.**

`git show --numstat a234d6b` gives **6 insertions, 4 deletions — net +2** on `ADR-017`, and the citation
moved `:162` → `:164`, which is exactly +2. Six is the insertion count; the shift is the net.

Worth pinning down because the net is what a reader would use to recompute an old citation, and "six"
would send them to `:168`.

## Verification beyond the criteria

**Parenthesis balance, before and after.** The conversion inserts `(…)` around line numbers, and
several citations already sat inside parenthesised groups — so it could have produced malformed
nesting silently. Every file is balanced overall, and **the count of groups wrapped across two lines is
identical before and after** (domain-model 12/12, decisions-open 8/8, requirements 0/0,
business-rules 0/0). The `))` sequences that appear are correct nesting, not damage.

I checked because a 31-replacement mechanical pass is precisely the kind that looks clean in a diff
stat and reads wrong on the page.

## For the architect

- **Blocked / needs a decision:** none. Two live citations remain, named above — one small task, or
  leave them as accurate-but-fragile.
- **Touched outside scope:** nothing. Four files, all inside the package. No Revision History row
  altered, no `src/` or `tests/` file, no ADR.
- **`ADR-021`'s citations are now anchored while `ADR-021` itself stays `Proposed`** — the anchors
  describe passages, not authority, and `DEC-L-024`'s re-point is untouched. Noted so the two are not
  confused later.

### On `DEC-L-025`

**Still no usage warning in my context.** Reported fresh.
