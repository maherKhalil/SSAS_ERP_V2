# B18 pass 03 — FP-006: 40 examined, 24 cited

**TASK gate green, 0 warnings. Control: 121 cited across all packages, zero dangling.**

## The two numbers

| | this pass | running |
|---|---|---|
| criteria **examined by body** | **10** (`AC-EMP-0031`…`0040`) | **40 of 47** |
| **cited** | **7** (`0031`, `0033`, `0034`, `0036`, `0037`, `0038`, `0039`) | **24 of 47** |
| ⚠ found **uncovered** | **0** | **0** |
| **partly pinned** | **0 new** | 2, both completed earlier |

## ⚠ MY TAIL FORECAST IS NOW WRONG TWICE, AND THE REASON IS NOT THE ONE I GAVE

| pass | examined | cited | rate |
|---|---|---|---|
| item 218 | 10 | 4 | 0.40 |
| pass 01 | 10 | 6 | 0.60 |
| pass 02 | 10 | 7 | 0.70 |
| **pass 03** | **10** | **7** | **0.70** |

**I predicted the tail would be harder because criteria would shift from domain tests to architecture
tests.** ⚠ **The shift HAPPENED — `0037`, `0038` and `0039` are pinned by `CutoverManifestArchitectureTests`,
exactly as forecast — and the rate did not fall.**

**So the mechanism was right and the consequence was wrong.** ⚠ **Architecture tests turned out to be
EASIER to map, not harder: `C6_6_Employee_is_ordered_after_company_and_branch_and_history_after_employee`
states its criterion in its method name almost verbatim, because an architecture test has to say what
structural claim it makes.** **A domain test can be called `A_transfer_moves_the_employee_and_appends_exactly_one_record`
and mean three things; an architecture test usually cannot.**

**The forecast is withdrawn on its consequence and kept on its mechanism.** The remaining seven —
`0027`–`0030`, `0032`, `0035`, `0040` — **are the real tail, and four of them already failed to resolve in
pass 02.**

## What was cited

- **`AC-EMP-0031`** — branch moves to destination **and history length is 2**: the initial assignment plus
  exactly one transfer record, which is the criterion's *"appends exactly one"* half.
- **`AC-EMP-0033`** — first transfer succeeds, second fails, **and the branch is the first winner's
  destination**, so it is one success and one deterministic conflict rather than two attempts that both
  moved something.
- **`AC-EMP-0034`** — ⚠ **two tests for two clauses**: terminated (`TransferAfterTermination`) and
  destination-equals-source (`TransferDestinationUnchanged`). **The second also asserts `Assert.Single` on
  the assignments, proving the refusal appended nothing** — a detail the criterion implies and the name
  does not.
- **`AC-EMP-0036`** — an administrator transfers an employee **out of a deactivated source**.
- **`AC-EMP-0037`/`0038`** — the cutover manifest's **exact expected list** names both `Employee` and
  `EmployeeBranchAssignment`, asserted against the derived plan.
- **`AC-EMP-0039`** — Employee after Company **and** after Branch, history after Employee. **Verbatim.**

## Left unresolved

**`AC-EMP-0032`** (dual branch authorization — source as execution context, destination through the live
resolver), **`0035`** (branch history in effective order, sufficient for point-in-time attribution),
**`0040`** (three independent dimensions: permission **and** company scope **and** branch scope).

⚠ **`0040` is the one already mentioned in prose** — `EmployeeBoundarySqlServerTests:2314` carries a
`P — THE PERMISSION IS ACTUALLY GRANTABLE` block. **A prose mention is not a claim**, and I did not promote
it to one.

## Scope
- **Seven citations, seven bodies read.**
- **`0037` and `0038` share one test**, cited twice on the same method — the manifest asserts both entities
  in one exact list, and splitting it would mean two tests asserting halves of one list.
- FP-006: **24 of 47 cited, 40 of 47 examined, 0 uncovered.** **Seven criteria remain.**
