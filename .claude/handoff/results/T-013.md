# T-013 result — DONE

- **Status:** DONE
- **Branch:** `agent/T-013-fp-014-amend` — pushed: yes, branched from `agent/T-008-fp-014-matrices`
- **Merged:** no — **the FP-014 chain is now complete and ready for the whole-package merge**
  (T-002 → T-006 → T-007 → T-008 → T-013). Awaiting your word.
- **Commits:** `d1b38c7` — docs(T-013): BR-SUB-0021, and the two sentences DEC-L-012 made stale
- **Reported:** 2026-08-25

## The question you told me to answer before assuming: no, it does not widen

**`BR-SUB-0021` needs no `AC-SUB` and no `TS-SUB`.** I verified that rather than assumed it, and did
not stop, because the condition you set was *"if it needs one, say so and stop"* — it does not.

Two independent reasons:

1. **Mechanically, `BR` identifiers are not in `trace-check`'s coverage chain.** Rules 3 and 4 are
   `REQ → AC` and `AC → TS`. `parse_chain` captures the rules cell as `cells[1]` and never
   coverage-checks it. A `BR` identifier only has to be **defined in its home file** to avoid an
   ORPHAN, which is exactly what this task does.
2. **Substantively, the behaviour is already covered.** `AC-SUB-0049`, `0050` and `0051` and
   `TS-SUB-0048`, `0049` and `0050` were written in T-008 and assert precisely what `BR-SUB-0021`
   states. **The rule is the statement of what those criteria already test**, not new behaviour
   needing new coverage — which is why the gap was expressible as "a rule is missing" rather than
   "a criterion is missing".

The green run below is the confirmation: had it needed coverage, rules 3 and 4 would now be failing.

## Acceptance criteria

- [x] **`business-rules.md` gains `BR-SUB-0021` stating the enforcement point per `DEC-L-009`, with
      the asymmetry against expiry stated.**
      The rule states admission-only enforcement naming cap, count and plan; login never refused;
      downgrade excess billed and reported. The asymmetry is a three-row table — when it arrives, who
      it affects, who can resolve it — plus the sentence that carries it: blocking a login would
      enforce against an arbitrary user who did nothing, at the moment they sat down to work, in an
      ERP of record.
- [x] **`traceability-matrix.md` carries `BR-SUB-0021` where it named it in words, and the chain
      resolves.** `REQ-SUB-0027`'s seat-admission row now reads `BR-SUB-0021`. **`BR-SUB-0006` moved
      off that row** onto the metering row where it belongs — see below.
- [x] **The "open ruling on the whole 70% class" sentence corrected.** Replaced: the class is closed,
      as `DEC-L-012`, and closed **forwards**.
- [x] **The "expect a third `UNPROMOTED` row" passage corrected to the sharper truth.** It now
      distinguishes explicitly: *before ratification it is the check working; after ratification the
      same row is a ratification defect.* Promotion is stated as an obligation of ratification, and
      the `BR-PAY`/`BR-ATT` rows are noted as sitting on `DEC-L-012`'s different, per-module terms.
- [x] **`trace-check` still GREEN, exit 0, `BR-SUB` contiguous `0001`–`0021`.** Output below.

## What changed

Two files. 50 insertions, 19 deletions.

### One thing I changed that the spec did not ask for, and why

**`BR-SUB-0006` no longer appears on `REQ-SUB-0027`'s seat-admission row.** Both `REQ-SUB-0027` rows
carried it after T-008, because at that point it was the only metering rule available. Now that
`BR-SUB-0021` exists, leaving `BR-SUB-0006` on the admission row would say that *how a quantity is
judged* governs *where a cap is enforced* — the exact conflation my `NEEDS-DECISION` answer argued
against.

So the metering row keeps `BR-SUB-0006` and the admission row takes `BR-SUB-0021`. It is one cell,
inside a file already in scope, and reverting it is trivial if you read it as scope creep — but
landing `BR-SUB-0021` while leaving the conflation in place would have made the amendment cosmetic.

### The rule, and why it is not `BR-SUB-0006` with more words

Stated in the file rather than left for a reader to reconstruct: `BR-SUB-0006` is about **judgement**
at a past instant, `BR-SUB-0021` is about **enforcement** at a present one, and **neither implies the
other** — a product could judge historically and still enforce at login, which is precisely the
design `DEC-L-009` rejected.

### Housekeeping under `DEC-L-013`

A stray doubled `---` from a T-008 edit is removed, and the "twenty rules" count in the matrix's
closing section is now twenty-one.

`git diff agent/T-008-fp-014-matrices...agent/T-013-fp-014-amend --stat`:

```
 docs/17-features/FP-014-subscription/business-rules.md      | 54 +++++++++++++++++++---
 docs/17-features/FP-014-subscription/traceability-matrix.md | 15 ++----
 2 files changed, 50 insertions(+), 19 deletions(-)
```

## Gate

**Not run** — `DEC-L-002`, no file under `src/` or `tests/`.

### Criterion 5 — `py scripts/trace-check.py FP-014-subscription`

```
FP-014-subscription  [GREEN]  0 failure(s), 1 warning(s), 4 declared gap(s)

  Inventory, derived:
    space           count  range        contiguous
    REQ-SUB            28  0001-0028    yes
    BR-SUB             21  0001-0021    yes
    AC-SUB             51  0001-0051    yes
    TS-SUB             50  0001-0050    yes
    DEC-SUB            12  0001-0012    yes
    OD-SUB             17  0001-0017    yes

  Owner decisions: 17 defined, 17 unruled -> BUILD BLOCKED

  WARNINGS:
    - BUILD BLOCKED: 17 owner decision(s) and no ratification file in this package

  Declared gaps (4) - deliberate, not defects:
    - REQ-SUB-0011 - tests cell is a declared gap
    - REQ-SUB-0026 - criteria cell is a declared gap
    - REQ-SUB-0026 - tests cell is a declared gap
    - 1 criterion carried by prose with no owning requirement: AC-SUB-0047

[TRACE GREEN] 1 package(s) clean          EXIT = 0
```

Run with every other package present. Failures, warnings and declared gaps are **identical to
T-008's** — the amendment added a rule and changed no coverage.

Check 7 confirms the promotion state: `BR-SUB - 21 rule(s) (BR-SUB-0001…BR-SUB-0021) defined in
FP-014-subscription/business-rules.md, and the master Business-Rules.md carries no BR-SUB rule at
all`. Correct pre-ratification, and now the file says why that changes at ratification.

## For the architect

- **Blocked / needs a decision:** none.
- **Touched outside scope:** nothing. Two files.

### `DEC-L-013` — I found and cleared a shared-tree hazard while finishing this

Since T-006 I have been restoring `scripts/trace-check.py` into the working tree **untracked**, with
`git show agent/T-005-trace-check:scripts/trace-check.py`, because the FP-014 chain branches from
`main` at `f9b247a` — *before* T-005 merged — so the script is not tracked on any branch in this
chain.

**That untracked file was a trap for whichever of us checked out `main` next.** `main` now tracks
`scripts/trace-check.py`, and `git checkout main` refuses when an untracked file would be overwritten
by a tracked one. With one shared tree, that error would have landed on you, on a path neither of us
would immediately connect to FP-014.

Cleared: I verified the copy was **byte-identical** to `origin/main:scripts/trace-check.py` (`diff -q`,
identical) and deleted it. **The tree is back to the same three entries it has carried all session** —
modified `.gitignore`, untracked `.claude/` and `D7-M2.bundle` — and `git checkout main` will now
succeed.

**Worth knowing for T-014:** the tool is only present in a working tree whose branch descends from
`fb23e23`. A gated task branching from current `main` gets it tracked and this problem disappears;
any further FP-014-chain task will need the same restore-and-remove dance, and should say so.

### Ready for the whole-package merge

The chain is `agent/T-002-fp-014-foundation` → `T-006` → `T-007` → `T-008` → `T-013`, each branched
from its predecessor, so **one PR from `agent/T-013-fp-014-amend` carries all five**. Twelve files,
`trace-check` GREEN, exit 0.

I have not opened it — non-gated work merges on your `MERGE`, and you have been holding this package
deliberately since T-006.

### Follow-ups noticed but not done

- **The two ratification concerns you recorded stay open** and both are now visible in the package
  rather than only in the handoff: `AC-SUB-0049` naming `TenantUser` as the seat-bearing entity while
  `DEC-L-009` says "seats" without defining one, and `REQ-SUB-0027` carrying two rows with different
  enforcement semantics. `BR-SUB-0021` inherits the first — it says "a user", which is the same
  undefined term wearing different clothes. If ratification defines a seat, both the rule and the
  criterion want re-reading together.
- **`TS-SUB-0033` is still red on arrival**, unchanged by this task, and T-014 is correctly queued
  ahead of any FP-014 build task for exactly that reason.
