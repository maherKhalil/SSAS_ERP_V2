# T-001 result — DONE

- **Status:** DONE
- **Branch:** `agent/T-001-start-here-state` — pushed: yes (`origin/agent/T-001-start-here-state`)
- **Merged:** yes — PR #53, `d29cc40` (merged 2026-08-25 under `DEC-L-007`, on the architect's `MERGE T-001`; non-gated, so it waited for that)
- **Commits:** `3c56915` — docs(T-001): make START-HERE's implementation state true through FP-013
- **Reported:** 2026-08-25

## Acceptance criteria

- [x] **FP-008 no longer described as unbuilt; `Position`, `JobGrade`, `SalaryGrade` recorded as delivered; `ADR-027` recorded as activated rather than "drafted alongside".**
      The paragraph now opens **FP-008 HR Position is delivered and merged** (PR #46, `0540de3`). The three
      aggregates are cited to `src/Modules/HR/SSAS.HR.Domain/Positions/` — `Position.cs`, `JobGrade.cs`
      and `SalaryGrade.cs` all verified present. `ADR-027` is worded "no longer drafted alongside the
      package: it is **activated** by the `OD-POS-004` ruling".
- [x] **FP-009, FP-011, FP-012, FP-013 each recorded as delivered with a traceable anchor.**
      FP-009 → PR #48 / `fc94cbd`; FP-011 → PR #50 / `9b1dd99`; FP-012 → PR #51 / `f465c9b`;
      FP-013 → PR #52 / `f9b247a`. All four shas read out of `git log --oneline --merges` on `main`, and
      each was confirmed to carry `src/` changes for its own module via
      `git diff --name-only <sha>^1 <sha>`.
- [x] **FP-010 recorded as CLOSED — deferred to V5 Document Management (`OD-DOC-009`).**
      Wording: "**FP-010 HR Employee Documents is CLOSED, and is not pending HR work.** … V5 Document
      Management owns the capability, ruled by `OD-DOC-009` on 2026-08-23. Do not treat it as an unbuilt
      package in the HR sequence." Sourced from that package's own `status:` front-matter line.
- [x] **Every factual claim traceable to a merge commit, a file path, or a `status:` line that exists
      today.** Citation table below — one row per claim added.
- [x] **`git diff main...agent/T-001-start-here-state --stat` shows exactly one file changed.**
      Verified; output pasted below. `git diff -U0` shows a single hunk, `@@ -269 +269,11 @@`, entirely
      inside the section's line range.

### Citation table — every added claim and where it was read

| Claim added | Verified against |
| --- | --- |
| FP-008 merged, PR #46 | `git log --oneline --merges` → `0540de3 Merge pull request #46 from maherKhalil/codex/fp-008-position-phase-1` |
| `Position` / `JobGrade` / `SalaryGrade` exist | `src/Modules/HR/SSAS.HR.Domain/Positions/{Position,JobGrade,SalaryGrade}.cs` |
| `Employee.PositionId` mandatory, two migrations | `.../TenantErp/Migrations/20260821070819_AddHrPosition.cs` and `20260821161613_AddEmployeePosition.cs` |
| `ADR-027` money = `decimal(19,4)`, inherited downstream | `src/Modules/HR/SSAS.HR.Infrastructure/Persistence/SalaryGradeConfiguration.cs`; same precision in `SSAS.GL.Domain/Journals/JournalDraft.cs`, `SSAS.Payroll.Infrastructure/Persistence/PayrollConfigurations.cs`, `SSAS.Attendance.Infrastructure/Persistence/AttendancePersistenceConstants.cs`. Named as binding by `FP-011/README.md:35,80` and `FP-012/README.md:135`. |
| FP-009 merged, PR #48 | `fc94cbd Merge pull request #48 …/fp-009-import-export-phase-1` |
| `EmployeeImportRun` / `EmployeeExportRun` / `ImportKey` | `src/Modules/HR/SSAS.HR.Domain/ImportExport/` (five files present) |
| FP-009 migration and guard | `20260822112538_AddEmployeeImportExportRuns.cs`; `tests/Architecture.Tests/ImportExportArchitectureTests.cs`, added by `fc94cbd` |
| FP-010 CLOSED / `OD-DOC-009` / 2026-08-23 | `docs/17-features/FP-010-hr-employee-documents/README.md` front-matter `status:` line, verbatim |
| FP-011 merged, PR #50 | `9b1dd99 Merge pull request #50 …/fp-011-gl-analysis` |
| GL tree, migration, suite, guard | `src/Modules/Finance/SSAS.GL.Domain/{Accounts,Calendar,Journals}`; `20260823151722_AddGlFoundation.cs`; `tests/Finance.Tests/`; `tests/Architecture.Tests/GlArchitectureTests.cs` |
| "first module outside HR" | `src/Modules/` holds `Attendance`, `Finance`, `HR`, `Payroll`; Finance is the earliest of the three non-HR trees by merge order (#50 < #51 < #52) |
| FP-012 merged, PR #51 | `f465c9b Merge pull request #51 …/fp-012-payroll` |
| Payroll tree, migration, suite, guard | `src/Modules/Payroll/SSAS.Payroll.Domain/{Compensation,Elements,Runs}`; `20260824175418_AddPayrollFoundation.cs`; `tests/Payroll.Tests/`; `tests/Architecture.Tests/PayrollArchitectureTests.cs` |
| Jurisdiction-neutral, no tax tables, `DEC-PAY-0016` | `docs/17-features/FP-012-payroll/README.md`, the "⚠ THE BOUNDARY OF THIS FEATURE" block — paraphrased from it, not invented |
| FP-013 merged, PR #52 | `f9b247a Merge pull request #52 …/fp-013-attendance`; it is `main`'s tip, hence "most recent module" |
| Attendance tree, migration, suite, guard | `src/Modules/Attendance/SSAS.Attendance.Domain/{Calendars,Leave,Periods,Records}`; `20260825024834_AddAttendanceFoundation.cs`; `tests/Attendance.Tests/`; `tests/Architecture.Tests/AttendanceArchitectureTests.cs` |
| Payroll → Attendance via contracts only, `DEC-ATT-0002` / `ADR-012` | `src/Modules/Payroll/SSAS.Payroll.Application/SSAS.Payroll.Application.csproj:26` — the sole `ProjectReference` into Attendance — and its comment at line 22; `PayrollRunCommandHandlers.cs:2,205`; the contract itself is `SSAS.Attendance.Contracts/Summaries/IAttendanceSummary.cs` |

## What changed

One paragraph became six. The old line 269 — "FP-008 HR Position is approved for implementation and not
yet built" — is gone; in its place are delivered-paragraphs for FP-008, FP-009, FP-011, FP-012 and FP-013,
plus a CLOSED paragraph for FP-010 so the numbering has no silent gap that a reader would fill in as
"pending".

Design choices made inside the given constraints:

- **Structure kept.** Prose paragraphs with **bold** lead-ins, matching the FP-006 and FP-007 paragraphs
  directly above. No table, no bullet list; still readable top to bottom.
- **One anchor shape throughout.** Every new package carries `(PR #NN, sha)`. FP-007 above cites only
  `(PR #45)` with no sha; I did not touch it to match, since it is outside the criteria and outside my hunk.
- **Citations a reader can check by hand.** Each paragraph names the source tree, the migration, the test
  suite and the architecture guard — things you can `ls` — rather than milestone or sprint language, which
  the spec forbids inventing.
- **The FP-012 boundary was carried upward.** `DEC-PAY-0016` — jurisdiction-neutral, no tax tables, no
  statutory deductions — is the kind of fact that is expensive to learn late, and START-HERE is where a new
  reader actually is.
- **The closing warning is byte-identical.** Not re-worded, not "strengthened". It is what makes the
  section self-correcting, and it was already correct.
- **Nothing else in the file moved.** Sprint Order, Mandatory Reading Order, and the section's own opening
  two paragraphs are untouched.

`git diff main...agent/T-001-start-here-state --stat`:

```
 docs/START-HERE.md | 12 +++++++++++-
 1 file changed, 11 insertions(+), 1 deletion(-)
```

`git diff -U0 main...agent/T-001-start-here-state` hunk header — one hunk, inside the section:

```
@@ -269 +269,11 @@ The **Branch foundation** (branch persistence, lifecycle, mandatory user branch
```

`git status --short` after staging — the four protected paths left alone, only `docs/START-HERE.md`
staged, via `git add docs/START-HERE.md` by path and never `git add -A`:

```
 M .gitignore
M  docs/START-HERE.md
?? .claude/
?? D7-M2.bundle
?? scripts/trace-check.py
```

## Gate

**Not run, by the T-001 spec and DEC-L-002.** *Files in scope* contains no file under `src/` or `tests/`.
The only changed file is `docs/START-HERE.md`, which is not compiled and is not read by any suite. The
verification for this task is the `--stat` assertion above, and it passed: exactly one file changed.

| Suite               | Result |
| ------------------- | ------ |
| build (0 warnings)  | not run — no compiled file in scope (DEC-L-002) |
| Architecture.Tests  | not run — same |
| Platform.Tests      | not run — same |
| HR.Tests            | not run — same |
| API.Tests           | not run — same |
| module suite        | n/a — no module changed |

No `.cs` file was needed at any point, so the `BLOCKED` escape hatch in the ruling was never reached.
There is no test output to paste, and none is claimed.

## For the architect

- **Blocked / needs a decision:** none. The task completed as specified.

- **Touched outside scope:** nothing. One file, one hunk, inside the stated line range. `.gitignore`,
  `.claude/`, `D7-M2.bundle` and `scripts/trace-check.py` are exactly as I found them — none staged, none
  modified.

- **Reported, not fixed — the four you named:**

  1. **`ADR-026` and `ADR-027` both carry `status: Proposed`.** Confirmed at
     `docs/14-Engineering/ADR/ADR-026-…md:6` and `ADR-027-…md:6`. This is now slightly sharper than when
     you raised it. My new FP-008 paragraph states `ADR-027` is **activated**; `FP-011/README.md:35` says
     "`ADR-027` unchanged and unamended"; `FP-012/README.md:135` treats `decimal(19,4)` as settled. So
     START-HERE, two approved packages and four modules of shipped code now all rest on an ADR whose own
     front-matter still says Proposed. I used "activated" because the criterion asked for it and because
     it is the package's own language (`OD-POS-004`) — but the ADR does not yet say so itself. Same shape
     for `ADR-026` under FP-007.
  2. **`FP-010`'s README references `ADR-028`, which does not exist.** Confirmed — the ADR directory runs
     `ADR-001` … `ADR-027`, plus `ADR-Template.md` and `README.md`. Untouched.
  3. **The Sprint Order section (line 229) is stale.** It stops at Sprint-04 Payroll and then says
     "Remaining ERP Modules", which no longer matches a `main` that has shipped Attendance. Untouched, as
     instructed — separate concern, separate task.
  4. **Not on your list; found while sourcing anchors — two feature packages contradict their own shipped
     code.** `docs/17-features/FP-013-attendance/README.md` says **"Status: DRAFT — analysis only. No code,
     no schema, no ADR"** three lines below its own "RATIFIED 2026-08-25" banner, while PR #52 shipped
     `src/Modules/Attendance/*`, a migration and three test suites. `FP-012-payroll/README.md` is still
     titled "(analysis package)" for the same reason. Neither file carries a `status:` front-matter key at
     all, unlike FP-001 … FP-011. This bears on the loop directly: the T-001 spec named "a feature-package
     `status:` line" as an acceptable citation, and for FP-012 and FP-013 that source is either absent or
     actively wrong — so I anchored both on merge shas and source trees instead. Left exactly as found.

- **Follow-ups noticed but not done:**
  - FP-007's paragraph cites `(PR #45)` with no sha, so the section is now mixed in citation style. Trivial,
    but outside the criteria and outside my hunk, so I left it rather than widening the diff.
  - FP-007's paragraph also states the cutover manifest "covers seven tenant-owned entities". That was true
    at FP-007. FP-008, FP-009, FP-011, FP-012 and FP-013 each extended the cutover inventories —
    `tests/Integration.Tests/CutoverTenantModelSource.cs` and `TenantCutoverCopySqlServerTests.cs` are
    touched by every one of those five merges — so the number is certainly stale now. I did not verify the
    current count and did not restate it anywhere, deliberately, per "say less rather than more". A
    one-line correction task would close it.
  - `git` warned `LF will be replaced by CRLF the next time Git touches it` on this file. Pre-existing
    line-ending configuration, not something this change introduced — the diff is 11 insertions and 1
    deletion, so there is no whole-file line-ending churn. Noted only so it is not mistaken for damage.
