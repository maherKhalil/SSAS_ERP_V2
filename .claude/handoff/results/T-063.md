# T-063 result — one declaration, and two classifications that rewrite nothing

- **Status:** PENDING MERGE. **Non-gated** — `scripts/` only, one line. **Not self-merged.**
- **Branch:** `agent/T-063-classify`, from `ClaudeBranch` at `ab7c8e0`
- **No document was rewritten.** No citation was touched. No GL coverage was authored.

## Part A — FP-015 declared

```
                    before   after
total failures          34      33
green packages           6       7
```

Declared **`modern`** from what the package *contains*, not from what it will contain: **zero `BRULE-`
identifiers**, and an FP-014-shaped skeleton two files deep (`README.md`, `decisions-open.md`).

**`DEC-L-061` failed the newest package in the repository within an hour of its creation**, which is
the rule working exactly as ruled — on its author, without anyone deciding it should.

## Part B — FP-009's eight are EXTERNAL REFERENCES. Reading 1, and reading 3 is wrong.

**The reading that would have rewritten a document is the one the evidence refuses.**

FP-009's matrix does not carry these citations by accident. It carries them in an explicit **handover
table**, immediately above a sentence stating the policy:

```
| Scenarios | `TS-DOC-0019`, `TS-DOC-0020`, ... `TS-DOC-0024` |
| Decisions | `DEC-DOC-0010`, `DEC-DOC-0011`, `DEC-DOC-0012`, `DEC-DOC-0013` |

**Every one kept its number.** Neither package will reallocate an identifier the other used.
```

**And FP-010 defines them.** `carried-analysis.md` holds a proper table:

```
| `TS-DOC-0020` | H | `View` without `Download` lists a document then requests content -> 403 ...
| `TS-DOC-0023` | S | **Cutover custody.** A tenant holding one document is copied Shared->Dedicated ...
```

**So nothing was left behind.** The split is documented, the numbers were deliberately retained, and
the identifiers exist. **What fails is the instrument, twice over:**

1. **`carried-analysis.md` is not a home file** for `TS-`, so the definitions are invisible and FP-010
   reports its own scenarios as `UNRESOLVED`.
2. **`DOC` is judged FP-009's own module** — it holds 96 `DOC` identifiers against FP-010's 25 — so a
   citation of FP-010's identifier is treated as internal and orphaned rather than external.

**Both packages use the `DOC` space, legitimately, by a recorded agreement.** The checker has no way to
express that, which is the same gap `DEC-L-061` filled for conventions and has not filled for shared
identifier spaces.

**Not fixed here.** Two candidate remedies — declare a per-package home-file addition, or model a
shared space with two owners — and choosing between them is a ruling, not a detail.

## Part C — FP-011's five are TRACED. They are a rule misapplied, not gaps.

**All five appear on a matrix row that also names a test scenario:**

```
| `nvarchar` everywhere                       | standing constraint | `AC-GL-0019` | `TS-GL-0017`, `TS-GL-0019` |
| No cross-database FK                        | standing constraint | `AC-GL-0020` | `TS-GL-0018` |
| E3 manifest + ten-site inventory            | `DEC-POS-0022`      | `AC-GL-0021` | `TS-GL-0015`, `TS-GL-0016` |
| Permission defined, not merely named        | FP-006P             | `AC-GL-0018` | `TS-GL-0024` |
| Administer grants no functional permission  | `ADR-025` d8        | `AC-GL-0017` | `TS-GL-0022` |
```

**Four cells, criteria in cell 3, tests in cell 4 — and cell 1 is a prose capability name carrying no
identifier.** T-062's parser anchors on `REQ-`/`BR-`/`BRULE-`/`DEC-` in the first cell, so it never
sees these rows.

**The contiguity was a false signal.** `AC-GL-0017`..`0021` being consecutive looked systematic and
was: they are consecutive because they were *written* consecutively, in one block of the criteria
file, and land on adjacent matrix rows. **It said nothing about whether they were covered** — which is
the caution the task carried, and it held.

### And it explains three packages the earlier work left unexplained

FP-002, FP-005 and FP-006 still report *"no parseable chain rows"* for the same reason: their chain
tables lead with a **Capability** column. **Measured, single variable, everything else unchanged:**

```
package                                 now   would be
FP-002-authentication-token-lifecycle     2          1   -1
FP-005-company-legal-entity               2          1   -1
FP-006-hr-employee                        2          1   -1
FP-011-gl-foundation                      5          0   -5
TOTAL                                    33         25   -8
```

**Nothing gets worse and no package changes.** The variable: a row carrying **both** an `AC-` and a
`TS-` identifier counts as a chain row even when its first cell is prose.

**Not built here** — the task is classify-before-fixing, and this is the fix. **It is one variable and
a measured −8**, which is the smallest well-evidenced change left in this checker.

## What remains after this, and what it is

```
FP-003    10   REAL — its own file marks AC-TEN-0078..0093 "implementation pending"
FP-009     8   external refs to FP-010's identifiers; two instrument gaps, no document defect
FP-011     5   traced; rows the parser cannot anchor
FP-002/5/6 6   two each: one contiguity, one unparseable-chain — same anchor cause
FP-001     1   contiguity, TS-IAM gaps
FP-008     1   contiguity, TS-POS-0068..0069
TOTAL     33
```

**Exactly ten of the thirty-three are gaps in the corpus, and all ten are declared by the package that
has them.** The rest are the instrument.

**That matters for wiring `trace-check.py` into CI**, which is the next task and correctly not this
one: a gate that goes red on thirty-three items is ignored by the second week, and **twenty-three of
those thirty-three would be the checker complaining about itself.**

## For the architect

- **Blocked / needs a decision:** two. **FP-009's shared `DOC` space** — home-file addition or a
  two-owner model. **The prose-anchored chain row** — measured at −8 and not built.
- **Touched outside scope:** nothing. One line in `PACKAGE_CONVENTION`.
- **Checked back before rewriting a citation, as instructed** — and the evidence says not to rewrite
  one at all.
