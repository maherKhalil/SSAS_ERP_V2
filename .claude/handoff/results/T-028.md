# T-028 result — DONE

- **Status:** DONE
- **Branch:** `agent/T-028-br-sub-0020-scope` — pushed, branched from `main` at `ac9e502`
- **Merged:** no — non-gated (`DEC-L-002`). Awaiting `MERGE T-028`.
- **Commits:** `b80ca3c` — docs(T-028): `BR-SUB-0020` gains the scope `ADR-029` actually rules
- **Reported:** 2026-08-25

## Acceptance criteria

- [x] **`BR-SUB-0020` states the product-wide scope**, citing `ADR-029` alongside `OD-SUB-0016`.
- [x] **Recorded as an amendment to a ratified package**, in T-024/T-026's manner.
- [x] **The master copy updated to match** — both now carry the identical sentence.
- [x] **`decisions-ratified.md` records the ordering fact**, with commits and times.
- [x] **`trace-check` byte-identical to the T-022 baseline** — `UNPROMOTED` still 2, **no new ORPHAN**,
      FP-014 GREEN `0 unruled`.

## The rule, before and after

```
before  …or logged **by this package**
after   …or logged **anywhere in SSAS**
```

**The prohibition is identical.** It already forbade storing, transmitting in any request or response,
and logging — all three verbs are untouched. **Only the boundary becomes explicit.** The rule gained no
new teeth; it stopped pointing at nothing.

Both copies were changed in the same commit, so the source and the promoted copy cannot drift apart —
which is the failure a promoted copy invites and the reason `DEC-L-012` exists at all.

## I verified the ordering rather than taking it, and it is stronger than stated

You gave T-007 and T-010. **Confirmed in git, with a detail that closes the remaining gap:**

| Commit | Time | What |
|---|---|---|
| `187445a` | **16:19:07** | T-007 — writes `BR-SUB-0020`, scope *"by this package"* |
| `d1b38c7` | **16:51:39** | T-013 — last touch of this file before `ADR-029` existed; adds `BR-SUB-0021`, **does not revisit `BR-SUB-0020`** |
| `f03c5ff` | **20:43:57** | T-010 — writes `ADR-029`: no cardholder datum enters **SSAS** |

**More than four hours, and the one intervening edit to this very file did not look at the rule.** So
there was never a moment at which someone could have narrowed the scope in light of `ADR-029` — the ADR
did not exist yet. **Nothing ever ruled the narrow scope.**

That middle row is what makes it evidence rather than inference. Without it, "the rule predates the ADR"
leaves open that someone reviewed it afterwards and chose to leave it; the file's own history shows
nobody touched it again until T-024, which changed a different rule's authority.

**Recorded in `decisions-ratified.md` because ordering is invisible later.** A future reader who finds a
package rule and an ADR disagreeing has no way to tell which came first, and would reasonably assume the
narrower one was deliberate.

## `ADR-029`'s wording checked, not assumed

Decision 1 reads *"Capture is tokenized. **No cardholder data enters SSAS, ever.**"* and *"They do not
traverse **any SSAS component.** Not a controller, not a DTO, not a domain type…"*. **SSAS, the
product** — so "anywhere in SSAS" is the ADR's own boundary rather than a phrase I chose.

Per your constraint I did **not** import the reasoning: the rule states the prohibition and cites
`ADR-029`; the ADR keeps the *why*. Terminal punctuation left off, per T-022.

## I minted no ledger identifier

You ruled this in the T-028 issue and **assigned no `DEC-L`** — the board's highest is `DEC-L-025`. So
the amendment is recorded by date and by the authority that justifies it, and says plainly that no
identifier was minted.

**Deliberate.** Minting a ledger identifier is your act, and inventing one here would repeat the
`REQ-PLT-0071` mistake I made in T-002 — inventing an identifier in the package about identifiers. If
you want this on the ledger, it needs a number from you.

## FINDING — T-022 falsified this file's own header, and I did not fix it

**`FP-014/business-rules.md` opens with a shouted block that is now the opposite of true:**

> ## ⚠ THESE RULES ARE NOT IN THE MASTER REGISTER, AND PROMOTING THEM IS A SEPARATE ACT
>
> **`docs/00-Master-Product-Specification/Business-Rules.md` contains no `BR-SUB` rule.** It carries
> four business-rule prefixes … `SUB` is in neither. … every rule below is a **proposal for that file,
> not a citation from it** … **this package does not perform it.**

Every clause was true when written and every clause is false now. It also says *"A third row appears
once this file lands"* — that row appeared, and T-022 closed it. The document title
(*"Proposed business rules"*) and the table heading (*"## Proposed rules"*) carry the same staleness.

**This is the third instance of one defect class today** — `README.md:17` after T-024, closed concern
3's present tense after T-021, and now this. Each time the document that *records* a change is the one
left describing the world before it.

**I did not fix it, and the reason is scope rather than doubt.** The file is in scope for this task's
rule amendment, but the header is ~50 lines of analysis prose, the staleness was caused by **T-022**
rather than by anything here, and repairing it means rewriting an argument rather than correcting a
citation. When I stopped at four sites in T-024 you upheld it; the same discipline applies.

**Worth keeping rather than deleting when it is fixed** — the middle of that block explains *why*
promotion belongs to ratification and how `BR-PAY`/`BR-ATT` were lost, which is still true and is the
reasoning `DEC-L-012` rests on. It needs its tense changed, not its content removed.

## For the architect

- **Blocked / needs a decision:** the header above, and whether this amendment gets a `DEC-L` number.
- **Touched outside scope:** nothing. Three files, exactly those in scope. `ADR-029` untouched, the
  other twenty `BR-SUB` rules untouched, `BR-PAY` and `BR-ATT` untouched.
- **Your ORPHAN point applied to the amended copy**, as instructed: the full run is byte-identical to
  T-022's, so the amended rule is still cited and no orphan appeared. Editing a promoted rule's text
  cannot orphan it — the citation is by identifier, not by wording — but that is worth having checked
  once rather than assumed.

### On `DEC-L-025`

**Still no usage warning in my context.** Reported fresh.
