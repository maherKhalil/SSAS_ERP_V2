# T-021 result — DONE

- **Status:** DONE
- **Branch:** `agent/T-021-fp-014-ratify` — pushed, branched from `main` at `3a84056`
- **Merged:** no — non-gated (`DEC-L-002`). Awaiting `MERGE T-021`.
- **Commits:** `f3760f8` — docs(T-021): ratify FP-014 — the seventeen rulings move into the package
- **Reported:** 2026-08-25

## Acceptance criteria

- [x] **`decisions-ratified.md` exists with all seventeen `OD-SUB` rulings**, each attributed and
      carrying the reasoning as the ruling set states it. FP-013's shape.
- [x] **README status no longer says `DRAFT — analysis only`.** It records the package as ratified,
      names the ratification file as authority, and lists what is outstanding.
- [x] **`trace-check`: 17 unruled → 0, package GREEN.** Run with every other package present.
- [x] **Two ratification concerns carried open, not resolved.** Three, in fact — see below.
- [x] **`--stat` shows two files**, both under `docs/17-features/FP-014-subscription/`.

## The deliverable, and the whole diff of the full run

```
  Owner decisions: 17 defined, 0 unruled -> ratified, build may proceed
FP-014-subscription  [GREEN]  0 failure(s), 0 warning(s), 4 declared gap(s)
```

I ran the full 14-package check before and after and diffed the two outputs. **The entire difference
is three hunks**, and all three are this task:

```
<   73 identifier(s) defined; 1592 file(s) scanned      >  … 1593 file(s) scanned
< FP-014-subscription  [GREEN] 0 failure(s), 1 warning  > FP-014-subscription [GREEN] 0 failure(s), 0 warning
<   Owner decisions: 17 defined, 17 unruled -> BUILD BLOCKED
<   WARNINGS: - BUILD BLOCKED: 17 owner decision(s) and no ratification file in this package
>   Owner decisions: 17 defined, 0 unruled -> ratified, build may proceed
```

`[TRACE RED] 9 of 14` is **unchanged** — it is the pre-existing master-register state, identical to
the baseline I took before touching anything. **The `BR-SUB` UNPROMOTED finding under check 7 was
already present before ratification** and did not appear as a consequence of it; it is T-022's.

I chose the before/after diff over reading the after-run alone because a ratification file is exactly
the kind of change that can quietly satisfy one check while tripping another — the step-0 residue
check, for instance, treats `-dependent` markers differently inside `decisions-ratified.md` than
outside it.

## The attribution, since it was the point of the task

**Six owner, seven architect, four owner round 2 — and they land in a pattern worth naming.**

| Ruled by | Which | What they have in common |
|---|---|---|
| **Owner** | `0001`, `0009`, `0014`, `0015`, `0016`, `0017` | Every one is a question about **what the product sells and how it charges** — scope, term, trials, pricing, invoicing, metering. None could be derived from the codebase. |
| **Architect** | `0002`, `0003`, `0004`, `0005`, `0007`, `0012`, `0013` | Every one **follows from what is already built** — identifier registers, sequencing, cache invalidation, what a module is, what "menus" binds to, retention, which plane administers. `OD-SUB-0013` was not genuinely open at all: `ADR-005:248` and `ADR-015` already ruled it, so it is recorded rather than re-decided. |
| **Owner, round 2** | `0006`, `0008`, `0010`, `0011` | The four the architect **declined to take** — each trades something a structural argument cannot price. `0006` accepts a surface-enumeration disclosure to buy supportability; `0011` decides whether a grant may take something away. |

That split is the argument for the column: **an architect ruling can be reversed by showing the code
says otherwise; an owner ruling cannot be reversed by evidence at all.**

## Open at ratification — three, not two

The two you named are recorded as yours, unresolved. **The third is what the `DEC-L-021` check you
asked for actually returned**, and it does not clear.

### 1. The seat cap is ruled without a seat being defined — yours

`AC-SUB-0049` reads a seat as a **`TenantUser`** because that is the only reading the repository makes
available. Recorded as load-bearing and unratified, with what it silently decides: whether a
deactivated user occupies a seat, whether a user with memberships in two tenants consumes two, and
whether a platform-support principal consumes any.

### 2. `REQ-SUB-0027` — one row, two obligations — yours

**A note on your wording, because I could not verify it as stated.** You described it as *"two rows
with different enforcement semantics"*. In `requirements.md` it is **one row**, about metering only:
*"usage that affects price … is metered, and the product names exactly what is counted."*

What I can verify is a real problem of the same shape: **`OD-SUB-0017` ruled "seats plus limits", and
the limits half refuses while metering only measures.** The three criteria citing `REQ-SUB-0027` are
consequently not the same kind of thing — `AC-SUB-0049` is a refusal at admission, `AC-SUB-0050` is a
guarantee that no refusal happens anywhere else, `AC-SUB-0051` is a billing outcome with no refusal at
all. I recorded that, flagged rather than split, since renumbering a requirement space at ratification
is how a traceability matrix starts lying.

**If you meant a different pair of rows, say so and I will correct the entry.** I did not want to
record your concern in words I could not check against the file.

### 3. `ADR-021` is `Proposed`, and FP-014 rests on it — **new, and it is the reason to read this section**

Your constraint said to check `DEC-L-021` because *"the package will now be cited by things"*, and
named `ADR-017`, `ADR-027`, `ADR-029`, `ADR-030` as clear. **They are. `ADR-021` is not, and nobody
had looked at it.**

`ADR-021 Customer-Managed Tenant Database Connectivity and Operations` — `status: Proposed`. FP-014
stands in a **defined relationship** to it, not a passing one:

- **`REQ-SUB-0005`** cites `ADR-021:207` as its *authority* — the subscription surface must stay
  readable and administrable while the tenant's ERP database is unavailable.
- **`DEC-SUB-0004`** is built on the same passage, and **`OD-SUB-0004`'s ruling excludes a
  tenant-database projection because of it.** An architect ruling rests on a `Proposed` record.
- **`decisions-open.md:487`** states outright that `ADR-021` *"constrains this package"* and is not
  reopened by any `OD`.

`ADR-018` (`nvarchar`) and `ADR-020` (a diagram label) are also `Proposed`, but those citations are
incidental — I checked each rather than counting them.

**Ratification is what makes this bite.** An analysis package resting on a `Proposed` ADR is untidy;
an authority resting on one is the incoherence `DEC-L-021` names. **Not resolved here** — an ADR
status change is its own act on the T-011 and T-020 precedent, and it was out of scope.

## For the architect

- **Blocked / needs a decision:** `ADR-021` is the one worth ruling before FP-014 is cited further.
  The other two open items are recorded as yours and block nothing today.
- **Touched outside scope:** nothing. Two files, both in the package. No master register, no
  `Requirement-Numbering.md`, no `BR-SUB` promotion, no roadmap entry, no other package file.
- **No ruling contradicted a package document**, so no `NEEDS-DECISION` on that ground. The ruling set
  and the package agree everywhere I compared them; what the package carried were *conditionals*
  awaiting the rulings, and those are now resolved in the "What the rulings changed" table.

### Two things I left alone that you may want

- **The README title still reads `(analysis package)`.** Still true — there is no code — but it sits
  oddly beside `RATIFIED`. Your scope said *status only*, so I did not touch it.
- **`README.md:116` still describes `Authentication.md` as `Draft`.** Accurate: T-012 deliberately
  kept it `Draft` and added the authority block. Left as is, noted so it is not read later as a line
  T-012 forgot.
