# T-130 — **the dossier, and three of your eight items had moved. One is a live money path.**

- **Status:** DOCS ONLY, **non-gated — PUSHED AND STOPPED** per `DEC-L-002`. Awaiting your `MERGE T-130`.
- Branch `agent/T-130-owner-dossier`, from `ClaudeBranch` at `74dfc48`.
- Deliverable: `.claude/handoff/OWNER-DECISIONS.md` — eight sections, each with facts, blocking effect and
  options, and no recommendation where the call is the owner's.

---

# THE ESTABLISH YOU ASKED FOR: THREE CHANGED, AND ONE IS NOT WHAT IT WAS

## ⚠ 3. Overtime tiers — **stale, and the replacement finding is a silent underpayment**

**Your record: *"four layers, a fifth never written."* The fifth layer exists. The tier prices end to end:**

```
AttendanceRecord.OvertimeTier -> AttendanceSummaryResult.OvertimeQuantityByTier
  -> PayrollRunCommandHandlers:263 -> PayrollCalculator.OvertimeQuantity -> quantities[tier] * element rate
```

**What is missing is not a layer. It is a shared VOCABULARY, and the two sides normalise differently:**

```
Attendance   GroupBy(record.OvertimeTier, StringComparer.Ordinal)   case-sensitive
             AttendanceRecord stores the tier VERBATIM              no trim
Payroll      PayElement.SetOvertimeTier stores .Trim()              trims
match        quantities.TryGetValue(tier)                           ordinal, exact
```

**A record tagged `"Night"` against an element tagged `"NIGHT"` does not match, and `OvertimeQuantity`
returns `0m`.** Not an error — **the employee is paid no overtime for that tier, silently, on a payslip that
looks complete.**

**Every test on both sides uses the literal `"NIGHT"`. The mismatch is covered nowhere.**

**I did not fix it.** It is a behaviour decision with four real options — a tier catalog, normalising both
write points, refusing the run when a record's tier matches no element, or accepting it — **and "pay zero
silently" versus "refuse the run" is not mine to pick.** It is section 3 of the dossier with all four.

## ⚠ 1. Platform's administration transport — changed, and roughly twice the task

**`~23` handlers is 29** (a floor — a name in a comment counts as routed). **And the mapping half was not in
the record at all: 117 declared `TenantStorage` codes, 115 returned, 0 mapped.** It was *"three surfaces
have no routes"*; it is *"three surfaces have no routes **and 115 refusals with no statuses**"*.

## ⚠ 4. Self-service grants — **three permissions, not six**

```
Attendance.Records.ViewOwn   Attendance.Leave.ViewOwn   Payroll.Payslips.ViewOwn
```

The substance is unchanged — all per-user, no bulk grant — **but the number was double.**

## The other five are current, verified today

**Employment type** absent from HR entirely, three guards standing. **`POST /records/bulk`** specified in
FP-013 and never built. **Hours-per-day** absent from `src/` *and* `docs/`. **Calendar resolution** is
`IsDefault` then name, chosen for determinism, still an inference rather than a ruling, still governing leave
and pay together. **Period-versus-pay-date** deliberate and enforced by `PayDateBeforePeriod`.

---

# ⚠ AND I DID NOT MAKE THE `{companyId}` CHANGE YOU RULED. THE PREMISE IS BACKWARDS.

**You ruled: add `:guid`, on the grounds that consistency beats preserving an accident and that a malformed
id reaching a handler is the transport failing its one job.**

**It is not an accident. There is a test asserting it, written in the original transport commit:**

```
CompaniesReadEndpointTests.Detail_with_a_malformed_company_id_returns_400
  GET /api/platform/companies/not-a-guid  ->  400
```

**And 400 is the product's convention for a malformed identifier, in six tests across three modules:**

```
A5_A_malformed_company_header_is_a_validation_failure          HR
D30_A_missing_or_malformed_company_header_is_a_validation_failure   HR
A50_A_malformed_department_filter_is_a_validation_failure      HR
Update_with_a_malformed_rowversion_returns_400_...             Platform
Lifecycle_with_malformed_rowversion_returns_400_...            Platform
Detail_with_a_malformed_company_id_returns_400                 Platform
```

**FP-007's api-contracts states the principle directly** — *"parsing that refuses a malformed identifier
rather than treating it as 'no filter'"*.

**Meanwhile no Attendance test covers a malformed path id at all.** The `:guid` 404 is routing default —
**untested, unasserted, and decided by nobody.**

**So the inconsistency is real and the sides are the other way round.** Company's 400 is the deliberate,
tested, convention-following side; Attendance's 404 is the accident. **Adding `:guid` would move the one
considered case to match the unconsidered one, and would require editing a test that deliberately asserts
the current behaviour.**

**I made the edit, found the test, and reverted it** rather than proceeding — **not because the change is
hard, but because I would have been changing a deliberate behaviour on a premise I could see was wrong.**
The tree is untouched.

**If you still want `:guid`, say so and I will do it** — it is a defensible call on "route constraints
belong on route parameters" grounds, and it is yours to make. **But it is a change AWAY from the documented
convention, not toward it, and the four Attendance/sibling routes are the ones that diverge.**

---

## For the architect

- **Blocked on you:** `MERGE T-130`, and the `{companyId}` re-ruling.
- **One item I would put in front of the owner ahead of the others:** the overtime tier. **The other seven
  block features; that one silently underpays somebody.** It is section 3 either way — I have not
  re-ordered the dossier on my own judgement — **but if you want it first, it is a one-line move.**
- **`DEC-L-069`:** the eight verifications are full greps over `src/`, `docs/` and `tests/`, not samples. The
  29-handler figure remains the T-128 floor and is labelled as one in the dossier.
