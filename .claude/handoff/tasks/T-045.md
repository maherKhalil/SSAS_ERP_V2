# T-045 — Mark the seventeen ruled `OD-SUB` in FP-014's open register

- **Branch:** `agent/T-045-od-sub-ruled-markers` — branch from `main`
- **Module:** Platform (commercial plane)
- **Issued:** 2026-08-26
- **Depends on:** T-044 merge (PR #86)

## Goal

Check 8 measured the scope rather than estimating it: **16 divergent decisions in 1 package.** And you
established the seventeenth — `OD-SUB-0014`'s marker sits in the heading, while **FP-013's convention,
which predates T-036, is `**RULED: <the answer>**` on the line below.** Bringing it into line is part
of adopting the convention, not an extra item.

`decisions-ratified.md` holds all seventeen rulings. The register a human opens still says *"each one
blocks the build prompt"* and *"`OD-SUB-0001` is decided first. Sixteen of the seventeen open decisions
are conditional on it."*

## Acceptance criteria

- [ ] **All seventeen `OD-SUB` headings carry FP-013's marker**, in FP-013's placement and wording —
      not the T-036 heading-level form. **`OD-SUB-0014` is brought into line rather than left as the
      exception that started this.**
- [ ] **Each marker states the answer, not merely that one exists** — `RULED: <the answer>` is FP-013's
      shape, and a marker saying only "ruled" sends the reader to a second file for the thing they came for.
- [ ] **The preamble is corrected**, since it now describes the opposite of the truth: no decision
      blocks the build prompt, and `OD-SUB-0001` is not awaiting a first decision. `DEC-L-027` — correct
      the tense, keep the content that is still true.
- [ ] **The rejected options and the analysis stay.** `DEC-L-039` and your own T-036 reasoning: a ruling
      whose alternatives have been erased reads as the only thing anyone considered. Where a survey line
      was true on a date and is now false, **date it rather than correcting it.**
- [ ] **`py scripts/trace-check.py` reports check 8 `[AGREED]`** — 17 of 17, FP-014 no longer divergent,
      FP-013 unchanged at 16 of 16. Paste the section. **Every other verdict byte-identical to a
      before-run**, compared mechanically as in T-036 and T-044.
- [ ] `git diff --stat` shows **at most two files**, both under `docs/17-features/FP-014-subscription/`.

## Files in scope

- `docs/17-features/FP-014-subscription/decisions-open.md`
- `docs/17-features/FP-014-subscription/decisions-ratified.md` — only if adopting the convention
  requires a pointer there; **report if it does not**

## Out of scope

- **FP-013.** It already agrees and it is the model, not a target.
- FP-010's missing ruling register — check 6's question, not this one.
- Every other package: eleven cannot diverge and check 8 says so.
- Any file under `src/` or `tests/`.

## Design constraints

- **Adopt the existing convention rather than the recent one.** FP-013's predates T-036 by a
  ratification, and the only reason the T-036 form exists is that nobody looked. Matching the older
  convention deliberately is the point of the task.
- **A marker is not a ruling.** It points at where the ruling lives and states its answer; it does not
  restate the reasoning. `decisions-ratified.md` remains the authority.
- **`DEC-L-043` applies to your own run:** if check 8 reports something other than 17 of 17, find out
  which is wrong — the check or the edit — before recording either.

## Tests required

**None** — `DEC-L-002`. The check 8 output is the mechanical evidence.

## References

- The model: `docs/17-features/FP-013-attendance/decisions-open.md`, all sixteen headings.
- The authority: `FP-014/decisions-ratified.md`.
- `DEC-L-027`, `DEC-L-039`, `DEC-L-043`. Check 8, added in T-044.
