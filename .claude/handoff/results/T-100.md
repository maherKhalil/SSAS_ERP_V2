# T-100 result — the six reconciled, and a correction to the premise behind the establish

- **Status:** BUILT AND PUSHED, **NOT MERGED.** Branch `agent/T-100-fp013-contracts`, from `ClaudeBranch`
  at `e7da10a`, pushed on start.
- **`GATE_SCOPE=TASK` GREEN, 0 warnings.** No suite moved — correct, nothing under `src/` or `tests/`
  changed.
- **Why not merged:** the task's only file is under `docs/`, which makes it **non-gated** under `DEC-L-002`
  / `DEC-L-007` — *"that waiver removes the only mechanical check, so the architect's verification is the
  entire check."* It is ready; say `MERGE T-100` and it goes.

---

## The six, as ruled

```
CORRECTED  GET  /calendars/{id}/working-days   ->  /calendars/working-days
CORRECTED  PUT  /leave-balances/{id}           ->  /leave-balances
ADDED      POST /records/{id}/adjustments          attributed to OD-ATT-0012
ADDED      GET  /me/records                        attributed to FP-015 / T-089
ADDED      GET  /me/leave-requests                 attributed to FP-015 / T-089
KEPT       POST /records/bulk                      marked SPECIFIED, NEVER BUILT
```

**Verified after the edit:** the documented set and the live set now differ by exactly one row —
`/records/bulk` — and nothing live is undocumented.

**The divergence and its date are stated above the block**, not folded in silently, because a document
reconciled to code without saying so teaches nobody that it drifted.

### One thing I did rather than tidy

The block carried a standing note: *"the identity→employee mapping exists but no route reads it... if the
mapping is later created, that field becomes optional and the route gains a self-service meaning."*

**I answered it rather than rewriting it.** The anticipation was correct and self-service did arrive — but
**not by relaxing `employeeId` on the administrative route.** It arrived as two separate `/me/` routes with
their own permissions, because *a route that infers its subject when a field is absent and accepts one when
it is present is two authorization rules wearing one contract*. `POST /leave-requests` is still the
administrator's route and its `employeeId` is still mandatory.

**Rewriting the paragraph would have deleted a correct prediction and the reason the prediction was not
followed.**

---

# ESTABLISH — AND THE PREMISE NEEDS CORRECTING FIRST

## FP-013's PACKAGE is not `(proposed)`. It is `DELIVERED`.

```
README.md:  status: DELIVERED — all sixteen owner decisions ruled (2026-08-25),
                     merged to main by PR #52 (f9b247a)
```

**`(proposed)` is a per-DOCUMENT title marker, not the package status** — and it sits on **eight of FP-013's
thirteen documents**, never updated when the package moved.

**That inverts the question.** FP-013 is not an unratified specification whose contents may never have been
binding; **it is a delivered one whose document titles went stale.** So the six divergences were divergences
from a document that *does* describe a shipped module — which makes reconciling them more clearly right, not
less.

## And it is not FP-013's problem. It is nearly everyone's.

```
FP-006-hr-employee                 0 of 12 documents say "proposed"
FP-011-gl-foundation               5 of 12       APPROVED
FP-009-hr-employee-import-export   5 of 12       Approved for Implementation
FP-013-attendance                  8 of 13       DELIVERED
FP-014-subscription                8 of 13       RATIFIED
FP-012-payroll                     9 of 13       DELIVERED
```

**Five of six packages carry `proposed` in document titles while their README says otherwise.** Only FP-006
is clean. **The marker is a convention nobody maintains**, and the status lives in the README and moves
there alone — `837eea6 docs(T-004): reconcile the FP-012 and FP-013 README status with what shipped`
reconciled the READMEs and left every document title untouched.

## Which statuses exist — six terms, one vocabulary they do not share

```
Approved for Implementation   FP-001..FP-009   (nine packages)
CLOSED                        FP-010
APPROVED                      FP-011
DELIVERED                     FP-012, FP-013
RATIFIED                      FP-014
DRAFT                         FP-015
```

**There is no single status vocabulary.** Whether `APPROVED`, `RATIFIED` and `DELIVERED` are three points on
one scale or three words for one state is not derivable from the documents.

## Nothing mechanical reads any of it

Swept `.cs`, `.sh`, `.ps1`, `.yml`, `.yaml` outside `docs/` for every status term. **Every hit is prose in a
comment** — "the architect ratified the separation explicitly", "TS-Storage Phase E2 delivered them" — and
none reads a package's status.

**So the status is prose.** No guard, no `trace-check`, nothing gates on it. **A stale status misleads a
reader and breaks nothing**, which is exactly why it can sit wrong for a whole package and nobody notices.

---

## For the architect

- **Not changed, as instructed:** FP-013's status, and every document title.
- **The premise correction matters to your ruling:** FP-013's package moved to `DELIVERED` on 2026-08-25.
  What is stale is eight document titles, and the same is true of four other packages. **Moving one
  package's titles fixes one instance of a convention nobody follows** — the choice is whether the marker
  should exist at all.
- **FP-015 is the different case and it is yours.** `DRAFT — owner decisions unruled; specification only`
  while the feature shipped is not a stale title; **it is a status line contradicting the tree**, and you
  report five of its files are not on `ClaudeBranch` at all.
- **Merge authority:** this is docs-only and therefore non-gated. Pushed and stopped; `MERGE T-100` releases
  it.
