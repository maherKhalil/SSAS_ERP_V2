# T-105 result — the `src/` class, and a fifth site no advisory will ever see

- **Status:** BUILT, GREEN, **MERGE PENDING** under `DEC-L-007` (gated).
- **`GATE_SCOPE=TASK` GREEN, 0 warnings.** Every suite unchanged — 532 / 1088 / 326 / 774 / 46 / 56 / 71.
- **Condition 4: `ok: no non-comment change under src/`** — exactly right for a comments-only task, and it
  is the condition that proves the claim rather than my saying so.

---

# THE COUNTS, REPORTED BEFORE EDITING

```
41  files in src/ + tests/ carrying the self-service topic
26  absence-claim comment lines inside them
 4  FILES CARRY THE CLASS — ALL IN src/. ZERO IN tests/.
 7  sites amended
```

```
AttendancePermissionNames.cs             4 sites   the flagship
LeaveRequest.cs                          1
PayrollPermissionNames.cs                1
AttendanceTransportContracts.cs          1        NEW — found by the sweep, not given
```

---

# THE FLAGSHIP IS CONTRADICTED BY THE DECLARATION IT SITS UNDER

```
:93    public const string ViewOwnRecords = "Attendance.Records.ViewOwn";
:95    public const string ViewOwnLeave  = "Attendance.Leave.ViewOwn";
:101   // **There is no `ViewOwn` of any kind.** No `Attendance.Records.ViewOwn`, ...
```

**Eight lines. Same file, same class body, naming the identical string declared above it.**

**And the second half of that sentence is TRUE.** `Attendance.Leave.RequestOwn` appears in exactly one place
in the repository — that comment. **A sweep that deleted the sentence would have destroyed a fact recorded
nowhere else**, which is why it became *"reading shipped; requesting did not"* rather than a deletion.
`DEC-L-074`.

**`:111-112` keeps its sentence, as ruled** — it is right about restatement and was defeated by something
else. **What follows it now records what defeated it:** `AC-ATT-0032` stopped asserting an absence in T-102
and every citer kept reading it as one, this comment included. `DEC-L-082`, recorded where it happened.

---

# ⚠ THE FIFTH SITE, AND IT IS T-106's FLOOR

`AttendanceTransportContracts.cs:134-139` contradicted **itself** in four lines — *"the mapping exists"* at
`:135`, *"if that mapping is later built"* at `:138`.

**And the prediction was ANSWERED in T-100 while the answer never reached the code.** `api-contracts.md`
records that self-service arrived as separate `/me/` routes rather than by relaxing `employeeId`. The
document was corrected; the contract comment it was written from was not.

**There is no identifier here to key on.** The document and the comment restated one fact, the document
moved, and nothing connected them. **A citer-sweep advisory cannot see this site and never will** — which
makes `DEC-L-080` and `DEC-L-082` one failure with two faces: two places holding one fact, **with or without
a link between them.**

**T-106 should declare that in its own output**, the way the error-mapping register declares its floors.
**An advisory that silently covers half a class is worse than one that names which half.**

---

# `tests/` CARRIES NONE OF THE CLASS

Twenty-six absence claims, four files, **zero in `tests/`** — and `AttendanceArchitectureTests.cs:278-292`
was already correct before this task: *"FP-015 has now landed, so the absence is false. The question the
guard answers is not."*

**A guard's comment is maintained because the guard fails and someone reads it. A module's prose is
maintained by nobody.** Every instance of this class since T-083 has been in module source or specification
prose, and none in a test. **Measured, not argued.**

## What the sweep excluded, and one is the cleanest `DEC-L-074` yet

**`EmployeeReadScopeArchitectureTests.cs:349,839` — *"NO DEFERRED QUERY CROSSES THE APPLICATION BOUNDARY"*.**
That is `IQueryable` deferred execution. **Same word, unrelated meaning** — and the reason the sweep
intersected absence-phrasing with the topic set instead of trusting either alone. An absence-phrase grep
alone returned 141 lines across 100 files; the intersection returned 26 across 41.

Also excluded: eight *"does not exist"* lines about missing records and links, a counterfactual in
`PayrollSelfServiceTests.cs:33`, a test deferred in T-077, and two about leave accrual.

---

## For the architect

- **Blocked / needs a decision:** nothing.
- **T-106's floor is declared above** and it is not a nicety: the fifth site is invisible to the instrument
  being built, and it was the one that contradicted itself hardest.
- **Not touched:** every `tests/` file, and every comment outside the class.
