# T-031 — The last two live line citations in FP-014

- **Branch:** `agent/T-031-last-anchors` — branch from `main`
- **Module:** Platform (commercial plane)
- **Issued:** 2026-08-26
- **Depends on:** T-030 merge

## Goal

Two live `ADR-005:248` citations fell outside T-030's four-file cap: `authorization-model.md:24` and
`decisions-ratified.md:49`. This closes the package.

Both are the **fragile-not-wrong** kind — you verified `ADR-005` is unedited and `:248` still reads
"- Subscription management" — so the fix is the anchor, not a correction.

## Acceptance criteria

- [ ] Both converted on T-030's pattern: `ADR-005` § Platform Administration, line beside it.
- [ ] **The four deliberately-left citations stay left**, and the result says so again: two inside the
      framed "position BEFORE the amendment" block, two in Revision History rows. Both exclusions are
      yours and both are right — a record of what was cited then must not be rewritten.
- [ ] **`ADR-0##:<line>` appears nowhere in FP-014 as a bare handle.** Grep the package and show it —
      every remaining occurrence is anchored, inside a historical block, or in a Revision History row.
- [ ] `trace-check` **byte-identical** to the T-030 baseline; FP-014 **GREEN `0 unruled`**.
- [ ] `git diff --stat` shows **exactly two files**.

## Files in scope

- `docs/17-features/FP-014-subscription/authorization-model.md`
- `docs/17-features/FP-014-subscription/decisions-ratified.md`

## Out of scope

- Every file outside FP-014. The eight `ADR-0##:<line>` citations elsewhere in the repository remain
  undecided as a repository-wide act, and I am not deciding it here.
- Any file under `src/` or `tests/`.

## Design constraints

- Use the same mechanised split-at-`# Revision History` approach you used in T-030. It caught rows a
  careful manual pass would have swept up, and the same hazard exists in `decisions-ratified.md` here.
- Re-run the parenthesis-balance check. A two-replacement pass is small enough to eyeball and small
  enough to get wrong.

## Tests required

**None** — `DEC-L-002`.

## References

- Your T-030 result, which names both and verifies `ADR-005:248` as accurate.
- `DEC-L-028`.
