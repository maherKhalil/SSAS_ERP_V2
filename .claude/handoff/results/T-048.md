# T-048 result — why the tenant context scaffolds a destructive migration

- **Status:** DONE — **complete diagnosis. The half open at 21:50 was resolved at 22:16** on the owner's instruction to continue.
- **Branch:** `agent/T-048-tenant-scaffold-diagnosis` — branched from `main` at `16debb0`
- **Reported:** 2026-08-26 21:50, extended 22:17
- **Gated:** no — nothing under `src/`, `tests/` or `docs/` was modified. `DEC-L-002`.

## The priority question, and the answer is negative

**No committed migration was scaffolded this way.**

All **13** tenant migrations carry **zero `DropTable` in their `Up`**, checked per migration with `Up`
separated from `Down` **by line range** rather than by grepping the file whole.

**That method is the reason the answer is trustworthy rather than reassuring:** every one of those
migrations contains `DropTable` in its `Down`, so a whole-file grep would have reported the opposite
result with equal confidence.

**This is a tooling hazard, not a data one.** Nothing in the repository carries operations nobody
intended.

## The mechanism — established, and my own suspicion named the wrong component

**`ComposedTenantModelSource` is not involved.** It is a *runtime* `IModelSource` and is absent from the
design-time path entirely. The suspicion I carried into this task was right about the shape — a partial
model — and wrong about the part.

**The cause is a constructor parameter that is not passed.**

```
TenantDbContext(options, currentUser, currentTenant, dateTimeProvider,
                …, IEnumerable<ITenantModelContributor>? modelContributors = null, …)

OnModelCreating:  foreach (var contributor in modelContributors ?? []) contributor.Configure(modelBuilder);
```

`TenantDbContextDesignTimeFactory.CreateDbContext` constructs the context with **four arguments** and
omits the contributors. The design-time model is therefore built **with no contributors at all**, and
contains none of the module entities.

**The four contributors, listed rather than counted** (`DEC-L-044`): `HrTenantModelContributor`,
`GlTenantModelContributor`, `PayrollTenantModelContributor`, `AttendanceTenantModelContributor`.

### The join between "not composed" and "emitted as DropTable"

`Up` emits **exactly 32 `DropTable` and nothing else.** Those 32 are exactly the module-owned surface:

| Contributor | Tables dropped |
|---|---|
| Attendance | 7 — calendars, holidays, leave balances/requests/types, periods, records |
| GL | 6 — accounts, fiscal years/periods, journal drafts/draft lines, entries/lines |
| Payroll | 6 — elements, element assignments, employee compensation, periods, runs, run lines |
| HR | 13 — employees, departments, positions, grades, assignments, import/export runs |

**The tables the contributors configure are precisely the tables that get dropped**, because from the
design-time model's point of view they do not exist. That is the evidence; the count matching is only
corroboration.

## A correction to the figure this task was raised on

**"157 operations" was `Up` and `Down` combined.** The destructive part is **32 `DropTable` in `Up`**;
the remaining 125 are `Down` restoring them — 32 `CreateTable` plus 93 `CreateIndex`.

**My first pass reported "32 `DropTable` AND 32 `CreateTable`"** and I was one step from recording *"it
drops and recreates the entire module surface"* — a different and more alarming claim than the truth. I
had counted operation shapes across the whole file without separating the two halves. **It was caught
because the symmetry looked wrong, not because I was being careful** — `DEC-L-043` firing on the trigger
it was rewritten to have.

## The historical half — RESOLVED, and the defect is not where it looked

*This section replaced an "unresolved, three candidates" section at 22:16, after the owner asked for the
work to continue rather than wait. The candidates and how each was closed are kept below, because a
resolved question whose rejected paths have been erased reads as though only one was ever considered.*

**There is a third design-time factory, and it is the one the thirteen migrations used.**

`tools/SSAS.Tenant.MigrationTool/ComposedTenantDbContextFactory.cs` constructs `TenantDbContext` **with
all four contributors named explicitly**. Its own header says why it exists:

> *"Platform's factory builds the model Platform can see: its own tenant entities and nothing else, which
> is correct for it and would silently produce an EMPTY migration for a module's tables."*

**Tested against the symptom rather than read and believed.** The same no-op scaffold, differing only in
`--startup-project`:

| `--startup-project` | Result |
|---|---|
| `src/Platform/SSAS.Platform.Infrastructure` | **32 `DropTable` in `Up`** |
| `tools/SSAS.Tenant.MigrationTool` | **EMPTY — zero operations** |

**So `TenantDbContextDesignTimeFactory` is not broken.** It is correct for its own scope, and the
thirteen migrations were scaffolded through the tool, which is why they are clean.

### The defect is in the published procedure

**`ADR-018` documents the tenant stream with `--project` and `--startup-project` BOTH pointing at
`src/Platform/SSAS.Platform.Infrastructure`** — the contributor-less factory. That is harmless for
`database update`, which the ADR shows, because applying a migration runs recorded SQL rather than
diffing a model.

**It is not harmless for `migrations add`.** Anyone who follows the documented project pair and scaffolds
instead of applies gets the destructive output. **The procedure is correct for the command it shows and
wrong for the neighbouring one, and nothing says so.**

### And the tool's own comment understates the failure

It predicts Platform's factory would *"silently produce an **EMPTY** migration for a module's tables."*
**It produces a destructive one.** Empty is benign — a no-op nobody commits. **Thirty-two `DropTable` is
not**, and a reader reassured by that sentence would not check.

### How the three candidates closed

| Candidate | Outcome |
|---|---|
| A startup project whose service provider EF preferred | **Refuted, and it cannot even run.** `--startup-project src/Host/SSAS.Host.API` fails with *"Could not load file or assembly `Microsoft.EntityFrameworkCore.Design`"* — the Host does not reference the design package |
| A different context or command | **Confirmed, and this is it** — a different **startup project**, the migration tool |
| Hand-authored migrations | **Not needed as an explanation**, and unsupported: the tool path reproduces clean scaffolding today |

**One earlier hypothesis of mine was also refuted on the way**: that module configurations once lived in
Platform's tenant configuration namespace and were later moved out. That directory has held exactly
**Branch** and **Company** in every commit that ever touched it — checked across its whole history, not
sampled.

## The superseded section — what was open at 21:50, kept rather than erased

**"The factory is broken, so every scaffold since has been destructive" cannot be the account.**

| Fact | Established by |
|---|---|
| The factory has **never** supplied contributors | `git log -S modelContributors` over that file returns **nothing, ever** |
| The factory **predates every module migration** | factory `ead6a27`, 2026-08-14; first module migration 2026-08-19; last 2026-08-25 |
| Before the contributor mechanism existed, `OnModelCreating` applied **only** the Platform-assembly tenant configuration namespace | `git show ea335c1^` — no module entities by any route |

**So the design-time model appears never to have contained module entities, while thirteen module
migrations were nevertheless scaffolded correctly.** Something else supplied a complete model on those
dates and I have not established what.

**Three candidates, none tested against the symptom:**

1. a **startup project** whose service provider EF preferred over the design-time factory;
2. a **different context or command** than the one I probed;
3. **hand-authored** migrations.

**This is recorded as unresolved rather than as a probable cause**, and that is the deliberate outcome.
Today produced four demonstrations that a plausible cause recorded confidently costs more than an open
question — two of them mine, one of which reached a task spec carrying the word *verified*.

### Why this half decides what the fix is

**If something else supplied a complete model for those thirteen, the factory is not the thing that
changed** — something else stopped working, was never used, or is used only under conditions nobody has
written down. **A fix to the factory would then look correct and be beside the point.**

*That paragraph was written as a caution and it turned out to be the answer. Kept verbatim, because it
is the reason the wrong fix was not proposed at 21:50 when it would have been easy to.*

### And the fix section as it stood at 21:50, kept for the same reason

> **Not proposed yet, and deliberately.** `DEC-L-045` applies to the proposal itself: a repair that makes
> the scaffold quiet without making the model right would replace a loud defect with a silent one — which
> is exactly what T-047 caught. Until the historical half is settled, any fix I proposed would be a fix to
> the half I understand, recommended for a defect I have only half explained.
>
> What can be said now: **the obvious repair — pass the contributors in the design-time factory — is
> untested against the question of why it was ever unnecessary.**

**It was untested, and testing it is what killed it.** The section below replaces this one on evidence
rather than on second thoughts.

## The fix — proposed, not applied, and the obvious one is wrong

**The repair that looked obvious before the historical half was settled — pass the contributors in
`TenantDbContextDesignTimeFactory` — is the wrong fix**, and this is exactly the case `DEC-L-045` warns
about. It would make the destructive scaffold quiet by making Platform's factory duplicate the tool's
job, leaving **two places that must be kept in step** as modules are added, with nothing to report a
drift. That is a loud defect traded for a silent one.

Three options, with their costs, for the architect to rule on.

**A — Make the wrong path refuse.** `TenantDbContextDesignTimeFactory` fails fast when used for
`migrations add`, naming the migration tool. Cheapest, and it fits the factory's existing posture: it
already fails fast on a missing connection string for the same reason. **Cost:** the factory must
distinguish scaffolding from applying, and EF does not hand it that intent — so this may only be
expressible as a refusal to build a *contributor-less model at all*, which would also affect
`database update`. Needs checking before it is chosen.

**B — Correct `ADR-018` and leave the code alone.** The published tenant-stream command names the wrong
startup project for scaffolding; give it the tool, and say plainly that `migrations add` and
`database update` do not take the same project pair. Cheapest and most honest about where the defect is.
**Cost:** it is documentation defending against a destructive default, and today has produced repeated
evidence that a rule enforced only by habit is a rule enforced only until someone is in a hurry.

**C — Both**, with A as whatever refusal proves expressible.

**My reading: B is necessary, A is what makes it hold, and neither is mine to choose.** The one thing I
would argue against is the fix that was obvious an hour ago.

**And a smaller correction that belongs with any of them:** the migration tool's own comment predicts an
**empty** migration from Platform's factory. It should say **destructive**, because a reader reassured by
"empty" will not check.

## Verification of the tree

- **Probe removed:** `0` files matching `t048` remain under the tenant migrations directory.
- **`src/`, `tests/`, `docs/` modified: `0`.**
- **`TenantDbContextModelSnapshot.cs` identical to `HEAD`** — asserted with `git diff --quiet HEAD --`,
  not assumed from `ef migrations remove` reporting success.

## For the architect

- **Blocked / needs a decision:** nothing. The unresolved half is tomorrow's first question, not a block.
- **Touched outside scope:** nothing. Read-only across the repository plus this file.
- **Where tomorrow starts:** whether something else supplied a complete model for the thirteen committed
  migrations. That question decides the fix.
