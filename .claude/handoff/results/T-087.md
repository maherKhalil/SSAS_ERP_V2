# T-087 result — the guard asserts what, and the criterion is the handle

- **Status:** MERGED under the restored protocol (green gate = merge authority). **Gated.**
- **Branch:** `agent/T-087-rename-guard`, from `ClaudeBranch` at `e5cd252`
- **`GATE_SCOPE=TASK` GREEN.** `condition 4: ok: no non-comment change under src/`. **No baseline movement** — a rename moves no count, as required.

---

## ⚠ FIRST: MY COUNT WAS WRONG, AND YOU BUILT THE TASK ON IT

**I told you six citations pointed at the guard's name. Three do.**

```
name in the live tree      4     3 citations + the declaration itself
name in .claude/handoff    4     category H, untouched (DEC-L-071)
AC-ATT-0032 in live tree  10     the criterion was ALREADY the well-cited handle
```

**I conflated citations of the criterion with citations of the name.** In T-086 I added many of the former
and three of the latter, and reported the larger number against the smaller thing.

**It does not change your ruling** — the criterion is still the durable handle and the name still carried an
expired reason into every run's output. **But the premise you ruled on was mine and it was loose**, and the
correction runs in the direction of the rename being cheaper than either of us thought, not dearer.

---

## The rename

```
No_self_service_permission_is_declared_because_the_subject_cannot_be_resolved
No_self_service_permission_is_declared
```

**Asserts what, not why** — matching its neighbour `Every_attendance_permission_follows_the_three_part_grammar`.

**The assertion body is untouched.** Same two `Assert` calls, same subject, same strictness. Nothing about
what it checks moved, which is why no count moved either.

**The reason now lives in the comment**, where it says plainly that the subject *can* now be resolved, why a
reason in an identifier cannot be corrected without changing every citation of it, and that `AC-` identifiers
are the vocabulary that survives refactors while test names are not.

**And a `[Trait("Criterion", "AC-ATT-0032")]` was added** — the guard now carries the handle it is cited by,
in the form the suite can filter on.

## The three citations, repointed at the criterion alone

```
src/.../AttendancePermissionNames.cs
src/.../Leave/LeaveRequest.cs
docs/17-features/FP-013-attendance/authorization-model.md
```

**None of them names a method any more.** They cite `AC-ATT-0032`, and the guard cites `AC-ATT-0032` back —
**so one grep answers from either direction**, which is the property the method name never had. A reader who
finds the criterion finds the guard; a reader who finds the guard finds the criterion.

**Four occurrences in `.claude/handoff/results/` were left**, per `DEC-L-071`: T-071 and T-086 record what a
task found on a date, and one of them is the report that identified the name as non-durable. **Rewriting it
would erase the evidence for the rename.**

## For the architect

- **Blocked / needs a decision:** nothing.
- **Corrected: three name citations, not six.** The criterion already had ten.
- **`DEC-L-074` applied**: the four handoff occurrences are records and quotations, not live citations, and
  a phrase sweep would not have told them apart.
