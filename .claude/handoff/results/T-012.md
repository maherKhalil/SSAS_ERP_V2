# T-012 result — DONE

- **Status:** DONE
- **Branch:** `agent/T-012-authentication-disclaimer` — pushed, branched from `main` at `bd2280a`
- **Merged:** no — non-gated (`DEC-L-002`). Awaiting `MERGE T-012`.
- **Commits:** `ba9f6d8` — docs(T-012): Authentication.md takes the supersession-disclaimer shape
- **Reported:** 2026-08-25

## Acceptance criteria

- [x] **Lifecycle authority block in `Tenant-Management.md`'s shape** — same position (after `Status`,
      before the first `---`), same heading, same sentence form. **Five** approved packages, not the two
      the spec anticipated.
- [x] **The expired-subscription rule points at `REQ-SUB-0018`.** The restatement under
      `Business Validations` is gone, replaced by a citation naming `OD-SUB-0009`.
- [x] **Swept before writing** — all 17 sections, listed below.
- [x] **`status:` still `Draft`.** The file is not wholly superseded; four subjects survive.
- [x] **`--stat` shows exactly one file** — 17 insertions, 1 deletion.

## The sweep — all 17 sections

`Tenant-Management.md` names one superseding package. This file needed five, because it describes the
whole login path and the login path crosses every platform package.

| Section | Verdict |
|---|---|
| Feature ID / Status | Metadata. Where the block went. |
| Requirement References | `REQ-PLT-0021` (JWT + refresh) superseded by FP-002. **`NFR-0300` (TLS in transit) is not** — it is a master NFR cited, not restated. |
| Business Rules | Citations to `BR-PLT-0100`/`0101`. See finding 3. |
| Description | Restates `BR-PLT-0100`. No independent content. |
| Actors | Superseded by **FP-001** — and it does not agree with it. |
| Preconditions | User exists / active → FP-001 + FP-002. Tenant active → FP-003. **Company active → nothing.** |
| Workflow | Superseded by **FP-002**. Its *"System determines Company Context"* step has no counterpart there. |
| Business Validations | Password, lockout, inactive user → FP-002. Suspended tenant → FP-003. Expired subscription → now a pointer. **Inactive company → nothing.** *"Username must be unique"* → FP-001 makes **email** unique per tenant and carries no username rule. |
| Failure Scenarios | Credential, lockout, expired-password, unexpected → FP-002. Inactive Tenant → FP-003. **Inactive Company → nothing.** |
| UI | Login and Forgot Password are FP-002 flows. Language Selection → FP-004. **Remember Me and Theme Selection → nothing.** |
| API | **Superseded in writing.** `FP-002/api-contracts.md:19`: *"These exact `/api/platform/auth/*` routes supersede every former Draft `/api/auth/*` route."* All five routes here are the old form. |
| Database | `TBL-PLT-*` names superseded by the FP-001, FP-003 and FP-005 data models. |
| Permissions | Anonymous / Authenticated. FP-001 owns the permission model. |
| Audit | **Not superseded by a package that specifies it** — FP-002 lists immutable audit storage among its own deferrals. |
| Notifications | Same — FP-002 defers notification delivery. |
| Acceptance Criteria | Superseded by FP-002, and one item is **contradicted**. See finding 1. |
| Future Enhancements | MFA, SSO, external providers — all named in FP-002's deferral list. **Still the only statement of intent.** |

**Per `DEC-L-022`: this is what a section-by-section read found, not a claim of exhaustiveness.** Every
"→ nothing" above means *no approved package covers it*, established by reading the five packages'
scope statements. It is not a search of every document in the repository.

## Finding 1 — the document asserts something FP-002 forbids

Not silence, a direct contradiction — and the reason I put a sentence about it in the block rather than
leaving supersession to do the work:

- `Authentication.md` — *"✓ JWT contains Company ID"*, and *"System determines Company Context"*.
- `FP-002/authentication-model.md:16` — *"Tokens exclude … `CompanyId` …"*, repeated at
  `business-rules.md:55` and `decisions-approved.md:642`.

A reader who accepts "superseded within FP-002's scope" reaches the right answer, but only by
inference. The block now says which one governs, in one checkable sentence.

## Finding 2 — a rule this document states three times has no owner

**Whether Company status gates login.** It appears as a precondition, a business validation and a
failure scenario. No approved package carries it, and FP-002 pushes the other way —
`decisions-approved.md:379`: *"Subscription, billing, **company**, and role state are not tenant
lifecycle state."* FP-005 gives Company `Active`/`Inactive`/`Archived` but never connects it to
authentication.

So it is deferred rather than superseded, and I have marked it deferred. **Flagging it because it is
the same shape as the finding that produced this task** — a `Draft` document as the only statement of a
login refusal — and because it is one ruling away from being either real or deleted. **Not promoted,
not in scope.**

## Finding 3 — `BR-PLT-0100` / `BR-PLT-0101`, reported and not promoted

As instructed (`DEC-L-012`). One correction to the framing, though: **UNTRACED does not mean unused.**

- `BR-PLT-0100` (authentication is mandatory) — cited by `FP-003/traceability-matrix.md:45`.
- `BR-PLT-0101` (authorization is role-based) — cited by `FP-006/decisions-approved.md:205` and by that
  package's traceability matrix.

Both are load-bearing in approved packages. What they lack is a **requirement** referencing them, which
is what check 7 measures. Worth knowing before anyone reads UNTRACED as dead.

## One deviation from the spec's wording, and why

The spec said the block should name *"the approved packages that supersede it."* **FP-014 is not
approved** — its README reads `DRAFT — analysis only`, and `trace-check` still reports it BUILD BLOCKED
on 17 unruled `OD-SUB`. So I could not write *"approved FP-014 supersedes"* without the block asserting
something false in its first outing.

The expired-subscription paragraph therefore takes a different form from the other five: the rule is
binding **by `OD-SUB-0009`**, it **lives at `REQ-SUB-0018`**, and the package is named as not yet
approved. The pointer is what the criterion asked for; the authority cited is the ruling, which is the
thing actually in force.

## For the architect

- **Blocked / needs a decision:** none for T-012. Findings 1 and 2 are yours to rule or discard.
- **Touched outside scope:** nothing. One file. `Tenant-Management.md` untouched, no package touched, no
  rule promoted, no `status:` changed.
- **`trace-check` unaffected** — it globs `docs/17-features/` and `docs/00-Master-Product-Specification/`
  only. `docs/02-Functional/` is outside both roots, so the pre-existing `[TRACE RED]` is unchanged by
  this diff. Read from the scanner's paths rather than inferred from a before/after run.

### The shared-tree collision — resolved, and my earlier report of it was wrong

`main` would not fast-forward after PR #65. I reported the `.claude/` files as **45 differing**. That was
**false, and my own fault**: under Git Bash, `git show origin/main:<path>` was rewritten to
`origin\main;<path>`, so `git show` produced **nothing** and I compared every file against an empty
string. With path conversion disabled the real answer is **44 byte-identical, one differing.**

The one is `BOARD.md`, and it differed because you had edited it after committing it (T-018 → `DONE`,
T-012 → `IN-PROGRESS`, the PR #65 log row). **Your working copy is preserved and restored, unstaged.**
Nothing was lost: the 44 identical files were removed and restored by the pull, and the five untracked
files — `results/T-018.md`, `tasks/T-012.md` and the three ignored session pointers — are intact.

**The lesson is not about `.claude/`.** A comparison reporting *everything differs* deserves the same
suspicion as a test that fails everywhere. I treated 0-of-45 as a finding and started reasoning about
`commit-tree` normalisation, when that split is far more likely to mean the comparison itself is broken.
`DEC-L-013` held perfectly — your `commit-tree` build never touched my tree.
