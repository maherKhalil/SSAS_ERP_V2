# T-039 result — DONE

- **Status:** DONE
- **Branch:** `agent/T-039-expiry-amendment` — branched from `main` at `c7df09b`
- **Merged:** yes — PR #82, `aff12ec` (non-gated, `DEC-L-002`; merged on your `MERGE` and `DEC-L-035`)
- **Commits:** `1cecf9f` — the FP-014 amendments; `c94fa3c` — the promoted copy and two missed statements
- **Reported:** 2026-08-26

## Acceptance criteria

- [x] **Both amendments recorded as amendments**, with `OD-SUB-0009`'s original preserved verbatim.
- [x] **`REQ-SUB-0018` rewritten** — expiry denies gated modules, never authentication, citing
      `REQ-SUB-0013` as what makes the platform plane reachable without a special case.
- [x] **Every other statement found and corrected** — including four files beyond the spec's list.
- [x] **The trial recorded as a plan with a term**, `OD-SUB-0014` cited as used rather than overturned.
- [x] **`DEC-L-009`'s asymmetry collapsed**, with its original reasoning kept.
- [x] **`trace-check` GREEN, `0 unruled`**, all fourteen package verdicts unchanged.
- [x] **`DEC-L-035`** — the promoted copy amended in the same PR; the two are byte-identical on `main`.

## The search found four files beyond the list, and one was a ruling

The spec named `lifecycle-model.md`, `requirements.md`, `README.md` and `business-rules.md`, and said to
search rather than assume. The search also hit `api-contracts.md`, `acceptance-criteria.md`,
`test-scenarios.md`, `traceability-matrix.md` — and **`OD-SUB-0010`'s own ruling row in
`decisions-ratified.md`**, reading *"Orthogonal — **both checked at login**"*.

That was true only while expiry blocked login. **Had I trusted the list, this task would have shipped an
amended package whose own ratification file contradicted the amendment** — the exact `DEC-L-027` defect,
inside the task applying `DEC-L-027` deliberately.

**The cut taken:** the mechanism sentence changed, the ruling did not. `TenantStatus` is checked at
authentication and commercial state at the enablement gate; **orthogonality is untouched**, and so is
"no commercial reason joins `TenantStatusChangeReason`".

## My own sweep missed two, and the reason generalises

**My first search was line-oriented, and both survivors wrapped across a line break:**

| Site | The claim |
|---|---|
| `business-rules.md:148` | *"expiry remains the only commercial event that blocks a ⏎ login"* |
| `decisions-ratified.md:103` | *"`OD-SUB-0009`'s expiry remains the **only** commercial event that blocks ⏎ login … that asymmetry is deliberate"* |

Both were **live claims and both were false**. I found them only because amending the master copy made
me `grep` for `BR-SUB-0013` again and the fragment appeared in a different shape.

**A multiline-aware re-sweep of the whole repository now returns no live claim.** Every survivor is
framed as historical — an amendment record, a preserved original, or `decisions-open.md`'s option table.

**The lesson is `DEC-L-028`'s shape one layer out:** a search that looks exhaustive and is line-bounded
has a blind spot precisely where prose is wrapped, which in this repository is everywhere. Reported
rather than quietly fixed, because the first sweep is the one that will be trusted next time.

## `DEC-L-035` — the maintenance nobody priced

`Business-Rules.md` § `BR-SUB-0013` carried the superseded rule in **both its title and its body**:
*"Expiry Refuses Login, Distinctly"*. Amended in the same PR, so `main` never carried a governing
document stating a rule the owner had overturned.

The statement is **verbatim from FP-014** and the two are byte-identical, with the previous text kept in
a note naming `DEC-L-033` and the reason. `DEC-L-012` made promotion an obligation and T-022 performed
it; this is the first time the loop has had to **amend** a promoted copy rather than create one.

## What did not need touching, and why that is the finding

**`Authentication.md` needed no edit.** T-012 replaced its expiry rule with a **pointer** to
`REQ-SUB-0018` rather than a restatement. Because it points rather than states, it stayed correct
through a change to the rule it points at, **without being touched**.

That decision was taken on the argument that *"two copies of a ruled rule is how they drift"*. This is
the first case that tested it, and it is the counterfactual: the file that restated the rule
(`BR-SUB-0013`, twice over) needed amending in two places, and the file that pointed at it needed
nothing. **Verified the pointer resolves rather than assuming it.**

## One thing the analysis got right a day early

`decisions-open.md` offered exactly three options for `OD-SUB-0009`. The owner chose the second —
*"dated term, expiry blocks login for the whole tenant"* — and has now moved to the third:

> **Dated term, expiry disables modules but preserves login and read access.** Softer and probably more
> sellable; a tenant can still log in, see its data and renew.

**The options table had already written down the reasoning the owner arrived at independently.** Left
untouched: it is the record of what was asked, and an options table that survives the decision shows
what was weighed.

## For the architect

- **Blocked / needs a decision:** none.
- **Touched outside scope:** the master `Business-Rules.md`, on your `DEC-L-035` ruling and no earlier.
- **Noticed, not acted on:** `business-rules.md`'s caching passage — *"expiry writes nothing; no row
  changes when a term lapses"* — is unchanged and **more load-bearing now**. Expiry used to be evaluated
  at login; it is now evaluated at the enablement gate, which is exactly where the cache sits. The
  passage already requires the cached entry to carry the term and be evaluated against the clock. **That
  is a direct constraint on T-040**, and it was written before anyone knew it would be.

### On `DEC-L-025`

**Still no usage warning in my context.** Reported fresh.
