# T-129 — **three inventories, three plants, and the T-122 landmine is 117 codes rather than three**

- **Status:** BUILT, GREEN, **MERGE PENDING** under `DEC-L-007` (gated).
- **`GATE_SCOPE=TASK` GREEN, 0 warnings.** No condition-4 ATTENTION.
- Branch `agent/T-129-platform-inventories-three`, from `ClaudeBranch` at `259bfd1`.

---

# THE THREE, AND EACH WAS PLANTED SEPARATELY

```
CompanyRouteInventoryTests                  7 routes, 3 permissions
TenantUserRouteInventoryTests               4 routes, 4 permissions
PlatformSupportAuthorityRouteInventoryTests 9 routes, 1 permission
```

**Three plants, not one — your point, and T-114's.** Each expectation is read off a different running route
table, so `DEC-L-070` applies to each by construction:

```
1  Company     archive gated ViewCompanies not CompanyLifecycle   permission FAIL, surface PASS
2  TenantUser  employee-link/remove renamed to /detach            surface FAIL + permission FAIL
3  Support     revoke moved to the TENANT plane prefix            permission FAIL + plane-separation FAIL
```

**Plant 3 fired two tests independently**, which is the plane-separation assertion earning its own name
rather than hiding inside the permission comparison. Residue clean on all three.

## Sharing the helper rather than making it seven copies

**The four tenant-module inventories each carry a private copy of the enumeration helpers — the exact shape
T-127 deleted from Localization one task ago.** I was not going to add three more, so the three new files
share `PlatformRouteInventory`.

**The existing four were NOT migrated and the file says so.** That touches four green guards to change
nothing observable, which belongs on its own branch. **This is 3-of-7 adoption and it is recorded, because
T-127's lesson is that the un-migrated remainder is invisible until someone looks.**

---

# ⚠ THE VACUITY, STATED — AND MORE PRECISELY THAN "VACUOUS"

**All nine support routes carry `Platform.Support.Administer`, so a mis-gated route is not expressible
there** and the permission property is green by construction. **That is in the file, in those words.**

**But it is not useless, and saying "vacuous" flat would have been its own floor-stated-too-narrowly.** The
test still catches a gate REMOVED and a gate replaced by a FOREIGN one — **and the foreign case is real and
serious here:** a tenant-plane `Permission:` prefix where `PlatformPermission:` belongs would authorise a
support route from a tenant user's grants, against `ADR-015`'s plane separation.

**So the file says: what it cannot catch is a mix-up among the group's OWN permissions, because there is
only one.** And that property has its own named test, which plant 3 proved fires.

---

# ⚠ THE LANDMINE IS FORTY TIMES BIGGER THAN THE RECORD SAID

You asked me to connect the transport gap to T-122's three `RestoreChain*` codes. **Checking rather than
taking it found the record was far too small.**

```
TenantStorageErrors declared codes                117
returned somewhere in src/                        115
mapped to a status by any API error mapper          0
files in SSAS.Platform.API naming any TenantStorage code   0
```

**It is not three codes. No file in `Platform.API` names a `TenantStorage.` code at all** — the entire
family is unmapped, because there is no transport.

**The day Platform's administration transport is built, that is 115 live 500s arriving in one task** — the
failure T-118 and T-125 each found a single instance of.

**Written where it will be found:** a header block on `TenantStorageErrors` itself, carrying the counts, the
16-of-28 and the 29-handler floor with its direction of error, and the three surfaces. **Whoever builds the
transport opens that file; nothing in the routing work points at a mapper.**

## And two of the 117 are returned by nothing and named by no test

```
TenantStorage.MigrationOwnershipNotAcquired
TenantStorage.RestoreVerificationRestoreFailed
```

**NOT deleted — the opposite call from T-125, deliberately.** There, a dead code sat beside a **live route**
whose behaviour proved it surplus. **Here there is no transport to prove anything**, and a constant named
`RestoreVerificationRestoreFailed` describing a condition the restore-verification flow plainly has is **at
least as likely to be a missing RETURN as a surplus declaration.** Deleting it would erase the evidence of
whichever it is. Recorded for whoever owns that flow.

---

# WHAT I DID NOT BUILD, SO THE ABSENCE READS AS A DECISION

- **`IdentityAccess` — one route, `GET /api/platform/roles`. No file.** An inventory of one row pins almost
  nothing, and **the interesting property is the 16 unrouted permissions sitting behind it, which an
  inventory is structurally incapable of expressing.**
- **`Authentication` (4) and `PlatformSupportAuthentication` (3) — no files.** Five of their seven routes
  are `AllowAnonymous` by nature, so route-to-permission says almost nothing, **and the property that does
  matter — the two planes staying structurally separate — already has a home in
  `PlatformSupportAuthenticationSurfaceArchitectureTests`.** A second would be `DEC-L-080`.

**Platform is now 3 groups inventoried, 1 skipped as not worth a file, 2 covered elsewhere, 1 from T-128 —
and every one of those six states its reason in its own file or here.**

---

# ONE THING RECORDED IN THE COMPANY FILE AND NOT ACTED ON

**`{companyId}` carries no route constraint**, where `{tenantUserId:long}` and `{principalId:long}` do and
Attendance is `:guid` throughout. **A non-GUID company id reaches the handler rather than failing to
match**, so the caller's answer depends on parsing rather than routing.

**Not changed — adding a constraint moves a 400 to a 404 for malformed ids**, which is a transport behaviour
change needing a task that says so. **Pinned by a named test so it is a decision when somebody makes it.**

---

## For the architect

- **Blocked / needs a decision:** nothing for T-129. **Two items for the owner's queue**, both recorded
  rather than started: the administration transport, and the 117-code mapping that comes with it.
- **The 117 changes the shape of the transport task.** It was "three surfaces have no routes"; it is now
  "three surfaces have no routes **and 115 refusals with no statuses**". **The second half is the larger
  piece of work and it was invisible in the record.**
- **`DEC-L-069` on my sweeps:** the 117/115/0 counts are full parses of the declaring file and full greps
  over `src/`, not samples. The 29-handler figure remains the floor from T-128.
