# T-008 result — DONE

- **Status:** DONE
- **Branch:** `agent/T-008-fp-014-matrices` — pushed: yes, branched from `agent/T-007-fp-014-surface`
- **Merged:** no — non-gated, and FP-014 merges as one package. **The package is now complete: twelve
  files, `trace-check` GREEN.** Awaiting your instruction.
- **Commits:** `9252212` — docs(T-008): FP-014 matrices — criteria, scenarios, traceability
- **Reported:** 2026-08-25

## Acceptance criteria

- [x] **`acceptance-criteria.md` — every `REQ-SUB` gains at least one `AC-SUB`, contiguous from
      `0001`, observable rather than "correctly implemented".**
      **51 criteria, `0001`–`0051`, contiguous.** 27 of the 28 requirements carry criteria;
      `REQ-SUB-0026` is the declared gap you instructed. Every criterion states a named behaviour, a
      refused request or a value that must hold.
- [x] **`test-scenarios.md` — every `AC-SUB` gains a `TS-SUB`, contiguous from `0001`, each naming
      its suite.** **50 scenarios, `0001`–`0050`, contiguous.** 17 domain (`Platform.Tests`), 18 API,
      9 integration, 6 architecture. 50 of the 51 criteria reach one; `AC-SUB-0047` is the declared
      gap explained below.
- [x] **`traceability-matrix.md` chains `REQ → AC → TS` with the `REQ-` identifier in the first
      cell; counts derived by running the script.** 30 rows for 28 requirements. Counts taken from
      the script's inventory output, not typed.
- [x] **The three obligations carried into the criteria.** `AC-SUB-0044`/`TS-SUB-0033` (Platform-DB
      guard), `AC-SUB-0045`/`TS-SUB-0043` (permission-plane guard), `AC-SUB-0046`+`AC-SUB-0047`/
      `TS-SUB-0038` (no backfill, gate held back). Each also has a row in a dedicated matrix
      subsection naming where it was found.
- [x] **`trace-check` runs GREEN.** `0 failure(s)`, exit `0`, run with every other package present.
      Output below.

## What changed

Three files, 454 lines. The six existing package files are untouched.

### The mid-task amendment

`DEC-L-009` and `DEC-L-010` arrived while I was writing. Both are absorbed:

**The seat-cap gap is written properly, not declared.** `AC-SUB-0049`–`0051` and
`TS-SUB-0048`–`0050`: refused at admission with the cap, the count **and** the plan named; login
never refused for a cap and the seat check **not consulted** on the authentication path; a downgrade
deactivating nobody. `TS-SUB-0048` also covers **reactivating** a deactivated user into a full
tenant, which is the case a create-only check misses.

**The asymmetry is stated where it would otherwise read as inconsistency** — expiry blocks login and
a seat cap does not, because expiry is dated, foreseeable and whole-tenant while a seat excess is
incremental and arbitrary in whom it would hit. You were right that the pair I raised was false;
writing it out made that obvious in a way my framing had not.

**The permission guard is widened to the whole platform-plane set**, `Platform.Support.*` included,
in `AC-SUB-0045` and `TS-SUB-0043`, each naming `DEC-L-010` and why: guarding only the new six would
leave the ambiguity that already shipped unasserted.

### Three failures found and fixed before commit — all self-inflicted

The first run came back `RED 3 failures`. I want them in the record because two are instructive:

1. **`RANGE traceability-matrix.md: 'AC-PAY-0006`-`0008' is the only citation of 2 identifiers.**
   I wrote the FP-012 example out **literally, inside the bullet forbidding ranges**. The check
   caught it, which is the argument for the rule stated better than the bullet did. The bullet now
   describes the failure without committing it, and says so.
2. **`COVERAGE REQ-SUB-0004 has no acceptance criterion (criteria cell: 'TS-SUB-0043')`** and
   **`no test scenario (tests cell: 'T-007, writing the permission set')`.** My build-obligations
   table had `REQ-SUB-0004` in a row's **first cell**, so `parse_chain` read a prose table as a chain
   row and lined its columns up one off. Fixed by naming the requirement in words there — the
   criterion already carries the identifier.
3. The same table then produced a misleading declared-gap line, so it is now a `###` subsection
   **inside** `## The chain`, where the prose scan correctly skips it.

**Lesson worth carrying: any pipe table whose first cell can contain a `REQ-` identifier is a chain
row to this parser, whatever the heading above it says.** That is worth knowing before T-009.

### The two declared gaps

- **`REQ-SUB-0026`** — chain written exactly as you specified: `REQ-SUB-0026 → BR-SUB-0020 → T-010`,
  criteria `—`, tests `—`. The boundary *is* asserted, by `AC-SUB-0048`/`TS-SUB-0047` under
  `REQ-SUB-0025`, so the package tests that no cardholder datum crosses it without pretending to have
  specified capture.
- **`AC-SUB-0047`** — the release condition. No suite can observe a release boundary: a test
  asserting "the gate is off" passes trivially before the gate exists and fails permanently once it
  is on, so it would assert the opposite of the intent.

## Gate

**Not run** — `DEC-L-002`, no file under `src/` or `tests/`. `DEC-L-008` noted and understood; it
does not bite on a docs task, and the `2,597`-passing baseline is recorded for when it does.

### Criterion 5 — `py scripts/trace-check.py FP-014-subscription`

```
FP-014-subscription  [GREEN]  0 failure(s), 1 warning(s), 4 declared gap(s)

  Owns: SUB   External refs: 1

  Inventory, derived:
    space           count  range        contiguous
    REQ-SUB            28  0001-0028    yes
    BR-SUB             20  0001-0020    yes
    AC-SUB             51  0001-0051    yes
    TS-SUB             50  0001-0050    yes
    DEC-SUB            12  0001-0012    yes
    OD-SUB             17  0001-0017    yes

  Owner decisions: 17 defined, 17 unruled -> BUILD BLOCKED

  WARNINGS:
    - BUILD BLOCKED: 17 owner decision(s) and no ratification file in this package

  Declared gaps (4) - deliberate, not defects:
    - REQ-SUB-0011 - tests cell is a declared gap
    - REQ-SUB-0026 - criteria cell is a declared gap
    - REQ-SUB-0026 - tests cell is a declared gap
    - 1 criterion carried by prose with no owning requirement: AC-SUB-0047

[TRACE GREEN] 1 package(s) clean          EXIT = 0
```

**Run with every other package present**, per your correction. The full-repo run reports
`[TRACE RED] 9 of 14` — **the same nine that were red before FP-014 existed, out of one more
package.** FP-014 joins as the fifth green one and adds no failure.

`BUILD BLOCKED` is expected: `decisions-ratified.md` is not mine to write.

That last declared-gap line reads *"no owning requirement"* and **`AC-SUB-0047` does have one** —
`REQ-SUB-0011`, second row. The script is describing the section it found the criterion discussed in,
and the substance is right: it is the one criterion no scenario covers. The matrix says so inline so
nobody misreads the output.

### Check 7 — your policy prediction, verified

```
UNPROMOTED (3):
  - BR-PAY  13 rule(s) ... master Business-Rules.md carries no BR-PAY rule at all
  - BR-ATT  12 rule(s) ... carries no BR-ATT rule at all
  - BR-SUB  20 rule(s) ... carries no BR-SUB rule at all
```

The third row appeared exactly as `business-rules.md` predicted it would.

## For the architect

- **Touched outside scope:** nothing. Three new files.

### The `NEEDS-DECISION` you invited — and my answer is narrower than your question

You asked whether `DEC-L-009` makes `BR-SUB-0006` incomplete. **It does not.**

`BR-SUB-0006` states that *a metered quantity is judged against the subscription record in force when
it was observed*. That is about **judgement**, it is still exactly right, and `DEC-L-009` does not
touch it — `AC-SUB-0040` and `AC-SUB-0041` continue to cover it unchanged.

**What the package lacks is a different rule: where the cap is enforced.** "Refused at admission,
never at login, an excess from a downgrade billed rather than enforced" is a new statement, not a
missing clause of an existing one. It is a **twenty-first `BR-SUB` rule**, and
`business-rules.md` is outside T-008's scope, so I have not reached into it.

**I deliberately did not write that identifier anywhere in the package.** A `BR-SUB` number cited in
the matrix but undefined in `business-rules.md` is an ORPHAN, and would have turned the package red
on the run that is this task's deliverable. The matrix names it in words instead. **If you issue the
amendment, the identifier and the matrix cell can land together.**

### `DEC-L-012` makes one sentence in `business-rules.md` stale

That file says the owner *"has an open ruling on the whole 70% class"*. `DEC-L-012` closed it. The
substance survives — promotion is still not this package's act — but the reason has changed: it is
now *"promotion happens at ratification"* rather than *"the class is unruled"*.

Sharper: `business-rules.md` says a third `UNPROMOTED` row is **"the check working, not a defect in
this package"**, and under `DEC-L-012` that is true **only until ratification**. After it, the same
row is a ratification defect. The matrix carries the corrected obligation; the out-of-scope file
still carries the old framing. **Also yours to amend, or to leave if you read the distinction as
adequately drawn by the matrix.**

### Follow-ups noticed but not done

- **`TS-SUB-0033` is red on arrival, by design.** It is the executable form of the Platform-DB guard
  obligation and stays red until `PlatformDbContext` gains `PreventAppendOnlyMutation`. Under
  `DEC-L-008` that matters: **the first FP-014 code task cannot report a green gate until that guard
  exists**, because the scenario its own package requires will be failing. Worth sequencing before
  the build task rather than discovering at merge time.
- **`AC-SUB-0049` names `TenantUser` as the seat-bearing entity.** That is the only reading available
  — `TenantUser` is what `ADR-017` lists as Platform-database tenant membership — but the ruling says
  "seats" and does not define one. If a seat is ever something other than an active `TenantUser`, the
  criterion needs revisiting rather than reinterpreting.
- **`REQ-SUB-0027` now carries two rows with different enforcement semantics** — metering judged
  historically, admission enforced immediately. They coexist correctly, but a reader skimming one row
  could take it for the whole requirement. Ratification is the moment to decide whether that argues
  for splitting the requirement itself.
