# T-085 result — `ActingEmployee`, and both legs of the ruling verified

- **Status:** PENDING MERGE. **Gated.** **Not self-merged.**
- **Branch:** `agent/T-085-acting-employee`, from `ClaudeBranch` at `db2a330`
- **`GATE_SCOPE=TASK` GREEN, 0 warnings.** Baseline `Attendance|Debug|67 -> 71` — **exactly the four tests added, and nothing else moved.**
- **No behaviour changed.** All eight T-084 tests pass with their meaning intact; only their parameter spelling moved.

---

# YOUR SECOND LEG IS RIGHT, AND I MEASURED IT RATHER THAN REASONING ABOUT IT

**You ruled that `null` is not free because the tree is nullable-enabled and the gate fails on any warning. Both halves confirmed:**

```
Directory.Build.props:5      <Nullable>enable</Nullable>
Directory.Build.props:8      <TreatWarningsAsErrors>false</TreatWarningsAsErrors>

a bare null at a call site   0 Error(s)   1 Warning(s)
                             CS8625: Cannot convert null literal to non-nullable reference type

the gate on that build       !!! WARNINGS (Debug): 1 -- DEC-L-008 condition 1 is zero. This gate is RED.
                             [GATE RED]
```

**The compiler permits it and the policy does not.** That is exactly the arrangement you described, and it is
stronger than the *"unresolved must be named"* I had narrowed it to. **My narrowing was wrong in your
favour** — I looked only at what the language could guarantee and stopped there.

**And the warning arrived unprompted before I went looking**: converting the T-084 domain tests produced
CS8625 on the two `null` call sites, which is the same evidence from a direction I did not plan.

---

## What landed

```
src/Modules/Attendance/.../Leave/ActingEmployee.cs        the type
src/Modules/Attendance/.../Leave/LeaveRequest.cs          both AtRoot verbs take it non-nullable
src/Modules/Attendance/.../Leave/LeaveCommandHandlers.cs  ONE place performs the translation
tests/Attendance.Tests/Leave/LeaveDomainTests.cs          converted, plus 4 tests on the type itself
```

**Home is `SSAS.Attendance.Domain`, beside `LeaveRequest`. No new project reference** — Attendance's
Application already depends on its Domain, so the handler sees it for free.

**It is a class, and that is forced**, exactly as I flagged: a struct's guaranteed `default(T)` would be a
reachable, constructible, silently unresolved instance — the type reintroducing the defect while wearing a
name.

**No default constructor, no `Empty`, no conversion from `Guid?`.** `Resolved(Guid.Empty)` throws rather
than coercing: an empty identifier would look resolved and match nothing, which is the unresolved case
arrived at by accident.

**`Matches` lives on the type** rather than the identifier being unwrapped. An unresolved actor matches
nobody **by construction**, so "we do not know who this is" can never read as "this is the requester" at
any call site.

---

# THE PLANT — AND IT IS THE POINT OF THE TYPE

**Planted `ActingEmployee.Unresolved()` at the approve handler in place of the resolved value — the skip as
a NAMED act:**

```
The_handler_resolves_the_acting_user_before_deciding_at_root          RED   Expected: [4242]
The_one_department_manager_cannot_approve_their_own_leave_at_root     RED   the defect returns

ActingEmployeeTests (all four)                                        GREEN
every domain test                                                     GREEN
69 passed, 2 failed
```

**Exactly the two you predicted, and the aggregate tests stayed green** — the type did not weaken the
guard, and the resolution assertion survives the refactor, which is your test for whether it was cosmetic.

```
LeaveCommandHandlers.cs   pre-plant 2d5da9b   after named-skip revert 2d5da9b   after null revert 2d5da9b
```

---

## The four new tests pin the ruling's own constraint

**`There_is_no_way_to_construct_one_without_naming_which_kind_it_is`** asserts no public constructor, no
implicit or explicit conversion, and no public static field of the type. **If any appears, the wrapper has
become a name over the same silent skip and the ruling behind it is void.**

The other three: an unresolved actor matches nobody; **the control** — a resolved actor matches exactly one
employee and no other, without which the first passes against a `Matches` that always returns false and
silently disables the bar; and an empty identifier is refused.

---

## Condition 4

```
condition 4: ok: 1 of 7 suite total(s) moved, with 63 non-comment line(s) changed under src/
```

**Correct.** Attendance moved `67 -> 71`, exactly the four tests on the new type. The 63 `src/` lines are
the type, the two signatures and the one translation site. **No other suite moved, because no behaviour
changed.**

## For the architect

- **Blocked / needs a decision:** nothing.
- **Both legs of your ruling verified by measurement**, including the one I had narrowed away.
- **What the type buys, stated where it lives:** declared, not unrepresentable. The file says so in its own
  header rather than leaving a reader to discover the limit. `ModulePermissionDefinition` remains the
  higher standard and this does not reach it.
- **Not touched, per your instruction:** the stale comments in `LeaveApprovalRouter.cs`,
  `LeaveCommandHandlers.cs` and `AttendanceConfigurations.cs`. **T-086.**
