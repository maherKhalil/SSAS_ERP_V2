# T-157 — **`/records/bulk` is the third answer: a route-table row in a proposal block that was already wrong five times out of six**

- **Status:** ESTABLISH ONLY. Read-only, no build, nothing merged. Docs-only — pushed and stopped.
- Branch `agent/T-157-bulk-establish`, from `ClaudeBranch`.

---

# THE ANSWER TO ALL THREE QUESTIONS

```
1. deferred by decision?      NO   no guard, no criterion, no scope statement defers it
2. specified to that depth?   NO   one route-table row + one prose note, in one file, total
3. done by another name?      NO   zero in Attendance source; the command is strictly singular
```

**It is not the `AC-TEN-0020` shape. There is nothing to supersede, because nothing decided against it.**

---

# 1. NOT DEFERRED — AND THE DIFFERENCE FROM TENANT TRANSPORT IS SHARP

**Tenant endpoints had a live guard actively asserting their absence**
(`Tenant_endpoints_remain_deferred_and_the_platform_api_does_not_reach_tenant_application`, trait
`AC-TEN-0020`), backed by a milestone scope statement listing eleven concerns.

**`/records/bulk` has none of that.** No guard names it. No criterion defers it. No scope statement mentions
it. **The nearest thing to a decision is the api-contracts note explicitly declining to make one:**

> *"Whether it is still wanted is the owner's call, **not this reconciliation's**."*

**That is an open question recorded, not a deferral ruled.** T-100 declined to decide and said so — the
opposite of `AC-TEN-0020`, which decided and was enforced.

## ⚠ One guard WOULD go red, and the distinction is the whole point

`AttendanceRouteInventoryTests` pins the route surface as an **exact set** (`Assert.Equal(expected, actual)`).
Adding `/records/bulk` reddens it.

**But it reddens for "a route was added without a line in the list", not for "this route is deferred".** It
is a surface pin every module has, not a scope decision. **A coder hitting it updates the list; a coder
hitting T-155's blocker must stop.** Same colour, entirely different meaning.

---

# 2. THE SPECIFICATION IS ONE ROW AND ONE NOTE — MEASURED ACROSS ALL THIRTEEN FP-013 DOCUMENTS

```
FP-013-attendance/  README, acceptance-criteria, api-contracts, authorization-model, business-rules,
                    data-model, decisions-open, decisions-ratified, domain-model, lifecycle-model,
                    requirements, test-scenarios, traceability-matrix        (13 files)

occurrences of "bulk":  3   — ALL THREE in api-contracts.md, and all three are the same
                              route-table row plus the note explaining why it was kept
```

**Zero in `requirements.md`. Zero in `acceptance-criteria.md`. Zero in `test-scenarios.md`. Zero in
`traceability-matrix.md`.** No `REQ-ATT-`, no `AC-ATT-`, no `TS-ATT-` names it.

**The document's own claim — *"this document is the only place it exists"* — is verified true**, rather than
taken. Given the day's lesson that a document's absence claim is still an absence claim, it was measured.

**Contrast with what T-156 found behind tenant transport:** ten acceptance criteria, fifteen test scenarios,
an ADR and a decision id. **That is a specification. This is a line in a table.**

## ⚠ AND THE BLOCK IT SITS IN HAS A MEASURED TRACK RECORD, WHICH IS THE REAL FINDING

The same `api-contracts.md` header says this block **was written as a proposal before the module shipped,
and nothing compared the two until T-099 built the route inventory.** T-100 then found **six divergences**:

```
2   paths carrying an {id} the live routes do not take     corrected TO THE CODE
3   routes built after the proposal and never added        corrected TO THE CODE
1   /records/bulk                                          KEPT as a recorded intention
```

**Where this block disagreed with the shipped module, it was wrong five times out of five.** Every
divergence that could be adjudicated was resolved in the code's favour. **`/records/bulk` is the one
survivor — and it survived not because it was validated, but because it was the one claim with no code to
contradict it.**

**So "specified in FP-013" is true and misleading in the same breath.** It was specified by a proposal whose
accuracy, wherever it was testable, was zero.

---

# 3. NOTHING IS DOING IT BY ANOTHER NAME

```
"bulk" in src/Modules/Attendance                                     0 files
"bulk" in tests/ that are Attendance                                 0 of 15 (all HR, positions,
                                                                     tenant cutover/backup SqlBulkCopy)
concept sweep in FP-013 requirements/criteria/scenarios:
  many|multiple employees, at once, in one request, csv,
  upload, file, spreadsheet                                          0 real hits
```

`RecordAttendanceCommand` is **strictly singular** — one `CompanyId`, one `EmployeeId`, one
`AttendanceDate`. There is no collection anywhere on the path.

## ⚠ One near-miss, and it is the same false-positive shape as T-156's

**`AttendancePermissionNames.cs` contains the word `Import`.** It is in a **comment** citing HR's naming
convention as an example — *"HR is granular and per-act: `HR.Employees.Create`, `.Update`, `.Terminate`,
`.Transfer`, `.Import`"*. **Zero occurrences in the permission catalog contributor, zero in the Attendance
API. There is no Attendance import permission.**

**A word-sweep would have reported a permission with no route behind it** — the `salaryType` shape, and
exactly what I was looking for. It is not there. Same trap as `TenantConfiguration` matching "configuration"
in T-156, and worth naming because it was the most promising lead in this establish.

---

## For the architect

- **Which of the three it is: the THIRD, with one qualification.** It is not a bare prose bullet — a
  route-table row naming a path, a verb and a permission is more than nothing. **But there is no
  specification behind it, so it is yours to rule, not the owner's.**
- **The owner's list item is not wrong in substance but is wrong in WEIGHT.** *"Specified in `FP-013`'s
  api-contracts and never built"* is literally accurate and invites the reader to picture FP-013 having
  specified it. **What specified it was a proposal block that lost five of its six arguments with the
  code.** If §5 stays on the owner's list, that sentence is the one to change.
- **What I did NOT establish:** whether attendance import is *wanted* — device or spreadsheet import is a
  product question and I did not touch it. **The `OWNER-DECISIONS.md` §5 "what it blocks" line already
  states it; nothing I found contradicts or supports it.**
- **`DEC-L-069`:** all three questions were measured by full enumeration, not sampling — every one of the
  thirteen FP-013 documents, all fifteen `bulk`-matching test files listed and classified individually, and
  the concept sweep run separately from the word sweep.
