# T-021 — Ratify FP-014: record the seventeen rulings in the package itself

- **Branch:** `agent/T-021-fp-014-ratify` — branch from `main`
- **Module:** Platform (commercial plane)
- **Issued:** 2026-08-25
- **Depends on:** T-012 merge

## Goal

Your T-012 deviation found a real gap and it is mine. I have been treating FP-014 as authority for
six tasks. **It is not approved.** Its README says `DRAFT — analysis only`, it carries no
`decisions-ratified.md`, and `trace-check` still reports **17 unruled, BUILD BLOCKED**.

All seventeen `OD-SUB` *are* ruled — six by the owner, seven by me, four more in a second round. But
the rulings live in `.claude/handoff/notes/2026-08-25-fp-014-rulings.md`, an architect note. A ruling
recorded outside the package is a ruling the package cannot cite, and `trace-check` cannot see.

That note is now **tracked** (PR #65, `bd2280a`), so for the first time it can be cited as a repository
document rather than a scratch file. This task moves the rulings into the package where they belong.

**Package-internal only.** The master-register acts — `Requirement-Numbering.md`, promoting `BR-SUB`,
the roadmap entry — are a separate task, because they change governing documents and deserve their own diff.

## Acceptance criteria

- [ ] `docs/17-features/FP-014-subscription/decisions-ratified.md` exists, recording **all seventeen**
      `OD-SUB` rulings, each with **who ruled it and on what basis** — owner or architect, and the
      reasoning as the ruling set states it. Follow `FP-013-attendance/decisions-ratified.md`'s shape.
- [ ] `README.md`'s status line no longer says `DRAFT — analysis only. No code, no schema, no ADR, no
      roadmap entry.` It records the package as **ratified**, names the ratification file as
      authoritative, and states plainly what is still outstanding: no code, and the master-register
      promotion not yet done.
- [ ] **`py scripts/trace-check.py FP-014-subscription` reports `0 unruled`** and the package stays
      GREEN. Run it with every other package present. That number going from 17 to 0 is the deliverable.
- [ ] **Two ratification concerns are resolved or explicitly carried**, not silently dropped:
      `DEC-L-009` rules the seat cap without defining a **seat** — `AC-SUB-0049` reads it as `TenantUser`
      because that is the only reading available; and `REQ-SUB-0027` carries two rows with different
      enforcement semantics that may want splitting. **Both are mine.** Record them in the ratification
      file as open at ratification rather than resolving them yourself.
- [ ] `git diff main...agent/T-021-fp-014-ratify --stat` shows **at most two files**, both under
      `docs/17-features/FP-014-subscription/`.

## Files in scope

- `docs/17-features/FP-014-subscription/decisions-ratified.md` — new
- `docs/17-features/FP-014-subscription/README.md` — status only

## Out of scope

- **`Requirement-Numbering.md`.** `OD-SUB-0002` ruled a new `REQ-SUB` prefix *at ratification*, and
  that is the next task, not this one. Same for `BR-SUB`.
- **Promoting `BR-SUB-0001`…`0021` to the master `Business-Rules.md`.** `DEC-L-012` makes it a
  ratification obligation and it is real work with real review — its own diff.
- **`Product-Roadmap.md`.** The commercial plane gains its entry with the master-register task.
- The other ten package files. If a ruling contradicts one, that is a `NEEDS-DECISION`.
- Any file under `src/` or `tests/`.

## Design constraints

- **Attribute every ruling.** Six are the owner's, seven mine, four the owner's second round. A future
  reader must be able to tell which rulings were commercial decisions and which were architectural,
  because reopening one is a different act from reopening the other.
- **Do not soften a ruling into a recommendation.** `OD-SUB-0016` says the product issues invoices and
  captures payment; `ADR-029` then rules capture tokenized. Both stand — record the pair.
- **`DEC-L-021` applies.** The package will now be cited by things; check it stands in no defined
  relationship to something still `Proposed`. `ADR-027` and `ADR-017` are `Accepted` as of T-011,
  and `ADR-029` and `ADR-030` were born `Accepted`.
- Where the ruling set and a package document disagree, **the ruling set wins and you report it** —
  it is the later record and it is what the owner and I actually decided.

## Tests required

**None in the suites** — `DEC-L-002`. Criterion 3's `trace-check` run is the mechanical check; paste it.

## References

- **The authority, now tracked:** `.claude/handoff/notes/2026-08-25-fp-014-rulings.md` — all three rounds.
- Shape to imitate: `docs/17-features/FP-013-attendance/decisions-ratified.md`.
- The board's `DEC-L-004`, `006`, `009`–`010`, `018` for the rulings' own reasoning.
