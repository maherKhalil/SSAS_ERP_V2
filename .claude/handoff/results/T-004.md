# T-004 result — DONE

- **Status:** DONE
- **Branch:** `agent/T-004-fp-status-reconcile` — pushed: yes (`origin/agent/T-004-fp-status-reconcile`)
- **Merged:** yes — PR #54, `0649666` (merged 2026-08-25 under `DEC-L-007`, on the architect's `MERGE T-004`; non-gated, so it waited for that)
- **Commits:** `837eea6` — docs(T-004): reconcile the FP-012 and FP-013 README status with what shipped
- **Reported:** 2026-08-25

## Acceptance criteria

- [x] **Both READMEs carry a `status:` front-matter key, in FP-011's position and format, recording
      delivery and naming the PR.**
      FP-011's shape is `package` / `title` / `module` / `status` / `version` / `date`. Both new blocks
      use those six keys in that order:
      `status: DELIVERED — all eighteen owner decisions ruled (2026-08-24), merged to main by PR #51 (f465c9b)`
      and `status: DELIVERED — all sixteen owner decisions ruled (2026-08-25), merged to main by PR #52 (f9b247a)`.
      Merge dates confirmed as 2026-08-25 for both via `git log -1 --format=%ad --date=short`.
- [x] **FP-013's DRAFT line no longer contradicts the RATIFIED banner; FP-012 is no longer titled as an
      analysis package.**
      Both titles drop the `(analysis package)` suffix. FP-013's banner now reads **DELIVERED**, names
      PR #52 and the four artefacts it landed, and the old DRAFT sentence is **quoted verbatim** in a
      block headed *Status at analysis time, superseded by the line above*, followed by "All sixteen
      are now ruled, and the build has shipped."
- [x] **Any remaining sentence asserting the module is unbuilt, has no schema or has no code is
      corrected — searched rather than assuming only the two known ones.**
      I ran a fourteen-pattern grep over both files, then walked every hit. **Both files carried more
      than the two I reported in T-001** — details in the table below. Two hits were verified as
      *still true* and left standing deliberately.
- [x] **Analysis content preserved, not rewritten.**
      Nothing was deleted. Every correction is an added marker or a tense change on the sentence it
      corrects; the sweeps, the authority tables, the boundary sections, the inherited blockquotes and
      the decision rationale are all intact. `--stat` shows 124 insertions against **16 deletions**,
      and every one of those 16 is a line replaced in place by a corrected version of itself.
- [x] **`git diff main...agent/T-004-fp-status-reconcile --stat` shows exactly two files changed, both
      under `docs/17-features/`.** Output below.

### Every stale assertion found, and what was done with it

| File | Line, as found | Verified against | Treatment |
|---|---|---|---|
| FP-013 README | title `(analysis package)` | PR #52 `f9b247a` | suffix dropped |
| FP-013 README | `Status: DRAFT — analysis only. No code, no schema, no ADR` | `src/Modules/Attendance/` exists | quoted verbatim, marked superseded |
| FP-013 README | sweep table — no Attendance project / tree / test home | all three now exist | as-of marker on the sweep heading |
| FP-013 README | `Requirement-Numbering.md` row — "NO `REQ-ATT` PREFIX EXISTS … lists nine" | the file now lists **ten** and `Requirement-Catalog/ATT.md` exists | marked superseded, row kept |
| FP-013 README | "**That last one is a live test asserting this module does not exist.** It passes today…" | `tests/Payroll.Tests/Runs/PayrollCalculatorTests.cs:151` — the slot records the replaced guard | tense corrected, *Discharged as ruled* note added |
| FP-013 README | "The identifier space itself has to be created" | space created at ratification | *Done* marker above the retained argument |
| FP-013 README | "**This package SPECIFIES that follow-up obligation; it does not implement it.**" | `PayElement.cs:106` `OvertimeHourly = 5`, `:121` `UnpaidAbsenceDeduction = 6`; `PayrollCalculator.cs:18` consumes `AttendanceSummaryResult` | marked discharged in the same delivery |
| FP-013 README | "If FP-013 creates a working calendar…" | `SSAS.Attendance.Domain/Calendars/` exists; `OD-ATT-0015` **ruled** — proration unchanged, lever untaken | condition marked met, ruling named |
| FP-012 README | title `(analysis package)` | PR #51 `f465c9b` | suffix dropped |
| FP-012 README | `Status: RATIFIED 2026-08-24` | shipped 2026-08-25 | replaced by a DELIVERED banner that keeps the ratification detail |
| FP-012 README | sweep table — no Payroll project / tree / test home | all three now exist | as-of marker on the sweep heading |
| FP-012 README | "**Payroll is genuinely unbuilt.**" | `Program.cs:68` calls `AddPayrollModule()` | tense corrected, superseding fact named |
| FP-012 README | *Incidental finding* — `SSAS.GL.Contracts/` holds "only gitignored `bin/` and `obj/` build residue … not a skeleton" | `git ls-files` shows `Posting/IJournalPoster.cs`, `Posting/JournalPostingContracts.cs`, `.csproj` tracked | *Overtaken by this package's own delivery* note |
| FP-012 README | authority table — "**No `PAY.md`**" | `Requirement-Catalog/PAY.md` exists | as-of marker on the section |
| FP-012 README | "**Attendance is unbuilt, and it is a separate Roadmap V2 line.**" | PR #52; `SSAS.Payroll.Application.csproj:26` references `SSAS.Attendance.Contracts` | tense corrected, `DEC-PAY-0002` recorded as lifted |

**Two hits verified as still true and deliberately left standing** — this is the part I most want
checked, because leaving a true-looking sentence alone is indistinguishable from missing it:

- **FP-013's `Business-Rules.md` row** ("No `BR-ATT` rule"). Still true of the **master** register:
  `grep -c 'BR-ATT' docs/00-Master-Product-Specification/Business-Rules.md` returns **0**, and
  Attendance still sits under *Future Modules* at `:551`. The package drafted `BR-ATT-0001`–`0012` in
  its own `business-rules.md` (12 unique, verified) and `Requirement-Numbering.md` carries the prefix
  — **but the rules were never promoted to the master register.** The row now says exactly that
  instead of being marked stale.
- **FP-012's `BR-PAY` row.** Same shape, same reason — no `BR-PAY` rule in the master register.
  Untouched.

## What changed

Two files, 124 insertions, 16 deletions, no file outside `docs/17-features/`.

Design choices inside the constraints:

- **The DRAFT sentence is quoted, not paraphrased.** A reader auditing this later can see the exact
  words that were wrong, which a rewrite would have destroyed. Same for the RATIFIED banner's
  substance, which is folded into the DELIVERED banner rather than replaced.
- **One marker per section, not one per sentence.** The sweeps and authority tables get a single as-of
  note at the head rather than an annotation on every row, so the historical tables stay readable as
  tables.
- **FP-012's boundary section is explicitly exempted from the historical marker**, because
  `DEC-PAY-0016` — jurisdiction-neutral, no tax tables — **still binds**. Marking the whole file
  "historical" would have quietly demoted the one section that is still load-bearing. That exemption
  is stated in the marker itself.
- **The `OD-ATT-0015` claim was corrected mid-task.** I first wrote that it "is the live question";
  checking `decisions-ratified.md:27` showed it was ruled — *Payroll proration UNCHANGED, calendar
  days, lever recorded as untaken*. The file now names the ruling.

`git diff main...agent/T-004-fp-status-reconcile --stat`:

```
 docs/17-features/FP-012-payroll/README.md    | 65 +++++++++++++++++++++---
 docs/17-features/FP-013-attendance/README.md | 75 +++++++++++++++++++++++++---
 2 files changed, 124 insertions(+), 16 deletions(-)
```

`git status --short` after the push — the four protected paths exactly as found, nothing else:

```
 M .gitignore
?? .claude/
?? D7-M2.bundle
?? scripts/trace-check.py
```

## Gate

**Not run, by the T-004 spec and `DEC-L-002`.** No file under `src/` or `tests/` is in scope; two
Markdown files are not compiled and are not read by any suite. Verification is criterion 5, which
passed. No `.cs` file was needed, so the `BLOCKED` hatch was never reached.

| Suite               | Result |
| ------------------- | ------ |
| build (0 warnings)  | not run — no compiled file in scope (`DEC-L-002`) |
| Architecture.Tests  | not run — same |
| Platform.Tests      | not run — same |
| HR.Tests            | not run — same |
| API.Tests           | not run — same |
| module suite        | n/a — no module changed |

### `trace-check.py` — run on both edited packages, not modified, not committed

Run after the edits to confirm I had not introduced an orphaned identifier while adding citations.
**Both green.**

```
FP-012-payroll  [GREEN]  0 failure(s), 2 warning(s), 5 declared gap(s)
  Owns: PAY   External refs: 8
  REQ-PAY 18 · BR-PAY 13 · AC-PAY 31 · TS-PAY 33 · DEC-PAY 18 · OD-PAY 18 — all contiguous
  Owner decisions: 18 defined, 0 unruled -> ratified, build may proceed

FP-013-attendance  [GREEN]  0 failure(s), 0 warning(s), 1 declared gap(s)
  Owns: ATT   External refs: 2
  REQ-ATT 25 · BR-ATT 12 · AC-ATT 45 · TS-ATT 52 · DEC-ATT 14 · OD-ATT 16 — all contiguous
  Owner decisions: 16 defined, 0 unruled -> ratified, build may proceed
```

The warnings and declared gaps are pre-existing and untouched by this change.

## For the architect

- **Blocked / needs a decision:** none.

- **Touched outside scope:** nothing. Two files. `.gitignore` shows as modified in the working tree —
  **that is the pre-existing modification from before this session and I did not stage it**; it is not
  in the commit, as `--stat` above confirms.

### Answering your point 1 — there was not a third; there were six

You asked me to confirm there was not a third contradiction. **There were four more in FP-013 and
three more in FP-012**, all listed in the table above. The two I reported in T-001 were the loudest,
not the whole set. Three of the extra ones are worth your attention specifically:

1. **FP-013's "specifies but does not implement" clause was the most misleading of all of them.** It
   told a reader the Payroll-side follow-up had *not* been done. It had — in the same PR — and the
   evidence is unambiguous: `PayElement.cs:106` and `:121` carry `OvertimeHourly` and
   `UnpaidAbsenceDeduction`, and `PayrollCalculator` consumes `IAttendanceSummary`. A reader
   trusting that sentence would have gone looking for unbuilt work that shipped.
2. **FP-012's `SSAS.GL.Contracts` incidental finding had inverted.** It described the directory as
   untracked build residue "referenced by nothing". FP-012's own delivery made it a real, tracked
   project holding `IJournalPoster` — the contract Payroll posts through. The finding did not just go
   stale; the package's own work is what falsified it.
3. **FP-013's `REQ-ATT` row was false in a way that would have re-triggered `OD-ATT-0002`.** It said
   the prefix does not exist and the file lists nine. It lists ten, and `ATT.md` is in the catalog.

### Answering your point 3 — the same contradiction in other files of both packages

Reported, not fixed, as instructed. Each needs its own task and its own diff.

**FP-013 — eleven of its twelve other files are already correct.** Each carries the
`> **RATIFIED 2026-08-25.**` banner at line 3 and none carries a DRAFT contradiction. **The defect
was README-only in that package.** Three files carry now-completed future-tense statements, which is
a milder class than what T-004 fixed:

- `requirements.md:61` — `REQ-ATT-0022` still reads **"PAYROLL-SIDE FOLLOW-UP (specified here, not
  built here)"**. Same sentence I just corrected in the README; it shipped.
- `test-scenarios.md:97` — "`No_attendance_driven_behaviour…` **will fail**" — it did, and was
  replaced.
- `acceptance-criteria.md:103` — `AC-ATT-0044` is phrased as a criterion still to be met. It **was**
  met; recording that would close the loop rather than leave a satisfied criterion looking open.

**FP-012 — two files carry the flat "Attendance is unbuilt" claim, now false:**

- `decisions-approved.md:49` — "Attendance is a separate Roadmap V2 line and is **unbuilt**."
- `requirements.md:75` — "…unbuilt, so the input does not exist."

**And a structural difference worth a ruling rather than a task:** FP-012's twelve other files carry
**no status banner at all** — no `status:` key, no RATIFIED line — where FP-013's carry one on every
file. FP-012 is the outlier across the whole `docs/17-features/` tree. Normalising it is a bigger
question than one package, so I have not touched it.

### Follow-ups noticed but not done

- **`BR-ATT-0001`–`0012` and the `BR-PAY` rules were drafted in their packages and never promoted to
  `docs/00-Master-Product-Specification/Business-Rules.md`.** Two delivered modules have business
  rules that exist only inside their feature packages while the master register still lists both
  under *Future Modules*. This is the same orphan class as `CON-0001`, one level down, and neither
  `trace-check.py` nor anything else currently looks for it — the script checks *inside*
  `docs/17-features/`, so a package that defines `BR-ATT` locally looks complete to it. It is another
  argument for the upward extension you already proposed.
- **My read on the `trace-check.py` task, since you asked for it and I have now used the script three
  times:** commit it and extend it in one task, and make the extension the `UNRESOLVED` check applied
  one level up. That check is the part that has paid twice — it caught my minted `REQ-PLT-0071` in
  T-002, and here it confirmed I had not broken either package while adding citations. Extending it to
  `CON-*` and `BR-*` above `docs/17-features/` catches the class that swallowed `CON-0001`; the
  unpromoted-`BR` gap above is a second instance of that same class and would fall out of the same
  check. I would keep them one task because the extension needs a committed baseline to be reviewable
  as a diff.
