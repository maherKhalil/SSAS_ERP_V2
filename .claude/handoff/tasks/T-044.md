# T-044 — Make `trace-check` assert that the two decision registers agree

- **Branch:** `agent/T-044-register-agreement` — branch from `main`
- **Module:** Tooling
- **Issued:** 2026-08-26
- **Depends on:** T-036 merge (PR #85)

## Goal

**Your correction changes this task, so I have rewritten it rather than issuing what I queued.**

I called the two-register divergence "the third instance of that shape today". You pointed out that is
the third **you reported**, that you have not checked the other thirteen packages, and that FP-014 is
not obviously special — **so the number is a floor, not a count.**

That makes the cleanup the wrong thing to do first. **Build the check, let it enumerate the real
scope, then fix what it finds.** A sixteen-item cleanup scoped by hand is a guess; the same cleanup
scoped by a checker is a list. And you already said the check is worth more than the cleanup either way.

## Acceptance criteria

- [ ] **`trace-check` gains a check asserting `decisions-open.md` and `decisions-ratified.md` agree**:
      an `OD-` recorded as ruled in the ratification file and still presented as open in the register
      is a finding. Report it per package, in the existing output shape.
- [ ] **It goes RED on `main` as it stands** — sixteen `OD-SUB` in FP-014 alone. `DEC-L-016` is
      satisfied by construction here, but **say so with the output** rather than by assertion.
- [ ] **It enumerates every package, and the result reports the true scope** — how many packages carry
      the divergence and how many decisions in total. That number is the deliverable as much as the
      check is; **do not fix anything it finds.**
- [ ] **The new check is reported separately and does not perturb the existing verdicts.** Every
      package's current `[GREEN]`/`[RED]` and the master-register section must be byte-identical to a
      before-run apart from the new section — compare mechanically, as you did in T-036.
- [ ] **It states what it cannot see.** A ruling recorded in neither file, or recorded only in an
      architect note, is invisible to it — as `DEC-L-034` was until T-036. Say so in the output or the
      header, so nobody reads GREEN as "every decision is recorded somewhere".

## Files in scope

- `scripts/trace-check.py`

One file.

## Out of scope

- **Fixing any divergence it finds**, in FP-014 or anywhere else. That is the follow-up, and its
  scope is this task's output.
- Every other `trace-check` behaviour. Checks 1–7 are proven and are not to be restructured.
- Wiring into CI — still undecided.
- Any file under `src/`, `tests/`, or `docs/`.

## Design constraints

- **Extend beside, do not restructure.** T-005's discipline: the existing checks and their output
  format are why this script is trusted.
- **Report, never mutate.** It reads and prints.
- **Match how a ruling is actually recorded**, not how you would record one. `decisions-ratified.md`'s
  shape is the input; if packages differ in how they mark a ruling, **report that rather than picking
  one and calling the others wrong.**
- `DEC-L-031` applies to the check itself: **would it fire if the divergence were fixed?** Prove both
  directions — red on `main`, and green against a package where the registers do agree, even if you
  have to construct that case in a scratch copy.

## Tests required

**None in the suites** — `DEC-L-002`, no file under `src/` or `tests/`. The evidence is the two runs:
red on `main` with the count, and green on a corrected copy.

## References

- The finding: T-036 result, and the board entry naming `trace-check` satisfied while the register a
  human opens says BUILD BLOCKED.
- `DEC-L-016`, `DEC-L-031`. The precedent for extending this script: T-005.
