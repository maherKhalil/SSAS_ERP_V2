# item 204 — the eight pairs are different items, and the real ambiguity is elsewhere

**Report, plus one recovery.** ⚠ **Nothing renumbered — a renaming would break every existing citation.**

## (a) All eight pairs describe DIFFERENT items. None is a duplicate

| n | `T-nnn.md` | `item-n-*.md` |
|---|---|---|
| 156 | `AC-TEN-0020`'s remaining concerns are six, not eight | what protects the twelve platform-support routes |
| 157 | `/records/bulk` is a route-table row in a proposal block | the anonymous door: rate limiting, lockout, token lifetimes |
| 158 | two list items are stale on their stated facts | the password hasher's actual algorithm and cost |
| 179 | all six deletions confirmed | is any filter WRONG rather than absent? |
| 180 | schema assertions: 0 wrong in 40 | what the `AccountActionToken` index rests on |
| 191 | tests that cannot prove the guard they name | the competitor loops, and the window is measured |
| 194 | what is missing before the ERP is done | what starts a process at all |
| 201 | of the 67 documented-but-unrouted rows, how many are ours? | the `agent/*` branches, by the corrected method |

**Eight numbers, sixteen distinct subjects, zero overlap in content.**

## ⚠⚠ BUT "ITEM 157" IS NOT UNRESOLVABLE, AND THE RULING'S PREMISE NEEDS CORRECTING

**The two series are distinguished by their PREFIX.** `T-157` resolves to `T-157.md`; `item 157` resolves
to `item-157-anonymous-door.md`. **Both citations work.** The collision is in the *number*, not in any
citation this loop actually writes — and a bare "157" was never a citation form here.

## ⚠⚠ THE REAL AMBIGUITY IS A **THIRD** SERIES, AND IT IS THE ONE THAT DOES NOT RESOLVE

Three `T-` files carry an internal label in their own header:

> `# T-191 (item 40)` · `# T-194 (item 41)` · `# T-201 (item 48)`

**and `T-181`'s status line reads `ESTABLISH ONLY, item 26`.** So in the `T-` era, **"item N" meant a
different counter entirely.**

**Measured:** `item N` is cited for **25 distinct numbers below 152** — 1, 4, 5, 8, 9, 10, 16, 24, 25, 40,
41, 47, 48, 56, 64, 90, 91, 95, 100, 116, 120, 123, 125, 133, 136 — and ⚠ **NOT ONE of them has an
`item-N.md` file.** The `item-` filename series begins at 152.

**So a reader chasing "item 40" finds nothing, and there is no rule in the repository that tells them to
look in `T-191`'s header.** ⚠ **That — not the eight pairs — is the citation that cannot be resolved**, and
it is 25 numbers wide.

**Today the two file series do not collide** (internal labels span 40–48; filenames start at 152), so
nothing currently resolves to the *wrong* file. **The failure is resolving to nothing.**

## The two duplicated `T-` numbers are variants, not distinct items

**`T-056`** → `T-056.md` + `T-056-design.md`; **`T-098`** → `T-098.md` + `T-098-part1.md`. **Suffixed parts
of one item each**, which is why 139 files carry 137 numbers.

⚠ **And `T-056.md` is PRESENT**, though `BOARD` records `T-056.md` and `T-057.md` being *"copied to
scratchpad before removal"*. **Only `T-057` stayed removed** — consistent with item 203, where 057 is one
of the 27 and 056 is not.

## (b) `T-181` is recovered and committed

From `agent/T-181-fiscal-overlap-establish` (`995ccac`, 2026-08-29) — **5,877 bytes, 112 lines**, verbatim,
with a recovery note added at the top and nothing else changed. **It is the only one of item 203's 27 that
exists anywhere in the repository; the other 26 have no history on any branch**, so this is the whole of
what recovery can achieve.

## What I am NOT proposing

**No renaming.** A renumbering would break every citation already written — including the ones in this
file. ⚠ **And the `item N < 152` problem is not fixed by renaming anything**: those numbers have no files
to rename. **The only thing that would resolve them is a mapping table**, which is documentation and the
architect's.

## Scope
- The eight pairs were compared **by header subject**, not line by line. Two files could share a subject
  and differ in substance; none of these eight comes close.
- **25 is the count of distinct low numbers CITED**, not of items that existed under the old counter — the
  old series may be larger, and nothing here enumerates it.
