# T-011 result — DONE

- **Status:** DONE
- **Branch:** `agent/T-011-adr-status` — pushed, branched from `main` at `2f8db29`
- **Merged:** yes — PR #61, `a907848` (on the architect's `MERGE`; `main` `2f8db29` → `a907848`)
- **Commits:** `a234d6b` — status + evidence; `42bfe68` — Revision History rows (architect's amendment)
- **Reported:** 2026-08-25

**You asked me to test the ruling rather than execute it. I did, all three clear — but not equally,
and one of them named its own acceptance test.**

## Acceptance criteria

- [x] **All three carry `status: Accepted` with a date, in the shape `ADR-015` and `ADR-029` use.**
- [x] **Each gains a line naming what evidences the acceptance.** Checkable in every case — a closed
      owner decision, a PR, a file, a `[Trait]` on a running test. No "it is widely used".
- [x] **Nothing else in the three files changes.** Verified line by line: every removed line is
      front-matter or `# Status` body, plus three README rows. **No Context, Decision, Consequences or
      Alternatives text was touched.** Revision History gained one *appended* row per file under the
      architect's amendment — nothing existing in those tables was altered.
- [x] **README index reflects the three statuses.**
- [x] **`--stat` shows four files, all under `docs/14-Engineering/ADR/`:** 38 insertions, 22 deletions
      across the two commits.

## The evidence, per ADR — and they are not equally strong

### `ADR-026` — it named its own acceptance test, and I checked the test rather than the conclusion

Its Status section said:

> "Three of them (`decision 4`, `decision 9`, `decision 10`) depend on owner input recorded as
> `OD-DEP-001`, `OD-DEP-003` and `OD-DEP-005` in the FP-007 package. **The ADR should not be accepted
> until those are answered.**"

**So the question was never whether the ADR is widely used. It was whether those three were answered.**
They were, all on 2026-08-20:

| Precondition | Closed as |
|---|---|
| `OD-DEP-001` | `DEC-DEP-0009` — `BR-HR-0005` enforcement, Option A, fail-loud |
| `OD-DEP-003` | `DEC-DEP-0014` — `BR-HR-0007` scope, reading (iii) adopted |
| `OD-DEP-005` | `DEC-DEP-0019` — Department is not an authorization dimension |

FP-007's register states "All five owner decisions (`OD-DEP-001` … `OD-DEP-005`) were closed". Plus
`Department` shipped in PR #45, and `DepartmentApplicationArchitectureTests.cs` carries executable
guards tagged `[Trait("Decision", "ADR-026")]` — the ADR is asserted by running tests, not only cited.

**This is the one that would have been wrong to accept on assertion**, and the one where the
verification actually changed what I could write in the evidence line.

### `ADR-027` — the strongest: acceptance was recorded, not inferred

`OD-POS-004` closed 2026-08-21 and FP-008's register states, verbatim: *"`DEC-POS-0015` and
`DEC-POS-0016` activate, and **`ADR-027` with them**"*. The ADR's own Revision History already said it
was "**activated** the same day".

**So this one was accepted in substance on 2026-08-21 and the front-matter simply never caught up** —
which is exactly what `DEC-L-011` claimed of all three, and is straightforwardly true here. Four
modules then inherited `decimal(19,4)`: `SalaryGradeConfiguration`, `JournalDraft`,
`EmployeeCompensation`, `AttendanceRecord`.

### `ADR-017` — a gate that was passed rather than left open

Written as the storage-topology gate to resolve **before** substantial tenant-owned ERP persistence
was implemented. That persistence has since shipped across four modules on this topology.

`TenantDatabase`, `TenantDatabaseAssignment` and `TenantCutoverOperation` ship;
`TenantCutoverCopyPlan.Build` derives the Shared→Dedicated manifest by reflecting over the
`ITenantOwnedEntity` boundary this ADR defines; `FP-014`'s `DEC-SUB-0003` inherits its residency
ruling verbatim, citing `:162` and `:475`, rather than re-deciding it.

**Its evidence is the weakest of the three in kind** — no closed decision says "ADR-017 is accepted",
so acceptance is inferred from the fact that four modules were built on it. That inference is sound,
and it is the inference `DEC-L-011` rests on. I am naming it as an inference rather than dressing it
as a record.

## The one place I exceeded "front-matter plus one line each"

**Each `# Status` section body changed, and it was not avoidable.** Every one of those bodies existed
solely to explain why the record stood at `Proposed`, so each became false the moment the status
changed.

`ADR-026`'s was the sharp case: leaving *"the ADR should not be accepted until those are answered"*
underneath `status: Accepted` would have been **self-contradictory in the one file whose precondition
I had just verified**. A stale explanation is worse than none — the same rule T-015 applied when it
deleted the reachable-past comment rather than leaving it to rot.

`ADR-027`'s body claimed `ADR-024`, `ADR-025` **and `ADR-026`** "all stand at `Proposed` alongside
delivered work". Two of those remain true; the third stopped being true in this same commit. The new
text names only `ADR-024` and `ADR-025`.

**If you read that as scope creep, the revert is three paragraphs and I will do it** — but the
alternative is three Accepted ADRs each carrying a paragraph explaining why they are Proposed.

## Other choices worth naming

- **Dates are today**, per the constraint. The original authoring dates survive in each Revision
  History, so nothing is lost — I checked that before changing them.
- **`version:` now bumps on all three** — 1.6→1.7, 1.0→1.1, 1.0→1.1. My first commit deliberately
  left it alone, reasoning that a status correction is not a content revision. **The amendment
  changed that calculation rather than overruling it:** once each file carries a Revision History row,
  the row needs a version, and a row carrying a version the front-matter does not know about is the
  same drift this task exists to remove. Every row therefore states **"No decision changed"**
  explicitly, which is what my original reasoning was actually protecting.
- **Revision History rows added, on the architect's amendment.** I had left them out as a second
  changed line against an explicit constraint, and raised the argument for them rather than deciding
  either way. The amendment ruled for them: a `Proposed` → `Accepted` move is a substantive document
  event and an ADR's own history is where a reader looks — git is not the document.

  One row each, matching each file's own column header (`ADR-017` heads its fourth column
  *Description*; `ADR-026` and `ADR-027` head it *Change*). **Versions bump with the rows** — 1.6→1.7,
  1.0→1.1, 1.0→1.1 — so front-matter and history agree; a row carrying a version the front-matter did
  not know about would be the same drift this task exists to remove. Every row states **"No decision
  changed"** explicitly, because a version bump on an ADR otherwise reads as a content revision and
  none of these is one.

  Each row records *why* in `DEC-L-020` terms rather than uniformly: `ADR-026` against its own
  precondition, `ADR-027` as recorded acceptance, `ADR-017` as a labelled inference.

## Gate

**Not run** — `DEC-L-002`, no file under `src/` or `tests/`.

```
 4 files changed, 38 insertions(+), 22 deletions(-)

 a234d6b  status, evidence lines, README index
 42bfe68  one Revision History row per ADR, versions bumped to match
```

## For the architect

- **Blocked / needs a decision:** none. All three cleared; nothing was queried.
- **Touched outside scope:** nothing. Four files, all under `docs/14-Engineering/ADR/`.

### `ADR-029` no longer depends on three proposals

That was the correctness argument for this task, and it is closed: `ADR-029` is `Accepted` and its
`depends_on` — `ADR-001`, `ADR-012`, `ADR-017`, `ADR-027` — now resolve to Accepted records
throughout.

### A finding, reported not fixed, per the out-of-scope instruction

**`ADR-024` and `ADR-025` remain `Proposed` while their features have shipped**, on the same reasoning
`DEC-L-011` rejected for the other three. `ADR-024` (Employee Branch Assignment and Transfer) and
`ADR-025` (Company Execution Context and Authorization) both underpin delivered HR and Platform code —
`ADR-025` is cited by `CompanyOwnershipArchitectureTests` and by FP-014's authorization model.

**I did not touch them and I am not asserting they should move.** Two things give me pause, and after
today they are worth stating rather than assuming:

1. `ADR-027`'s old Status text used their `Proposed` status as evidence that the status means nothing
   here. Moving three of the four and leaving two makes that reading harder, not easier.
2. **Twice this week a finding of mine turned out to be the opposite of what it looked like** — the
   `gate.sh` abort and the `ADR-028` reservation. The pattern in both was assuming a gap where there
   was a deliberate choice. `ADR-024` and `ADR-025` may be pending something I have not read.

So: a candidate for a task, with the evidence to be tested the way `ADR-026`'s was, not a defect I am
confident in.
