# T-113 result — the money path was broken, and the guard caught a second one on its first run

- **Status:** BUILT, GREEN, **MERGE PENDING** under `DEC-L-007` (gated).
- **`GATE_SCOPE=TASK` GREEN, 0 warnings.** Branch `agent/T-113-chain-test`, from `ClaudeBranch` at `94c1fd5`.
- **Integration, run separately:** `PayrollChainSqlServerTests` 2/2 and `ConstructorKeyedEntityModelTests`
  2/2 against a real SQL Server.

---

# ⚠ CONDITION 4 RAISED ATTENTION, AND IT IS RIGHT TO

```
ATTENTION: totals unchanged in all 7 suite(s) while 62 non-comment line(s) under src/ changed
           -- condition 4 is yours to judge
```

**Judged: correct, and it is T-113's own finding restated by the gate.** The `src/` change is EF
configuration and repository child-deletion. **The only tests that can observe it are Integration tests,
which `GATE_SCOPE=TASK` excludes by design** — so seven suites moving not at all is exactly what should
happen, and it is also exactly why this defect survived three weeks.

**The coverage is real and it is elsewhere:** four Integration tests, reported above, run against a real
server. **A reader who takes the unmoved totals as "untested" would be wrong, and a reader who takes the
green TASK gate as "covered" would also be wrong.** Condition 4 exists to make someone say which.

---

# ⚠ WHAT A GREEN HERE DOES AND DOES NOT PROVE

**Proven:** the calculate path saves against a real SQL Server, and six hours of overtime become money in a
posted, balanced journal.

**NOT proven, and a reader will assume otherwise:**

```
SalaryType (T-107)          defaults to Monthly -> only the monthly arm is ever walked.
                            HOURLY AND DAILY HAVE NEVER TOUCHED SQL SERVER, and daily's base
                            arithmetic changed twice this week.
StandardWorkingDays (T-108) zero in the fixture -> a daily salary would REFUSE, not compute.
OneOffPayment (T-110)       no instruction exists -> the null-compensation payee the whole task
                            exists for is never constructed.
ReversedUtc (T-112)         nothing is reversed -> the filtered unique index is never exercised,
                            and a filtered index is exactly the thing that behaves differently on
                            a real server than in anyone's head.
```

**Four constructs shipped this week. The spine breaks the build on two of them and silently skips all
four.** **This green means the PRE-T-107 money path is proven.** Extending the spine is T-114 and
deliberately not this task — mixing it in would make the red-to-green transition unreadable, and that
transition is the evidence this task exists to produce.

---

# 1. THE RED, REPRODUCED BEFORE ANY FIX

```
System.InvalidOperationException : Tenant ownership cannot be changed after an entity is created.
  at PersistenceDbContext.ApplyPersistenceRules()      PersistenceDbContext.cs:141
  at CalculatePayrollRunCommandHandler.HandleAsync()   PayrollRunCommandHandlers.cs:298
```

**Both chain tests, that exact message, on the calculate path, on `ClaudeBranch` with no fixes applied.**
The branch's claim was true and is now observed rather than believed.

## The coverage measurement — three compile errors, all constructor arity, all mine

```
AttendanceSummaryService           missing IWorkingCalendarRepository   T-108
CalculatePayrollRunCommandHandler  missing IOneOffPaymentRepository     T-110
ApprovePayrollRunCommandHandler    missing IOneOffPaymentRepository     T-110
```

Fixed minimally — three constructor calls and one `using`. **No defect fix, which is what made the red
observable.**

---

# 2. TWO DEFECTS, MEASURED SEPARATELY

**Fixing them one at a time was worth the extra run:**

```
after ValueGeneratedNever alone   1 of 2 green
                                  the survivor: "The association between PayrollRun and
                                  PayrollRunDraftLine has been severed... configure cascade deletes"
after explicit child deletion     2 of 2 green
```

**Two independent defects on one path**, and a single combined apply would have reported one fix for both.

---

# ⚠ 3. THE GUARD FAILED ON ITS FIRST RUN, AND IT CAUGHT MY OWN ENTITY

```
Every_constructor_keyed_entity_declares_its_key_value_generated_never   FAIL
  "... OneOffPayment.Id is OnAdd"
```

**`OneOffPayment` is T-110's, four tasks ago, and it had the identical defect.** It would have thrown the
same tenant-ownership error the moment anyone saved one against a real database.

**And nothing could have caught it.** T-110's tests are in-memory domain tests and stubbed API tests;
**no test in the product executed a one-off payment against SQL Server.** I shipped an aggregate with a
persistence defect and a green gate.

**This is the argument for the guard, made by the guard, on the code of the person evaluating it.**

**Fixed:** `.ValueGeneratedNever()` on `OneOffPaymentConfiguration`, with the reason recorded where the next
person meets it. **All four tests now pass.**

## Why this guard, having argued against two others

**T-097 and T-111 had zero live instances. This has three modules' worth plus one I added while the branch
sat unmerged.** A guard earns its place from the defect behind it, not from the shape of what it checks —
and this one starts green, states what it does not look at, and has a non-vacuity test of its own
(`The_sweep_covers_the_whole_composed_model_rather_than_one_module`) that passes.

---

# 4. WHAT I LEFT BEHIND, AND ONE CORRECTION TO THE PREMISE

**`acfe8ad` — the cutover convergence — NOT taken.** A refactor of a passing test riding with a defect fix
is borrowed risk, and you blessed leaving it.

**The correction:** `CutoverTenantModelSource.cs` **was already on `ClaudeBranch`** — `40d40c8` is an
ancestor of the branch's own history. So the 132-line change is `acfe8ad` re-shaping a test around a file
that already exists, not an extraction arriving with the fix. **`TenantCutoverCopySqlServerTests.cs` is
untouched in my tree.**

---

## For the architect

- **Blocked on you:** nothing.
- **T-114 is now better justified than when you queued it:** the spine found a broken money path the first
  time it ran, **and the guard found a second defect the spine itself does not cover.** Four money-handling
  constructs have still never met a real database.
- **`OneOffPayment` is the one to tell the owner about** — it is this week's work, not three weeks ago's.
