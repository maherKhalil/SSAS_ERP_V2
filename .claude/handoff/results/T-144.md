# T-144 — **all three docs branches are superseded, and the reason they differ from `chain-test` is worth stating**

- **Status:** ESTABLISH ONLY. **Nothing merged, nothing deleted.** Docs-only — pushed and stopped.
- Branch `agent/T-144-codex-docs-branches`, from `ClaudeBranch` at `1484d55`.

---

# THE VERDICT

```
codex/architecture-tenant-storage   13 Aug   2826 insertions   SUPERSEDED
codex/fp-005-milestone-1            10 Aug    472              SUPERSEDED
codex/fp-007-department-analysis    20 Aug   1843              SUPERSEDED
```

**Every file each branch touches already exists on `ClaudeBranch`, and ours is a superset in every case.**

## 1. `architecture-tenant-storage` — five ADRs, all present and further along

```
                                              branch      ours
ADR-017 Tenant Storage Topology            v1.3 Proposed   v1.7 ACCEPTED (2026-08-25)
ADR-018 Schema Health / Migration          533 lines       708
ADR-019 Dynamic Tenant Placement           397             397  (identical)
ADR-020 Shared-To-Dedicated Cutover        527             564
ADR-021 Customer-Managed Connectivity      601             628
```

**Fifteen lines total exist on the branch and not on ours, and they are stale status metadata** —
`status: Proposed`, older `version:` numbers, a `**Proposed**` heading. **Ours has moved past Proposed.**

**The one substantive-looking block was ADR-017's implementation bullets, and I checked them rather than
assuming:** `IDbContextFactory` appears 4 times in ours, `context pooling` once, `migration stream` three
times. **The content is present; only the phrasing differs.**

## 2. `fp-005-milestone-1` — FP-003 tenant lifecycle

**Nine files, all present on ours, all supersets.** `api-contracts.md` and `ADR-015` are byte-identical;
the rest differ by 1–13 lines on the branch against 15–282 on ours.

## 3. `fp-007-department-analysis` — FP-007 HR department

```
api-contracts.md   branch: v0.1  "Draft — Owner Decision Required"
                   ours:   v1.0  "Approved for Implementation"
```

**The branch's 32 unique lines are a draft route table**, and **every route in it is in ours** — the
collection, the detail, `/hierarchy`, `/parent`, `/manager` assign and clear, deactivate, reactivate.

---

# ⚠ WHY THIS ANSWER DIFFERS FROM `chain-test` AND `post-phase-e-hardening-h1`, AND IT IS NOT LUCK

**You were right that *"it is old and conflicts"* is not evidence a branch is worthless** — two branches
proved it this fortnight, one holding a broken money path and one a live discarded failure on the cutover's
data-loss path.

**But both of those held CODE, and code can be right about a defect while the tree has moved on around it.**
**These three hold only documentation, and documentation of decisions the tree has since made.** A superseded
ADR is not a dormant finding: **the later version IS the resolution of the earlier one.**

**So the distinction is not age or conflict, it is whether a branch asserts something the tree has ANSWERED
or something the tree has FORGOTTEN.** `chain-test` was forgotten; these three were answered.

---

## For the architect

- **Blocked on you:** nothing. **I deleted nothing** — the verdict is that they hold nothing unique, and
  acting on that is a separate decision from establishing it.
- **The `codex/*` sweep now stands at:** two branches held live defects (both merged or actioned), three
  hold superseded documentation, **and eighteen remain unexamined.** **This was three of twenty-three.**
- **`DEC-L-069`:** every file on every branch was compared, not sampled — `git diff --name-only` per branch,
  then a per-file numstat against ours, then the unique lines read for the four files where they exceeded
  ten.
