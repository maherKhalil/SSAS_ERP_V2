# T-004 — Reconcile the FP-012 and FP-013 README status with what actually shipped

- **Branch:** `agent/T-004-fp-status-reconcile`
- **Module:** Docs
- **Issued:** 2026-08-25
- **Depends on:** none — independent of the FP-014 decision round

## Goal

You found this during T-001 and it is worse than the defect T-001 fixed, because these files outrank
`START-HERE.md` inside their own scope.

`docs/17-features/FP-013-attendance/README.md` says **"Status: DRAFT — analysis only. No code, no
schema, no ADR"** three lines below its own "RATIFIED 2026-08-25" banner — while PR #52 shipped
`src/Modules/Attendance/*`, a migration and three test suites. `FP-012-payroll/README.md` is still
titled "(analysis package)" for the same reason. **Neither carries a `status:` front-matter key at
all**, unlike FP-001 … FP-011.

Under the repository's own precedence rule an approved Feature Package supersedes older functional
text within its scope. Two of them currently state that the code they shipped does not exist.

## Acceptance criteria

- [ ] `FP-012-payroll/README.md` and `FP-013-attendance/README.md` each carry a `status:` front-matter
      key, in the same position and format as FP-011's, recording that the package is delivered and
      naming the PR that landed it (#51 and #52 respectively).
- [ ] The `Status: DRAFT — analysis only. No code, no schema, no ADR` line in FP-013's README no longer
      contradicts the RATIFIED banner three lines above it, and FP-012's README is no longer titled as
      an analysis package when it is a delivered one.
- [ ] Any remaining sentence in either README that asserts the module is unbuilt, has no schema, or has
      no code is corrected. Search both files for such claims rather than fixing only the two you know
      about — you found two; confirm there is not a third.
- [ ] The **analysis content is preserved, not rewritten.** The sweep sections, the decision rationale
      and the boundary warnings stay as the historical record of what was decided and why. You are
      correcting status assertions, not re-authoring the packages.
- [ ] `git diff main...agent/T-004-fp-status-reconcile --stat` shows **exactly two files changed**, both
      under `docs/17-features/`.

## Files in scope

- `docs/17-features/FP-012-payroll/README.md`
- `docs/17-features/FP-013-attendance/README.md`

## Out of scope

- The other twelve files inside each of those two packages. If one of them carries the same
  contradiction, **report it, do not fix it** — it becomes its own task with its own diff.
- Every other feature package. FP-001 … FP-011 already carry `status:` keys; do not normalise,
  restyle or "improve" them.
- `docs/START-HERE.md`. It is now correct and it is not to be touched again in this task.
- Any file under `src/` or `tests/`.

## Design constraints

- **Match FP-011's front-matter shape exactly** — same keys, same order, same value style. It is the
  most recent package that carries one properly, and the point of this task is that these two rejoin
  the convention rather than inventing a third form.
- **Do not delete the DRAFT-era text wholesale.** A package that shipped still needs its analysis
  legible; a reader must be able to see what was open at analysis time and how it was ruled. Correct
  the status assertion, keep the reasoning.
- Where a sentence is true of the analysis phase but false now, prefer marking it as historical over
  deleting it. FP-013's own RATIFIED banner is the pattern: it resolves conditional passages inline
  and names the ratification file as authoritative.

## Tests required

**None** — `DEC-L-002` applies. No file under `src/` or `tests/` is in scope. Verification is
criterion 5, the `--stat` assertion.

## References

- The convention to match: `docs/17-features/FP-011-gl-foundation/README.md` front-matter.
- The disposition style for a package whose status changed: `docs/17-features/FP-010-hr-employee-documents/README.md`,
  whose `status:` line records both the outcome and the ruling that caused it.
- Your own T-001 result, "For the architect" item 4, which is where this was found.
