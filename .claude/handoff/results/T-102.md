# T-102 result — the amendments applied, and four sites the amendment leaves false

- **Status:** BUILT AND PUSHED, **NOT MERGED.** Branch `agent/T-102-fp013-amendments`, from `ClaudeBranch`
  at `0995696`, pushed on start.
- **Docs-only, so non-gated** — `DEC-L-002` / `DEC-L-007`. Say `MERGE T-102` and it goes.
- **`git diff --numstat`: `1 1` on each of three files.** One line in, one line out, as constrained.

---

## What changed

```
acceptance-criteria.md   AC-ATT-0032   absence criterion -> exact inventory of two
requirements.md          REQ-ATT-0023  new wording, the termination pointer, and the Authority column
traceability-matrix.md   the row       BLOCKED -> DELIVERED under FP-015, and TS-ATT-0036 marked
```

**`AC-ATT-0032` carries its own amendment note**, in the form the criterion it replaced already used: it
records that it *read* "no `ViewOwn` permission exists" and why an absence criterion had to go — **it
answers its question exactly once and then has to be deleted, while an exact inventory keeps answering it.**

**The Authority column moved from `UNAUTHORED` to `FP-015 REQ-SS-0004` — delivered T-089.** That column
cites what authorises a requirement, and eight rows in this package still say `UNAUTHORED`; this one no
longer can.

**`REQ-ATT-0023` cites the termination rule rather than restating it**, exactly as ruled. A reader of the
requirement alone would otherwise conclude a terminated employee may still read.

---

# ⚠ THE AMENDMENT LEAVES FOUR SITES ASSERTING THE OLD STATE

**Applying the ruling literally creates four fresh stale claims inside the same package.** I did not touch
them — *"touch nothing else in FP-013"* — and they need a ruling.

```
acceptance-criteria.md:120   "Anything self-service. ... blocked on FP-015's permission and endpoint.
                              AC-ATT-0032 asserts the absence rather than the behaviour, which is the
                              only honest thing to assert today."
                              -> BOTH CLAUSES ARE NOW FALSE. It is not blocked, and AC-ATT-0032 no
                                 longer asserts an absence.

authorization-model.md:14    "REQ-ATT-0023 proposes that an employee may read their own attendance and
                              leave records ... and it is not implementable today."
                              -> FALSE. It is implemented, and this section quotes PayrollPermissionNames'
                                 deferral at length as the reason.

traceability-matrix.md:92    "OD-ATT-0013 read permissions | REQ-ATT-0023, REQ-ATT-0024, REQ-ATT-0025;
                              the self-service blocker"
                              -> FALSE. There is no blocker. A DIFFERENT row from the one ruled.

test-scenarios.md:69         "TS-ATT-0036 | The permission catalog contains no ViewOwn of any kind"
                              -> FALSE, and it is the scenario the amended criterion cites.
```

**`TS-ATT-0036` is the one you anticipated** — *"a scenario reference that no longer exists"*. It exists and
asserts the opposite of what the module does. **Reported rather than forced:** rewriting a test scenario is
not an amendment to a criterion, and the row now says so inline so a reader meets it rather than trusting
the citation.

**Its replacement already exists in code** — `The_self_service_permissions_are_exactly_the_two_that_were_decided`
in `AttendanceArchitectureTests`, built in T-089 as the exact-inventory guard `AC-ATT-0032` now describes.

## One site I left deliberately, and it is not in that list

```
decisions-ratified.md:97   "REQ-ATT-0023 self-service | BLOCKED and deferred; no ViewOwn permission
                            exists, asserted"
```

**`DEC-L-071`: a dated decision record is historical, not stale.** That line records what was ratified on
the day it was ratified, and it was true then. **Editing it would rewrite the minutes to match the
outcome** — which is the opposite of the property the loop has been protecting.

**Naming it here so its absence from the list above is a decision rather than an oversight.**

---

## For the architect

- **Blocked on you:** `MERGE T-102`, and a ruling on the four sites above. **Three are live claims that are
  now false; the fourth is a test scenario whose replacement already exists in code.**
- **The pattern is the one you carried up.** This amendment corrected a requirement's status and, by doing
  so, made four neighbouring statements wrong. **A status that lives in five places has five chances to be
  stale** — the same shape as the per-document marker T-101 dissolved, one layer further in.
- **Not touched:** the README status, the vocabulary, and anything else in FP-013.
