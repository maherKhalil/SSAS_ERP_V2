# T-114 result — four constructs met a real database, and all four were already correct

- **Status:** BUILT, GREEN, **MERGE PENDING** under `DEC-L-007` (gated).
- **`GATE_SCOPE=TASK` GREEN, 0 warnings.** **Integration: 8/8** — six chain tests and the two guard tests.
- Four commits, one per construct, as advised.

---

# THE HEADLINE, AND IT IS NOT THE ONE EITHER OF US EXPECTED

**You said to expect red, and that a green first run is a worse outcome than a red one. All four went
green on the first run.**

**So I planted all four, separately.** Each plant failed, each restore passed:

```
DAILY     999999 -> FAIL 8.48s   "Actual: 1900.0000"
ONE-OFF   777777 -> FAIL          "Actual: 4000.0000"
REVERSAL  asserted the OLD broken behaviour -> FAIL
HOURLY    555555 -> FAIL          "Actual:  200.0000"
```

**Every figure was computed by the product, not asserted into existence.** The four constructs this week
shipped are correct against a real database, and that is now known rather than believed.

## On planting after the artefact was explained

You named the temptation precisely and it was real: after establishing that `Duration: < 1 ms` is a vstest
assembly-line artefact, **the next `< 1 ms` reads as the same known artefact rather than as an unverified
claim.** But the plant was never evidence about the timer — **it was evidence that THAT assertion, on THAT
path, can fail**, and four paths need four plants. It cost about ten seconds each.

---

# THE FOUR

**1. DAILY.** 21 working days (January 2026 less five Fridays and five Saturdays), two unpaid, **19 × 100 =
1900**. Overtime 150, unaffected by salary type. **And no absence deduction line** — T-109's ruling holding
where a returning defect was worth 1900/31 × 2 = **122.58** of somebody's money.

**2. THE ONE-OFF PAYEE.** An employee with an instruction and **no compensation record at all**, paid 4000
on a single line, with no base, no proration and no absence deduction, **and the salaried employee
unaffected** — which is what makes the inclusion rule correct rather than merely wider. **Also the first
time `OneOffPayment` has ever been persisted**, four tasks after it shipped.

**3. REVERSE AND RERUN.** A live posted run refuses a second; the run is reversed through the real handler
and the real ledger; **the period then accepts a correction; and that correction is itself live, so a third
is refused again.** T-112's filtered unique index, exercised on a real server for the first time.

**4. HOURLY.** 8 hours attended at 25 = **200**, overtime 150 on its own quantity, **and no deduction line
despite two unpaid days recorded** — the double-count hourly was excluded from, priced against a
calendar-day divisor that means nothing against an hourly rate.

---

# ⚠ WHAT THE FIXTURE COST, WHICH YOU ASKED FOR AND IS A FINDING IN BOTH DIRECTIONS

**DAILY cost one defaulted parameter.** You expected the fixture to need a working calendar and for building
it to be a finding. **`SeedAttendanceAsync` already seeds one** — `"Standard"`, Friday/Saturday,
`isDefault: true` — because FP-013's own chain needed it. **So `StandardWorkingDays` was never zero once
T-113 wired the repository in; it reads 21.**

**Daily was one parameter away from reachable, and the only thing between it and a real database was that
nobody had asked.** That is a more uncomfortable finding than the one you expected.

**THE ONE-OFF PAYEE cost more, and still not much:** one `INSERT` reusing the department and position the
first employee created, one `OneOffPayment.Create`, and a `BONUS` element assigned to nobody. **No
compensation row, no salary type, no rate — because that is the whole point of the person.**

**REVERSE AND RERUN cost the most**, and the cost was informative: **the fixture creates runs straight
through the aggregate and the context, bypassing `CreatePayrollRunCommandHandler`'s courteous pre-check** —
which is exactly what puts the *database index* in the path rather than the repository's `AnyAsync`. That
made it the right shape by accident, and I kept it deliberately.

---

# TWO THINGS THAT DID NOT HAPPEN

**Condition 4 did not fire**, and you pre-empted that it would. **T-114 changes no `src/` at all** — it is
test-only, so there is no `src/` movement for unmoved suite totals to be suspicious about. **The pairing you
wanted stated twice is stated once, in T-113, where it applied.**

**No fix was needed.** You said fixes are in scope when small and to hand you anything that is not.
**Nothing was found to fix**, which is the report.

---

## For the architect

- **Blocked / needs a decision:** nothing.
- **The value here is negative evidence, and it is worth naming as such.** T-113 found a broken money path
  on its first run; T-114 found none. **The difference is that T-113's target was three weeks old and
  T-114's was written this week under a loop that was already checking its own arithmetic** — the in-memory
  tests were right, and now that is measured rather than assumed.
- **Not covered, and deliberately:** hourly and daily against a MID-PERIOD JOINER. The fixture's employee is
  hired in 2020, so "no proration" is asserted by the absence of a factor rather than by a joiner who would
  expose one. **Proving it needs a second employment date, which is a fixture change I did not make.**
