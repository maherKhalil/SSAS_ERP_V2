# T-145 — **the `codex/*` sweep is finished. Thirty-two branches hold nothing; the list is no longer an unaudited source of work.**

- **Status:** ESTABLISH ONLY. **Nothing merged, nothing deleted.** Docs-only — pushed and stopped.
- Branch `agent/T-145-codex-sweep`, from `ClaudeBranch`.

---

# THE WHOLE LIST, ACCOUNTED FOR

```
38 local codex/* branches

  2   held LIVE DEFECTS          chain-test (broken money path), post-phase-e-hardening-h1 (discarded failure)
  1   held a GUARD               sqlserver-unicode-enforcement  -> merged T-133
  3   held SUPERSEDED DOCS       architecture-tenant-storage, fp-005-milestone-1, fp-007-department-analysis
 32   FULLY CONTAINED            zero commits ahead of ClaudeBranch
```

**Every one of the thirty-two is an ancestor of `ClaudeBranch`.** Not "similar", not "mostly merged" — `git
rev-list --count ClaudeBranch..<branch>` is **0** for each, and `git merge-base --is-ancestor` confirms it.

## ⚠ A uniform zero across thirty-two measurements needed a control, so it got one

**Thirty-two identical answers is exactly the shape that has been wrong all day** — a walk that reports
nothing may be reporting on nothing. So I ran the instrument against a branch I *knew* was not contained:

```
codex/fp-013-attendance                ancestor = YES
codex/fp-012-payroll                   ancestor = YES
codex/test-infrastructure-reliability  ancestor = YES
codex/post-phase-e-hardening-h1        ancestor = NO      <- the control
```

**The instrument distinguishes.** The uniform YES is a property of the branches, not of the query.

---

# WHAT THIS CLOSES

**`chain-test` was found by accident** — a failed `git worktree add` printed the branch list — **and it held a
broken money path nobody knew about.** That justified treating the whole list as unaudited work, and it was
the right call: **the second branch examined held a live discarded failure on the cutover's data-loss path.**

**Two of the first five held real defects. None of the remaining thirty-two holds anything at all.**

**So the risk was real and it is now bounded.** The list is audited end to end, and **the answer to *"is there
another `chain-test` in there"* is no** — not "probably not", not "we looked at some".

## The test that did the work

*"Does a branch assert something the tree ANSWERED, or something it FORGOT?"*

```
FORGOT     code the tree moved past without resolving      chain-test, post-phase-e-hardening-h1
ANSWERED   decisions the tree has since made and versioned the three docs branches
NEITHER    already merged                                  the thirty-two
```

**Age and conflict predicted nothing.** `fp-004-milestone-1` is the oldest branch here (8 Aug) and is fully
merged; `post-phase-e-hardening-h1` is nine days younger and held a live defect.

---

## For the architect

- **Blocked on you:** nothing.
- **Nothing deleted.** Thirty-two branches that are ancestors of `ClaudeBranch` can be pruned safely, **but
  pruning is an action on a public repository and it is not mine to take** — and it is worth almost nothing,
  since a contained branch costs a line of output and no risk.
- **The one thing this does NOT cover:** branches that exist only on the remote and not locally. **25 remote
  against 38 local**, and I compared the local set. A remote-only branch would be invisible to this sweep —
  **`git ls-remote` earlier showed 25 remote names all present locally, so I believe there are none, but the
  sweep proper was local.**
- **`DEC-L-069`:** all thirty-two were measured, not sampled, by two independent methods with a negative
  control.
