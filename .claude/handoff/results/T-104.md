# T-104 result — the class swept, and the `DEC-L-082` sweep lands entirely in T-105

- **Status:** BUILT AND PUSHED, **NOT MERGED.** Branch `agent/T-104-selfservice-class`, from `ClaudeBranch`
  at `f6117f9`, pushed on start.
- **Docs-only, non-gated** — `DEC-L-002` / `DEC-L-007`. `MERGE T-104` and it goes.
- **`trace-check`: FP-013 `[GREEN] 0/0`, FP-012 `[GREEN] 0 failures, 2 warnings`** — both unchanged from
  before the edit.

```
git diff --numstat
12 12   FP-013-attendance/authorization-model.md    five sites
 3  3   FP-012-payroll/authorization-model.md       one site
 2  2   FP-012-payroll/traceability-matrix.md       one site
```

**Balanced on every file. One line in, one line out throughout — no site needed an exception.**

---

# THE COUNTS, REPORTED BEFORE EDITING

```
PACKAGE   mentions   amended   untouchable (DEC-L-071)   not the class
FP-006        1         0                0                    1
FP-012        5         2                2                    1
FP-013       23         5               10                    8   (+1 held)
FP-014        5         0                0                    5
FP-015        —     EXCLUDED — pre-correction copies; editing them conflicts with #171
```

**45 mentions, 7 amendments.** The class is larger than T-086's four — **and it is not where the mention
count points.** FP-013's 23 mentions yielded five sites, all in one file; FP-014's five yielded none.

## What was amended

**FP-013 `authorization-model.md`, five sites** — the document's own summary (`:7`, "the self-service
blocker … the most important thing in this document"), the section heading, the "depends on an input the
product does not have" paragraph, the four-claim paragraph you scoped, and the *"What is deliberately
absent"* entry.

**The fourth claim survives and is now the only thing that entry asserts:** `Attendance.Leave.RequestOwn`
does not exist, because requesting one's own leave was never built. **Reading shipped; requesting did not**,
and the amended entry says exactly that instead of "no `ViewOwn` of any kind".

**FP-012, two sites** — carrying the precise form: **the permission was created; only the spelling was
refused.**

---

# ⚠ THE PREMISE THAT MOVED — PAYROLL SELF-SERVICE SHIPPED

**`Payroll.Payslips.ViewOwn` is declared, catalogued and routed.** Resolved at three definitions rather than
from recollection — `DEC-L-081`, one task after naming it:

```
PayrollPermissionNames.cs:91               public const string ViewOwnPayslips = "Payroll.Payslips.ViewOwn";
PayrollPermissionCatalogContributor.cs:52  catalogued
PayrollEndpointRouteBuilderExtensions.cs:122-123  GET /me/payslips  .RequirePermission(ViewOwnPayslips)
```

**`payroll.payslip.view.self` is refused because a permission name is three segments and that string is
four** (`PayrollPermissionNames.cs:82`) — *"the ruling's substance is untouched"*. The capability shipped.

**FP-012's T-086 note claimed the opposite and cited `AC-ATT-0032` as "the live assertion".** Both halves
false: the permission and endpoint are built, and `AC-ATT-0032` stopped asserting an absence in T-102.

---

# `DEC-L-082` — I SWEPT `AC-ATT-0032`'s CITERS, AND ALL THE ROT IS IN `src/`

**Eleven citers. Nine are correct; two are stale, and both are `src/`:**

```
AttendancePermissionNames.cs:107-109  "what keeps the absence true is ... that FP-015's permission and
                                       endpoint have not been built ... AC-ATT-0032 — the one thing that
                                       FAILS THE DAY A ViewOwn IS ADDED here"
LeaveRequest.cs:15-17                 "what defers self-service today is the absent PERMISSION and
                                       ENDPOINT ... AC-ATT-0032 is what FAILS THE DAY THAT CHANGES"
```

**A `ViewOwn` was added, and `AC-ATT-0032` did not fail — it was amended.** Both comments read the criterion
as an absence assertion, which is what it stopped being.

**And the irony is load-bearing.** `AttendancePermissionNames.cs:112` closes with *"Cited rather than
restated, because restating it in a third file is exactly how this sentence went stale in nine at once."*
**The comment that applied `DEC-L-080` went stale anyway — by citing an identifier whose meaning moved.**
That is `DEC-L-082` demonstrated by the sentence that was written to prevent it.

**Neither touched.** Both are `src/`, which makes the task gated and changes its merge rule. **They are
T-105, and the sweep has now handed T-105 its first two entries.**

---

## Three scoping calls, and what each turns on

**FP-014's five are not the class.** `OD-SUB-0013` ruled *tenant subscription* self-service out — a
different feature, still true. **A phrase sweep hits all five and deletes true statements** — `DEC-L-074`.

**FP-006's one is a Milestone 1 scope list**, true of M1.

**`FP-013/traceability-matrix.md:135` HELD.** Its table is headed *"Claim as first drafted | What the code
says"*, introduced by *"three claims were checked against the repository and two of them were wrong."*
**Minutes of a verification, historical by construction** — `DEC-L-071`, and amending it would rewrite what
the check found.

---

## For the architect

- **Blocked on you:** `MERGE T-104`.
- **T-105 has two entries already**, both from the `AC-ATT-0032` citer sweep, both the shape you described.
- **Not touched:** every `decisions-*` file, FP-015 entirely, and both `src/` sites.
