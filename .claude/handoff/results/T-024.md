# T-024 result — DONE

- **Status:** DONE
- **Branch:** `agent/T-024-repoint-adr-021` — pushed, branched from `main` at `a791578`
- **Merged:** no — non-gated (`DEC-L-002`). Awaiting `MERGE T-024`.
- **Commits:** `61332c8` — docs(T-024): re-point FP-014's `ADR-021` citations to `ADR-017` under `DEC-L-024`
- **Reported:** 2026-08-25

## Acceptance criteria

- [x] **`REQ-SUB-0005` cites `ADR-017`**, both line references, not `ADR-021:207`.
- [x] **`DEC-SUB-0004` re-pointed on the same basis**, with the two-step derivation written out.
- [x] **`decisions-ratified.md` records the re-point** — amendment banner, the `OD-SUB-0004` entry, open
      concern 3 closed, and a 1.1 Revision History row.
- [x] **Every surviving `ADR-021` mention in the edited files is labelled** as amended-from or as
      implementation-deferred. None stands as bare authority.
- [x] **`decisions-open.md:487` corrected** — `ADR-021` removed from the list of documents that
      constrain this package, with the reason stated.
- [x] **`trace-check` GREEN, 0 unruled**, run with every other package present.
- [x] **`--stat` shows four files**, all under `docs/17-features/FP-014-subscription/`.

## The mechanical check

```
FP-014-subscription  [GREEN]  0 failure(s), 0 warning(s), 4 declared gap(s)
Owner decisions: 17 defined, 0 unruled -> ratified, build may proceed
```

**The full 14-package run is byte-identical to the post-ratification baseline from T-021** — `diff`
returns nothing at all. That is the right result for this change and worth stating as the check it is:
an authority re-point should move **no identifier, no count, no warning and no gap**. If any line had
moved, the re-point would have changed something it was not supposed to touch.

## I verified your line references and one is off by two

You gave `ADR-017:162` for subscription/plan metadata. **It is `:164`.** `:162` is *"Global catalogs and
reference definitions, including `LocalizationCatalogState`."* I cited `:164` and read each passage
rather than trusting the numbers — `:169`, `:171` and `:376`–`:378` are all exactly as you described.

**The derivation holds completely.** Both facts sit in an `Accepted` ADR:

1. **`ADR-017:164`** — *"Subscription/plan metadata when introduced"* sits in the list of what the
   Platform database holds.
2. **`ADR-017:169`** — keeping that data in the Platform database means *"authentication remains a
   single-database operation"* and *"never depends on tenant-database routing or availability"*;
   **`:376`–`:378`** complete it — an unavailable tenant database yields *"a controlled unavailability
   result"*, never a fallback.

## The cost, and a hazard in the derivation that I checked before relying on it

**The cost you asked me to name is written into `DEC-SUB-0004`, not just this result.** `ADR-021:207`
said *subscription* in the word. `ADR-017` never puts the two facts side by side, so the derivation is
now an inference across two passages instead of a quotation from one. A reader who expected the obvious
citation now sees why it was dropped rather than assuming it was overlooked.

**And one thing I checked before treating the derivation as sound.** `ADR-017:171` — the passage that
states the rule most forcefully — opens *"Customer-managed hosting makes this decisive."* If that were
the only support, the re-point would have smuggled the deferred decision back in through its parent,
and the repair would be circular.

It is not. **`:169` states the independence unconditionally**, for any tenant database, as a
consequence of Platform-database residency alone; `:171` explains what makes it *decisive*, not what
makes it *true*. `:376`–`:378` are likewise unconditional. **I cited `:169` and `:376`–`:378` and left
`:171` out**, and said so in the file, because a derivation that reads well but routes through
customer-managed hosting would have been the same defect wearing a different hat.

## Scope — the citation is in six files and the cap is four

**Stated plainly because it means the job is not finished.** You anticipated *"one more within the
package if a citation is found there"*. There were **three** more.

| Site | What it says | Done? |
|---|---|---|
| `requirements.md:44` `REQ-SUB-0005` | Authority column | **re-pointed** |
| `requirements.md:67` `REQ-SUB-0013` | Authority column | **re-pointed** |
| `decisions-open.md` ×5 (`:28`, `:68`, `:197`, `:240`, `:487`) | `DEC-SUB-0004` and four arguments | **re-pointed** |
| `decisions-ratified.md` | `OD-SUB-0004`, concern 3, banner, 1.1 row | **amended** |
| **`business-rules.md:76` `BR-SUB-0008`** | Authority column | **re-pointed** |
| `data-model.md:21` | *"it is load-bearing, not incidental — `ADR-021:207` requires…"* | **NOT DONE** |
| `README.md:160` | *"`ADR-021` requires the subscription surface to survive…"* | **NOT DONE** |
| `README.md:17` | *"three concerns are open… the still-`Proposed` `ADR-021`"* | **NOT DONE — now stale** |

**I chose `business-rules.md` as the fourth file over `data-model.md`, and the reason is your own
sequencing.** You said you want FP-014's citations sound *before its rules go into governing documents*.
`BR-SUB-0008` is a **rule**, its Authority cell named `ADR-021:207`, and T-022 promotes `BR-SUB-0001`…
`0021` into the master `Business-Rules.md`. Leaving that one would have put a `Proposed` ADR's citation
into a governing document by the very next task — which is the failure the sequencing exists to prevent.

**`README.md:17` is stale because of me.** I wrote that line in T-021, and closing concern 3 makes it
wrong: the README still says three concerns are open. It is a two-word fix and I did not take it,
because the cap is a stated acceptance criterion and exceeding it on my own judgement is not mine to do.

**Recorded inside the package, not only here.** `decisions-ratified.md`'s closed concern 3 names
`data-model.md:21` and `README.md:160` as not yet re-pointed, so a reader of the package finds the
remainder without reading this result.

## For the architect

- **Blocked / needs a decision:** authorise the remaining three sites — `data-model.md:21`,
  `README.md:160` and `README.md:17`. One small task, or an extension of this branch before merge.
  **Until then FP-014 still asserts `ADR-021` as binding in two places**, which is the condition
  `DEC-L-024` was ruled to end.
- **Touched outside scope:** nothing. Four files, all inside the package. `ADR-021` untouched —
  still `Proposed`, content unchanged, which was the whole point. `ADR-017` untouched.
- **No requirement, rule or ruling changed in substance.** `REQ-SUB-0005` still says the subscription
  surface stays readable and administrable during a tenant-database outage, in the same words.

### On `DEC-L-025`

Read and in force. **I have seen no usage warning in my own context** — stated because the rule says
not to assume the other window has seen what I have, and the same holds in reverse. T-024 was carried
to a pushed branch and a full report rather than stopped mid-way, per step 1.
