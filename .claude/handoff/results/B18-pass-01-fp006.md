# B18 pass 01 — FP-006: 20 examined, 10 cited

**Standing backlog entry, not a queue row. Stoppable by design, and stopped here.**
**TASK gate green, 0 warnings. Control: 107 cited across all packages, ZERO dangling.**

## The two numbers

| | |
|---|---|
| criteria **examined by body** | **20 of 47** (`AC-EMP-0001`…`0020`, file order) |
| **cited** | **10** — `0005`, `0006`, `0008`, `0010`, `0011`, `0012`, `0014`, `0015`, `0017`, `0019` |
| ⚠ found **uncovered** | **0** |

**FP-006 goes 1 → 10 Trait-claimed.** This pass added six beyond item 218's four.

## What was added this pass

- **`AC-EMP-0011`** *(every persisted Employee string column is `nvarchar`)* →
  `Every_persisted_string_in_the_tenant_model_is_unicode`. ⚠ **A SUPERSET citation, and the comment says
  so**: the guard walks the whole tenant model, Employee is tenant-owned and cannot escape the walk. **No
  narrower Employee-only assertion exists, and it would be weaker if it did.**
- **`AC-EMP-0012`** *(the listed transitions are permitted)* → `The_approved_transitions_are_permitted`,
  which walks Active→Inactive→Active→Terminated asserting success at each step.
- **`AC-EMP-0015`** *(a `Terminated` employee cannot be updated, activated, deactivated, transferred)* →
  **three tests**, one per clause. ⚠ **The criterion's *"remains retrievable by id"* clause is pinned by
  NONE of them, and the citation comment records that** — a partial pin, stated rather than implied.
- **`AC-EMP-0017`** *(no physical deletion)* → `An_employee_cannot_be_physically_deleted`, against the real
  `TenantDbContext`. ⚠ **Also partial**: the criterion additionally bans a delete command, repository
  method, permission and endpoint from **existing**, and that structural half is asserted nowhere here.
- **`AC-EMP-0019`** *(stale rowversion returns a conflict)* → `A_transfer_with_a_stale_rowversion_is_refused`.

## ⚠ Two candidates rejected before citing

**`A_stale_row_version_is_refused_on_every_family`** and
**`A_stale_row_version_is_rejected_for_both_lifecycle_transitions_without_mutating_state`** both match
`AC-EMP-0019`'s words closely. **They are Position and PlatformSupport tests.** ⚠ **Opening the files was
the only thing separating a correct citation from a confidently wrong one** — the third near-miss of this
kind, after a heading and a synonym.

## ⚠⚠ A METHOD FAULT OF MINE, THIRD OCCURRENCE TODAY

I keep writing `echo "[empty = no such test]"` **after** a grep. **The echo prints unconditionally.** Three
times today it has told me a non-empty result was empty — most recently claiming FP-006 had no
concurrency or deletion test **while the grep above it listed five**, two of which became citations.

**A label that does not depend on the result is not a reading of the result.** ⚠ **Same family as the
stale-binary green: a positive that was never conditioned on anything.**

## The running picture

| package | defined | Trait-claimed |
|---|---|---|
| FP-002 | 51 | 19 |
| FP-003 | 93 | 13 |
| FP-004 | 64 | 0 |
| **FP-006** | **47** | **1 → 10** |

**Still zero uncovered criteria found, across four packages of spot-checks and twenty full body readings.**

## Judgement on continuing

**Worth continuing.** The yield held at this batch size — **6 citations from 10 criteria**, the same rate as
item 218's 4 from 10 — and the near-miss rate is high enough that the mappings are genuinely not
recoverable by grep later.

⚠ **The remaining 27 include the harder half.** Criteria 1–20 are creation, identity and lifecycle; the
tail covers transfer channels, authorization edges and inventory assertions, where the criterion is more
likely to be pinned by an architecture test than a domain one — **which is where the superset and partial
citations above will become the normal case rather than the exception.**

**Stopped here rather than pushing on**, per standing permission: the value is per-citation and banked.
