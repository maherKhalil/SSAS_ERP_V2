# T-127 — **identical. Token-for-token, all seven methods. Trivial removal, and the finding is elsewhere.**

- **Status:** BUILT, GREEN, **MERGE PENDING** under `DEC-L-007` (gated).
- **`GATE_SCOPE=TASK` GREEN, 0 warnings.** Condition 4 ATTENTION — judged below.
- Branch `agent/T-127-localization-strict-reader`, from `ClaudeBranch`.

---

# THE DIFF, DONE FIRST AND MECHANICALLY

**Not one method — SEVEN.** Localization carried private copies of the whole reader:

```
ReadStrictJsonAsync   5 call sites      HasOnly     5      TryRequired  7
TryOptional           4                 TryInt      3      TryBool      2      IsOneOf  4
```

**Token-diffed rather than eyeballed**, normalising two differences noted separately:

```
BuildingBlocks tokens: 263   Localization tokens: 256
only difference: brace-vs-braceless single-statement `return null;`  x4
```

**The seven are behaviourally identical.** The two real differences are both non-behavioural:
`ArgumentNullException.ThrowIfNull` on `context`/`fields`, **neither of which can be null on a route path**,
and `IReadOnlyDictionary` vs `Dictionary` in a parameter position **whose call sites all pass dictionary
literals**.

**So: identical → trivial removal, as you specified.** Five routes were NOT validating differently. Nobody's
assumption was wrong.

---

# ⚠ WHAT WAS ACTUALLY WORTH FINDING

```
route groups already binding through StrictRequestReader   13
the one holdout                                             Localization
```

**And BuildingBlocks' own header says why:** *"Mirrors the established localization strict-parsing
convention."* **The shared reader was EXTRACTED FROM Localization, and Localization was the only group never
migrated onto the extraction taken from it.** Every other module adopted it; the origin kept its copy.

**No other private copy exists anywhere in `src/`.** Checked, not assumed.

---

# THE REMOVAL IS PROVED, NOT ASSERTED

**`DEC-L-070` — the five routes going green after the swap is not evidence they run through the new path.**
So I broke the SHARED reader and watched Localization fail:

```
PLANT  StrictRequestReader: unknown/duplicate/wrong-kind members no longer rejected
       -> Failed: 11, Passed: 38   (LocalizationEffectiveApiTests.Batch_rejects_... among them)
RESTORE by inverse edit -> green, and `git diff` on the reader is EMPTY
```

**That proves both directions at once:** the five routes really bind through the shared reader now, **and** the
existing tests really cover the behaviour rather than merely existing.

**Restored by inverse edit, not `git checkout --`** — T-109's lesson, and there were live edits in the tree.

**A pleasing side effect:** the covering test is named
`Shared_strict_json_binding_rejects_unknown_duplicate_missing_and_wrong_typed_fields_before_handlers`. **It
said "shared" while the routes used a private copy. The name is now true.**

## Condition 4, judged

```
totals unchanged in all 7 suite(s) while 98 non-comment line(s) under src/ changed
```

**Correct and expected. A de-duplication of token-identical code introduces no behaviour for a test to
observe**, and the movement is 98 lines of deletion plus call-site qualification. **Unusually, this is not a
judgement call: the plant is direct evidence that the existing tests exercise the changed path.**

---

# YOUR QUESTION — THE INVENTORY AS ENFORCEMENT POINT. **THE PREMISE HAS A HOLE, AND IT IS WHERE WE HAVE BEEN WORKING.**

**You asked whether route inventories are the place to attach wire-contract coverage. They exist and are
maintained, as you said — for FOUR module groups:**

```
AttendanceRouteInventoryTests   HrRouteInventoryTests   GlRouteInventoryTests   PayrollRouteInventoryTests
```

**Platform has SEVEN route groups and NO inventory at all.**

```
Authentication  PlatformSupportAuthentication  Company  IdentityAccess  Localization
PlatformSupportAuthority  TenantUsers
```

**Both of this week's invisibility findings are in Platform.** T-126's routed unseeded handler is in
`PlatformSupportAuthentication`; T-127's duplicate reader is in `Localization`. **Attaching the obligation to
the inventories would leave uncovered precisely the assembly where the problem was found both times.**

**So my answer is: not yet, and the gap is the finding rather than the mechanism.** Extending the inventory
to Platform is worth a task on its own merits — it is a real hole in a guard we both believed was
product-wide — and **whether a coverage column is cheap is a question I would rather answer against an
inventory that covers everything than against one that covers 4 of 11.**

**I have not measured the cost of the column itself and I am not going to guess it.** The honest concern is
`DEC-L-080`'s: the existing inventories stay current because the test compares them against the ACTUAL route
surface, so drift fails. **A coverage column has no equivalent self-check unless it reflects over test
methods**, and that is the brittle part I would want to prototype before recommending.

## And your residual risk is right, with one thing to add

**A wire-contract test written by the task adding the route is a habit, and the seed list was a habit too.**
Three exist today — `EmployeeErrorWireContractTests`, `PositionChangeErrorWireContractTests`,
`PayrollCalculatorErrorWireContractTests`.

**Your bound is the reason it is still better, and it is worth writing down as you said:** a missing seed made
the register **systemically silent**; a missing wire-contract test costs **exactly one route group's error
mapping**. **Bounded failure instead of unbounded silence.**

---

## For the architect

- **Blocked / needs a decision:** whether Platform's missing route inventory becomes a task. **I did not
  build it** — it is scope you have not given me, and it is bigger than a comment.
- **Nothing about the five routes changed.** That is the whole result, and it is the outcome you named as
  possible.
- **`DEC-L-069` on my own sweep:** the "no other private copy" check is a full grep over `src/` for the three
  declaration signatures, not a sample. The inventory count is a full `find`.
