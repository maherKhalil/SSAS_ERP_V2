# T-031 result — DONE

- **Status:** DONE
- **Branch:** `agent/T-031-last-anchors` — pushed, branched from `main` at `a9e1cdc`
- **Merged:** no — non-gated (`DEC-L-002`). Awaiting `MERGE T-031`.
- **Commits:** `35dba5a` — docs(T-031): the last two live line citations in FP-014
- **Reported:** 2026-08-26

## Acceptance criteria

- [x] **Both converted** — `ADR-005` § Platform Administration (`:248`).
- [x] **The four deliberately-left citations stay left**, restated below.
- [x] **`ADR-0##:<line>` appears nowhere in FP-014 as a bare handle.** Grep pasted.
- [x] **`trace-check` byte-identical** to the T-030 baseline; FP-014 GREEN `0 unruled`.
- [x] `--stat` — exactly two files, 2 insertions, 2 deletions.

## The closing grep

```
decisions-ratified.md:208:`ADR-021:207`     ← framed historical block
decisions-ratified.md:222:`ADR-021:37`      ← framed historical block
decisions-ratified.md:268:`ADR-017:164`     ← Revision History row
decisions-ratified.md:269:`ADR-017:164`     ← Revision History row
```

**Four occurrences in the whole package, all in one file, none in live prose.** The classification is
mechanical rather than asserted: `# Revision History` begins at line **263**, so `:268` and `:269` are
below it and `:208`/`:222` are inside the framed *"describes the position BEFORE the amendment"* block
that opens concern 3 at `:194`.

**Both exclusions are deliberate and both should stay.** A revision row records what was cited at the
time; the framed block records what the package cited before `DEC-L-024` moved it. Rewriting either
would make a record report something that did not happen — which is the defect the whole arc from
`DEC-L-027` onwards exists to prevent.

## I re-verified rather than trusting my own T-030 result

The spec said both are fragile-not-wrong *because I had verified it*. I checked again before
converting, because a task that treats last week's verification as current is the same class of error
as a citation that treats last week's line number as current:

```
ADR-005:248  →  "- Subscription management"          ← still exact
git log -1   →  4753a85 "last documents"             ← ADR-005 unedited since
```

Had `ADR-005` moved in the interim, the correct action would have been a correction, not an anchor —
and I would not have known.

## Both checks re-run

- **Parenthesis balance:** both files balanced, **0 groups wrapped across lines before and after**.
  Neither citation sat inside an existing parenthesised group, so this pass could not have collided —
  but that is a fact I established by checking, not one I assumed from the small diff.
- **`trace-check`:** byte-identical to the T-030 baseline. Fifth consecutive citation task where the
  full 14-package run does not move.

## What this closes

`DEC-L-028` is now fully applied inside FP-014. Every citation into an ADR leads with a section that
cannot rot, and keeps its line number as the convenience a 819-line document needs.

**Two instances started this, and I caused both** — T-011 shifted `ADR-017`, T-022 shifted
`Business-Rules.md`. The package is now immune to the third.

### The `ADR-021` note, restated because it will confuse someone

`ADR-021`'s citations are **anchored** while `ADR-021` itself remains **`Proposed`**. These are
unrelated facts and the anchoring does not soften `DEC-L-024`:

- **An anchor describes a passage.** `ADR-021` § 10 Outage behaviour is where that text lives, whatever
  the ADR's status.
- **`DEC-L-024` removed `ADR-021` as an authority.** Nothing in FP-014 derives from it; every
  derivation runs through `ADR-017`, which is `Accepted`.

**An anchored citation to a `Proposed` ADR is a reference, not a dependency.** The two would be easy to
conflate on a later reading, and conflating them would look like the re-point had been quietly undone.

## For the architect

- **Blocked / needs a decision:** none.
- **Touched outside scope:** nothing. Two files, two lines. No Revision History row altered, no file
  outside FP-014, nothing under `src/` or `tests/`.
- **The eight `ADR-0##:<line>` citations elsewhere in the repository are untouched** and remain
  undecided as a repository-wide act, as your scope says. **I have not re-counted them since T-029** —
  if that number matters to a future task, it should be re-derived rather than inherited from me, given
  I have already reported one count in this arc that was wrong.

### On `DEC-L-025`

**Still no usage warning in my context.** Reported fresh, and it stays fresh each turn rather than
carried.

### Ready

Idle and holding for the owner's direction. Not picking my own work.
