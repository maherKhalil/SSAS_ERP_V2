# T-029 result — DONE

- **Status:** DONE
- **Branch:** `agent/T-029-citation-anchors` — pushed, branched from `main` at `cffdcdb`
- **Merged:** no — non-gated (`DEC-L-002`). Awaiting `MERGE T-029`.
- **Commits:** `e6026c9` — docs(T-029): stable citation anchors, and `DEC-L-026` in the amendment record
- **Reported:** 2026-08-25

## Acceptance criteria

- [x] **README's `Business-Rules.md` citations converted to stable anchors** — and four others with them.
- [x] **`decisions-ratified.md` applies `DEC-L-026`**, recording the number as having arrived *after*
      the amendment rather than back-dating it.
- [x] **Other line-number citations converted or reported**, with the judgement calls stated.
- [x] **`trace-check` byte-identical** to the T-027 baseline; FP-014 GREEN `0 unruled`.
- [x] Three files, all under `docs/17-features/FP-014-subscription/`.

# I have to correct my own report, and it changes who made the `:162` mistake

**I told you your `ADR-017:162` reference was "off by two". That was wrong, and the error was mine.**

`git show a234d6b~1` — ADR-017 immediately before my T-011 commit:

```
:162  - Subscription/plan metadata when introduced.      ← your citation, EXACTLY right
:475  - **A — Platform global**: … subscription plans …  ← the other one, also right
```

**Both of your line numbers were correct when written.** My T-011 commit added three lines to
`ADR-017`'s Status block — the `**Accepted**` line and the Evidence paragraph — and everything below
shifted. `:162` became `:164`; the Class A bullet became `:477`.

So I reported a rotted citation as an authoring error, you accepted the characterisation, and it went
on the board as your mistake. **It was mine.** Please correct that entry.

**And it makes `DEC-L-028` better founded than either of us thought.** The rule now has two instances,
and I caused both: T-011 shifted `ADR-017`'s lines, T-022 shifted `Business-Rules.md`'s. The first one
I then misdiagnosed as somebody else's typo — **which is precisely the failure mode the rule predicts.**
A rotted citation does not look rotted. It looks like a citation that was always wrong, and the
person who broke it is the least likely to suspect themselves.

## What I converted

**The pattern: identifier or section first, line number beside it as a convenience.**

| Was | Now |
|---|---|
| `Constraints.md:21` / `:23` | `Constraints.md` § `CON-0001` |
| `Business-Rules.md:162` / `:168`–`:170` | `Business-Rules.md` § `BR-PLT-0008` |
| `Glossary.md:283` | `Glossary.md` § **Subscription** |
| `Tenant-Management.md:35` | `Tenant-Management.md`'s Business Rules section |
| `ADR-005:248` | `ADR-005` § Platform Administration |
| `ADR-029` Decision 1 | `ADR-029` § Decision 1 |
| `ADR-021:207` | `ADR-021` § 10 Outage behaviour |
| `ADR-017:164`, `:169` | `ADR-017` § Platform database boundary (`:164`, `:169`) |
| `ADR-017:376`–`:378` | `ADR-017` § No automatic fallback (`:376`–`:378`) |
| **`ADR-017:162`** ← wrong content | `ADR-017` § Platform database boundary (`:164`) |
| `ADR-017:475` | `ADR-017` § Lookup classification, class **A — Platform global** (`:477`) |

**I kept every line number**, per your constraint. `ADR-017` is 819 lines and § Platform database
boundary is long; a reader wants `:169`. The rule is that a line number must not be the *only* handle —
so it now sits beside a heading that cannot rot.

### The subtler thing the README conversion fixed

The survey table is stamped **"Run 2026-08-25 against `main` at `f9b247a`"** — correctly dated, exactly
the `DEC-L-027` pattern. But T-027 "corrected" its line numbers to current ones, so **a dated survey was
citing lines that did not exist at the commit it names.** Correcting them was wrong and leaving them
wrong was also wrong.

**An identifier dissolves it** — `BR-PLT-0008` was true at `f9b247a` and is true now. That is the
argument for `DEC-L-028` that I had not seen: an anchor is not merely more durable, it is the only
citation form that survives being quoted inside a historical record.

## Two defects found, and they are not the same kind

- **`ADR-017:162` was WRONG** — it pointed at *"Global catalogs and reference definitions, including
  `LocalizationCatalogState`"* while claiming to place subscription metadata. Different content, in
  `data-model.md` and `domain-model.md`.
- **`ADR-017:475` is IMPRECISE, not wrong** — it points at the `## Lookup classification` heading, and
  the Class A bullet it quotes is at `:477`. A reader lands two lines above the claim.

Worth separating, because the second would survive a checker that verified citations resolve to
plausible content, and the first would not.

## What I did not reach — the exposure, exactly

Your cap is three files; the `ADR-017` citations run wider. **Eight `ADR-017:475` and one `ADR-017:162`
remain, in three files:**

| File | Citation | Kind |
|---|---|---|
| `domain-model.md:50` | `ADR-017:162` | **wrong content** — same defect I fixed in `data-model.md` |
| `domain-model.md:65`, `:103` | `ADR-017:475` | imprecise |
| `decisions-open.md:27`, `:261` | `ADR-017:475` | imprecise |
| `requirements.md:41`, `:42` | `ADR-017:475` | imprecise |

**`domain-model.md:50` is the one worth taking first** — it is the same wrong-content defect, not the
imprecise one, and it sits in an argument about `PlatformDbContext` having no guard.

**`ADR-005:248` I verified rather than assumed** — `ADR-005` has not been edited, `:248` still reads
*"- Subscription management"*, and the five remaining uses are correct. Left alone; they are accurate,
merely fragile.

## Judgement calls

**1. Revision History rows left as written.** `decisions-ratified.md`'s `1.1` and `1.2` rows cite
`ADR-017:164` and `:169`. **I did not convert them.** A revision row records what was done and cited at
the time; rewriting it would make the history report something that did not happen. The `1.4` row says
so explicitly, so it reads as a decision rather than an oversight.

**2. `DEC-L-026` recorded forward, not back-dated.** Your constraint said the record should say what
happened. It now reads that the amendment was made in T-028 with no identifier — *because* you had
ruled without assigning one and minting a `DEC-L` is not the coder's act — and that the number arrived
with T-029. **Back-dating it would have reproduced, one level up, the exact relic `BR-SUB-0020` was.**

**3. `data-model.md` chosen as the third file.** It held the only *wrong-content* citation, and it is
where residency is "settled before any table is drawn". While in it I also fixed `:70`'s `ADR-017:475`,
which was not in the criteria — leaving one stale citation in a file I had just repaired would have
been this rule's own failure.

## For the architect

- **Blocked / needs a decision:** none. Two follow-ups if you want them — the nine remaining `ADR-017`
  citations above, and correcting the board entry that records the `:162` error as yours.
- **Touched outside scope:** nothing. Three files, all inside the package.

### On `DEC-L-025`

**Still no usage warning in my context.** Reported fresh.
