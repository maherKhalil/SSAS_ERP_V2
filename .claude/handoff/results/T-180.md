# T-180 — **schema assertions: 0 wrong in 40. Citations: 1 wrong in 70, and it was four hours old.**

- **Status:** item 25, Attendance and GL. **Stopping here for the rate, as instructed.** One repair merged with it.
- Branch `agent/T-180-comment-schema-sweep`, from `ClaudeBranch`.

---

# THE RATE

```
                        examined   wrong   rate
schema assertions             40       0    0%
citations                     70       1    1.4%
                        ---------
comment lines in scope       952
files in scope                16      (Attendance + GL, persistence and handler layers)
```

**The floor is the point: 952 comment lines were read to find 40 assertions and 70 citations.** A zero from
a sweep that examined nothing looks identical to this one, which is why the denominators are here.

---

# 1. SCHEMA ASSERTIONS — ALL FORTY HOLD

**The three known instances were all found earlier today and all are already repaired**
(T-112's pre-filter guard comment, T-172's "the index does not exist", T-166's "the only trace"). **No
fourth exists in Attendance or GL.**

The ones worth naming, because they are ABSENCE claims — the class that was wrong twice today:

```
"AttendanceRecords carries no unique index on (Tenant, Employee, Date)"   TRUE — two indexes, neither unique
"The search column. No index"  (LeaveType.NormalizedName)                 TRUE — the only index is on Code
"no index can express overlap" (Attendance and GL, four sites)            TRUE — DEC-L-084, and both modules
                                                                          state it at the index that does exist
"the chart is TENANT-level, so uniqueness is asked without a company"     TRUE — UX_GlAccounts_Tenant_NormalizedCode
```

**Each was checked by reading the whole `Configure` block, not by grepping the entity name** — the method
that failed on the leave balance.

---

# 2. CITATIONS — ONE BREAK, AND I INTRODUCED IT THIS AFTERNOON

```
CalendarCommandHandlers.cs:60   `ICalendarRepository.NameExistsAsync`   -> no such type
```

**The type is `IWorkingCalendarRepository`. I wrote that citation in T-176, four tasks ago**, from a
template where I typed the interface name from memory rather than from the file. It named nothing, and
nothing noticed — the same shape as `PayrollApiErrorMapper.cs:110`, which cited a code that had been
deleted three tasks earlier.

**Two instances, both mine, both from today.** The class is not historical debt: **it regenerates, and it
regenerates fastest in the comments written to explain a fix.**

## The other flagged row is my instrument's error, not the code's

`AccountConfiguration.cs:20` cites `Constraints.md`, which **exists** at
`docs/00-Master-Product-Specification/Requirement-Catalog/Constraints.md`. My checker resolved filenames
against `.cs` files only. **Reported as a false positive rather than silently dropped**, because the
denominator matters more than the tidiness of the finding.

---

# ⚠ 3. THE CHECKER CONFIRMED ITSELF ON THE FIRST RUN, AND REPORTED ZERO

**The first version resolved citations against a blob that included COMMENTS.** A comment citing a dead
identifier therefore found that identifier — in its own line. **It reported 0 unresolvable out of 70.**

Stripping comments before resolving found **2**. So the honest sequence is: a clean zero, a suspicion, and
a real result — and the suspicion came only from having spent the day watching instruments agree with
themselves.

**This is `DEC-L-070` at the instrument level: the checker was not evidence about the citations, because
the citations were part of what it searched.** It is the same shape as verifying a claim at the coordinates
the claim supplies.

---

## For the architect

- **The rate is worth stopping on, and it says continue for citations and not for schema assertions.**
  0-for-40 on schema claims in the two modules with the most database reasoning is a real acquittal; the
  three known instances were the population, not a sample of it.
- **1-in-70 citations, with both known breaks authored today**, argues the citation class is worth a guard
  rather than a sweep — it is mechanically checkable, which is exactly what a sweep proves and a guard
  enforces. **I have not built one**; say the word and it is the same shape as the api-contracts row guard.
- **`DEC-L-069`:** 952 comment lines read to produce the two denominators; every flagged row adjudicated
  individually, including the one that turned out to be my own checker's fault.
