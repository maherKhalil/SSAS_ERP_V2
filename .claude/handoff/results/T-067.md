# T-067 result — the floor scoped to its evidence, and the owed run closed

- **Status:** PENDING MERGE. **Gated by effect** — the owed `GATE_SCOPE=TASK` run is in this result.
  **Not self-merged.**
- **Branch:** `agent/T-067-floor-scope`, from `ClaudeBranch` at `d21ed13`
- **T-065's PARTIAL is CLOSED.**

## Authorisation, stated because it has to be

**The owner instructed this and went further than the architect ruled.** The architect proposed scoping
the floor and measuring a `TASK`-calibrated replacement; the owner ruled **no floor at all when
Integration is out of scope**, which the architect had explicitly declined to infer.

**I put it to the owner rather than acting on the relay.** This is the one action I refused earlier
today on charter grounds, and the instruction reached me from a peer and **widened** what I could do
rather than narrowing it — the case my own rule this morning says goes to the owner. It did.

**No override variable was used.** `GATE_MEMORY_FLOOR_MB` is untouched; the floor now applies where its
evidence came from, so the run passed its preconditions honestly.

## The finding: every number behind the floor is an Integration number

```
the deaths it exists to prevent    14 MB and 92 MB free    BOTH Integration legs, no TRX
the four healthy measurements      551-1510 MB min_free    all Integration legs
peak testhost working set          564-641 MB              Integration
the sampler that produced them     runs ONLY for Integration
```

**So no `TASK` run's memory had ever been measured, because the only instrument that measures it does
not run for one.** The floor was calibrated on the heaviest leg and enforced on the lightest run.

It refused a run at **1072 MB free on a box whose own browsers held 1.4 GB** — and nothing about that
run was at risk.

**The free figure is still printed every leg.** Observation without assertion: a future `TASK` death
gets a reading beside it, which is exactly what the 2026-08-24 deaths did not have.

**Derived from the suite list, not the scope name**, so `GATE_INTEGRATION=1` under `TASK` keeps the
floor:

```
GATE_SCOPE=TASK  GATE_INTEGRATION=0  -> floor applies: 0
GATE_SCOPE=TASK  GATE_INTEGRATION=1  -> floor applies: 1
GATE_SCOPE=PHASE                     -> floor applies: 1
```

## The owed run

```
--- free physical memory before Debug: 1954 MB (no floor: Integration is not in scope)
    0 Warning(s)   0 Error(s)
Architecture 509 · Platform 1037 · HR 326 · API 724 · Finance 46 · Payroll 56 · Attendance 59
--- condition 4: ok: no non-comment change under src/; 7 suite total(s) checked
TRACE BASELINE — 15 package(s) unchanged, 11 failure(s) standing
[GATE GREEN -- TASK scope: seven suites, NO Integration, Debug only]     exit 0     82 seconds
```

**2757 tests, every suite on baseline.** `trace-check` held at 11 and neither baseline file changed —
which is the end-to-end proof T-065 could only harness: **the wiring sits correctly in the run and the
verdict reads green.**

## The measurement, and it complicates the story rather than confirming it

110 samples taken from **outside** the gate while it ran:

```
testhost peak working set    314 MB      against 564-641 MB for Integration legs
free physical memory         min 262 MB, max 2220 MB
```

**The suite's own footprint is half an Integration leg's**, which is what the ruling assumed.

**But the box was observed at 262 MB free during the window** — lower than any Integration reading ever
recorded, including the 551 MB that note 7a calls the lowest seen. **I cannot attribute that to the
gate**: the sampler ran ~99 s against an 82-second gate, and the box's browsers move by hundreds of
megabytes on their own.

**Reported because it cuts against the conclusion, not despite it.** What it establishes is narrower
and stronger than "TASK is light": **a `TASK` run completed green while the box was at 262 MB free.**
The floor would have refused to start it at any point below 2048, and it was never at risk — which is
the owner's ruling demonstrated rather than assumed.

**The raw sample file was not written.** `Export-Csv` failed with `FileOpenFailure` and only the
computed min/max/peak survive. Said plainly because a summary whose underlying data no longer exists is
weaker evidence than one whose data does, and the difference is invisible in the numbers above.

## For the architect

- **Blocked / needs a decision:** nothing. **T-065's PARTIAL is closed by the run above.**
- **Touched outside scope:** nothing. `scripts/gate.sh`.
- **Open, and smaller than it was:** a `TASK`-calibrated floor is no longer needed for the ruling, but
  **314 MB peak testhost and 262 MB observed free** are the first `TASK` numbers this repository has,
  and they belong beside note 7a's Integration figures if anyone ever wants one.
