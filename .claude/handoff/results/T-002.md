# T-002 result — DONE

- **Status:** DONE
- **Branch:** `agent/T-002-fp-014-foundation` — pushed: yes (`origin/agent/T-002-fp-014-foundation`)
- **Commits:** `f0e349c` — docs(T-002): FP-014 Subscription foundation — sweep, requirements, decision register
- **Reported:** 2026-08-25

## Acceptance criteria

- [x] **`README.md` exists and opens with the sweep, in the FP-013 manner — what was searched, what
      was found, what the false positives were; stating with paths that GL's trial balance and
      Attendance's leave entitlement are not commercial-plane code, and that `Tenant` carries no
      commercial state.**
      The sweep is an eleven-row table, then a *false positives, named so nobody re-derives them*
      block giving the four classes with file and line. `Tenant` is covered by its own row and by the
      `TenantStatus` row beneath it. The boundary section precedes the sweep, per the design
      constraint below.
- [x] **`requirements.md` states requirements with a `Scope`, an `Authority` column citing the
      authorising document, and the decisions each depends on; `BR-PLT-0008`'s menus-and-APIs clause
      carried as a requirement, not as prose.**
      28 rows in seven tables. The clause gets its own section, *Enforcement — `BR-PLT-0008`, second
      sentence*, quoting it and splitting it into six requirements: `REQ-SUB-0011` (APIs),
      `REQ-SUB-0014` (menus), plus uniformity, the platform-plane exemption, the permission-catalog
      surface and data survival. `Authority` reads **UNAUTHORED** on 11 rows and
      **NON-AUTHORITATIVE** on 1, rather than citing something that does not carry the weight.
- [x] **`decisions-open.md` carries two registers on the FP-013 pattern; every `OD-SUB` states options
      and consequences; none carries a default.**
      12 `DEC-SUB` (settled by inheritance or mechanical proof) and 17 `OD-SUB`. Every `OD-SUB` has an
      options table or an enumerated option list with the consequence of each. The preamble is the
      FP-013 one, extended by one clause: *"Where this package has no opinion worth offering, it says
      that too rather than manufacturing one to fill the space"* — which `OD-SUB-0002` then does.
- [x] **The identifier-space question is itself an `OD-SUB`, argued both ways and not silently decided.**
      `OD-SUB-0002`. Four arguments for a new `REQ-SUB` prefix, five for extending `REQ-PLT`, two
      sub-questions (`BR-` prefix, catalog file), and **the opinion is explicitly withheld** because
      the two cases turn on how large the plane is, which is `OD-SUB-0001`. Verified that
      `Requirement-Numbering.md` lists ten functional prefixes — PLT, HR, GL, INV, CRM, PRJ, PAY, ATT,
      PRC, MFG — and that `SUB` is not among them. **The file was not edited.**
- [x] **`git diff main...agent/T-002-fp-014-foundation --stat` shows only files under
      `docs/17-features/FP-014-subscription/` and no file anywhere else.** Output below — three files,
      all under that directory, all additions.

## What changed

Three files, 858 lines, one new directory. No file outside it.

**`README.md` (222 lines).** The boundary section comes *first*, before the sweep, because the
conflation it prevents would invalidate everything after it. Then the sweep, then *What authority
actually exists — three lines*, then a section naming the material that looks like authority and is
not, then the four in-force documents that constrain the package, then the enforcement problem, then
an explicit list of the ten files the package does **not** contain and why.

**`requirements.md` (147 lines).** Scope codes `E` / `C` / `*`, counts given per scope ruling so the
owner can see what each choice costs. Six commercial rows are present but marked conditional and
struck entirely if the ruling is `E`.

**`decisions-open.md` (489 lines).** `OD-SUB-0001` (scope) first, since sixteen of the seventeen are
conditional on it.

Design choices inside the constraints:

- **The two billing directions get a table, not a sentence.** Seven rows — who pays whom, what it
  buys, where it is recorded, who may see it, authority, tenancy — plus the concrete failure
  conflation produces: a vendor invoice as a GL journal would be copied into a customer-managed
  database by `TenantCutoverCopyPlan` on the next cutover and would be readable by any tenant user
  with GL read permission. That is checkable against `TenantCutoverCopyPlan.cs:31`, which is why it
  is stated that way rather than as a warning.
- **"State what authority does not exist" was taken literally**, per the design constraint. The
  package says the basis is three sentences, then spends a section on the four documents that look
  authoritative and are not — with the disclaimers quoted. `TBL-PLT-Subscription` is explicitly
  called *a name in a deferred draft, not a design*, and the package refuses to inherit its implied
  single-table shape. This is the FP-013 "Leave Request is an illustration" move.
- **`ADR-027` inherited, never restated** — `DEC-SUB-0008`, one paragraph, no new precision decision.
- **The enforcement seam is described from the code's own words.** `PermissionEndpointConventions`
  documents its design in-file, and `DEC-SUB-0006` quotes it rather than paraphrasing, which is what
  makes "the gate is a sibling of this, not new architecture" checkable.
- **Amending `CON-0001` by ADR is written in as a real option** (`OD-SUB-0003`, third row), because
  `Constraints.md`'s own preamble permits it. Presenting only "build it" or "keep violating it" would
  have been a false choice.

`git diff main...agent/T-002-fp-014-foundation --stat`:

```
 docs/17-features/FP-014-subscription/README.md     | 222 ++++++++++
 .../FP-014-subscription/decisions-open.md          | 489 +++++++++++++++++++++
 .../FP-014-subscription/requirements.md            | 147 +++++++
 3 files changed, 858 insertions(+)
```

`git status --short` after the push — the four protected paths untouched, nothing else modified:

```
 M .gitignore
?? .claude/
?? D7-M2.bundle
?? scripts/trace-check.py
```

## Gate

**Not run, by the T-002 spec and `DEC-L-002`.** *Files in scope* contains no file under `src/` or
`tests/`; three new Markdown files under `docs/17-features/` are not compiled and are not read by any
suite. Verification is criterion 5, the `--stat` assertion, which passed.

| Suite               | Result |
| ------------------- | ------ |
| build (0 warnings)  | not run — no compiled file in scope (`DEC-L-002`) |
| Architecture.Tests  | not run — same |
| Platform.Tests      | not run — same |
| HR.Tests            | not run — same |
| API.Tests           | not run — same |
| module suite        | n/a — no module changed |

No `.cs` file was needed, so the `BLOCKED` hatch was never reached.

### `trace-check.py` — run, not modified, not committed

Run as `py scripts/trace-check.py FP-014-subscription` (the `python3` alias on this machine hits the
Microsoft Store shim; `py` is Python 3.13.5). The script is untouched and unstaged.

```
FP-014-subscription  [RED]  28 failure(s), 2 warning(s), 0 declared gap(s)

  Owns: SUB   External refs: 1

  Inventory, derived:
    space           count  range        contiguous
    REQ-SUB            28  0001-0028    yes
    DEC-SUB            12  0001-0012    yes
    OD-SUB             17  0001-0017    yes

  Owner decisions: 17 defined, 17 unruled -> BUILD BLOCKED

  WARNINGS:
    - no traceability-matrix.md - coverage rules 3 and 4 not checked
    - BUILD BLOCKED: 17 owner decision(s) and no ratification file in this package
```

**The RED is the expected and correct state of this package, not a defect.** All 28 failures are the
same one — `COVERAGE REQ-SUB-00NN is defined in requirements.md but absent from the chain table` —
and there is no chain table because `traceability-matrix.md` is **out of scope by the task spec**.
The package cannot be green until the model documents exist, and they cannot exist until the owner
rules. I did not add the file to clear the check.

**What the run did prove:** all three identifier spaces are contiguous from `0001` with no gaps, the
count of each is derived rather than typed, and `BUILD BLOCKED` is correctly reported with all 17
owner decisions unruled.

**It also caught a real defect in my draft.** The first run reported
`UNRESOLVED REQ-PLT-0071 - cited in decisions-open.md and defined in no package or global register`.
In arguing `OD-SUB-0002` I had written `REQ-PLT-0071 → BR-PLT-0008` as an illustration — minting an
identifier that does not exist, inside the very package whose subject is an orphaned identifier. It
is fixed: the bullet now makes the point without an example ID and says in-line why no example is
written. **That is the script doing exactly what its docstring says it was built for, on its first
contact with new work.**

## For the architect

- **Blocked / needs a decision:** none. The task completed as specified.

- **Touched outside scope:** nothing. Three files, one new directory, all additions.
  `Requirement-Numbering.md`, `Product-Roadmap.md`, every ADR, `scripts/trace-check.py`, `.gitignore`,
  `.claude/`, `D7-M2.bundle` — all untouched.

### One deliberate divergence from a design constraint — please rule

Your spec said: *"Where the record lives relative to `ADR-017`'s topology — platform database versus
tenant database — is a consequence of that, and is an `OD-SUB`, not an assumption."*

**On re-verification, `ADR-017` has already ruled it, twice and explicitly:**

> `ADR-017:162`, in the Platform-database residency list — "**Subscription/plan metadata when
> introduced.**"
>
> `ADR-017:475`, lookup classification — "**A — Platform global**: `Country`, `Currency`, `Language`,
> **subscription plans**, permission catalog, module definitions, global localization catalog. Stored
> in the Platform database. **Tenants cannot create global rows.**"

Writing that as an open decision would have invited the owner to rule against an in-force ADR without
being told it existed. So I recorded plan residency as **`DEC-SUB-0003` (settled by inheritance,
`ADR-017` quoted)** and kept as **`OD-SUB-0004`** only the part `ADR-017` genuinely does not answer:
where the **per-tenant assignment** lives, and whether resolved entitlement is projected anywhere for
read speed — which has a real correctness consequence, since one of its three options contradicts
`DEC-SUB-0004` and another makes `REQ-SUB-0009` false by construction.

**This is narrower than your constraint, and it is your call.** If you want residency reopened as an
`OD-SUB` regardless, say so and I will move it — but the `ADR-017` citation should travel with it
either way.

### Three corrections to the finding note, all in the same direction

The note is right on every conclusion. Three details are sharper than it recorded, and two of them
change what a builder must do:

1. **`Program.cs` mounts seventeen route groups, not twelve.** Verified: seventeen `app.Map*` calls at
   lines **126–152** — one Host, six Platform, seven HR, one each for GL, Payroll and Attendance —
   after five `Add*Module()` registrations at lines **53–78**. The retrofit is larger than recorded.
2. **`CON-0001` the *identifier* appears once; the *concept* does not.** Confirmed that `CON-0001`
   occurs exactly once in `docs/` and `BR-PLT-0008` twice. But "subscription" as a word appears across
   `Tenant-Management.md` (seven places, including a `TBL-PLT-Subscription` table and an onboarding
   step), `Authentication.md` (a login refusal — "Expired subscriptions cannot login"), `ADR-005:248`,
   `ADR-021:207`, `ADR-017:162` and `:475`, and two `FP-002` exclusion clauses. **None of it is
   authority** — `Tenant-Management.md:13` disclaims its own subscription material and
   `Authentication.md` is `Draft` — but a builder who greps for the concept will find a great deal
   and could easily mistake it for a specification. That is now `DEC-SUB-0012`.
3. **`FP-002` forbids entitlements in the access token, and nobody had recorded it.** The token model
   is *exact claim cardinality* — the listed singletons plus `role`/`permission`, duplicates invalid,
   8192-byte ceiling rejected rather than truncated (`authentication-model.md:14`) — and it explicitly
   excludes "subscription or billing information" (`:16`, restated `business-rules.md:55`). This is
   the constraint most likely to be violated by accident, because a claim is the cheapest-looking
   place to put an entitlement flag and the token already carries permission values that sit right
   beside it. `DEC-SUB-0005`.

### Follow-ups noticed but not done

- **`ADR-017` carries `status: Proposed` (v1.6).** This is the same defect class you already hold open
  for `ADR-026`/`ADR-027`, but with a wider blast radius: the entire tenant-storage topology, the
  Shared→Dedicated cutover, and now this package's residency ruling all rest on it. For contrast,
  `ADR-015` — which this package also leans on — is `status: Accepted`. Recorded as a caveat inside
  `DEC-SUB-0003` and reported here; not fixed.
- **`docs/02-Functional/Platform/Authentication.md` describes a login refusal for a state the product
  cannot represent** ("Expired subscriptions cannot login", `:123`; "Expired Subscription" as a
  failure scenario, `:141`). Whatever `OD-SUB-0009` rules, that file needs a corresponding edit —
  either the clause becomes real or it is removed. It is outside this package and is a task of its
  own.
- **`docs/02-Functional/Platform/README.md:25` lists "Subscription" as a Platform area** with no
  document behind it. Trivial, but it is another entry in the orphan class this package exists because
  of.
- **`trace-check.py` earns its commit.** It found a fabricated identifier in a brand-new package on
  first contact, inside the one package least able to afford one. Your note already proposes extending
  it upward to `CON-*` and `BR-*`; on this evidence I would add that its `UNRESOLVED` check is the
  part that pays for itself immediately. Left untracked and unmodified, as instructed.
- **`Business-Rules.md` has four `BR-` prefixes** — PLT, HR, GL, ATT — and no `BR-SUB`. Carried as a
  sub-question of `OD-SUB-0002` rather than as a separate decision, since ruling them apart from the
  requirement prefix should be deliberate rather than incidental.
