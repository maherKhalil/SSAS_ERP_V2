# T-126 — **the seed list is 128 entries against 195 unseeded files. It cannot be completed, so the guard cannot assert completeness.**

- **Status:** MEASURED + one comment correction. **No new guard written**, and that is the finding rather than an omission.
- **`GATE_SCOPE=TASK` GREEN, 0 warnings.** Started green and stayed green — `tests/` comment only.
- Branch `agent/T-126-seed-floor`, from `ClaudeBranch` at `01a495c`.

---

# THE NUMBERS, RE-DERIVED. YOURS WAS RIGHT ABOUT HANDLERS AND THE TOTAL IS TEN TIMES BIGGER.

```
files in src/ that RETURN an error (Result.Failure ... SomethingErrors.X)   265
  of those, files declaring NO seeded type                                  195
seed list size                                                              128
```

**195 unseeded error-returning files.** You estimated the invisible set at three shapes and I expected
dozens. **It is not dozens.**

By shape, over 223 unseeded types that return errors:

```
                      not named in an API assembly   named in one
  static                                       17              6     T-117's shape, guarded
  handler                                      23              1     T-125's shape, unguarded
  other  (aggregates, VOs, resolvers)         126             46     unguarded
  errors-decl                                   4              -
```

**Your handler slice — 24 of 114 — is corroborated exactly.** 23 with no API presence plus 1 with.

---

# ⚠ BUT YOUR HYPOTHESIS OVER-REPORTS, THE WAY THE LAST ONE DID

*"A handler NAMED IN AN API ASSEMBLY must be seeded"* holds for handlers. **Applied to the other shapes it
pulls in `Account`, `Company`, `Department`, `Employee`, `PayElement`, `Position`, `Tenant` — every aggregate
and value object in the product**, because their names appear in transport contracts and route files as a
matter of course. 46 candidates in the `other` row, almost all noise.

**So: the exemption-list version, as you pre-authorised.** And it does not scale either — see below.

---

# ⚠ AND THE ONE ROUTED UNSEEDED HANDLER IS NOT A SECOND 500. IT IS A THIRD SHAPE OF INVISIBILITY.

`RefreshPlatformAuthenticationSessionCommandHandler` **is live**:

```
PlatformSupportAuthenticationEndpointRouteBuilderExtensions.cs:115
  var handler = context.RequestServices.GetRequiredService<RefreshPlatformAuthenticationSessionCommandHandler>();
```

**Resolved from DI, not declared as a route-delegate parameter** — which is why your parameter-based scan did
not see it, and why the closure walk cannot either.

**Its two codes are unmapped and it does not matter**, which I checked before reporting it as a defect:

```
AuthenticationErrors.AccessTokenIssuanceUnavailable  ->  Problem(context, 503, "service.unavailable")
AuthenticationErrors.GenericRefreshFailure           ->  Problem(context, 401, "authentication.refresh_failed")
```

**That route answers by SHAPE, not by code string.** It inspects the result and writes the status directly;
neither code is named in the file. **There is no fall-through to 500 because there is no code-keyed mapper in
the path at all.**

**Worth stating plainly: "unmapped" is only meaningful for routes that map by code.** I very nearly reported
this as a live 500 on the strength of a grep against the wrong file — `DEC-L-082`, a citation resolves but
does not validate, applied to my own measurement.

---

# WHAT I DID INSTEAD OF A GUARD

**Corrected the floor statement in the register**, which is the thing that was actually wrong. T-120 wrote it
as *"static classes are invisible"*. **That is one shape of three, and a floor stated too narrowly is worse
than no floor because it reads as complete.** The comment now names all three, carries the 195/128
measurement, and says what the register asserts:

> **A property of the SEEDED sites, not of the product.** *For the closures named here, every error they
> reach is mapped.* It has never asserted that the seeds are complete, and after T-126 it says so.

**The static-class rule stays because that shape is small — 23 — and structurally invisible.** A `KnownUnseeded`
of 195 is `UnroutedFamilies` at ten times the scale: a list nobody reads, that goes stale silently, and whose
green means only that somebody appended to it.

---

# MY RECOMMENDATION, AND IT ARGUES AGAINST THE TASK

**Do not widen this guard.** Not on the exemption-list version either.

**The register earns its place by being right when it fires** — three real defects this fortnight. Widening
it to a completeness claim it cannot support would make it fire constantly and be ignored, **which costs the
thing that currently works.**

**If you want coverage of handler and return-only shapes, the lever is not this register.** It is that
`PayrollCalculatorErrorWireContractTests` shape — direct assertions on the mapper for a route's codes,
written by the task that adds the route. **T-118 and T-125 both did that, and both found live 500s the
register was green about.** It scales with new work rather than against 195 existing files.

---

## For the architect

- **Blocked / needs a decision:** whether to accept the recommendation above. **I have built nothing that
  presumes the answer** — the correction stands on its own and is worth keeping either way.
- **The 195 is a floor, not a ceiling.** It counts files whose returned symbol matches `SomethingErrors.X`
  near a `Result.Failure`. Errors constructed inline or returned through a helper are not in it.
- **`DEC-L-069` applies to my own numbers:** the shape table is a full sweep, not a sample, but the
  routed-handler check was one file read after one grep. **One instance verified, not a category cleared.**
