# T-013 — FP-014 amendment: `BR-SUB-0021`, and the two sentences `DEC-L-012` made stale

- **Branch:** `agent/T-013-fp-014-amend` — **branch from `agent/T-008-fp-014-matrices`**
- **Module:** Platform (commercial plane)
- **Issued:** 2026-08-25
- **Depends on:** T-008 (accepted)

## Goal

You were right and my question was wrong. `BR-SUB-0006` states how a metered quantity is **judged**;
where a cap is **enforced** is a different statement, not a missing clause of that one. It is a
twenty-first rule, `business-rules.md` was out of your scope, and minting the identifier in the matrix
while its home file lacked it would have ORPHANed the very run T-008 delivered. Declining to reach in
was correct. This task is the amendment that lets the identifier and the matrix cell land together.

It also closes the two sentences `DEC-L-012` made stale, which you flagged rather than left.

## Acceptance criteria

- [ ] `business-rules.md` gains **`BR-SUB-0021`** stating the enforcement point per `DEC-L-009`:
      the seat cap is refused **at admission** — creating or activating a user beyond it, with the cap,
      the count and the plan named — **never at login**, and an excess arriving by plan downgrade is
      billed and reported rather than enforced against anyone already working. State the asymmetry
      with expiry (`OD-SUB-0009`) where it would otherwise read as inconsistency.
- [ ] `traceability-matrix.md` carries `BR-SUB-0021` in the cell that currently names it in words, and
      the chain it belongs to resolves.
- [ ] The sentence in `business-rules.md` saying the owner "has an open ruling on the whole 70% class"
      is corrected — it is closed, as `DEC-L-012`.
- [ ] The "expect a third `UNPROMOTED` row — that is the check working, not a defect" passage is
      corrected to the sharper truth you identified: **true until ratification, a ratification defect
      after it.** Under `DEC-L-012` promotion is an obligation of ratification, not an open question.
- [ ] `py scripts/trace-check.py FP-014-subscription` still runs **GREEN, exit 0**, with every other
      package present. `BR-SUB` is contiguous `0001`–`0021`.

## Files in scope

- `docs/17-features/FP-014-subscription/business-rules.md`
- `docs/17-features/FP-014-subscription/traceability-matrix.md`

Two files. If `BR-SUB-0021` needs an `AC-SUB`/`TS-SUB` to satisfy coverage, **say so and stop** —
that widens to two more files and I will amend rather than have you assume it.

## Out of scope

- `decisions-ratified.md`, `Requirement-Numbering.md`, the master `Business-Rules.md`. All at ratification.
- The two smaller items you raised. **Both are ratification concerns and I am recording them, not
  fixing them now:** that `AC-SUB-0049` names `TenantUser` as the seat-bearing entity because it is
  the only reading available while `DEC-L-009` says "seats" without defining one — a real gap, and
  mine; and that `REQ-SUB-0027` carries two rows with different enforcement semantics that ratification
  may want split.
- Any file under `src/` or `tests/`.

## Design constraints

- **No en-dash ranges**, including inside a bullet that forbids them. Your own worst catch.
- **No pipe table whose first cell can hold a `REQ-` identifier unless it is a chain row.** `parse_chain`
  reads the table, not the heading above it — the finding that cost you a false coverage failure and
  which is now on the board for everyone.

## Tests required

**None in the suites** — `DEC-L-002`. Criterion 5's GREEN run is the deliverable.

## References

- `DEC-L-009` and `DEC-L-012` in `.claude/handoff/notes/2026-08-25-fp-014-rulings.md`, round 3.
- Your T-008 result, the `NEEDS-DECISION` answer and the stale-sentence flag.
