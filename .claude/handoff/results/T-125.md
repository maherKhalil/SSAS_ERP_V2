# T-125 result — a live 500, a duplicate code, and a dead one

- **Status:** BUILT, GREEN, **MERGE PENDING** under `DEC-L-007` (gated).
- **`GATE_SCOPE=TASK` GREEN, 0 warnings.** `API 779 -> 787` — eight tests, exactly the eight added.

---

# THE LIVE ONE

```
POST /employees/{id}/one-off-payments   amount: 0   ->  500   BEFORE
                                                    ->  400   NOW
```

**`RecordOneOffPaymentCommandHandler` was never added to the register's seeds.** T-110 added the route and
the handler and did not seed them, **so every code they return sat outside every closure since.**

**Five shape refusals → 400.** The request is malformed and no state will make it succeed.
**Two consumption refusals → 409.** They arrive from APPROVAL, not the instruction's own route: the request
is well-formed and the world disagrees with it.

---

# ⚠ AND TWO CONSTANTS THAT SHOULD NOT HAVE EXISTED, BOTH MINE FROM T-110

**`Payroll.OneOffPaymentPayElementNotFound` was a duplicate.** `PayElementErrors.NotFound` is
`Payroll.PayElementNotFound` and has been mapped to **404 since FP-012**.

**The same fact under two names — `DEC-L-080` in the error vocabulary — and the second was unmapped. So
*the pay element you named does not exist* answered 404 from one route and 500 from another.** Deleted; the
handler returns the existing code.

**`Payroll.OneOffPaymentNotFound` was returned by nothing at all** — a code for a lookup path that does not
exist, which reads to the next person as evidence that one does. Deleted.

**Ten codes were eight all along**, and finding that out cost one grep I did not run when I wrote them.

---

# ONE STAYS UNMAPPED, DELIBERATELY

`OneOffPaymentConsumingRunRequired` — `MarkConsumedBy` refusing a `Guid.Empty` run id. **No route can
produce that**: the only caller passes `run.Id` from a run already loaded from the database.

**A guard against a programming error, not a business refusal.** Mapping it would put a status on a path no
request can take — **a dead arm, which T-095 established is worse than an honest gap.** In `KnownUnmapped`
with that reason.

---

# THE SEEDS, AND THE SECOND ONE IS THE INTERESTING ONE

```
RecordOneOffPaymentCommandHandler   a route parameter, never seeded
OneOffPayment                       the AGGREGATE returns the consumption refusals
```

**The aggregate needed seeding for a reason distinct from T-120's static classes:** a repository *returns*
it, and **the closure follows parameter TYPES rather than return types.** Neither a static class nor a
handler — a third shape the walk cannot reach.

---

# ⚠ THE FLOOR IS WIDER THAN T-120 STATED IT, AND THAT IS T-126

**T-120's `KnownUnseeded` fires only for static classes. An unseeded HANDLER or AGGREGATE fires nothing at
all** — which is why this survived T-120 by three tasks and was found by writing an unrelated test.

**A floor stated too narrowly is worse than no floor, because it reads as complete.**

---

## For the architect

- **Blocked / needs a decision:** nothing.
- **T-126 next**, and I will re-derive your 24/114 rather than take it — **you asked me to, and last time my
  criterion found more than yours.**
- **The tree is on `ClaudeBranch` after this merges** — I will hold before cutting T-126's branch so the
  board append gets its window.
