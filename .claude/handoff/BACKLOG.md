# The backlog — pull from here whenever the queue is empty

⚠ **AN EMPTY QUEUE IS NOT A REASON TO STOP. TAKE THE TOP ITEM HERE, START IT, AND SAY WHICH ONE IN YOUR
NEXT MESSAGE.** No permission needed. Everything here is gated (`src/` + `tests/`) unless marked otherwise,
so `DEC-L-007` applies: green gate, merge immediately.

**If this file is also empty**, pick the highest-value thing you can defend from your own knowledge of the
tree, state the reasoning in one line, and start. **Never send a message whose only content is that you
have no work** — if you are reporting an empty queue, you should already be working and naming what.

**Offline:** read this from `C:/Users/User/Documents/SSAS_ERP_V2/SSAS_ERP_V2-board/.claude/handoff/` when
`git fetch` fails.

---

## Standing work, roughly in value order

| # | item | why it is real |
|---|---|---|
| ~~B1~~ **DONE 2026-08-30** | **Re-verify `api-contracts.md`, `data-model.md`, `domain-model.md`, `lifecycle-model.md` in `docs/17-features/FP-015-self-service/` against the tree.** Report discrepancies only; the documents are the architect's to edit. One at a time, reporting each. | Landed 2026-08-30 **explicitly marked as never re-checked**. The one document that WAS checked lost four of eight sections in three days, so the base rate is high. | **All five re-verified: 4, 3, 0, 0, 1 discrepancies. Every one an absence claim; no descriptive section wrong.** |
| ~~B2~~ **ALREADY DONE (item 116)** | **Group B's five persistence-discard comments must name the MECHANISM that makes them safe** — the `when (e.InnerException is SqlException { Number: 2601 or 2627 })` filter — not "acceptable information loss", which 92 proved false. Establish whether it landed; if not, write them. | The original instruction was amended after measurement and may never have been carried out. |
| ~~B3~~ **PROMOTED to queue item 120** | **Audit a guard BECAUSE it has never failed.** Principle 16's own trigger, applied to the guards not covered by 95's inventory — anything outside the 18 text-matching ones: reflection-based, type-based, assembly-based. **Has each been observed to fail for the reason it claims to test?** | 95 scoped itself to text scans. Principle 16 says the rule is not about text; it is about whether a check has ever been seen to fail. |
| ~~B4~~ **DONE 2026-08-30 — now Principle 18** | **The `Error(Code, Message)` record cannot carry structured detail.** Noted twice — 97 (the `SqlException` dies at the catch) and Group B. **Establish the cost: how many places would want a third field, and what do they do instead today?** Report; propose nothing. | Recorded as a limitation twice without anyone measuring how often it bites. |
| ~~B5~~ **DONE — 3 pairs found, promoted to queue item 125** | **Re-run the sibling-route ambiguity enumeration** across all modules now that 71 constraints are gone. 84's check was per-module at the time of removal. **Confirm the current tree still has zero method+shape groups with two patterns.** | The constraint removals landed module by module; nothing has re-checked the whole surface since. |
| ~~B6~~ **DONE — MEASURED, NOTHING TO DO: all five already redden on a dead filter** | **Were the guards fixed in 102 and 103 planted for a dead FILTER, or only a dead ROOT?** The post-filter insight arrived *after* most of those plants. `src` → `sources` empties a root; `Platform` → `PlatformX` empties a filter — **and only the second is the failure that a refactor actually causes.** Re-read each planted guard and establish which mutation its recorded evidence describes. **Report the split before fixing; where only a root plant exists, add the filter one.** | The rule that says a filter is the likelier silent failure was published after those files were planted, so nothing establishes they meet it. |
| ~~B7~~ **STATED — 20 files, ZERO floors; middle number being finished** | **Guards that enumerate something other than assemblies or files.** 95 covered text scans (18) and B3 covered assembly enumerations (21). **What enumerates DI registrations, configuration sections, route tables, or EF model metadata — and can any of those collapse to empty and still pass?** Enumerate the class first and report it; probe second. | Two inventories have each found a vacuum-prone majority in their own class. **Nothing has looked at the guards that enumerate neither files nor types.** |
| ~~B8~~ **DONE — all 25 pass, and that IS the finding: the question was too weak** | **Run the cheap test across every existing floor: is the floored quantity the one its failing assertion reads?** Principle 16's new form. **Five guards are confirmed correct; nothing establishes the rest are** — a floor on files enumerated rather than routes matched looks prudent and covers nothing. **Report the split before changing anything.** | The rule that makes a floor meaningful was published *after* most floors were written. |
| ~~B9~~ **DONE (PR 364)** | **Is the `.Map(Get\|Post\|…)` regex complete?** It is a filter, so **a route registered in a form it does not match is invisible to the constraint guard.** `MapMethods`, `MapFallback`, `MapGroup`, a bare `Map`, anything else ASP.NET offers. **Enumerate the registration forms actually used in `src/`, then say which the regex covers.** | Same blind-spot family as composed identifiers: the guard is exhaustive over the forms it knows about, and nothing has enumerated the forms. |

---
| ~~B10~~ **DONE — 1 genuine defect, made caller-visible by item 123** | **For the 21 multi-condition messages, does every raise site actually match its code's NAME?** The pagination code was raised for an **export ceiling** — a cap on total rows, not a page of anything — **and nobody noticed because the name made the mismatch invisible.** A caller told *"page size invalid"* for a bad ceiling goes looking at a parameter it never sent, **which is worse than an ambiguous message: it is a wrong one.** 21 codes, raise sites enumerated, **report mismatches only.** | The fourth condition behind `InvalidPagination` was only visible once the code was split. **The number of conditions hiding behind an ambiguous code is unknown until you split it** — so the same may be true of the other 20. |
| ~~B11~~ **DONE — no new defects; 5 codes reach 2+ modules and four are correct** | **Now that messages are caller-visible, which others name a specific domain while being raised from SHARED code?** `Persistence.*` is the family B10 found — an identity-flavoured message returned for every aggregate in the product. **The question is whether `BuildingBlocks` and other shared assemblies hold more.** Enumerate errors declared in shared code, check each message for module nouns, **report before changing wording.** | ⚠ **Item 123 raised the quality bar on all 344 messages retroactively, in one commit, without inspecting a single string.** They were written when nobody could read them. |
| ~~B12~~ **CLOSED — arm kept deliberately; reason recorded on the board and in ADR/handoff, 2026-08-30** | ⚠ **`PayElement.SetOvertimeTier` is NOT dead code — it is an unfinished capability, and the mapper arm is the only surviving trace of it.** The consequence was measured in 135: **attendance-driven hourly overtime cannot be paid**, locked twice independently, and the guard that would flag it **fails open** because its precondition is supplied by the broken feature. **Removing the arm would erase the evidence.** Finishing the capability is the owner's call. |
| ~~B13~~ **DISSOLVED — all three arms are alive; the finding was an instrument defect** | **Three mapper arms reference domain codes that no `*Errors.cs` declares** — `Company.ListFilterInvalid` (Company mapper), `Gl.InvalidActor` (GL), `Pagination.Invalid` (IdentityAccess). **The last is plausibly stale since T-260 split pagination into three named codes.** Same shape as B12's dead arm: **an arm for something nothing can raise.** ⚠ **Establish for each whether the CODE should exist before removing the arm** — an arm with no code is either dead or a rename that lost its other half. **`"request.invalid" => InvalidRequest` in `LocalizationApiErrorMapper` is NOT one of these**: that mapper keys on wire codes by its own convention. | Found during 133, reported and not touched. |

| ~~**B14**~~ **PROMOTED to queue item 170 (2026-08-31)** | ⚠ **WHICH OF 165's 61 UNCONSUMED INTERFACE MEMBERS ARE REACHED WITHOUT A COMPILE-TIME REFERENCE?** The `[Obsolete]` probe is blind to **reflection, DI resolution and serialization** — a member resolved by the container or bound by a serializer emits no warning and reads as unconsumed. ⚠ **That is exactly the shape of repository and service interfaces, which is most of the 61.** **Establish which are reached at runtime, and validate the instrument against a member known to be DI-resolved before trusting a single negative.** Only two of the 61 were verified by hand. | The census's own stated blind spot, named by the window that built it. **Until it is closed, "unconsumed" means "unconsumed at compile time", and no removal can rest on it.** |

| ~~**B15**~~ **PROMOTED to queue item 172 (2026-08-31)** | ⚠ **WHAT HAPPENS WHEN A DOMAIN-EVENT CONSUMER THROWS AFTER COMMIT?** `EfUnitOfWork.CommitAsync` saves, commits, then dispatches — **so a consumer that throws propagates into the catch, which then calls `RollbackAsync` on an ALREADY-COMMITTED transaction.** What that does is not established: it may throw a second exception masking the first, may be a silent no-op, or may leave the unit of work in a state the next call misreads. **Establish it, then say whether the behaviour is right** — a consumer failure after a successful commit is not a reason to report the command as failed. | Named by the window that exercised the flow in 166 and explicitly excluded from its tests. **The commit has already succeeded, so whatever this does, the data is written and the caller may be told otherwise.** |

| ~~**B16**~~ **PROMOTED to queue item 174 (2026-08-31)** | ⚠ **DETACHED MUTATION WITHOUT `AsNoTracking` — the form of the domain-event hazard that 167 could not search for.** An entity materialised in one context and mutated against another, or **rebuilt from a DTO**, raises events dispatch never sees, and **`AsNoTracking` is the SEARCHABLE form of the hazard, not the only one.** Also unsearched: `tests/`, and aggregate types that raise **through a helper** rather than calling `RaiseDomainEvent` in their own file — 167's 14 types came from files containing that call. **Enumerate the unsearchable forms and say whether any is reachable.** | Named by the window that closed the searchable form. **Not urgent: the reachable form is now pinned, and this one is a comment until something reaches it** — but it is the residue of a measurement, and residues in this repository have twice turned out to be the real finding. |

| **B17** ⚠ **DOWNGRADED 2026-08-31 — `test-baseline.txt` ALREADY RECORDS BOTH AS EMPTY SCAFFOLDS AND CALLS THEIR ABSENCE CORRECT, so this is a known placeholder rather than an unnoticed gap; the architect's *names assert coverage that does not exist* was written without reading the file that answers it** | **`Performance.Tests` AND `UI.Tests` CONTAIN ZERO SOURCE FILES — a `.csproj` each and nothing else** (verified excluding `obj/` and `bin/`, which is what made a first count say seven). **Their names assert coverage that does not exist**, and a reader taking the solution's test projects as an inventory concludes this product has performance and UI tests. **Establish whether either was ever populated — check the history — and report what the projects were FOR before anything is removed.** ⚠ **Do not delete them on this basis alone: an empty project may be a placeholder someone intends to fill, and that intention lives outside the repository.** | The inverse of this loop's usual failure — **a NAME asserting PRESENCE, rather than a document asserting absence.** Recorded in owner decision 20 as a related fact. **Not urgent, and not a measurement: deciding what to do with an empty project is the owner's or the architect's.** |

| **B18** ⚠⚠ **STANDING — PULL THIS WHENEVER THE QUEUE IS EMPTY, AND STOP WHENEVER YOU LIKE** | **CITATION-BUILDING: read a criterion's BODY, find the test whose ASSERTIONS satisfy it, and cite the id in the test.** ⚠ **This is NOT a gap hunt and must not be reported as one: of ten criteria examined by body plus three packages of spot-checks, ZERO were uncovered. The yield is DURABLE MAPPINGS.** **Cost measured (T-218): 84 files, 1,124 methods for 46 criteria; four citations per ten examined; FP-006 has 36 left, about four passes.** ⚠⚠ **THE VALUE IS PER-CITATION AND ROUGHLY CONSTANT, SO STOPPING WASTES NOTHING — the opposite of a coverage audit, which is worthless until finished. That is why this is a BACKLOG entry and not a queue row: a queue row implies a completion this work does not have.** **Rules, all paid for: read the BODY not the heading; a SYNONYM misleads as readily (uniqueness is not equality); MATCH the existing trait key, never introduce a fifth; and if you did not read the assertion, SKIP IT AND SAY SO.** **Report how many you examined and how many you cited. Both numbers, every time.** ⚠⚠ **AND THE CITED NUMBER IS THE `[Trait]` COUNT, NOT A TEXT GREP — measured 2026-08-31, a grep counts a criterion NAMED IN A COMMENT exactly as it counts one CITED, and four packages read as covered while being at ZERO (`POS`, `IAM`, `SUB`, `LOC`).** **Strict 147 against loose 169 product-wide. Where the two differ, REPORT BOTH.** ⚠ **The fix is to count strictly and to keep writing informative comments: a comment naming a neighbouring criterion does real work, and the instrument was the thing that was wrong.** ⚠⚠ **AND REPORT PARTLY-PINNED CRITERIA SEPARATELY, BECAUSE THE BY-PRODUCT HAS OUTPERFORMED THE PRODUCT: three specified-but-unasserted halves have come out of this activity — a retrievability clause, a five-part structural ban, and a reservation clause — and EVERY ONE WAS A REAL GAP IN A PACKAGE NOBODY THOUGHT WAS UNDER-TESTED.** **A citation that says what it does NOT cover is how those were found. Keep saying it.** ⚠ **And check the STATE a criterion is about, not only the rule it names — an existing test can prove the right rule between the wrong two rows.** ⚠⚠ **START EACH PACKAGE FROM THE ARCHITECTURE SUITE. An architecture test has to say what STRUCTURAL claim it makes, so its name states its criterion almost verbatim — applying this for the first time turned FP-006's leftover set into the highest-yielding pass of the sweep, 6 of 7.** ⚠ **AND WHEN A REMAINDER RESISTS, SUSPECT THE SEARCH ORDER BEFORE THE CRITERIA: a leftover set is evidence about the method, not about what is left.** **Report UNEXAMINED and EXAMINED-BUT-UNRESOLVED as two numbers, never as one uncited total — they are different claims and collapsing them reports the sweep as more complete than it is.** ⚠⚠ **AND RUN THE ARCHITECTURE SWEEP ACROSS THE WHOLE TEST TREE, NOT THE FEATURE'S MODULE: `AC-EMP-0042`'s pin was in `BranchSessionArchitectureTests`, outside the HR suites entirely, because a structural claim is asserted where the STRUCTURE lives and not where the feature does.** **Measured on FP-006: two architecture-first passes averaged 0.86 against 0.57 for the three before them, and the new order was applied to the OLD ORDER'S LEFTOVERS — so the confound runs against the effect.** ⚠ **And when a criterion looks unsatisfiable, READ THE DECISION IT CITES BEFORE ESCALATING IT: `AC-EMP-0047` was an unscoped product-wide absence whose own governing decision merely deferred the subject to another package, and it was settleable inside FP-006 after all.** ⚠⚠ **AND BEFORE RECORDING A CRITERION *UNRESOLVED*, SEARCH THE MECHANISM — THE SYMBOL AND ITS CALL SITES — NOT ONLY TEST NAMES. THIS ACTIVITY HAS A CONTROL ON ITS CITATIONS (`zero dangling`) AND NONE ON ITS DISPOSALS, so a wrong *unresolved* is the one error that never surfaces.** **`AC-PAY-0002` was recorded unresolved while its near-verbatim pinning test sat four lines above the two that were read, and a grep on the domain method returned seven call sites and settled it in one command.** ⚠⚠ **AND TWO RULES THAT MAKE A NEGATIVE CLAIM CHEAP TO RE-CHECK, SINCE NOTHING CAN CONTROL ONE: (1) A DISPOSAL RECORDS THE SEARCH THAT PRODUCED IT — the symbol, the command, the hit count — so a later reader falsifies it by RE-EXECUTION rather than by re-reading. (2) A CITATION NAMES THE CLAUSE IT SATISFIES, NOT ONLY THE CRITERION — `AC-EMP-0035` was disposed of wrongly THREE times, and the over-claim would have been impossible to write down under this rule.** **A multi-clause criterion is several claims; treating it as one is right or wrong by luck.** ⚠ **RULE (2) IS BOUNDED TO CRITERIA WITH MORE THAN ONE CLAUSE — on a single-clause criterion it restates the criterion's only content, and a rule applied where it does nothing is how the four-trait-key sprawl started.** **When the clause count is arguable, enumerate: *single-clause* is the same judgement that missed four claims in `AC-EMP-0035`.** | **The convention is absent from entire packages — 255 criteria across four, 32 citations — and the two near-misses prove these mappings are NOT derivable later by grepping names.** |
| **B19** ⚠⚠ **STANDING — RUN `GATE_SCOPE=PHASE` ON A CADENCE, NOT ON A HYPOTHESIS** | **At the end of a working session, and after any `src/` change to `BuildingBlocks`, `Platform` persistence, or a shared context.** ⚠⚠ **THE REASON IS MEASURED, NOT ASSUMED: on 2026-08-31 a PHASE run queued for a named regression found that regression was not constructible — and found instead that EIGHT RELEASE BASELINES WERE STALE and nothing had checked CONFIGURATION DRIFT in eight suite-generations. There was none, which is a RESULT nobody could have stated beforehand.** **`GATE_SCOPE=TASK` never runs Release and excludes Integration, so the merge gate structurally cannot see either property.** ⚠ **A RUN QUEUED ON A HYPOTHESIS IS ONLY EVER SCHEDULED WHEN SOMEBODY HAS ONE, AND THE VALUE IS IN THE QUESTIONS NOBODY THOUGHT TO ASK.** **Cost: ~51 minutes unattended, and it blocks test-file edits while live — pair it with reading work, never with editing.** | **The gate writes all sixteen counts only on a green PHASE run, so `test-baseline.txt` states two different days until one happens.** |
| **B20** ⚠⚠ **STANDING — A UNIVERSAL IN A TEST NAME OVER AN ENUMERATED POPULATION** | **The most reliable defect shape found in this tree, and INVISIBLE TO EXACTLY THE SEARCH CITATION-BUILDING RUNS: a name says *every*, *all*, *no* or *never*, and the population is a hand-written `[InlineData]` list that nothing checks against the set it claims to cover.** ⚠ **Four instances inside one package in one night: `Every_department_mutation_requires_a_row_version` (seven hand-named commands), `Rowversion_columns_are_excluded_from_the_copy_mapping` (ONE entity), `A_stale_row_version_is_refused_on_every_family` (three POSITION families and no department at all), and the copy-plan selection idiom at six sites.** **SIZED: 757 test names carry a universal; 77 of them draw their population from `[InlineData]` (68) or `[MemberData]` (9).** ⚠⚠⚠ **CORRECTED 2026-09-01 BEFORE ANYBODY RAN IT: MY FIRST FILTER WAS WRONG AND COMMITTED THIS ITEM'S OWN DEFECT ONE LEVEL UP. I WROTE *a `[MemberData]` is DERIVED and fine* — TREATING THE ATTRIBUTE KIND AS EVIDENCE OF THE MECHANISM.** **All four `[MemberData]` sources in the category were opened: THREE ARE HAND-WRITTEN LITERALS and the fourth is a hand-written array.** ⚠ **`[MemberData]` MOVES THE LIST SOMEWHERE ELSE; IT DOES NOT DERIVE IT — and as written my filter CLEARED THE WORST INSTANCE IN THE TREE.** **THE REAL FILTER IS MECHANICAL AND IS THE CODER'S: `[InlineData(typeof(X))]` ENUMERATES A SET OF *TYPES*, WHICH IS DERIVABLE FROM THE ASSEMBLY AND THEREFORE THE RISK; `[InlineData(null)]`, `[InlineData("   ")]` ENUMERATES *INPUTS* AND CLAIMS NO POPULATION AT ALL.** **Its inventory: 67 universal-named tests with an enumerated population — 16 TYPES, 42 VALUES, 9 MEMBER — so 42 OF 67 ARE EXCLUDED BY AN INSTRUMENT RATHER THAN BY A JUDGEMENT, and nobody has to decide case by case.** ⚠ **The `MEMBER` nine must each be OPENED: the question is whether the population comes from a query over an assembly, an EF model or an endpoint data source, and there is NO ATTRIBUTE-LEVEL SHORTCUT.** **AND A NAME CAN ONLY OVERCLAIM WHEN ITS QUANTIFIER IS OPEN: `Both_run_records_are_tenant_owned_and_append_only` STATES ITS OWN ARITY over a two-row list and is fine.** **Then, per member: does anything assert the list equals the set the name claims?** | **The fix is usually `assert the list's SIZE against a derived count`, not lengthening the list — and where the population genuinely derives, replace it.** ⚠ **PER-MEMBER AND STOPPABLE like B18: the split is the deliverable and survives you stopping.** **Do NOT report a test as defective merely for using `[InlineData]` — the defect is the NAME claiming more than the LIST covers, and a narrow name over a short list is correct.**  ⚠⚠⚠ **AMENDED 2026-09-01 WITH THE CONDITION THIS ITEM WAS MISSING, LEARNED BY BUILDING ONE: A DERIVED POPULATION HAS A FAILURE MODE A HAND-WRITTEN LIST DOES NOT — IF THE SELECTION PREDICATE MENTIONS THE PROPERTY BEING ASSERTED, THE MEMBERS THAT FAIL THE PROPERTY ARE THE MEMBERS THAT LEAVE THE POPULATION.** **A guard over *types that open a transaction and read a period*, asserting that the transaction opens first, CANNOT CONTAIN A HANDLER THAT OPENS NONE — it is strongest exactly where nothing is wrong.** ⚠ **SO THE RULE IS TWO STEPS, NOT ONE: DERIVE THE POPULATION, THEN CHECK THAT ITS PREDICATE DOES NOT NAME THE ASSERTION. State the population as the SUBJECT of the rule, never as its satisfaction.** ⚠⚠ **AND EVERY EXCLUSION CARRIES ITS REASON AS AN ASSERTION, NOT AS A COMMENT: an exempted member is asserted to be STILL IN the population and to STILL SATISFY the grounds of its exemption, so that removing the grounds turns the exemption red.** **An exemption whose grounds nothing checks is an exemption that outlives them.**  ⚠⚠⚠ **AND ONE EXCEPTION TO THIS ITEM'S OWN ADVICE, FOUND 2026-09-01 AND RECORDED SO NOBODY *FIXES* IT BACK: `[MemberData]` IS EVALUATED AT DISCOVERY TIME, BEFORE `IAsyncLifetime.InitializeAsync` BUILDS THE HOST — SO A HOST-DEPENDENT DERIVED POPULATION CANNOT FEED IT.** **In `PlatformSupportAuthorityAuthorizationTests` the routes do not exist when xunit asks for them, so the four guards are `[Fact]`s looping over the derived set rather than theories over it.** ⚠ **THE OBVIOUS IMPLEMENTATION IS IMPOSSIBLE, NOT MERELY AWKWARD — and the loop is therefore the CORRECT shape, not a compromise.** **Cost: four test results instead of thirty-six, so EACH ASSERTION MUST NAME ITS SUBJECT, AND THE LOOP MUST COLLECT OFFENDERS AND ASSERT THE COLLECTION IS EMPTY — an `Assert` inside the loop reports only the FIRST offender, which is the wrong answer for a guard over a population designed to grow.** |
| **B21** ⚠ **STANDING — FINISH THE ROUTE-INVENTORY ADOPTION, OR RE-AFFIRM THE STOP** | **`PlatformRouteInventory.cs:16-24` RECORDS ITS OWN UNFINISHED ADOPTION AND NAMES THE FOUR HOLDOUTS: `AttendanceRouteInventoryTests`, `HrRouteInventoryTests`, `GlRouteInventoryTests`, `PayrollRouteInventoryTests`, each holding its own copy of the derivation.** ⚠⚠ **THIS IS NOT A DEFECT AND MUST NOT BE WRITTEN UP AS ONE: THE COMMENT DRAWS ITS OWN CONSEQUENCE, GIVES ITS STOPPING REASON — *it touches four green guards to change nothing observable, which belongs on its own branch where a reviewer can see it* — AND CITES `T-127`'s LESSON THAT *the un-migrated remainder is invisible until someone goes looking*.** **It is the one place in this record where a fact was recorded WITH its interpretation.** **So the only live question is whether the deliberate stop STILL HOLDS.** | **On its own branch, as the comment specifies, or not at all.** ⚠ **Four green guards changed to produce nothing observable is exactly the edit that needs a reviewer who can see the whole of it — which is why it is a BACKLOG entry and not a queue row.** **If it is done: the anti-vacuity floor comes with the pattern (`Assert.NotEmpty(actual)` is already in the file being copied from), and each migration must be planted separately.** |
| **B22** ⚠⚠ **STANDING — A SUITE OF REFUSALS PROVES THE GUARD FIRES AND NEVER PROVES IT STOPS FIRING** | **A lifecycle or permission rule has two halves — the state that is refused and the state that is ALLOWED — and only the refusals attract tests, because a refusal is what a defect report is written about.** ⚠ **Measured instance, 2026-09-01, department lifecycle: present are `Deactivating_twice`, `Reactivation_beneath_an_inactive_parent`, `An_inactive_parent_is_refused`; ABSENT are all three positive claims — that deactivation WITH employees succeeds and they stay readable (`AC-DEP-0026`), that an inactive department is still readable and listed (`0030`), and that reactivation RESTORES the ability to receive employees (`0031`).** **A guard that over-fires — refusing a legitimate operation — passes every one of the refusal tests.** ⚠⚠ **AND THE NEAR-MISS IS THE POINT: `An_active_department_can_be_deactivated_and_reactivated` LOOKS like the positive half and is a STATE TRANSITION, not a claim about what becomes possible afterwards.** **Same shape as `AC-EMP-0027`, where the sharp test was the MAXIMUM ACCEPTED rather than out-of-range refused.** **This is the anti-vacuity control applied to a RULE instead of to a query.** | ⚠ **PER-RULE AND STOPPABLE. The population is derivable: every command handler that can return a refusal error, paired with the state in which it must SUCCEED.** **Start where a lifecycle has an explicit inactive/active axis — department, employee, branch, tenant — because that is where over-firing is silent and expensive.** **Do NOT write a positive test that merely re-asserts the transition; assert the CAPABILITY the criterion names.** **B18 passes generate these findings for free — record them as searched-and-absent and let this item collect them rather than widening the citation pass.** |
| **B23** ⚠⚠⚠ **STANDING — A NEGATIVE ASSERTION OVER A PREDICATE PASSES WHEN THE PREDICATE IS BROKEN, AND THE PREDICATE IS USUALLY A STRING** | **`Assert.DoesNotContain(xs, x => x.Name == "Foo")`, `Assert.Null(xs.FirstOrDefault(p))`, `Assert.Empty(xs.Where(p))`, `Assert.False(xs.Any(p))` — every one of them CANNOT DISTINGUISH *the thing is absent* from *my predicate matches nothing*.** ⚠ **Both outcomes are zero matches and both are green, so the test survives a rename, a typo, a moved namespace and a changed property with no signal at all.** **MEASURED AND PROVEN 2026-09-01, not argued: one literal in the cutover suite was planted as `"Departmentt"` and the suite returned PASSED, 6 OF 6, ZERO FAILURES.** ⚠⚠ **AND THE NEAR-MISS THAT MAKES THIS HARD TO SEE: THE SAME FILE'S OTHER NINE SITES GO THROUGH A HELPER ENDING IN `Assert.True(index >= 0, …)` AND REDDEN CORRECTLY — so a spot-check of the file finds the loud sites and concludes it is safe.** **Both windows published the claim that this file's literals fail silently, and it was true of four sites and false of nine.** | ⚠⚠ **SWEEP MECHANICALLY, NOT BY READING: `DoesNotContain`, `Assert.Null(`, `Assert.Empty(`, `Assert.False(` with a lambda, then ask of each — CAN THE PREDICATE MATCH ANYTHING AT ALL?** **TWO FIXES, AND PREFER THE FIRST: (1) make a wrong name a COMPILE ERROR (`nameof`, a typed reference, an enum) — it needs no maintenance and cannot go stale; (2) where the predicate must stay dynamic, ADD A POSITIVE COMPANION proving it matches in the case where the thing IS present.** ⚠ **This is `guards-need-an-anti-vacuity-control` at the scale of a single assertion — the same defect as a derived population with no floor, and the same remedy.** **PER-SITE AND STOPPABLE. Do NOT report a negative assertion as defective merely for being negative: the defect is a predicate NOTHING PROVES CAN MATCH.** |
| **B24** ⚠⚠ **STANDING — `CutoverCopyOrderUndecidable` IS ONE ERROR VALUE FOR THREE UNRELATED CAUSES, AND SPLITTING IT IS CHEAPEST BEFORE THE SURFACE EXISTS** | **`TenantCutoverCopyPlan` raises the same `Error` at `:99` (no primary key or no copyable columns), `:111` (no `TenantId` column) and `:153` (no table is ready — THE FOREIGN-KEY CYCLE).** ⚠ **The first two say *this table cannot be described*; only the third is the cycle that five production comments in `DepartmentManager.cs`, `DepartmentManagerConfiguration.cs`, `Position.cs`, `JobGradeConfiguration.cs` and `PositionConfiguration.cs` rely on.** ⚠⚠ **THE COST IS PAID TWICE. In TESTS, no assertion can distinguish the three — the cycle test had to be built by DIFFERENCING against a matched control instead. In PRODUCTION, an operator who ever sees this error cannot tell a model cycle from a table missing a `TenantId`, and the message does not say which.** **A shared error value is a merge of three diagnoses into one, performed at the throw site and unrecoverable downstream.** | ⚠⚠ **DO IT WHILE THE PATH HAS NO HTTP SURFACE — the tenant-administration endpoints do not exist yet (`OWNER-DECISIONS` #2), so the error code is not yet anybody's contract.** **Same argument as adding the missing mapper arm before the count became one: cheapest before a date somebody else controls.** **Three values, or one value carrying a discriminator — either resolves it; the second is smaller and keeps the mapper arms as they are.** ⚠ **AND IF IT IS DONE, THE TWO UNEXERCISED SITES BECOME TESTABLE INDIVIDUALLY, which is the real return: `:99` and `:111` are today asserted by nothing and cannot be, because a passing assertion on the shared value would not name them.** **Do NOT fold this into the cutover test item — that item's bound (`:99` and `:111` remain unexercised) is honest and must not be quietly closed by a refactor.** |
| **B25** ⚠⚠ **STANDING — THE TENANT-MODEL CENSUS DISCOVERS CONTRIBUTORS FROM ITS OWN TEST ASSEMBLY, AND IS PASSING ON AN ABSENCE** | **`The_composed_tenant_model_has_exactly_the_expected_number_of_entities` enumerates every `SSAS.*.dll` in its own output directory and instantiates every non-abstract `ITenantModelContributor` it finds — and `SSAS.Architecture.Tests.dll` is in that directory.** ⚠ **BOUNDED BY ENUMERATION 2026-09-01, AND IT IS NARROWER THAN FIRST REPORTED: only a contributor defined in `Architecture.Tests` ITSELF can do this. `Platform.Tests` already defines one and has never broken the census, because its assembly is not in that output directory — 34 `SSAS.*.dll` files there, no test assembly but its own.** **PROVEN: two probe contributors placed in `Architecture.Tests` took the count 36 → 38 and reddened the gate.** ⚠⚠ **THE GUARD'S CORRECTNESS RESTS ON AN ABSENCE — nobody has yet defined a contributor in that one project — WHICH IS NOT A PROPERTY OF ITS LOGIC.** **And its own comment, *discovery does not remove the list, it moves it* into the `<ProjectReference>` elements, is true and incomplete: the population also includes the test assembly itself, so the residual surface is every type any of them defines, INCLUDING TEST DOUBLES.** | ⚠⚠⚠ **ON ITS OWN MERITS AND ON ITS OWN BRANCH — NOT AS A PRECONDITION FOR ANYTHING. A shared-guard change made under pressure to unblock is a different act from the same change made deliberately, even when the diff is identical, and this one was nearly made under pressure.** **Narrowing to production assemblies removes ZERO production coverage, because no production entity lives in a test assembly.** ⚠ **THE EXCLUSION IS A NEGATIVE PREDICATE AND `B23` APPLIES: `*.Tests.dll` stops excluding the day an assembly is named `...Test.dll`, and UNDER-exclusion is caught by nothing while OVER-exclusion already reddens the count. Prefer a property-based filter; otherwise name the residual in the comment.** ⚠⚠ **DO NOT RAISE `ExpectedEntityCount` UNDER ANY CIRCUMSTANCES — that makes the alarm accommodate what it exists to catch, and the file's own comment warns against that reconciliation.** **Update the comment as part of the change.** ⚠⚠⚠ **AMENDED 2026-09-01 — THIS ITEM NOW HAS A KNOWN CONSUMER, AND THAT MUST NOT BECOME A REASON TO DO IT: `AC-DEP-0034`'s Department-shaped test can only live in `Architecture.Tests`, so it is blocked until this is done.** **THE CONSUMER IS RECORDED SO THE OPTION IS NOT FORGOTTEN, NOT SO IT CAN BE CITED AS URGENCY — a shared-guard change made to unblock is a different act from the same change made deliberately, even when the diff is identical.** ⚠ **AND THE CONSUMER RAISES THE BAR RATHER THAN LOWERING IT: that test would inject a FOREIGN-KEY CYCLE into a model TWO guards reason over — `TenantModelEntityCount` and `UniqueIndexFilter`; **CORRECTED 2026-09-01 from *five*, which counted three unrelated consumers of the same assembly-enumeration idiom**, and it may move NO entity count at all — so narrowing the discovery is necessary for it and NOT sufficient, and the cycle question must be answered separately before anything is placed there.** |
| **B26** ⚠⚠⚠ **STANDING — FOUR DISTINCT LEDGER REFUSALS REACH THE PAYROLL OPERATOR AS ONE MESSAGE, AND THE CONTRACT ITSELF ARGUES AGAINST IT** | **`PayrollRunCommandHandlers.cs:644` and `:758` map `JournalPostingStatus` with a TERNARY, not a switch: `PeriodClosed` is distinguished and `PeriodNotFound`, `AccountUnavailable`, `Unbalanced` and `ReversalTargetUnavailable` ALL COLLAPSE INTO `PayrollErrors.LedgerRefusedPosting`.** ⚠⚠ **AN OPERATOR WHOSE RUN FAILED BECAUSE AN ACCOUNT WAS UNAVAILABLE IS TOLD THE SAME THING AS ONE WHOSE JOURNAL WAS UNBALANCED — different problems, different fixes, one sentence.** ⚠⚠⚠ **AND `JournalPostingStatus`'s OWN COMMENT ON `PeriodNotFound = 2` IS THE ARGUMENT AGAINST THIS, WRITTEN IN THE CONTRACT THE CONSUMER READS: *distinct from closed, because the operator's remedy is different: define the calendar rather than reopen a period.*** **The contract states the reason for a distinction the consumer discards.** **Same shape as `CalendarErrors` closed-versus-absent, one module downstream.** | ⚠ **SEPARATE FROM 249 BY RULING, NOT BY OVERSIGHT: it is a USER-VISIBLE MESSAGE CHANGE and folding it into a concurrency fix would hide it in a commit about locks.** **249 leaves the four collapsed and, after its explicit-mapping change, collapses them BY DECISION AT THE MAPPING rather than by falling through — so this item starts from four named arms that happen to share a value, which is a much smaller job than it looks.** ⚠⚠ **EACH NEW `PayrollErrors` MEMBER IS A USER-FACING STRING AND A MAPPED STATUS CODE — check what the API answers for each before choosing, and do NOT invent a remedy the product cannot perform.** **PER-MEMBER AND STOPPABLE: distinguishing one of the four is a complete improvement on its own.** |

## How this file is maintained

**The architect adds items; the coder removes them by doing them and says so.** An item that turns out to
be already done, dissolved on measurement, or not worth doing is a **legitimate outcome** — report that and
strike it, exactly as 81 dissolved and 82 was cancelled. **A backlog that only grows is a wish list.**
