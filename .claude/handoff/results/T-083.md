# T-083 result — 27 sites, five categories, two corrected

- **Status:** PENDING MERGE. **Gated.** **Not self-merged.**
- **Branch:** `agent/T-083-stale-mapping-claims`, from `ClaudeBranch` at `e571216`
- **`GATE_SCOPE=TASK` GREEN.** `condition 4: ok: no non-comment change under src/` — **comment-only, as predicted.** No baseline movement.
- **No truncated search anywhere in this task** (`DEC-L-069`). Counts stated before any edit.

---

# THE COUNTS, BEFORE ANY EDIT

```
(1) the MAPPING does not exist          — now FALSE              9 sites
(2) SELF-SERVICE does not exist         — still TRUE, untouched  4 sites
(3) the mapping is WHY it is blocked    — reason expired         9 sites
(H) HISTORICAL RECORD                   — dated, must NOT change 7 sites
(T) a TRIGGER that has now fired        — actionable             1 site
```

**Two corrected, both ruled by you. Twenty-five reported and untouched.**

---

# ⚠ (T) THE FINDING I WOULD PUT FIRST — A PRE-REGISTERED TRIGGER HAS FIRED

`src/Modules/Attendance/SSAS.Attendance.Domain/Leave/LeaveRequest.cs:225`

> **"When the identity-to-employee mapping is built, this is the first thing that should be tightened."**

**It is built.** The comment sits above `ApproveAtRoot`, and the thing it says to tighten is real:

> *"The self-approval bar cannot be enforced on this path. `Approve` compares an approver EMPLOYEE to the
> requester; here the actor is a user whose employee identity is unknowable. If the root-fallback holder
> happens to be the requesting employee, nothing here can tell."*

**A holder of `Attendance.Leave.ApproveAtRoot` can approve their own leave request and nothing detects
it.** The file says so, calls it *"a real consequence of the missing mapping and not a design choice"*,
and names the mapping as the unblocking condition.

**This is not a stale claim to correct. It is a capability that became possible while nobody was
watching**, and it is the only site in the sweep with a live consequence rather than a wording problem.
**Reported, not acted on** — closing it is an Attendance change with an acceptance criterion behind it.

---

# (1) THE MAPPING DOES NOT EXIST — NOW FALSE. NINE SITES, NONE RULED, NONE TOUCHED.

```
src/Modules/Attendance/SSAS.Attendance.API/AttendanceTransportContracts.cs:135
src/Modules/Attendance/SSAS.Attendance.Application/Approval/LeaveApprovalRouter.cs:99
src/Modules/Attendance/SSAS.Attendance.Application/Leave/LeaveCommandHandlers.cs:214
src/Modules/Attendance/SSAS.Attendance.Domain/Leave/LeaveRequest.cs:15
src/Modules/Attendance/SSAS.Attendance.Domain/Leave/LeaveRequest.cs:212
src/Modules/Attendance/SSAS.Attendance.Infrastructure/Persistence/AttendanceConfigurations.cs:363
tests/Attendance.Tests/Leave/LeaveDomainTests.cs:285
docs/00-Master-Product-Specification/Requirement-Catalog/ATT.md:25
docs/17-features/FP-013-attendance/api-contracts.md:112
```

**All nine say some form of "no identity-to-employee mapping exists", most with "verified".** They are
flatly false now.

**Proposed wording, one shape for all nine** — they differ only in surrounding voice:

> **The mapping exists as of `ADR-030` / `UserEmployeeLink` (T-082); what these paths still lack is a
> resolved employee ON THIS REQUEST, because nothing yet reads the link.** The behaviour each comment
> justifies is unchanged — the actor is still authenticated as a user and no code resolves them to an
> employee — **but the reason is now "not wired" rather than "does not exist".**

**That distinction is why I did not write it.** Eight of the nine justify a behaviour that is still
correct; changing "does not exist" to "is not read here" keeps every one of them true. **But
`ATT.md:25` is a Master Product Specification line and `api-contracts.md:112` is a published contract —
those are yours, and possibly the owner's.**

---

# (2) SELF-SERVICE DOES NOT EXIST — STILL TRUE. FOUR SITES, UNTOUCHED.

```
docs/17-features/FP-013-attendance/acceptance-criteria.md:74     AC-ATT-0032
src/Modules/Attendance/.../AttendancePermissionNames.cs           the absence of the constant itself
src/Modules/Payroll/.../PayrollPermissionNames.cs:78              ViewPayslips, with no ViewOwn beside it
tests/Architecture.Tests/AttendanceArchitectureTests.cs:290       the assertion
```

**All true. T-082 created no permission and no endpoint.**

### But `AC-ATT-0032` is a category-2 assertion with a category-3 reason inside it

> **"No `ViewOwn` permission exists in the catalog**, because no identity→employee mapping exists to
> resolve its subject"

**The claim before the comma is true. The clause after it is false.** You ruled this site stays exactly
as it is, and I have left it — **but the ruling was made on the assertion, and the reason is embedded in
the same sentence.** Flagging it rather than assuming the ruling covered a half you had not separated.

---

# (3) THE REASON EXPIRED — NINE SITES. TWO CORRECTED, SEVEN REPORTED.

## Corrected, both ruled by you

**`tests/Architecture.Tests/AttendanceArchitectureTests.cs:279`** — replaced, not deleted. Now states
that the mapping exists, that FP-015's permission and endpoint are what is still absent, that the
assertion therefore stands, **and that the input it was waiting for has arrived.**

**`src/Modules/Payroll/.../PayrollPermissionNames.cs:68`** — same substance in that file's voice: the
dependency is satisfied, **the absence is now a scope decision rather than a blocked one**, and adding
`ViewOwn` would no longer rest on an unverified assumption — but it is FP-015's to add.

## Reported and untouched — seven

```
src/Modules/Attendance/.../AttendancePermissionNames.cs:86    the SAME sentence as the two you ruled
docs/17-features/FP-013-attendance/requirements.md:67         REQ-ATT-0023
docs/00-Master-Product-Specification/Requirement-Catalog/ATT.md:137
docs/17-features/FP-012-payroll/authorization-model.md:77
docs/17-features/FP-013-attendance/authorization-model.md:20
docs/17-features/FP-013-attendance/authorization-model.md:39
docs/17-features/FP-013-attendance/acceptance-criteria.md:120
```

**`AttendancePermissionNames.cs:86` is the third instance of the two you ruled** — the same claim, same
words, in Attendance rather than Payroll. **I did not write it, because you ruled two sites by name and
said you would not pre-authorise wording for text you had not read.** Proposed wording: the Payroll
replacement, in Attendance's voice.

**`REQ-ATT-0023` I also left.** You ruled *status unchanged, reason replaced* — but gave no wording, and
said you would not pre-authorise any. Proposed:

> **BLOCKED — NOT IMPLEMENTABLE TODAY:** it needs a self-service permission and an endpoint, neither of
> which exists. *(The identity-to-employee mapping it originally waited on now exists — `ADR-030`,
> `UserEmployeeLink`, T-082.)*

**`ATT.md:137`** — *"Defer self-service until an identity-to-employee mapping exists"* — **the condition
it names has been met.** Deferral may still be right; the stated trigger has fired.

---

# (H) HISTORICAL RECORD — SEVEN SITES. A CATEGORY YOU DID NOT NAME, AND I THINK IT MATTERS.

```
docs/17-features/FP-013-attendance/decisions-open.md:508, 517, 529
docs/17-features/FP-013-attendance/decisions-ratified.md:25
docs/17-features/FP-013-attendance/traceability-matrix.md:135, 147
docs/17-features/FP-012-payroll/decisions-approved.md:409, 412
```

**These record what was decided or found ON A DATE.** *"RULED: SELF-SERVICE DEFERRED. No
identity→employee assumption anywhere"* was true when it was ruled, and it is a record of a ruling
rather than a claim about today.

**Correcting them would rewrite a decision log**, which is a different and worse failure than leaving a
dated statement dated. **They are not stale; they are historical.** I am proposing they be explicitly
out of scope for this and any future sweep, rather than left to each sweeper's judgement.

*(The same reasoning excludes `.claude/handoff/results/` and `.claude/handoff/tasks/`, which quote the
old wording and are records of what a task found.)*

**`ADR-030` and everything under `docs/14-Engineering/ADR/` was excluded by your instruction and I did
not read it for this sweep beyond confirming the exclusion.**

---

# THE ASSERTION QUESTION — AND THE ANSWER IS SHARPER THAN "MOST COULD NOT"

**Three of the nine category-3 sites could have been ONE assertion, and it is the same one:**

```
AttendanceArchitectureTests.cs:279     "this build does not assert such a mapping exists"
PayrollPermissionNames.cs:69            the same sentence
AttendancePermissionNames.cs:86         the same sentence again
```

**A single test — no type in `SSAS.Platform.Domain` pairs a user identifier with an employee identifier
— would have covered all three, and T-082 would have reddened it on the day the mapping landed.**

**That is the finding.** Three files, in two modules, independently wrote the same unverifiable prose
claim, each ending in *"verified, not assumed"* — **and none of them verified anything.** The word
"verified" described work someone did once by hand, not a check that would run again.

**The remaining six could not have been assertions**, and that is not a failing:

- `REQ-ATT-0023`, `ATT.md:137`, the two `authorization-model.md` sites and `acceptance-criteria.md:120`
  are **specification prose about a decision**. An assertion could have checked the *fact* they rest on,
  but not the deferral itself, which is a judgement.
- **The nine category-1 sites are the same story**: the fact was assertable, the sentence was not.

**So the transferable form is narrower than "write assertions instead of comments":** *when a comment
says a thing does not exist and calls itself verified, the verification is the assertable part, and it
belongs in one place rather than repeated in three files that cannot notice each other going stale.*

---

## For the architect

- **Blocked / needs a decision:** the trigger at `LeaveRequest.cs:225`, and wording for the 16
  untouched category-1 and category-3 sites.
- **Two sites corrected, both yours, replaced rather than deleted.**
- **`AC-ATT-0032`'s embedded reason** — your ruling covered the assertion; the reason is in the same
  sentence and is false.
- **A fourth category, (H)**, proposed as permanently out of scope.
