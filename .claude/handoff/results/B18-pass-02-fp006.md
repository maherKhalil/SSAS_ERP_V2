# B18 pass 02 — FP-006: 30 examined, 17 cited

**TASK gate green, 0 warnings.**

## The two numbers

| | this pass | running |
|---|---|---|
| criteria **examined by body** | **10** (`AC-EMP-0021`…`0030`) | **30 of 47** |
| **cited** | **7** (`0020`–`0026`) | **17 of 47** |
| ⚠ found **uncovered** | **0** | **0** |
| ⚠ **partly pinned** (new B18 clause) | **0 new** | 2 completed in items 220–221 |

## ⚠⚠ THE SPEC AND THE TESTS SHARE A LETTER SCHEME, AND NOBODY WROTE IT DOWN

`AC-EMP-0020`…`0023` are titled **"V:"**, **"W:"**, **"X:"**, **"Y:"**. The tests are named
`V_Creating_a_real_employee_invokes_the_branch_write_authorizer_and_stamps_the_branch`,
`W_A_spoofed_branch_on_employee_create_is_refused`, `X_An_ordinary_update_cannot_change_an_employees_branch`,
`Y_A_cross_branch_employee_update_is_refused` and `Y_A_cross_branch_employee_delete_is_refused` — **and the
file's section comments repeat the letters.**

⚠ **A deliberate, exact, four-way correspondence that existed only as a shared prefix.** **The fourth time
this sweep has found a test that knew precisely which criterion it was proving, with nothing to say so.**

**I still read the bodies.** A letter is a name, and the discipline does not get suspended because the name
looks convincing:

- **W** asserts the create throws **and `EmployeeCountAsync() == 0`** — ⚠ which is what makes it *refused*
  rather than *silently rewritten*, the exact distinction its criterion draws.
- **Y** takes **two** tests because the criterion names update **and** delete.

## `AC-EMP-0024`–`0026`: the revocation trio

Near-verbatim names — *"Revoked Tenant Administrator authority removes implicit branch scope"* against
`Revoking_administrator_authority_mid_session_removes_implicit_branch_scope`.

⚠ **Each body carries its own anti-vacuity control without calling it one:** every test **creates
successfully first**, then revokes, then fails. **So the refusal is the revocation rather than a boundary
that refuses everything** — the same shape the file states explicitly for the V/W/X/Y block.

**`AC-EMP-0025` is the strongest of the three**: it first asserts the administrator holds **zero**
branch-access rows, so the create that succeeds is provably running on *implicit* scope, which is what the
criterion is about.

## Left unresolved this pass

**`AC-EMP-0027`** (bounded scoped search), **`0028`** (cross-boundary opacity), **`0029`** (explicit
company/branch predicates on every read), **`0030`** (no global company/branch query filter).

⚠ **Candidates exist but none matched cleanly** — `0028`'s nearest neighbours are *branch from another
tenant* and *company outside the authorized set*, which are **Branch and Company subjects, not Employee**.
**That is the foreign-subject near-miss again, and I did not cite past it.**

## The rate, and the judgement

| pass | examined | cited | rate |
|---|---|---|---|
| item 218 | 10 | 4 | 0.40 |
| pass 01 | 10 | 6 | 0.60 |
| **pass 02** | **10** | **7** | **0.70** |

⚠ **The rate ROSE, against my own prediction in pass 01 that the tail would be harder.** The cause is
visible: criteria 20–26 are the branch-boundary and revocation blocks, and **`EmployeeBoundarySqlServerTests`
was written directly against them** — the letter scheme is the evidence.

**My tail warning still stands but is deferred**: 27–30 are where it started to bite, and **all four went
unresolved this pass**. **Continuing is worth it; the next pass is the honest test of the prediction.**

## Scope
- **Seven citations, seven bodies read.** No citation rests on the letter scheme alone.
- The revocation trio's built-in controls were **read, not assumed** — the leading success is in each body.
- FP-006: **17 of 47 cited, 30 of 47 examined, 0 uncovered.**
