# T-111 — **`salaryType` WAS THE ONLY ONE. KIND 1 IS ZERO ACROSS ALL 59 CALL SITES.**

**You did not ship four features nobody can reach. You shipped one, and it is fixed.**

- **Status:** ESTABLISH ONLY. **Nothing built, no guard, no fix.** Branch `agent/T-111-strict-schema-sweep`,
  from `ClaudeBranch` at `e33c9ef`, pushed on start.

---

# THE COUNTS

```
KIND 1  contract member absent from the dictionary (UNREACHABLE)          0
KIND 2  dictionary key with no contract member (DEAD / TYPO)              0
KIND 3  value-kinds that cannot express the member's type                 1, and it is my own line
KIND 4  optional member named in requiredFields                           0 — the premise does not hold
```

**THREE of the four started non-zero and all three collapsed.** The raw walk reported 1 kind 2, **71** kind 3
and 46 kind 4. **Kind 2 and kind 4 I collapsed myself; kind 3 I shipped, and the architect caught it.** With
the requiredness join applied, kind 3 falls from 71 to 1 — and that one is a line I wrote yesterday.

---

# 59 CALL SITES, AND YOUR NUMBER WAS RIGHT — MY CORRECTION WAS WRONG

I first told you 61 hits minus **one** definition = 60. **It is 61 minus two = 59, exactly as you said.**

**The second definition is not in `StrictRequestReader`.** `LocalizationEndpointRouteBuilderExtensions.cs:439`
declares its **own private `ReadStrictJsonAsync<T>`** — and that file references `StrictRequestReader`
**zero times.**

**Localization is running a complete second implementation of the strict reader.** `DEC-L-080` at the
mechanism level: one rule, two implementations, and nothing makes them agree. **You were right for a reason
neither of us had.**

---

# KIND 2 — MY WALK'S OWN UNDER-RESOLUTION, CAUGHT

The walk reported `placeholderValuesByResource` as a dictionary key with no member. **It has one** —
`LocalizationTransportContracts.cs:24`:

```csharp
  [property: JsonPropertyName("placeholderValuesByResource")]
  IReadOnlyDictionary<string, IReadOnlyDictionary<string, string>>? PlaceholderValuesByResource);
```

**The attribute is on its own line and my regex required attribute and member together.** `DEC-L-078`
exactly: the walk under-resolved and produced a *finding* rather than an error. **Corrected by reading the
one hit rather than trusting the count** — which is the only reason a zero is a zero here.

---

# KIND 4 — THE PREMISE DOES NOT HOLD, AND 46 "DEFECTS" DISSOLVE

`CreateEmployeeRequest` declares **every** member nullable — `fullName`, `employmentDate`, all of them —
and its own comment at `:30` says `positionId` is *"REQUIRED FROM DAY ONE... the column ships NOT NULL."*

**Nullability in these DTOs is a DESERIALIZATION convention, not an optionality signal**: a missing JSON
member must deserialize to null rather than throw. **`requiredFields` is what expresses requiredness, and
the two are orthogonal.** So "optional in C#, required in the schema" is the *correct* pattern.

**The real kind-4 defect — a member the API INTENDS to be optional but which `requiredFields` makes
mandatory — is not detectable from types at all.** It needs intent, and no walk has it.

---

# KIND 3 — MY THIRD FALSE POSITIVE, AND THE ONLY ONE I SHIPPED

**CORRECTED after the architect checked it. The `reasonCode` "inconsistency" is the rule working.**

`StrictRequestReader.cs:45` — `var required = requiredFields ?? fields.Keys`. **A site that passes no
`requiredFields` makes EVERY declared key required.** Verified at all six sites:

```
Department:428  reasonCode NOT in requiredFields   optional   accepts Null   OK
Position:779    reasonCode NOT in requiredFields   optional   accepts Null   OK
Employee:376    reasonCode IS in requiredFields    required   refuses Null   OK
Employee:341    no requiredFields -> all required  required   refuses Null   OK
Employee:456    no requiredFields -> all required  required   refuses Null   OK
Company:210     no requiredFields -> all required  required   refuses Null   OK
```

**Six sites, one rule, zero divergence: a required member refuses explicit null, an optional one accepts
it.** My claim was *"nobody decided that"* — **the requiredness decided it.**

## And it is worse than not having looked

**My own analyzer had already parsed `requiredFields` — for kind 4 — in the same record.** The join was one
line away and I never made it. I compared the dictionary window against the record and never crossed it with
the argument list I had in hand.

**I collapsed kind 2 by reading the single hit and kind 4 by reading the record's comment, then reported
kind 3 without doing to it what I had just done twice.** A run that has already killed two findings feels
like it has paid its verification debt. **It has not — and the sweep's whole value was that its zeros are
trustworthy, so one unchecked claim rides out on that credit.**

## With the join applied, kind 3 is 1 — and it is mine

```
PayrollEndpointRouteBuilderExtensions.cs:183   RecordCompensationRequest.salaryType (SalaryType?)
                                                optional, nullable, declared [String, Number] with no Null
```

**The line I added yesterday.** By the rule the six `reasonCode` sites follow, an optional member accepts
explicit null — and mine does not. `"salaryType": null` is a 400 where `reasonCode: null` is a 200.

**Cosmetic** — a client omitting the field gets the same Monthly — **but it is the sweep's one surviving
finding and it is the sweep author's own line.** Not fixed here: T-111 was ruled no-fix. One token.

# THE GUARD — I ARGUE AGAINST BUILDING ONE, AS YOU EXPECTED TO HAVE TO ASK

**Kind 1 is zero.** The expensive defect has exactly one historical instance, it was introduced and caught
within days, and **the sweep that found it is now written and its floor is documented.**

**A guard would be one of the three you named, and none earns its price against a zero:**

- **A source walk** — `DEC-L-078` applies, and *this task is the evidence*: my own walk produced **118 false
  positives across three kinds** — 1, 71 and 46. **A guard built on it would have failed every build on day
  one, and the fix for each was reading, not code.**
- **~59 behavioural API tests** — tests behaviour rather than shape, which is the right instinct, but it is
  59 tests to defend a class with no live instance.
- **Nothing, with the count recorded** — **which is what I recommend.**

**What I would keep instead of a guard:** the sweep script itself is ~60 lines, resolves named static
dictionaries, and takes under a second. **Re-running it is cheaper than maintaining a guard, and unlike a
guard it cannot go green while under-resolving** — because a person reads its output.

---

# THE WALK'S FLOOR, STATED SO THE ZERO IS HONEST

```
RESOLVED   named static dictionaries — DraftFieldContract, DraftFieldContractWithRowVersion
RESOLVED   the attribute-on-its-own-line case, after it produced a false finding
NOT SEEN   additionalValidation lambdas — a member may be further constrained there
NOT SEEN   members inherited from a base record, if any request type ever gains one
NOT SEEN   any dictionary built conditionally rather than as a literal or a static field
```

**All 59 sites resolved to both a record and a dictionary. Zero unresolved.** That is what makes kind 1's
zero a measurement rather than a smaller closure — **but it is a measurement of what this walk can see.**

---

## For the architect

- **Answer to the question you wanted separately: `salaryType` was the only one.**
- **One thing worth a task, and it is not a guard:** Localization's duplicate strict reader.
- **One token, whenever it suits:** `["salaryType"]` gaining `JsonValueKind.Null`, so an optional member
  behaves like every other optional member. **Mine, and the sweep's only surviving finding.**
- **Recommend: no guard.** Count recorded, script kept — **and the script now makes the requiredness join it
  should have made the first time**, which is the one durable thing this correction produced.
