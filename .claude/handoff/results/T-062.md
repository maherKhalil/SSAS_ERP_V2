# T-062 result — rule 4, ranges, the parser, two criteria, and one false declaration

- **Status:** PENDING MERGE. **Gated** — `tests/` in scope. `[GATE GREEN]`, exit 0, **Platform 1032 →
  1037**, 0 warnings. **Not self-merged.**
- **Branch:** `agent/T-062-rule4-and-tests`, from `ClaudeBranch` at `96e0210`

## The numbers, per package, including the ones already green

```
py scripts/trace-check.py

package                                 start    now  delta
FP-001-identity-access                      2      1     -1  better
FP-002-authentication-token-lifecycle       2      2      0
FP-003-tenant-lifecycle                     2     12    +10  WORSE — the real gaps
FP-004-localization                         1      0     -1  better
FP-005-company-legal-entity                 2      2      0
FP-006-hr-employee                          2      2      0
FP-007-hr-department                        0      0      0  stays green
FP-008-hr-position                          2      1     -1  better
FP-009-hr-employee-import-export            9      8     -1  better
FP-010-hr-employee-documents                0      0      0
FP-011-gl-foundation                        7      5     -2  better
FP-012 / FP-013 / FP-014                    0      0      0  stays green
TOTAL                                      29     33     +4
```

**Five packages better, four modern packages untouched, and the only package worse is FP-003 — whose
ten additions are the real gaps T-061 identified and FP-003 itself marks *"implementation pending"*.**

Against T-061's prediction of 35, this lands at 33: the difference is FP-001, where the two criteria
below are now traced.

## The three pieces

**Piece 1 — rule 4 reads `test-scenarios.md`.** It was wrong about a *fact*: `AC → TS` is not recorded
only in the matrix. The pairing must be **on one row**; a file-wide test would make rule 4
unfalsifiable, since every criterion co-occurs with some scenario in a file listing both.

**Piece 2 — ranges expand**, in both spellings the corpus uses. A reversed span expands to nothing
rather than to a guess; a span wider than 200 is refused so a typo cannot mark a package covered.

**Piece 3 — the chain parser is anchored by `REQ-`, `BR-`, `BRULE-` or `DEC-`, and finds criteria and
tests by CONTENT rather than by column offset.** FP-003's chain table has seven columns; reading cell 2
and cell 3 is why a table could match the anchor and still yield nothing.

## Two criteria that were enforced and untested

`AC-IAM-0012` (cross-tenant assignment rejected) and `AC-IAM-0022` (a stale update is rejected without
overwriting newer data). Both invariants were **already live in the code** — which is what makes them
worth testing: an enforced guard with no test can be removed by a refactor and nothing catches it.

**`AC-IAM-0022` is two properties with two owners**, and testing them as one is what made it look
expensive:

- *"rejected"* — a handler test: a stale `ExpectedRowVersion` yields `ConcurrencyConflict` **and the
  unit of work is never asked to save.**
- *"without overwriting"* — a **model** test: `TenantUser.RowVersion` is a concurrency token in the
  built `IModel`. A round-trip would test EF Core's rowversion implementation, which is Microsoft's.
  **What can regress is someone deleting `.IsConcurrencyToken()`** — caught here in milliseconds
  instead of in a 33-minute Integration leg. **No database, no Integration, no condition 3.**

**Both have negative controls** — same-tenant assignment succeeds, a matching row version reaches the
save. Without them the tests pass against a guard that rejects unconditionally.

### A third test was not written

**`AC-IAM-0019` is not untraced and I said it was.** It sits inside the range
`AC-IAM-0018`–`AC-IAM-0020` on a row carrying four scenarios, and read as untraced only because rule 4
did not expand ranges. The architect verified its enforcement in the domain on the strength of my
report — **a correct answer to a question that should not have been asked.** No test was written to
make the earlier report true.

## FP-007's declaration, corrected

It said the matrix *"maps requirements to code symbols and test class names rather than to AC-/TS-
identifiers."* **The file holds four chain-bearing tables and 24 rows pairing an `AC-` with a `TS-`.**
True of the rows the parser matched; false of the file — and it was buying FP-007's green until piece 1
landed. It now describes **the rows it examined** and says the file may pair criteria with scenarios in
tables the parser does not match.

## Four attempts at piece 3, and the measured behaviour was the specification

The shipped parser did not match the T-061 dry run, and each attempt to improve on it made things
worse:

```
positional fallback for missing cells       76 failures
skip rows carrying neither AC nor TS        34, but FP-014 green -> red
narrowed declared-gap fallback              56
no fallback and no skipping (the harness)   33
```

**The harness appended every anchored row with empty cells, and `is_declared_gap("")` is true** — so a
row naming neither criterion nor scenario is *reported* as a declared gap rather than failed, exactly
as an em-dash is. I did not notice that until the fourth attempt, and **each of the three wrong
versions looked reasonable while I was writing it.**

The intermediate failures are worth keeping because of what they cost elsewhere: the skip version
turned FP-014 from green to red by dropping `REQ-SUB-0026`'s row, which declares an em-dash in both
cells — **a package saying honestly that it has a gap, failed for saying so.** That is the shape the
per-package table exists to catch, and a total would have shown 34 and looked like a win.

## For the architect

- **Blocked / needs a decision:** nothing.
- **Touched outside scope:** nothing. `scripts/`, one test file, FP-001's two tracing files, and the
  gate's baseline.
- **What remains:** FP-003's ten, which its own file marks implementation pending, and FP-009's eight,
  which are the orphan and contiguity classes T-060 scoped out.
