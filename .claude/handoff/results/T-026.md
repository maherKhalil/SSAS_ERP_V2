# T-026 result — DONE

- **Status:** DONE
- **Branch:** `agent/T-026-repoint-remainder` — **branched from `main`** at `7a61cc4`, pushed
- **Merged:** no — non-gated (`DEC-L-002`). Awaiting `MERGE T-026`.
- **Commits:** `316002e` — docs(T-026): finish the `DEC-L-024` re-point — `ADR-021` binds nothing in FP-014
- **Reported:** 2026-08-25

**Branched from `main`, not continued on the T-024 branch** — you merged T-024 before issuing this, so
`main` already carried it and continuing would have added nothing but a stale base.

## Acceptance criteria

- [x] **`data-model.md`** re-pointed — `ADR-017:164`, `:169`, `:376`–`:378`.
- [x] **`README.md`'s outage paragraph** re-pointed likewise.
- [x] **`README.md`'s ratification banner corrected** — three open concerns → two, with the third
      recorded as closed by `DEC-L-024` rather than deleted.
- [x] **`ADR-021` appears nowhere in FP-014 as a binding authority.** Package-wide grep below.
- [x] **`trace-check` GREEN, 0 unruled**, and the full 14-package run **byte-identical** to the T-024
      baseline.
- [x] `--stat` — three files, all under `docs/17-features/FP-014-subscription/`.

## The criterion that matters — the whole-package grep

`ADR-021` survives in **six files, 27 lines**. Every one falls into one of three classes, and none is
authority:

| Class | Where | Example |
|---|---|---|
| **Change record** — "formerly", "amended by", "was removed" | `requirements.md:44`, `:67`; `business-rules.md:76`; `data-model.md:24`–`:26`; `decisions-open.md:29`–`:30`, `:69`–`:71`, `:218`, `:262`, `:510`–`:511` | *"**Amended by `DEC-L-024`** — formerly `ADR-021:207`, which is `Proposed` and does not yet bind"* |
| **Explicit non-binding label** | `decisions-ratified.md:7` banner; `decisions-open.md:510` | *"`ADR-021` was in this list and was **removed** by `DEC-L-024`"* |
| **Historical record**, framed as such | `decisions-ratified.md:194`–`:222`, closed concern 3 | see below |

**All three authority cells now read `ADR-017`:**

```
REQ-SUB-0005  | `ADR-017:164` places subscription/plan metadata in the Platform database and
                `ADR-017:169` makes Platform-database operation independent of tenant-database
                availability; `:376`–`:378` require a controlled unavailability result rather than
                a fallback. Amended by `DEC-L-024` — formerly `ADR-021:207` …
REQ-SUB-0013  | `ADR-017:169`, `:376`–`:378` (amended by `DEC-L-024`, formerly `ADR-021:200`–`:207`) …
BR-SUB-0008   | `ADR-017:169`, `:376`–`:378` (amended by `DEC-L-024`, formerly `ADR-021:207`) …
```

### One thing the criterion caught that I would have missed

**Closed concern 3 was written in the present tense** — *"`REQ-SUB-0005` **cites** `ADR-021:207` as its
authority"*, *"`decisions-open.md` **states** outright that `ADR-021` constrains this package"*. Both
sentences were true when I wrote them in T-021 and are false now, sitting under a heading that says
`CLOSED`.

A strikethrough heading is not a label on the sentences beneath it. So the section gained a framing
block saying the text below describes the position **before** the amendment, that `ADR-021` still does
not bind, and that the citations it names have all been re-pointed. **The record is kept rather than
rewritten** — deleting the finding would erase why the amendment happened — but it can no longer be
read as a live statement.

I found this only because you asked for the grep as evidence rather than the edits as a checklist. My
own line-based filter passed those two lines over, since their labels sit on adjacent lines; reading
the section is what caught it.

## Both your corrections applied as binding

- **`:169`, not `:171`.** Every one of the three new derivations cites `:169` and `:376`–`:378`. `:171`
  appears nowhere in this diff. It is the passage that states the rule most forcefully and it opens
  *"Customer-managed hosting makes this decisive"* — citing it would have routed the repair back
  through the very decision being re-pointed away from.
- **`:164`, not `:162`.** Used throughout; `:162` is global catalogs and `LocalizationCatalogState`.

## The mechanical check

```
FP-014-subscription  [GREEN]  0 failure(s), 0 warning(s), 4 declared gap(s)
Owner decisions: 17 defined, 0 unruled -> ratified, build may proceed
```

**The full 14-package run is byte-identical to the T-024 baseline** — `diff` returns nothing. Same
check as T-024 and the same reasoning: an authority re-point should move no identifier, no count, no
warning and no gap. It is now the second consecutive run where that holds, which is what makes it
evidence rather than a coincidence.

## For the architect

- **Blocked / needs a decision:** none. `DEC-L-024` is fully applied.
- **Touched outside scope:** nothing. Three files, all inside the package. `ADR-021` untouched — still
  `Proposed`, content unchanged. `ADR-017` untouched.
- **No requirement, rule or ruling changed in substance** across T-024 and T-026 together. Five
  identifiers — `REQ-SUB-0005`, `REQ-SUB-0013`, `BR-SUB-0008`, `DEC-SUB-0004`, `OD-SUB-0004` — say
  exactly what they said before; only what stands under them moved.
- **FP-014's citations are now sound, which is what T-022 was waiting on.** `BR-SUB-0001`…`0021` can go
  into the master `Business-Rules.md` without carrying a `Proposed` ADR's citation with them.

### On `DEC-L-025` and your correction

Noted, and it makes the rule better rather than weaker: a rule that cannot self-trigger but governs how
we stop is still worth having, provided nobody believes it is watching for them. **I have still seen no
usage warning in my own context** — reported again rather than assumed carried over from last turn,
since that is the whole point of the protocol.

**And the same honesty applies to my end of it:** I cannot tell you how much allowance this session has
consumed, only that nothing in my context has warned me. If you see one first, I will not know unless
you say so.
