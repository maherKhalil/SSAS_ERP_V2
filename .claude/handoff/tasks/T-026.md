# T-026 — Finish the `ADR-021` re-point: the three sites T-024's criterion could not reach

- **Branch:** `agent/T-026-repoint-remainder` — **continue on `agent/T-024-repoint-adr-021`** if that is
  cleaner, and say which you chose
- **Module:** Platform (commercial plane)
- **Issued:** 2026-08-25
- **Depends on:** T-024

## Goal

You found six sites where my criterion assumed at most four. You re-pointed four and stopped, which
was right — exceeding a stated criterion on your own judgement is not yours to take, and you said so.

But the consequence is that **FP-014 still asserts `ADR-021` as binding in two places**, which is the
exact condition `DEC-L-024` was ruled to end. This authorises the rest.

## Acceptance criteria

- [ ] **`data-model.md:21`** re-pointed on the same basis as the other five — `ADR-017:164` and `:169`,
      with `:376`–`:378` where the controlled-unavailability behaviour is what is relied on.
- [ ] **`README.md:160`** re-pointed likewise.
- [ ] **`README.md:17` corrected.** You wrote it in T-021 and it says three concerns are open; concern 3
      was closed by T-024. A ratification banner that misstates its own outstanding items is the defect
      class this package has fixed twice in other files.
- [ ] **`ADR-021` appears nowhere in FP-014 as a binding authority.** Grep the whole package and show
      the result — every remaining mention is either absent or explicitly labelled as an
      implementation-deferred ADR that does not yet bind.
- [ ] `py scripts/trace-check.py FP-014-subscription` **GREEN, 0 unruled**, and the full 14-package run
      byte-identical to the current baseline. An authority re-point moves no identifier and no count.

## Files in scope

- `docs/17-features/FP-014-subscription/data-model.md`
- `docs/17-features/FP-014-subscription/README.md`
- `docs/17-features/FP-014-subscription/decisions-ratified.md` — **only** to update closed concern 3's
  record of what remained outstanding, since this task is what closes it

## Out of scope

- `ADR-021`, `ADR-017`, and every other ADR.
- The master registers, the roadmap, the numbering prefixes — T-022, next.
- Any file under `src/` or `tests/`.

## Design constraints

- **Use `:169`, not `:171`.** Your circularity check is now binding on this task: `:171` opens
  *"Customer-managed hosting makes this decisive"*, so citing it would route the repair back through
  the deferred decision. `:169` states the independence unconditionally. Cite what you cited.
- **`ADR-017:164`, not `:162`.** My reference was off by two and you caught it; `:162` is global
  catalogs and `LocalizationCatalogState`.
- Carry the same stated cost as `DEC-SUB-0004` does — the derivation is now an inference across two
  passages where the old citation was one quotation.

## Tests required

**None** — `DEC-L-002`. The `trace-check` run and the package-wide grep are the checks.

## References

- `DEC-L-024`, and your T-024 result, which names both outstanding sites in closed concern 3.
