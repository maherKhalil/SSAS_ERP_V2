# T-030 — The nine remaining FP-014 line citations

- **Branch:** `agent/T-030-remaining-anchors` — branch from `main`
- **Module:** Platform (commercial plane)
- **Issued:** 2026-08-25
- **Depends on:** T-029 merge

## Goal

You named the remainder exactly — nine citations across three files — and separated them into two
defect kinds rather than one list. This finishes the package.

**`domain-model.md:50` first**, because it carries the same **wrong-content** `ADR-017:162` defect: it
claims to place subscription metadata and reads *"Global catalogs and reference definitions"*. The
other eight carry the **imprecise** `:475`, which lands on the `## Lookup classification` heading two
lines above the bullet it quotes.

That distinction is now part of `DEC-L-028`, and it is yours.

## Acceptance criteria

- [ ] **`domain-model.md:50` corrected and anchored** — `ADR-017` § Platform database boundary, with
      the current line beside it. It is a *wrong* citation, not merely a fragile one.
- [ ] **The eight `:475` citations anchored** — `domain-model.md:65`/`:103`, `decisions-open.md:27`/`:261`,
      `requirements.md:41`/`:42`, and any other the file search finds. § Lookup classification class A,
      with the line beside it.
- [ ] **The five `ADR-005:248` uses are anchored too**, on the same basis. You verified the line is
      still accurate — `ADR-005` is unedited and `:248` still reads "- Subscription management" — so
      this is fragility, not error, and the fix is the anchor rather than a correction.
- [ ] **Every line number kept.** `ADR-017` is 819 lines and its sections are long; a reader wants the
      line. The rule is that it must not be the only handle.
- [ ] `trace-check` **full run byte-identical** to the T-029 baseline; FP-014 **GREEN `0 unruled`**.
      `git diff --stat` shows **at most four files**, all under `docs/17-features/FP-014-subscription/`.

## Files in scope

- `docs/17-features/FP-014-subscription/domain-model.md`
- `docs/17-features/FP-014-subscription/decisions-open.md`
- `docs/17-features/FP-014-subscription/requirements.md`
- One more inside the package if the search finds citations there — **report it**.

## Out of scope

- **Revision History rows anywhere.** Your T-029 judgement stands and generalises: a revision row
  records what was cited at the time, and rewriting it makes history report something that did not
  happen.
- **The eight `ADR-0##:<line>` citations outside FP-014.** Still undecided as a repository-wide act.
- Any file under `src/` or `tests/`.

## Design constraints

- **A dated statement keeps its dated citation.** You found that T-027 "corrected" a survey table
  stamped *"Run against `main` at `f9b247a`"* to current line numbers, so a dated record cited lines
  that did not exist at the commit it names. Where a passage is stamped to a commit, the anchor is the
  fix and the line number must not be silently updated to the present.
- Same conversion pattern as T-029: identifier or section first, line beside it.

## Tests required

**None** — `DEC-L-002`. The byte-identical `trace-check` run is the mechanical check.

## References

- `DEC-L-028` as it now stands, including the wrong-versus-imprecise distinction you drew.
- Your T-029 result, which enumerates all nine and verifies `ADR-005:248`.
