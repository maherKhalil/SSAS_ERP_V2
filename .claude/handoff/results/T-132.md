# T-132 — **GL's error mapping is clean. Its write path has never met a database, and the spec names a permission that does not exist.**

- **Status:** ESTABLISH ONLY. **No code changed.** Docs-only, non-gated — **pushed and stopped** per `DEC-L-002`.
- Branch `agent/T-132-gl-establish`, from `ClaudeBranch` at `eca3d7d`.

---

# ⚠ 1. THE DATABASE QUESTION — AND YOUR CONTEXT WAS HALF RIGHT, WHICH IS THE INTERESTING HALF

**`codex/fp-011-gl-analysis` is merged and it did real work.** GL has a real-SQL suite and the cutover
guards know its entities:

```
GlSchemaSqlServerTests          column types, FKs, unique indexes, no-rowversion    SCHEMA ONLY
TenantCutoverCopySqlServerTests GL's entities copied across a cutover
PayrollChainSqlServerTests      posting, end to end
```

**But not one of GL's ten command handlers has ever run against SQL Server.**

```
CreateAccount  CreateJournalDraft  DefineFiscalYear  DiscardJournalDraft  PostJournalDraft
RenameAccount  ReverseJournal  SetAccountActivation  SetFiscalPeriodState  UpdateJournalDraft
              integration = 0        unit/API = 1 each, against STUBS
```

`GlApiTestHost` is `StubAccountRepository`, `StubGlReads`, `StubCalendarRepository`, `StubUnitOfWork`.
**GL's handlers have never met a repository that can refuse them.**

## ⚠ AND THE POSTING PATH IS TWO IMPLEMENTATIONS, ONE OF WHICH IS THE TESTED ONE

**The only GL application code any real-SQL test touches is `SSAS.GL.Application.Posting`** — reached from
the payroll chain, not from GL's own route.

```
GlJournalPoster                  payroll's path      EXERCISED against SQL Server
PostJournalDraftCommandHandler   the ROUTE's path    never
```

**Both run the same sequence** — `BeginTransaction → EnsurePostable → resolve period → check accounts →
NextJournalNumberAsync → AddAsync → SaveChanges → Commit` — **and `GlJournalPoster`'s own comment says so:**

> *"THE TRANSACTION OPENS BEFORE THE CHECKS, exactly as `PostJournalDraftCommandHandler` does."*
> *"A second posting path…"*

**The domain rules ARE genuinely shared** — `JournalDraft.EnsurePostable`, `FiscalYear.ResolveOpenPeriodFor`,
`IJournalEntryRepository.NextJournalNumberAsync`. **What is written twice is the ORCHESTRATION:** the
transaction boundary, the order of the checks, and the save-then-commit.

**That is the overtime tier's shape exactly** — one rule written twice, a comment asserting the copies agree,
and nothing keeping them in step. **With one difference that makes it worse here: only one copy is tested,
so divergence would be invisible on the untested side.**

**And `NextJournalNumberAsync` is precisely the operation a stub cannot judge.** `GlSchemaSqlServerTests`
proves the database enforces `Journal_numbers_are_unique_within_company_and_fiscal_year`; **the route's
handler has never been run against that index.**

---

# 2. ROUTE INVENTORY — NONE, AND TWO LIVE ROUTES ARE UNDOCUMENTED

**21 routes in code. 15 route patterns named in `FP-011`'s api-contracts.** The two missing are both the
**undo half of a documented pair**:

```
POST /api/gl/accounts/{id}/activation        live, undocumented   (deactivation IS documented)
POST /api/gl/fiscal-periods/{id}/reopening   live, undocumented   (closure IS documented)
```

**Neither is exotic. Each reverses an operation the specification does describe** — so the reader of that
document believes deactivating an account and closing a period are one-way.

---

# ⚠ 3. THE SPECIFICATION NAMES A PERMISSION THAT DOES NOT EXIST

```
api-contracts.md:62-64   POST /accounts, PUT /accounts/{id}, POST /accounts/{id}/deactivation
                         all three documented as  `GL.Accounts.Manage`

in code:                 GL.Accounts.Create   GL.Accounts.Update   GL.Accounts.Deactivate
grep for Accounts.Manage across src/:   NOTHING
```

**Three real permissions, documented as one that was never declared.** `GL.Accounts.Create`,
`GL.Accounts.Update` and `GL.Accounts.Deactivate` appear in **no document at all** — the only three GL
permissions of thirteen with zero documentation hits.

**This is T-103's shape** — a permission name written into a specification rather than read from
`GlPermissionNames` — **except it is the specification that is wrong and the code that is right**, and it has
never been caught because **nothing mechanical reads prose** (`DEC-L-002`).

**I am not ruling which side should move.** The code is finer-grained than the spec, and whether GL wants one
account-management grant or three is a real question, not a typo. **What is certain is that they disagree and
have since the routes shipped.**

---

# 4. THE DRAFTS FAMILY — **ANSWERED, AND IT IS CLOSED**

**T-098 added the five `journal-drafts` rows**, and the document says so in its own words: *"The five
`journal-drafts` rows below were added in T-098, and four of them document routes that were…"*.

**`GL.Drafts.Manage` (8 doc hits) and `GL.Drafts.View` (1) are both specified.** **The family you asked about
is the one part of GL's surface that has been brought into line.** The gap moved to accounts.

---

# 5. ERROR MAPPING — **CLEAN, AND I WILL SAY IT AS LOUDLY AS THE ALTERNATIVE**

```
Gl.* codes declared        31
  returned in Finance src  29
  RETURNED BUT UNMAPPED     0
  declared, never returned  2   Gl.JournalImmutable   Gl.JournalNumberConflict
```

**Zero unmapped.** GL is a seeded site in the register and its mapper is complete against everything its own
code returns. **No T-118, no T-125 here.**

**The two dead codes are a T-125 shape and I did not delete them**, for T-129's reason: `Gl.JournalNumberConflict`
names a condition **the database demonstrably enforces** (`Journal_numbers_are_unique_within_company_and_fiscal_year`),
so it is **as likely to be a missing return as a surplus constant** — and the handler that would return it is
one of the ten never run against that index.

## ⚠ My first measurement said "0 codes declared" and I nearly reported that

`new(\n    "Gl.…"` — the declarations wrap, and my pattern required the quote adjacent to `new(`. **A
confident zero from a pattern that matched nothing.** Same failure you hit on `TenantStorageErrors` today,
four hours apart, on the same kind of file. **Caught because a module cannot have zero error codes, not
because the number looked odd.**

---

## For the architect — what I would fix, in order

1. **The untested orchestration.** A GL chain test through `PostJournalDraftCommandHandler` against SQL
   Server. **T-113's argument applies unchanged, and here the sibling path is already proven to matter.**
2. **`GL.Accounts.Manage`.** One of the two sides is wrong and a reader is being misled today.
3. **The two undocumented routes.** Cheap, and both invert something documented.
4. **A GL route inventory.** Would have caught 3 automatically. Same shape as T-129's three.

**Not started — you said establish and rule.** Nothing here is trivial enough to have folded in.

- **`DEC-L-069`:** handler coverage is a full grep of all ten names across `tests/`; the permission and route
  comparisons are full greps of `src/` against `docs/`. Not samples.
