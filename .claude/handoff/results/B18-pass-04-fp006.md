# B18 pass 04 — the residue was the EASIEST pass, and my forecast is refuted

**TASK gate green, 0 warnings. Control: 126 cited across all packages, zero dangling.**

## The two numbers

| | this pass | running |
|---|---|---|
| criteria **examined by body** | **7** — the residue of 1–40 | **40 of 47** |
| **cited** | ⚠ **6** (`0027`, `0028`, `0029`, `0030`, `0032`, `0040`) | **30 of 47** |
| ⚠ found **uncovered** | **0** | **0** |
| **partly pinned** | **0 new** | 2, completed earlier |

## ⚠⚠ THE FORECAST IS REFUTED ON ITS THIRD TEST, NOT CONFIRMED

| pass | examined | cited | rate |
|---|---|---|---|
| item 218 | 10 | 4 | 0.40 |
| pass 01 | 10 | 6 | 0.60 |
| pass 02 | 10 | 7 | 0.70 |
| pass 03 | 10 | 7 | 0.70 |
| ⚠ **pass 04 — the residue** | **7** | **6** | ⚠ **0.86** |

**I said the residue would be where the rate finally fell. It is the HIGHEST rate of the sweep.**

⚠ **And the cause is the ruling's own practical consequence, applied for the first time: START FROM THE
ARCHITECTURE SUITE.** **Three of the seven were resolved in a single search** —
`EmployeeReadScopeArchitectureTests` holds `No_global_query_filter_scopes_company_or_branch`, which is
`AC-EMP-0030` **verbatim**, and `The_scope_resolver_depends_on_all_three_authorization_sources`, which is
`AC-EMP-0040`'s three dimensions **as three `Assert.Contains` lines**.

**So the forecast was wrong three times, and each failure taught a different thing:** the block was
better-tested than expected (02), architecture tests are easier not harder (03), **and the residue was
only residue because I had been searching the wrong suite first (04).**

⚠ **The forecast is now withdrawn entirely — mechanism included.** **It survived two refutations by
correcting its consequence; a third correction would be fitting the story to the number.**

## ⚠ TWO CRITERIA NAMED THEIR OWN TESTS AND NOBODY FOLLOWED THE POINTER

- `AC-EMP-0029`: *"This is verified by **executable architecture guards**, not by inspection."*
- `AC-EMP-0030`: *"…and the **two architecture guards** assert that scope is carried by explicit predicates."*

**The criteria say where their tests are. The tests do not say which criteria they serve.** ⚠ **A one-way
pointer, and the direction it points is the one nobody reads while looking at a test.** **Fifth accidental
proof of the citation case, and the first where the specification tried.**

## What the bodies added

- **`AC-EMP-0028`** — `Assert.Equal(unauthorized.Error, unknown.Error)`. ⚠ **The criterion's word is
  *identical*, and this asserts the two errors are the SAME, not merely that both are failures.** A test
  asserting both fail would have satisfied the name and missed the criterion entirely.
- **`AC-EMP-0032`** — three tests for three clauses, and the third is the sharp one:
  `Revoking_destination_branch_access_before_the_transfer_refuses_it` revokes access **before the save**,
  which is what proves the destination is **revalidated inside the transaction** rather than trusted from
  the earlier authorization. **The criterion says "revalidates both inside the transaction"; only that test
  reaches it.**
- **`AC-EMP-0027`** — three tests: out-of-range refused, **documented defaults asserted** (`PageNumber == 1`),
  and **the maximum ACCEPTED** — the other side of out-of-range, which a refusal-only test would miss.

## The one that did not resolve

**`AC-EMP-0035`** — branch history in effective order, sufficient for point-in-time attribution.
⚠ **The nearest name matches are Localization's `Effective_*` tests, which are a foreign subject.**
`Branch_history_is_scoped_through_its_employee` exists and is about **scoping**, not ordering.
**Left unresolved rather than cited past.**

## ⚠ A method fault of mine, in this pass's own commands

Computing the remainder, I generated ids with a `sed` that stripped zero-padding and produced
`AC-EMP-001`, `AC-EMP-002` — **ids that do not exist**, against a correctly padded cited set. **The
padded-versus-unpadded family, in my own throwaway command, on the day it became a principle.** Recomputed
from the file's own headings instead of generating a range.

## Where FP-006 stands

**30 of 47 cited. 40 of 47 examined. 0 uncovered.** ⚠ **The 17 uncited are NOT 17 gaps:** ten
(`0001`–`0004`, `0007`, `0009`, `0013`, `0016`, `0018`, `0035`) were examined and unresolved, and **seven
(`0041`–`0047`) have never been examined at all.**
