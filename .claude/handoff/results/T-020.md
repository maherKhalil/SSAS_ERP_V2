# T-020 result — DONE

- **Status:** DONE
- **Branch:** `agent/T-020-adr-024-025` — pushed, branched from `main` at `a907848`
- **Merged:** no — non-gated (`DEC-L-002`). Awaiting `MERGE T-020`.
- **Commits:** `d74a380` — docs(T-020): ADR-024 and ADR-025 move to Accepted, on labelled inference
- **Reported:** 2026-08-25

**Outcome: both moved.** Not because you expected it — you explicitly did not rule — but because the
evidence clears for both, and it clears in a way that differs from all three in T-011.

## The test, applied in order

### Step 1 — does either state an acceptance precondition? **No. Neither.**

I searched **both documents in full**, not only their Status sections, for `should not be accepted`,
`until … answered`, `pending`, `OWNER-DECISION`, `OD-###-####`, `open question`, `unresolved`,
`awaiting`, `conditional`.

`ADR-025`: **no matches.** `ADR-024`: four matches, **all four about conditional *code paths*** — "a
conditional path to the write boundary", "the refusal is unconditional" — none about acceptance.

`ADR-026` carried such a clause in plain words and it was the strongest evidence in T-011. **These two
have nothing of the kind**, so `DEC-L-020` puts them on the inference branch and both rows say the
word *inference*.

### Step 2 — what they *do* state is a reason for being `Proposed`, and it has lapsed

| ADR | Its stated ground for `Proposed` | Status of that ground |
|---|---|---|
| `ADR-024` | "No production code implements it yet: **`Employee` does not exist**, and `Employee` is the first production `IBranchOwnedEntity`" | **Lapsed.** `Employee.cs` ships carrying `IBranchOwnedEntity`; `EmployeeBranchAssignment.cs` implements this ADR's transfer model |
| `ADR-025` | "No production code implements this ADR yet. **`ICompanyOwnedEntity` does not exist**, and `Employee` will be its first consumer" | **Lapsed exactly as written.** `ICompanyOwnedEntity.cs` ships; FP-006 records `Employee` as "the **first production consumer** of that interface" |

**This is stronger than `ADR-017`'s bare inference-from-use** — each names the specific fact whose
absence justified `Proposed`, and that fact is now demonstrably false — **but it is still not a
record.**

### Step 3 — what is absent, said out loud

FP-006's `decisions-approved.md` declares "the architecture recorded in `ADR-024` and `ADR-025` …
binding for FP-006 implementation", citing `ADR-024` decisions 1, 4, 5 and 9 against shipped
behaviour. Architecture guards assert both executably — `BranchTransferArchitectureTests` and
`EmployeeArchitectureTests` for `ADR-024`; `CompanyOwnershipArchitectureTests`,
`CompanyArchitectureTests` and `DepartmentApplicationArchitectureTests` for `ADR-025`.

**But no closed decision says either ADR is accepted**, the way `OD-POS-004` said it of `ADR-027`.
Each evidence line states that absence explicitly rather than letting the citations imply otherwise.
"Binding for FP-006 implementation" is a statement about a package's obligations, not about an ADR's
status, and the difference is exactly what `DEC-L-020` exists to keep visible.

## A second finding, which strengthens both and which I did not expect

**Both were `Proposed` while standing in a defined relationship to an `Accepted` ADR:**

- `ADR-025` **supersedes** the deferred portions of `ADR-014` — and `ADR-014` is `Accepted`. A
  `Proposed` record superseding an `Accepted` one is not a coherent pairing in either direction.
- `ADR-024` resolves "the one question `ADR-023` deliberately left open for the first branch-owned
  business entity" — and `ADR-023` is `Accepted`.

So the inconsistency `ADR-029` created with these two in T-011 was not new. **It already existed, one
layer down, between these two and their parents** — and it had been there since 2026-08-18.

## Acceptance criteria

- [x] **Each tested against its own stated precondition first.** Both searched in full; **neither
      states one**, and the result says so rather than skipping to inference.
- [x] **Where a precondition exists and is satisfied, move it.** Not applicable — neither has one.
- [x] **Where none exists, inference is permitted and the row says `inference` in those words.** Both
      rows do, and both evidence lines name what is absent as well as what is present.
- [x] **Where evidence does not clear, leave the file and report why.** Both cleared. Had either
      failed step 1 with an open condition, it would have stayed `Proposed`.
- [x] **`--stat` shows at most three files, all under `docs/14-Engineering/ADR/`:** three files,
      20 insertions, 12 deletions.

## A mistake I made and caught

My first pass replaced `ADR-025`'s trailing status paragraph but **left `**Proposed**` standing at the
head of the same section** — the file would have shipped asserting both verdicts three paragraphs
apart.

That is precisely the self-contradiction `ADR-026` was edited to avoid in T-011, reintroduced by me in
the next task. It was caught by **reading the rendered section afterwards rather than trusting the
patch to have covered it** — the two ADRs put their verdict in different structural positions, and a
find-and-replace written for one does not necessarily land correctly in the other.

## Gate

**Not run** — `DEC-L-002`, no file under `src/` or `tests/`.

```
 ADR-024-Employee-Branch-Assignment-and-Transfer-Model.md   | 13 ++++++++-----
 ADR-025-Company-Execution-Context-and-Authorization.md     | 15 ++++++++++-----
 docs/14-Engineering/ADR/README.md                          |  4 ++--
 3 files changed, 20 insertions(+), 12 deletions(-)
```

Every removed line is front-matter, a `**Proposed**` verdict, or a README row. No Context, Decision,
Consequences or Alternatives text touched. Versions bump with the rows — 1.0→1.1 and 1.1→1.2 — both in
each file's own column shape (**both head their fourth column "Description"**, unlike `ADR-026` and
`ADR-027` which head it "Change"), and both state "No decision changed".

## For the architect

- **Blocked / needs a decision:** none. No contradiction with shipped code was found in either.
- **Touched outside scope:** nothing. Three files.

### On your note about `.claude/` being untracked

You are right that it changes what an honest correction looks like, and it is worth stating what I
have actually been relying on. When I corrected the T-011 result file, the only reason the correction
is verifiable is that **the claims it corrected were about tracked files** — anyone can check
`ADR-017`'s version against git and see that "version is unchanged" stopped being true.

**A correction to a claim about something only recorded in `.claude/` would have no such backstop.**
Twenty rulings, every task spec and every result exist on one disk with no history. If that record is
ever lost or silently edited, nothing in the repository proves what was decided — including the four
`DEC-L` rulings that now govern how this loop merges code.

Raising it with the owner is the right call. I have not touched `.gitignore` or anything else about
it, and would not without a ruling.

### Follow-ups noticed but not done

- **`ADR-001` … `ADR-023` are unaudited**, per the out-of-scope instruction. I looked at exactly two
  of them incidentally — `ADR-014` and `ADR-023`, both `Accepted` — because these two ADRs stand in a
  stated relationship to them. **I am not asserting anything about the other twenty-one.** After
  `ADR-028` and the `gate.sh` abort, the honest position on files I have not read is silence.
- **`ADR-029`'s `depends_on` now resolves entirely to `Accepted` records**, which was T-011's
  correctness argument and is now true one layer deeper as well.
