# T-086 result — re-derived at `8eee3aa`, and the counts moved

- **Status:** PENDING MERGE. **Gated.** **Not self-merged.**
- **Branch:** `agent/T-086-stale-sweep`, from `ClaudeBranch` at `8eee3aa`
- **`GATE_SCOPE=TASK` GREEN.** `condition 4: ok: no non-comment change under src/` — **comment and doc only**, as required.
- Twelve files: six `src/` comments, six specification documents. **No baseline movement.**

---

# THE COUNTS AT `8eee3aa`, AND THEY DIFFER FROM T-083's

```
              T-083 (e571216)     T-086 (8eee3aa)
(1)  mapping absent      9                8       one corrected in T-084
(1b) code contradicts    —                2       NEW, created by T-084
(2)  self-service absent 4                4
(3)  reason expired      9                7       two corrected in T-083
(H)  historical          7                7
(T)  fired trigger       1                0       became 1b
```

**You were right that reusing the list would have been a sample of an older tree.** Three differences, and
two of them are findings rather than bookkeeping.

## The bookkeeping one

`LeaveDomainTests.cs:285` left category 1 because **I corrected it in T-084** — the exception I flagged at
the time. Category 3 lost two for the same reason, corrected in T-083.

## ⚠ FINDING 1 — T-084 LEFT A COMMENT CONTRADICTED BY THE CODE BENEATH IT

**Not stale. False about the file it sits in**, and my own task created it.

`LeaveRequest.cs`, immediately above `ApproveAtRoot`, still read:

> *"**The self-approval bar cannot be enforced on this path.** ... If the root-fallback holder happens to be
> the requesting employee, nothing here can tell."*
>
> *"When the identity-to-employee mapping is built, this is the first thing that should be tightened."*

**T-084 built the bar into the method directly below that sentence.** The trigger did not merely fire — **it
was acted on, and the comment still said it had not been.**

**This is a different failure from the sweep's subject.** A stale claim is wrong about the world; this was
wrong about the ten lines under it. **It happened because T-084's instruction said not to touch the stale
comments, and this one stopped being merely stale the moment T-084 landed.**

**I corrected it, which is beyond categories 1 and 3.** Flagged here rather than folded in, on the
`LeaveDomainTests.cs` precedent you endorsed: leaving a comment that denies the guard beneath it is shipping
a known falsehood, and the correction quotes the old text so the record survives.

## ⚠ FINDING 2 — TWO CORRECTED SITES LOOK STALE TO THE SWEEP THAT FOUND THEM

`PayrollPermissionNames.cs:71` and `AttendanceArchitectureTests.cs:282` match the phrase search **because
the T-083 corrections quote the sentence they replaced.**

**A phrase sweep cannot distinguish a claim from a quotation of a claim.** Both were verified by reading and
**left alone**; a mechanical re-run would have re-corrected already-corrected text. **Worth knowing before
anyone automates this.**

---

# WHAT WAS CORRECTED — AND EVERY CATEGORY-3 SITE CITES RATHER THAN RESTATES

**The instruction that shaped this task.** The nine category-3 reasons were the same fact restated in nine
files that could not see each other; **restating it a tenth time would rebuild exactly what went stale.** So
each now points at the live assertion:

```
AC-ATT-0032, enforced by
AttendanceArchitectureTests.No_self_service_permission_is_declared_because_the_subject_cannot_be_resolved
```

**Category 1 — eight sites.** State that the mapping exists and name `UserEmployeeLink`. **"Verified, not
assumed" is dropped and not replaced with a verification pointing the other way.**

**The asymmetry is stated once**, in `LeaveRequest.cs`'s header where it reads best: *an existence claim is
checkable by opening a file; a non-existence claim is not* — which is why the assurance was doing
load-bearing work it could not do, and why nine files went stale together without one noticing.

**Category 3 — seven sites, `[X]` taken from each file.** `AttendancePermissionNames.cs` and
`REQ-ATT-0023` → the permission and endpoint. `FP-012/authorization-model.md` → payroll's own. `ATT.md:137`
→ the condition it named has been met. **None guessed.**

**`REQ-ATT-0023` keeps `BLOCKED` and loses its reason**, as ruled — and one thing follows that I did not
expect: it was filed as *"`DEC-PAY-0002`'s shape — a missing input, not a scoping preference."* **It is no
longer that shape.** The input arrived; what remains is unbuilt scope. That is now stated.

**`AC-ATT-0032` split**, as ruled: assertion verbatim, trailing clause replaced, with the original clause
preserved parenthetically so the change is visible rather than silent.

---

## Flagged and left

**A quotation, not a claim.** `FP-013/authorization-model.md:19-22` block-quotes `PayrollPermissionNames` as
it stood when the package was written. **Correcting a quotation would misquote its source.** Left verbatim
with a note that the source has since changed; the three live claims around it are corrected.

**Category H untouched** — `decisions-open.md`, `decisions-ratified.md`, `traceability-matrix.md`,
`decisions-approved.md`. `DEC-L-071`. **No ADR file opened.**

**FP-015's own files untouched** — written knowing `ADR-030`; they describe the mapping as existing.

### ⚠ ONE THING I COULD NOT DECIDE, AND WHAT WOULD DECIDE IT

**The guard's own NAME encodes the expired reason:**

```
No_self_service_permission_is_declared_because_the_subject_cannot_be_resolved
```

**The subject can now be resolved.** The assertion is true and the name's `because` clause is not — the same
split you ruled for `AC-ATT-0032`, in an identifier rather than a sentence.

**I did not rename it.** Every category-3 correction in this task cites that name, so renaming it would
change six citations in the same commit that wrote them, and **a test name is a different kind of object
from a comment** — it appears in run output, in the baseline's meaning, and in this result.

**What would decide it:** whether you want the citation to be the name or the criterion. **If `AC-ATT-0032`
is the durable handle, the name can change freely and the citations should point at the criterion alone.**

## For the architect

- **Blocked / needs a decision:** the guard's name, above.
- **Two findings beyond the sweep**, both reported rather than absorbed: T-084's contradicted comment, and
  corrected sites matching the sweep that found them.
- **Counts re-derived, three differences, all explained.**
